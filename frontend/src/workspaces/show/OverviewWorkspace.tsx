import ComingSoon from "../ComingSoon";

export default function OverviewWorkspace() {
  return (
    <ComingSoon
      title="Overview"
      description="Show Overview isn't built into the desktop app yet."
      cliHint="Use golc show open / golc show inspect from the command line to review the current show."
    />
  );
}
