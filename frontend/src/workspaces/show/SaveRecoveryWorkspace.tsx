import ComingSoon from "../ComingSoon";

export default function SaveRecoveryWorkspace() {
  return (
    <ComingSoon
      title="Save & Recovery"
      description="Save & Recovery isn't wired into the desktop app yet."
      cliHint="golc show save, golc show save-as, and the recovery-offer flow on golc show open remain available from the command line."
    />
  );
}
