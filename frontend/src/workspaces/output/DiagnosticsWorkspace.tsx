import ComingSoon from "../ComingSoon";

export default function DiagnosticsWorkspace() {
  return (
    <ComingSoon
      title="Diagnostics"
      description="Diagnostics isn't wired into the desktop app yet."
      cliHint="Run golc show diagnose from the command line for a full integrity check."
    />
  );
}
