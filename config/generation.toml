# GOLC generation concern: source -> committed generated artifact rules.
#
# This file alone owns where generated review artifacts (JSON Schemas)
# live and how generated bytes are normalized so Windows and CI produce
# identical committed output (D-08).

schema_version = 1

[generation]
schemas_dir = "schemas"
line_endings = "lf"
