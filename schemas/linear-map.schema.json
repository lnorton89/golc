{
  "$comment": "GENERATED by github.com/lnorton89/golc/internal/contracts. DO NOT EDIT. Schema: linear-map.",
  "$id": "https://github.com/lnorton89/golc/internal/contracts/linear-map-schema",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "active_milestone": {
      "additionalProperties": false,
      "description": "The durable active milestone identity seed.",
      "properties": {
        "milestone_id": {
          "description": "Durable local milestone id; never changes on rename (D-14).",
          "pattern": "^milestone:v[0-9]+$",
          "type": "string"
        },
        "name": {
          "description": "Display-only milestone name.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "milestone_id",
        "name"
      ],
      "type": "object"
    },
    "entities": {
      "description": "The complete dynamically discovered local catalog in deterministic build order.",
      "items": {
        "additionalProperties": false,
        "properties": {
          "display": {
            "description": "Display-only text; renaming never changes identity (D-14).",
            "type": "string"
          },
          "kind": {
            "description": "Catalog entity kind.",
            "enum": [
              "project",
              "milestone",
              "phase",
              "req",
              "plan",
              "task"
            ],
            "type": "string"
          },
          "local_id": {
            "description": "Durable local id; identity never changes on rename (D-14).",
            "pattern": "^[a-z]+:[A-Za-z0-9._-]+$",
            "type": "string"
          },
          "parent_local_id": {
            "description": "Parent durable local id; empty only for the project root.",
            "pattern": "^([a-z]+:[A-Za-z0-9._-]+)?$",
            "type": "string"
          },
          "source": {
            "description": "Repository-relative planning artifact source; contained inside .planning/ (D-11: repository text is the authority).",
            "pattern": "^[.]planning(/[A-Za-z0-9._-]+)+$",
            "type": "string"
          }
        },
        "required": [
          "local_id",
          "kind",
          "parent_local_id",
          "display",
          "source"
        ],
        "type": "object"
      },
      "type": "array"
    },
    "remote_mappings": {
      "description": "One remote mapping per non-project entity; pending/null linkage is valid incomplete linkage and never a local failure (CONTEXT D-11).",
      "items": {
        "additionalProperties": false,
        "properties": {
          "identifier": {
            "oneOf": [
              {
                "description": "Human-facing Linear identifier (for example GOLC-123) once synchronized; null while pending.",
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          },
          "linear_type": {
            "description": "The Linear remote object type this local id maps to.",
            "enum": [
              "project",
              "project_milestone",
              "issue"
            ],
            "type": "string"
          },
          "linear_uuid": {
            "oneOf": [
              {
                "description": "Immutable Linear GraphQL UUID once synchronized; null while pending. Never invented offline.",
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          },
          "repo_id": {
            "description": "The mapped durable local id.",
            "pattern": "^[a-z]+:[A-Za-z0-9._-]+$",
            "type": "string"
          },
          "status": {
            "description": "Mapping status; for example pending before any remote synchronization has run.",
            "minLength": 1,
            "type": "string"
          },
          "url": {
            "oneOf": [
              {
                "description": "Linear web URL once synchronized; null while pending.",
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "required": [
          "repo_id",
          "linear_type",
          "status",
          "linear_uuid",
          "identifier",
          "url"
        ],
        "type": "object"
      },
      "type": "array"
    },
    "repository": {
      "additionalProperties": false,
      "description": "The durable repository root project identity seed.",
      "properties": {
        "name": {
          "description": "Display-only project name.",
          "minLength": 1,
          "type": "string"
        },
        "project_id": {
          "description": "Durable local project id; never changes on rename (D-14).",
          "pattern": "^project:[a-z0-9]+(-[a-z0-9]+)*$",
          "type": "string"
        }
      },
      "required": [
        "project_id",
        "name"
      ],
      "type": "object"
    },
    "schema": {
      "description": "Supported linear map schema version; only schema 2 (the migrated complete catalog map) is projected.",
      "enum": [
        2
      ],
      "type": "integer"
    }
  },
  "required": [
    "schema",
    "repository",
    "active_milestone",
    "entities",
    "remote_mappings"
  ],
  "type": "object"
}
