{
  "$comment": "GENERATED by github.com/lnorton89/golc/internal/contracts. DO NOT EDIT. Schema: fixture.",
  "$id": "https://github.com/lnorton89/golc/internal/fixture/fixture-definition",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "capabilities": {
      "description": "Declared capabilities in source order; must not be empty (GOLC_FIXTURE_EMPTY).",
      "items": {
        "additionalProperties": false,
        "properties": {
          "comment": {
            "description": "Optional human-readable note about this capability.",
            "type": "string"
          },
          "range": {
            "description": "Normalized [min max] value range within the 0 to 1 interval; never raw DMX.",
            "items": {
              "type": "number"
            },
            "maxItems": 2,
            "minItems": 2,
            "type": "array"
          },
          "type": {
            "description": "Capability type; must be one of the declared CapabilityType enum values.",
            "type": "string"
          }
        },
        "required": [
          "type",
          "range"
        ],
        "type": "object"
      },
      "minItems": 1,
      "type": "array"
    },
    "manufacturer": {
      "description": "Fixture manufacturer name.",
      "minLength": 1,
      "type": "string"
    },
    "model": {
      "description": "Fixture model name.",
      "minLength": 1,
      "type": "string"
    },
    "modes": {
      "description": "Declared operating modes.",
      "items": {
        "additionalProperties": false,
        "properties": {
          "channels": {
            "description": "Ordered DMX channel layout (D-16); each entry names the CapabilityType and same-type occurrence index this channel drives.",
            "items": {
              "additionalProperties": false,
              "properties": {
                "occurrence": {
                  "description": "0-based index selecting among the fixture's possibly-multiple same-Type Capabilities.",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": {
                  "description": "Capability type this channel drives; must be one of the declared CapabilityType enum values.",
                  "type": "string"
                }
              },
              "required": [
                "type",
                "occurrence"
              ],
              "type": "object"
            },
            "minItems": 1,
            "type": "array"
          },
          "name": {
            "description": "Mode name.",
            "minLength": 1,
            "type": "string"
          }
        },
        "required": [
          "name",
          "channels"
        ],
        "type": "object"
      },
      "minItems": 1,
      "type": "array"
    },
    "schema_version": {
      "description": "Supported fixture schema version.",
      "enum": [
        1
      ],
      "type": "integer"
    }
  },
  "required": [
    "schema_version",
    "manufacturer",
    "model",
    "modes",
    "capabilities"
  ],
  "type": "object"
}
