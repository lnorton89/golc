{
  "$comment": "GENERATED by github.com/lnorton89/golc/internal/contracts. DO NOT EDIT. Schema: config-toolchain.",
  "$id": "https://github.com/lnorton89/golc/internal/contracts/toolchain-schema",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "cache": {
      "additionalProperties": false,
      "description": "Repository-local cache directories.",
      "properties": {
        "downloads": {
          "description": "Content-addressed verified download cache directory.",
          "pattern": "^[.]tools(/[A-Za-z0-9._-]+)+$",
          "type": "string"
        },
        "gocache": {
          "description": "Repository-local GOCACHE directory.",
          "pattern": "^[.]tools(/[A-Za-z0-9._-]+)+$",
          "type": "string"
        },
        "gomodcache": {
          "description": "Repository-local GOMODCACHE directory.",
          "pattern": "^[.]tools(/[A-Za-z0-9._-]+)+$",
          "type": "string"
        }
      },
      "required": [
        "downloads",
        "gomodcache",
        "gocache"
      ],
      "type": "object"
    },
    "schema_version": {
      "description": "Supported concern schema version.",
      "enum": [
        1
      ],
      "type": "integer"
    },
    "toolchain": {
      "additionalProperties": false,
      "description": "Pinned Go toolchain identity.",
      "properties": {
        "go": {
          "additionalProperties": false,
          "properties": {
            "archive_sha256": {
              "description": "Lowercase hex SHA-256 of the archive.",
              "pattern": "^[0-9a-f]{64}$",
              "type": "string"
            },
            "archive_url": {
              "description": "Official go.dev archive URL.",
              "pattern": "^https://go[.]dev/dl/[A-Za-z0-9.-]+[.](zip|tar[.]gz)$",
              "type": "string"
            },
            "version": {
              "description": "Exact pinned Go version.",
              "pattern": "^[0-9]+([.][0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "version",
            "archive_url",
            "archive_sha256"
          ],
          "type": "object"
        }
      },
      "required": [
        "go"
      ],
      "type": "object"
    }
  },
  "required": [
    "schema_version",
    "toolchain",
    "cache"
  ],
  "type": "object"
}
