{
  "$comment": "GENERATED by github.com/lnorton89/golc/internal/contracts. DO NOT EDIT. Schema: config-toolchain.",
  "$id": "https://github.com/lnorton89/golc/internal/contracts/toolchain-schema",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "cache": {
      "additionalProperties": false,
      "description": "Repository-local cache directories.",
      "properties": {
        "downloads": {
          "description": "Content-addressed verified download cache directory.",
          "pattern": "^[.]tools(/[A-Za-z0-9._-]+)+$",
          "type": "string"
        },
        "gocache": {
          "description": "Repository-local GOCACHE directory.",
          "pattern": "^[.]tools(/[A-Za-z0-9._-]+)+$",
          "type": "string"
        },
        "gomodcache": {
          "description": "Repository-local GOMODCACHE directory.",
          "pattern": "^[.]tools(/[A-Za-z0-9._-]+)+$",
          "type": "string"
        }
      },
      "required": [
        "downloads",
        "gomodcache",
        "gocache"
      ],
      "type": "object"
    },
    "go_install": {
      "additionalProperties": {
        "additionalProperties": false,
        "properties": {
          "module": {
            "description": "Fully qualified go install module path.",
            "type": "string"
          },
          "version": {
            "description": "Exact pinned go install module version tag.",
            "pattern": "^v[0-9]+[.][0-9]+[.][0-9]+$",
            "type": "string"
          }
        },
        "required": [
          "version",
          "module"
        ],
        "type": "object"
      },
      "description": "Go-ecosystem CLI tools provisioned via a go install invocation",
      "type": "object"
    },
    "schema_version": {
      "description": "Supported concern schema version.",
      "enum": [
        2
      ],
      "type": "integer"
    },
    "toolchain": {
      "additionalProperties": false,
      "description": "Checksum-pinned per-platform toolchain archives.",
      "properties": {
        "deno": {
          "additionalProperties": false,
          "description": "Pinned Deno identity",
          "properties": {
            "official_host": {
              "description": "Allowlisted acquisition host (internal/bootstrap's OfficialSourcePolicy).",
              "type": "string"
            },
            "official_path_prefix": {
              "description": "Allowlisted acquisition URL path prefix.",
              "type": "string"
            },
            "platforms": {
              "additionalProperties": {
                "additionalProperties": false,
                "properties": {
                  "archive_sha256": {
                    "description": "Lowercase hex SHA-256 of the archive.",
                    "pattern": "^[0-9a-f]{64}$",
                    "type": "string"
                  },
                  "archive_url": {
                    "description": "Official archive download URL.",
                    "format": "uri",
                    "type": "string"
                  }
                },
                "required": [
                  "archive_url",
                  "archive_sha256"
                ],
                "type": "object"
              },
              "description": "Per-platform checksum-pinned archive",
              "type": "object"
            },
            "version": {
              "description": "Exact pinned tool version.",
              "pattern": "^[0-9]+([.][0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "version",
            "official_host",
            "official_path_prefix",
            "platforms"
          ],
          "type": "object"
        },
        "go": {
          "additionalProperties": false,
          "description": "Pinned Go toolchain identity -- bootstrap and every other tool's own go install step depend on it.",
          "properties": {
            "official_host": {
              "description": "Allowlisted acquisition host (internal/bootstrap's OfficialSourcePolicy).",
              "type": "string"
            },
            "official_path_prefix": {
              "description": "Allowlisted acquisition URL path prefix.",
              "type": "string"
            },
            "platforms": {
              "additionalProperties": {
                "additionalProperties": false,
                "properties": {
                  "archive_sha256": {
                    "description": "Lowercase hex SHA-256 of the archive.",
                    "pattern": "^[0-9a-f]{64}$",
                    "type": "string"
                  },
                  "archive_url": {
                    "description": "Official archive download URL.",
                    "format": "uri",
                    "type": "string"
                  }
                },
                "required": [
                  "archive_url",
                  "archive_sha256"
                ],
                "type": "object"
              },
              "description": "Per-platform checksum-pinned archive",
              "type": "object"
            },
            "version": {
              "description": "Exact pinned tool version.",
              "pattern": "^[0-9]+([.][0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "version",
            "official_host",
            "official_path_prefix",
            "platforms"
          ],
          "type": "object"
        },
        "mage": {
          "additionalProperties": false,
          "description": "Pinned Mage identity",
          "properties": {
            "official_host": {
              "description": "Allowlisted acquisition host (internal/bootstrap's OfficialSourcePolicy).",
              "type": "string"
            },
            "official_path_prefix": {
              "description": "Allowlisted acquisition URL path prefix.",
              "type": "string"
            },
            "platforms": {
              "additionalProperties": {
                "additionalProperties": false,
                "properties": {
                  "archive_sha256": {
                    "description": "Lowercase hex SHA-256 of the archive.",
                    "pattern": "^[0-9a-f]{64}$",
                    "type": "string"
                  },
                  "archive_url": {
                    "description": "Official archive download URL.",
                    "format": "uri",
                    "type": "string"
                  }
                },
                "required": [
                  "archive_url",
                  "archive_sha256"
                ],
                "type": "object"
              },
              "description": "Per-platform checksum-pinned archive",
              "type": "object"
            },
            "version": {
              "description": "Exact pinned tool version.",
              "pattern": "^[0-9]+([.][0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "version",
            "official_host",
            "official_path_prefix",
            "platforms"
          ],
          "type": "object"
        },
        "node": {
          "additionalProperties": false,
          "description": "Pinned Node identity",
          "properties": {
            "official_host": {
              "description": "Allowlisted acquisition host (internal/bootstrap's OfficialSourcePolicy).",
              "type": "string"
            },
            "official_path_prefix": {
              "description": "Allowlisted acquisition URL path prefix.",
              "type": "string"
            },
            "platforms": {
              "additionalProperties": {
                "additionalProperties": false,
                "properties": {
                  "archive_sha256": {
                    "description": "Lowercase hex SHA-256 of the archive.",
                    "pattern": "^[0-9a-f]{64}$",
                    "type": "string"
                  },
                  "archive_url": {
                    "description": "Official archive download URL.",
                    "format": "uri",
                    "type": "string"
                  }
                },
                "required": [
                  "archive_url",
                  "archive_sha256"
                ],
                "type": "object"
              },
              "description": "Per-platform checksum-pinned archive",
              "type": "object"
            },
            "version": {
              "description": "Exact pinned tool version.",
              "pattern": "^[0-9]+([.][0-9]+)*$",
              "type": "string"
            }
          },
          "required": [
            "version",
            "official_host",
            "official_path_prefix",
            "platforms"
          ],
          "type": "object"
        }
      },
      "required": [
        "go",
        "node",
        "deno",
        "mage"
      ],
      "type": "object"
    }
  },
  "required": [
    "schema_version",
    "toolchain",
    "go_install",
    "cache"
  ],
  "type": "object"
}
