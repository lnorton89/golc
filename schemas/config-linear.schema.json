{
  "$comment": "GENERATED by github.com/lnorton89/golc/internal/contracts. DO NOT EDIT. Schema: config-linear.",
  "$id": "https://github.com/lnorton89/golc/internal/contracts/linear-schema",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "linear": {
      "additionalProperties": false,
      "properties": {
        "env": {
          "additionalProperties": false,
          "description": "Documented environment variable names only; never credential values per D-19.",
          "properties": {
            "api_key": {
              "description": "Environment variable name carrying the Linear API key; never the credential value itself.",
              "pattern": "^[A-Z][A-Z0-9_]*$",
              "type": "string"
            },
            "team_id": {
              "description": "Environment variable name carrying the Linear team id.",
              "pattern": "^[A-Z][A-Z0-9_]*$",
              "type": "string"
            }
          },
          "required": [
            "api_key",
            "team_id"
          ],
          "type": "object"
        },
        "mapping_file": {
          "description": "Repository-relative durable local identity map path.",
          "pattern": "^[.]planning(/[A-Za-z0-9._-]+)+$",
          "type": "string"
        },
        "taxonomy": {
          "additionalProperties": false,
          "description": "Linear taxonomy labels.",
          "properties": {
            "requirement_label": {
              "description": "Linear label applied to requirement issues.",
              "pattern": "^[a-z][a-z0-9-]*$",
              "type": "string"
            }
          },
          "required": [
            "requirement_label"
          ],
          "type": "object"
        }
      },
      "required": [
        "mapping_file",
        "env",
        "taxonomy"
      ],
      "type": "object"
    },
    "schema_version": {
      "description": "Supported concern schema version.",
      "enum": [
        1
      ],
      "type": "integer"
    }
  },
  "required": [
    "schema_version",
    "linear"
  ],
  "type": "object"
}
