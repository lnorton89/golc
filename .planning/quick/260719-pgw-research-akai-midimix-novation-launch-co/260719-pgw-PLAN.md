---
phase: quick-midi-hardware-acceptance-set
plan: 260719-pgw
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260719-pgw-research-akai-midimix-novation-launch-co/260719-pgw-RESEARCH.md
  - .planning/midi/README.md
  - .planning/PROJECT.md
  - AGENTS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/research/FEATURES.md
autonomous: true
requirements: [PLAY-04, PLAY-05, EXTN-04]
must_haves:
  truths:
    - "Akai MIDImix, Novation Launch Control XL Mk2, and Worlde EasyControl 9 are recorded together as the selected Phase 6 physical acceptance set for generic MIDI learn and soft-takeover qualification."
    - "Selection is never presented as compatibility or support; every controller must independently satisfy MIDI-HW-02 for the exact hardware revision, firmware, Windows version, and GOLC build before a named compatibility/support claim."
    - "Manual-grounded differences and unknowns remain explicit, including Novation's richer supplied feedback protocol and the unresolved Akai/Worlde wire-level probes."
    - "MIDI-HW-01 is resolved, MIDI-HW-02 is open, PLAY-04/PLAY-05 remain generic, EXTN-04 remains deferred, and the counted release traceability stays exactly 84/84."
    - "MIDI-HW-01 and MIDI-HW-02 remain documentation gate labels only; the existing Phase 1 dynamic traceability catalog stays unchanged, with no new entity class, catalog entry, requirement, plan, Linear mapping, or implementation work."
    - "Phase 1 remains executable with no MIDI product work and no Phase 1 plan modification; readiness is verified read-only against the existing plan set."
    - "Phase 1 validation continues to report nyquist_compliant and wave_0_complete as false until execution creates and passes the planned test infrastructure."
  artifacts:
    - path: .planning/quick/260719-pgw-research-akai-midimix-novation-launch-co/260719-pgw-RESEARCH.md
      provides: "Corrected manual-grounded three-controller research and acceptance matrix"
    - path: .planning/midi/README.md
      provides: "Canonical manual index, hashes, evidence status, selected set, and remaining probes"
    - path: .planning/REQUIREMENTS.md
      provides: "Resolved MIDI-HW-01 and open independent-evidence gate MIDI-HW-02"
    - path: .planning/ROADMAP.md
      provides: "Phase 6 selected-set and evidence-gate wording without device-profile scope expansion"
    - path: .planning/PROJECT.md
      provides: "Canonical three-controller Phase 6 selection decision and evidence-gated support constraint"
    - path: AGENTS.md
      provides: "Generated project-instruction mirror of PROJECT.md with all unrelated managed sections preserved"
    - path: .planning/STATE.md
      provides: "Three-controller Phase 6 decision and blocker state while Phase 1 position, focus, progress, and readiness remain unchanged"
    - path: .planning/research/FEATURES.md
      provides: "Selected-set research wording with generic Note/CC and soft-takeover in Phase 6 and device-specific profiles/feedback retained under v1.x evidence gates"
  key_links:
    - from: .planning/midi/README.md
      to: .planning/midi/*.pdf
      via: "relative links and exact SHA-256 values"
      pattern: "Akai-MIDImix|launch_control_xl|Novation-Launch Control XL|Worlde-EasyControl"
    - from: .planning/PROJECT.md
      to: AGENTS.md
      via: "generated project constraint mirrors the canonical evidence-gated support rule"
      pattern: "MIDI hardware"
    - from: .planning/REQUIREMENTS.md
      to: .planning/ROADMAP.md
      via: "MIDI-HW-01 resolution and MIDI-HW-02 Phase 6 evidence gate"
      pattern: "MIDI-HW-0[12]"
---

<objective>
Correct the MIDI research and canonical planning record so all three user-owned controllers form the selected physical acceptance set, then prove Phase 1 remains execution-ready.

Purpose: Clear the controller-selection blocker without converting manual evidence into an unsupported compatibility claim or introducing MIDI implementation into Phase 1.
Output: Corrected research, a durable hashed manual index, synchronized project/requirements/roadmap/state wording, and read-only Phase 1 readiness evidence.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/linear-map.json
@.planning/quick/260719-pgw-research-akai-midimix-novation-launch-co/260719-pgw-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md

**USER-AUTHORIZED QUICK-TASK EXCEPTION:** The user explicitly authorized this quick task to edit only the Phase 6 `**Blocker:**` MIDI gate wording in `.planning/ROADMAP.md`. That authorization does not extend to Phase 6 requirements, goal, success criteria, implementation scope, or any other roadmap phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the three-controller research record and preserve its manual evidence</name>
  <files>.planning/quick/260719-pgw-research-akai-midimix-novation-launch-co/260719-pgw-RESEARCH.md, .planning/midi/README.md</files>
  <read_first>
    - `.planning/quick/260719-pgw-research-akai-midimix-novation-launch-co/260719-pgw-RESEARCH.md` - every decision, matrix, acceptance-outcome, planning-update, readiness, claim-language, and final-recommendation section that currently assigns unequal planning roles.
    - `.planning/midi/Akai-MIDImix-UserGuide-v1.0.pdf` - English setup, controls, bidirectional USB, Send All/pickup warning, and specifications on PDF pages 3-5 and 18.
    - `.planning/midi/launch_control_xl_programmer_s_reference_guide.pdf` - full nine-page MIDI, SysEx, LED, template, toggle, reset, flashing, and double-buffer protocol reference.
    - `.planning/midi/Novation-Launch Control XL GSG v2.pdf` - full seven-page hardware, class-compliant USB, template, and editor guide.
    - `.planning/midi/Worlde-EasyControl-9-UserManual.pdf` - full nine-page controls, banks, editor settings, outbound behavior, platform statement, and specifications.
  </read_first>
  <action>Rewrite every research passage that assigns the three devices unequal planning status. Apply the correction to the Decision and its primary-recommendation sentence, the capability-matrix planning-role row, per-device findings, HW-14, acceptance outcomes, canonical blocker wording, planning-update table, traceability note, Phase 1 impact wording, required claim language, and final recommendation. Each recommendation-bearing section must identify Akai MIDImix, Novation Launch Control XL Mk2, and Worlde EasyControl 9 together as the selected Phase 6 physical acceptance set for generic Note/CC learn and soft-takeover qualification. In each of `### 3.1 Akai MIDImix`, `### 3.2 Novation Launch Control XL Mk2`, and `### 3.3 Worlde EasyControl 9`, state that the subsection's controller is one of the three equal selected Phase 6 hardware acceptance targets for generic Note/CC learn and soft-takeover qualification, and name all three set members in that subsection. In the capability-matrix role row, HW-14 device cells, acceptance-outcome bullets, and claim-language bullets, give all three devices the identical role label `Selected Phase 6 physical acceptance-set member` before stating device-specific evidence gaps. Preserve every manual-grounded capability difference and unknown. In particular, retain the factual statement `Novation has the most thoroughly documented supplied LED/SysEx protocol` inside the Novation subsection, and state there explicitly that this richer documentation creates no priority, compatibility, or support implication. Akai's supplied guide still leaves default addresses/button/LED details to capture, and Worlde's guide still leaves current Windows, bank observability, runtime feedback, and defaults to physical probing. Preserve the per-device acceptance matrix and make every device's evidence independent. State that selection does not mean support and that a device may be named compatible/supported only after its independent MIDI-HW-02 evidence set passes for exact hardware revision, firmware, Windows version, and GOLC build.

Rewrite the Phase 1 traceability note and readiness/impact passages so `MIDI-HW-01` and `MIDI-HW-02` are explicitly documentation gate labels only. State exactly that they produce no new Phase 1 entity class, catalog entry, requirement, plan, Linear mapping, or implementation work, and that the existing dynamic traceability catalog remains unchanged. Remove the current scope-expanding claims enumerated by the automated gate, together with equivalent instructions to add these gate labels or this hardware decision to Phase 1 traceability, requirements, plans, or Linear mappings. Phase 1 may only be inspected and validated read-only by this quick task.

Convert the four primary-manual entries in the research source section to resolving relative links into `.planning/midi/`. Create `.planning/midi/README.md` as the canonical evidence index with relative links to all four manuals, their controller/role, manual-review status, physical-evidence status, selected-set statement, remaining per-device probes, and a reproducible `Get-FileHash -Algorithm SHA256` verification command. Record these exact hashes: Akai `203D4859E9C15364E7C228842BCAC0BF1AEED68E38ED56E6E5B964DF6BA5ECDA`; Novation programmer guide `076985FA9A0859A2ECCE0C35D1E843FC5815BD67062C0019DE9A8CDCA07F7C06`; Novation getting-started guide `5EC473BE4CEFAE0F694171A02DAEF686C134791D6EB7EB2A2E71D6A36E48CB1F`; Worlde `D4CCD8244410C3F9ECD7143350AE12ADF3A623B72630A0A55017C9FC858990B7`. Treat the four PDFs as immutable user-supplied canonical inputs: read and hash-verify them, link and index them, but do not edit, stage, or claim mutable ownership of them. The quick-task orchestrator stages those supplied planning references separately.</action>
  <verify>
    <automated>$expected=@{'Akai-MIDImix-UserGuide-v1.0.pdf'='203D4859E9C15364E7C228842BCAC0BF1AEED68E38ED56E6E5B964DF6BA5ECDA';'launch_control_xl_programmer_s_reference_guide.pdf'='076985FA9A0859A2ECCE0C35D1E843FC5815BD67062C0019DE9A8CDCA07F7C06';'Novation-Launch Control XL GSG v2.pdf'='5EC473BE4CEFAE0F694171A02DAEF686C134791D6EB7EB2A2E71D6A36E48CB1F';'Worlde-EasyControl-9-UserManual.pdf'='D4CCD8244410C3F9ECD7143350AE12ADF3A623B72630A0A55017C9FC858990B7'}; $researchPath='.planning/quick/260719-pgw-research-akai-midimix-novation-launch-co/260719-pgw-RESEARCH.md'; $indexPath='.planning/midi/README.md'; $index=Get-Content -LiteralPath $indexPath -Raw; $research=Get-Content -LiteralPath $researchPath -Raw; $devices=@('Akai MIDImix','Novation Launch Control XL Mk2','Worlde EasyControl 9'); foreach($name in $expected.Keys){$path=Join-Path '.planning/midi' $name; if(-not (Test-Path -LiteralPath $path)){throw "missing manual: $name"}; $actual=(Get-FileHash -LiteralPath $path -Algorithm SHA256).Hash; if($actual -ne $expected[$name]){throw "hash mismatch: $name"}; if($index -notmatch [regex]::Escape($name) -or $index -notmatch $expected[$name]){throw "index evidence missing: $name"}; if($research -notmatch [regex]::Escape($name)){throw "research source missing: $name"}}; foreach($device in $devices){if($research -notmatch [regex]::Escape($device) -or $index -notmatch [regex]::Escape($device)){throw "selected device missing: $device"}}; foreach($doc in @($research,$index)){if($doc -notmatch 'selected Phase 6 physical acceptance set' -or $doc -notmatch 'independent MIDI-HW-02 evidence set'){throw 'three-device selection or support-gate wording missing'}}; $sectionPatterns=@{'Decision'='(?ms)^## Decision\s*$(.*?)(?=^## |\z)';'Capability Matrix'='(?ms)^## 2\. Controller Capability Matrix\s*$(.*?)(?=^## |\z)';'Acceptance outcomes'='(?ms)^### Acceptance outcomes\s*$(.*?)(?=^## |\z)';'Planning Updates'='(?ms)^## 6\. Planning Updates Required\s*$(.*?)(?=^## |\z)';'Phase 1 impact'='(?ms)^## 7\. Phase 1 Readiness Impact\s*$(.*?)(?=^## |\z)';'Claim language'='(?ms)^### Required claim language\s*$(.*?)(?=^## |\z)';'Final recommendation'='(?ms)^## Final Planning Recommendation\s*$(.*?)(?=^## |\z)'}; foreach($entry in $sectionPatterns.GetEnumerator()){$match=[regex]::Match($research,$entry.Value); if(-not $match.Success){throw "research section missing: $($entry.Key)"}; foreach($device in $devices){if($match.Value -notmatch [regex]::Escape($device)){throw "$($entry.Key) omits selected-set member: $device"}}; if($entry.Key -ne 'Capability Matrix' -and $match.Value -notmatch 'selected Phase 6 physical acceptance set'){throw "$($entry.Key) does not select the complete set"}}; $primary=[regex]::Match($research,'(?im)^\*\*Primary recommendation:\*\*[^\r\n]+$').Value; if([string]::IsNullOrWhiteSpace($primary) -or $primary -notmatch 'selected Phase 6 physical acceptance set'){throw 'primary recommendation missing complete-set selection'}; foreach($device in $devices){if($primary -notmatch [regex]::Escape($device)){throw "primary recommendation omits: $device"}}; $equalRole='Selected Phase 6 physical acceptance-set member'; $equalRegions=@{'Capability matrix planning-role row'='(?im)^\| (?:Best |Phase 6 )?planning role \|.+$';'HW-14 row'='(?im)^\| HW-14 \|.+$';'Acceptance outcomes'='(?ms)^### Acceptance outcomes\s*$(.*?)(?=^## |\z)';'Claim language'='(?ms)^### Required claim language\s*$(.*?)(?=^## |\z)'}; foreach($entry in $equalRegions.GetEnumerator()){$region=[regex]::Match($research,$entry.Value).Value; if([string]::IsNullOrWhiteSpace($region) -or [regex]::Matches($region,[regex]::Escape($equalRole)).Count -ne 3){throw "$($entry.Key) does not assign the identical role to all three devices"}}; $selectedRegions=@{'Decision'='(?ms)^## Decision\s*$(.*?)(?=^## |\z)';'Canonical blocker wording'='(?ms)^### Canonical blocker wording\s*$(.*?)(?=^### |^## |\z)';'Planning-update table'='(?ms)^### Exact locations\s*$(.*?)(?=^### |^## |\z)';'Phase 1 impact'='(?ms)^## 7\. Phase 1 Readiness Impact\s*$(.*?)(?=^## |\z)';'Final recommendation'='(?ms)^## Final Planning Recommendation\s*$(.*?)(?=^## |\z)'}; foreach($entry in $selectedRegions.GetEnumerator()){$region=[regex]::Match($research,$entry.Value).Value; if([string]::IsNullOrWhiteSpace($region) -or $region -notmatch 'selected Phase 6 physical acceptance set'){throw "$($entry.Key) lacks the complete-set role"}; foreach($device in $devices){if($region -notmatch [regex]::Escape($device)){throw "$($entry.Key) omits selected-set member: $device"}}}; $perDevicePatterns=[ordered]@{'Akai MIDImix'='(?ms)^### 3\.1 Akai MIDImix\s*$(.*?)(?=^### 3\.2 |^## |\z)';'Novation Launch Control XL Mk2'='(?ms)^### 3\.2 Novation Launch Control XL Mk2\s*$(.*?)(?=^### 3\.3 |^## |\z)';'Worlde EasyControl 9'='(?ms)^### 3\.3 Worlde EasyControl 9\s*$(.*?)(?=^## |\z)'}; $researchScopes=[ordered]@{'complete research'=$research}; foreach($entry in $perDevicePatterns.GetEnumerator()){$match=[regex]::Match($research,$entry.Value); if(-not $match.Success){throw "per-device findings subsection missing: $($entry.Key)"}; $section=$match.Value; $researchScopes["per-device subsection $($entry.Key)"]=$section; foreach($device in $devices){if($section -notmatch [regex]::Escape($device)){throw "$($entry.Key) subsection omits equal-set member: $device"}}; $equalTargetPattern=[regex]::Escape($entry.Key)+'[^\r\n]{0,160}one of the three equal selected Phase 6 hardware acceptance targets for generic Note/CC learn and soft-takeover qualification'; if($section -notmatch $equalTargetPattern){throw "$($entry.Key) subsection lacks its explicit equal-target statement"}}; $novationSection=$researchScopes['per-device subsection Novation Launch Control XL Mk2']; if($novationSection -notmatch [regex]::Escape('Novation has the most thoroughly documented supplied LED/SysEx protocol') -or $novationSection -notmatch '(?i)(?:richer|documentation difference)[^\r\n]*(?:no|not)[^\r\n]*priority[^\r\n]*(?:compatibility|support)'){throw 'Novation subsection does not preserve the richer-protocol fact without priority/support implication'}; $staleLiterals=@('Select the Novation','should first use','Novation selection','Initial target','initial physical','selected acceptance target now','selected Novation','selection resolved to Novation','the Novation decision','selected hardware acceptance target','strongest fallback','fallback candidate','evaluation-only','evaluation candidate','evaluated candidates','comparison/fallback candidate','comparison/fallback','comparison-only'); $hierarchyPatterns=@('(?i)\b(?:evaluation|fallback)(?:[- /](?:only|candidate|target|role|member|device|option))\b','(?i)(Akai|Worlde)[^\r\n]{0,100}\b(secondary|fallback|evaluation|alternative|backup|contingency|less preferred|lower priority)\b','(?i)Novation[^\r\n]{0,100}\b(primary|preferred|priority|favored|first choice|ahead of|only candidate)\b'); foreach($scopeEntry in $researchScopes.GetEnumerator()){foreach($literal in $staleLiterals){if($scopeEntry.Value.IndexOf($literal,[System.StringComparison]::OrdinalIgnoreCase) -ge 0){throw "$($scopeEntry.Key) retains stale unequal-role wording: $literal"}}; foreach($pattern in $hierarchyPatterns){if($scopeEntry.Value -match $pattern){throw "$($scopeEntry.Key) retains an evaluation/fallback label or equivalent priority recommendation: $pattern"}}}; foreach($literal in @('need durable local planning identities in Phase 1','only Phase 1 work created by this decision is repository traceability','catalog the resolved selection')){if($research.IndexOf($literal,[System.StringComparison]::OrdinalIgnoreCase) -ge 0){throw "Phase 1 scope expansion remains: $literal"}}; foreach($pattern in @('(?im)^(?![^\r\n]*(?:\bno\b|\bnot\b|\bunchanged\b|\bdo not\b))[^\r\n]*(?:add|create|catalog|assign|register|map)[^\r\n]*(?:MIDI-HW-0[12]|resolved selection|acceptance item)[^\r\n]*(?:Phase 1|traceability|requirement|plan|Linear)[^\r\n]*$','(?im)^(?![^\r\n]*(?:\bno\b|\bnot\b|\bunchanged\b|\bdo not\b))[^\r\n]*(?:Phase 1|traceability|requirement|plan|Linear)[^\r\n]*(?:add|create|catalog|assign|register|map)[^\r\n]*(?:MIDI-HW-0[12]|resolved selection|acceptance item)[^\r\n]*$')){if($research -match $pattern){throw "equivalent Phase 1 catalog/requirement/plan/Linear instruction remains: $pattern"}}; $traceability=[regex]::Match($research,'(?ms)^### Traceability update\s*$(.*?)(?=^## |\z)').Value; $phaseImpact=[regex]::Match($research,'(?ms)^## 7\. Phase 1 Readiness Impact\s*$(.*?)(?=^## |\z)').Value; foreach($regionName in @('traceability','phase impact')){$region=if($regionName -eq 'traceability'){$traceability}else{$phaseImpact}; if([string]::IsNullOrWhiteSpace($region)){throw "$regionName region missing"}; foreach($required in @('documentation gate labels only','no new Phase 1 entity class, catalog entry, requirement, plan, Linear mapping, or implementation work','existing dynamic traceability catalog remains unchanged')){if($region -notmatch [regex]::Escape($required)){throw "$regionName omits scope fence: $required"}}; foreach($gate in @('MIDI-HW-01','MIDI-HW-02')){if($region -notmatch [regex]::Escape($gate)){throw "$regionName omits documentation gate: $gate"}}}; if($research -notmatch [regex]::Escape('Novation has the most thoroughly documented supplied LED/SysEx protocol')){throw 'Novation documentation-strength fact missing'}; if($research -notmatch '(?i)documentation difference[^\r\n]*(?:no|not)[^\r\n]*(?:priority|compatibility|support)'){throw 'documentation distinction lacks non-priority/support caveat'}; foreach($docPath in @($researchPath,$indexPath)){$raw=Get-Content -LiteralPath $docPath -Raw; $links=[regex]::Matches($raw,'\((?:<)?([^)>]+\.pdf)(?:>)?\)'); if($links.Count -lt 4){throw "manual links missing: $docPath"}; foreach($link in $links){$target=[uri]::UnescapeDataString($link.Groups[1].Value); $resolved=Join-Path (Split-Path -Parent $docPath) $target; if(-not (Test-Path -LiteralPath $resolved)){throw "broken manual link: $docPath -> $target"}}}</automated>
  </verify>
  <acceptance_criteria>
    - [ ] The Decision/primary recommendation, capability-matrix planning roles, each of the three numbered per-device findings subsections, HW-14 row, acceptance outcomes, blocker wording, planning-update table, Phase 1 impact, claim-language section, and final recommendation all name the same three-device selected acceptance set with no hierarchy among members; each per-device subsection identifies its controller as one of three equal generic Note/CC learn and soft-takeover hardware acceptance targets.
    - [ ] The traceability note and Phase 1 impact state that MIDI-HW-01/MIDI-HW-02 are documentation gate labels only, create no new Phase 1 entities/catalog entries/requirements/plans/Linear mappings/implementation, and leave the existing dynamic traceability catalog unchanged.
    - [ ] The research preserves that Novation has the most thoroughly documented supplied LED/SysEx protocol, explicitly without treating that documentation advantage as priority, compatibility, or support; Akai and Worlde unknowns remain explicit and every physical acceptance row remains pending.
    - [ ] The research source section and `.planning/midi/README.md` each contain resolving relative links to all four manuals; the index records the exact SHA-256 values above, labels manual evidence separately from physical evidence, and lists remaining probes for each controller.
    - [ ] The exact support-language rules appear in both the research and canonical manual index.
    - [ ] The four user-supplied PDFs remain byte-identical immutable inputs, are absent from executor-owned mutable file lists, and are left for the quick-task orchestrator to stage as supplied planning references.
  </acceptance_criteria>
  <done>The research and manual index preserve the evidence accurately, select all three controllers equally for physical qualification, and make no compatibility/support claim.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize the canonical project and requirement decision gates</name>
  <files>.planning/PROJECT.md, AGENTS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/PROJECT.md` - current MIDI context, MIDI hardware constraint, and Key Decisions table.
    - `AGENTS.md` - generated `GSD:project-start` block whose MIDI constraint must mirror PROJECT.md; leave stack, conventions, architecture, workflow, and profile blocks intact.
    - `.planning/REQUIREMENTS.md` - PLAY-04, PLAY-05, EXTN-04, Open Blockers, Definition of Done, traceability table, and 84/84 coverage totals.
    - `.planning/midi/README.md` - Task 1's canonical selected-set and evidence wording.
    - `.planning/linear-map.json` - schema-1 seed and nullable pending remote-link boundary; inspect only.
    - `C:/Users/Lawrence/.codex/gsd-core/templates/claude-md.md` - generated AGENTS.md marker ownership and the required generator command.
  </read_first>
  <action>In `.planning/PROJECT.md`, replace the unresolved-controller context and selection-gated constraint with the three-device selected set and an evidence-gated support constraint. Add a dated Key Decisions row stating that Akai MIDImix, Novation Launch Control XL Mk2, and Worlde EasyControl 9 together are the selected Phase 6 physical acceptance set for generic MIDI learn/soft takeover; selection does not establish support. After PROJECT.md is saved, do not hand-edit `AGENTS.md`: regenerate its managed sections from canonical sources by running exactly `node "C:\Users\Lawrence\.codex\gsd-core\bin\gsd-tools.cjs" generate-claude-md` from the repository root. Confirm the regenerated `GSD:project-start source:PROJECT.md` block mirrors the canonical MIDI context, constraint, and Key Decision, while the stack, conventions, architecture, skills, workflow, and profile blocks remain semantically unchanged as intended by the generator. Before editing `.planning/REQUIREMENTS.md`, load its committed baseline with `git show HEAD:.planning/REQUIREMENTS.md`. Replace only the hardware decision/evidence-gate section outside the feature catalogs: mark MIDI-HW-01 RESOLVED on 2026-07-19 with all three devices and all four supplied manuals, and add MIDI-HW-02 OPEN requiring independent physical Windows acceptance for each device before named compatibility/support. State the exact hardware revision, firmware, Windows version, and GOLC build evidence dimensions. Preserve the complete `## v1 Requirements` section and the entire `## Traceability` section exactly after newline normalization, including all 84 feature bodies, headings, order, row mappings, and statuses. Keep PLAY-04 and PLAY-05 exactly generic and keep EXTN-04 under its existing deferred heading. Treat MIDI-HW-01 and MIDI-HW-02 as documentation gate labels, not counted feature requirements or traceability-table rows, so the release total stays 84/84; they create no Phase 1 catalog entries, requirement/plan identities, or Linear mappings. Do not edit `.planning/linear-map.json`, call Linear, read credentials or any `.env` file, or create remote IDs.</action>
  <verify>
    <automated>
      $project=Get-Content -LiteralPath '.planning/PROJECT.md' -Raw; $agents=Get-Content -LiteralPath 'AGENTS.md' -Raw; $requirements=Get-Content -LiteralPath '.planning/REQUIREMENTS.md' -Raw;
      $projectMidi=[regex]::Match($project,'(?m)^- \*\*MIDI hardware\*\*: .+$').Value; $agentsMidi=[regex]::Match($agents,'(?m)^- \*\*MIDI hardware\*\*: .+$').Value; if([string]::IsNullOrWhiteSpace($projectMidi) -or $projectMidi -ne $agentsMidi){throw 'PROJECT/AGENTS MIDI constraint drift'}; $projectBlock=[regex]::Match($agents,'(?ms)<!-- GSD:project-start source:PROJECT\.md -->.*?<!-- GSD:project-end -->').Value; if([string]::IsNullOrWhiteSpace($projectBlock)){throw 'generated PROJECT block missing'}; foreach($device in @('Akai MIDImix','Novation Launch Control XL Mk2','Worlde EasyControl 9')){if($project -notmatch [regex]::Escape($device) -or $projectBlock -notmatch [regex]::Escape($device) -or $requirements -notmatch [regex]::Escape($device)){throw "canonical device missing: $device"}}; foreach($phrase in @('selected Phase 6 physical acceptance set','selection does not establish support','MIDI-HW-02')){if($project -notmatch [regex]::Escape($phrase) -or $projectBlock -notmatch [regex]::Escape($phrase)){throw "generated PROJECT block does not mirror MIDI wording: $phrase"}}; foreach($stale in @('The initial MIDI controller is not yet selected','until the target controller is selected')){if($project.IndexOf($stale,[System.StringComparison]::OrdinalIgnoreCase) -ge 0 -or $projectBlock.IndexOf($stale,[System.StringComparison]::OrdinalIgnoreCase) -ge 0){throw "stale PROJECT/AGENTS selection wording remains: $stale"}};
      $baselineAgents=(git show HEAD:AGENTS.md | Out-String); if($LASTEXITCODE -ne 0){throw 'cannot load baseline AGENTS.md'}; $normalizeTrimmed={param($text) (($text -replace "`r`n","`n").TrimEnd())}; foreach($section in @('stack','conventions','architecture','skills','workflow')){$pattern="(?ms)<!-- GSD:$section-start.*?<!-- GSD:$section-end -->"; $current=[regex]::Match($agents,$pattern).Value; $baseline=[regex]::Match($baselineAgents,$pattern).Value; if([string]::IsNullOrWhiteSpace($current) -or (& $normalizeTrimmed $current) -ne (& $normalizeTrimmed $baseline)){throw "generated AGENTS block changed unexpectedly: $section"}}; $profilePattern='(?ms)<!-- GSD:profile-start.*?<!-- GSD:profile-end -->'; if((& $normalizeTrimmed ([regex]::Match($agents,$profilePattern).Value)) -ne (& $normalizeTrimmed ([regex]::Match($baselineAgents,$profilePattern).Value))){throw 'AGENTS profile block changed unexpectedly'};
      $baselineRequirements=(git show HEAD:.planning/REQUIREMENTS.md | Out-String); if($LASTEXITCODE -ne 0){throw 'cannot load baseline REQUIREMENTS.md'}; $normalizeNewlines={param($text) (($text -replace "`r`n","`n") -replace "`r","`n")}; $v1Pattern='(?ms)^## v1 Requirements\r?\n.*?(?=^## v1\.x Requirements\r?$)'; $tracePattern='(?ms)^## Traceability\r?\n.*?(?=^---\r?$|\z)'; $currentV1=[regex]::Match($requirements,$v1Pattern).Value; $baselineV1=[regex]::Match($baselineRequirements,$v1Pattern).Value; $currentTrace=[regex]::Match($requirements,$tracePattern).Value; $baselineTrace=[regex]::Match($baselineRequirements,$tracePattern).Value; if([string]::IsNullOrWhiteSpace($currentV1) -or [string]::IsNullOrWhiteSpace($baselineV1)){throw 'complete v1 Requirements section missing'}; if([string]::IsNullOrWhiteSpace($currentTrace) -or [string]::IsNullOrWhiteSpace($baselineTrace)){throw 'complete Traceability section missing'}; if((& $normalizeNewlines $currentV1) -cne (& $normalizeNewlines $baselineV1)){throw 'v1 requirement bodies, headings, or order changed from HEAD'}; if((& $normalizeNewlines $currentTrace) -cne (& $normalizeNewlines $baselineTrace)){throw 'Traceability rows, mappings, statuses, or order changed from HEAD'}; $v1Ids=[regex]::Matches($currentV1,'(?m)^- \[ \] \*\*([A-Z]+-\d+)\*\*:') | ForEach-Object {$_.Groups[1].Value}; $traceIds=[regex]::Matches($currentTrace,'(?m)^\| ([A-Z]+-\d+) \| Phase \d+ \|') | ForEach-Object {$_.Groups[1].Value}; foreach($entry in @(@{Name='v1 requirements';Ids=@($v1Ids)},@{Name='traceability rows';Ids=@($traceIds)})){if($entry.Ids.Count -ne 84 -or ($entry.Ids | Sort-Object -Unique).Count -ne 84){throw "$($entry.Name) must contain exactly 84 unique IDs"}}; if($currentV1 -match 'MIDI-HW-0[12]' -or $currentTrace -match 'MIDI-HW-0[12]'){throw 'documentation gate was promoted into a feature catalog'};
      $baselineGate=[regex]::Match($baselineRequirements,'(?ms)^## Open Blockers\r?\n.*?(?=^## Definition of Done\r?$)').Value; $currentGate=[regex]::Match($requirements,'(?ms)^## (?:Open Blockers|MIDI Hardware Decision and Evidence Gates)\r?\n.*?(?=^## Definition of Done\r?$)').Value; if([string]::IsNullOrWhiteSpace($baselineGate) -or [string]::IsNullOrWhiteSpace($currentGate)){throw 'hardware decision/evidence-gate section boundary missing'}; $baselineOutside=$baselineRequirements.Replace($baselineGate,"<MIDI-HARDWARE-GATES>`n"); $currentOutside=$requirements.Replace($currentGate,"<MIDI-HARDWARE-GATES>`n"); if((& $normalizeNewlines $currentOutside) -cne (& $normalizeNewlines $baselineOutside)){throw 'REQUIREMENTS.md changed outside the hardware decision/evidence-gate section'};
      if($requirements -notmatch 'MIDI-HW-01[^\r\n]*RESOLVED' -or $requirements -notmatch 'MIDI-HW-02[^\r\n]*OPEN'){throw 'hardware gate state mismatch'}; foreach($manual in @('Akai-MIDImix-UserGuide-v1.0.pdf','launch_control_xl_programmer_s_reference_guide.pdf','Novation-Launch Control XL GSG v2.pdf','Worlde-EasyControl-9-UserManual.pdf')){if($requirements -notmatch [regex]::Escape($manual)){throw "resolved gate omits supplied manual: $manual"}}; if($requirements -notmatch [regex]::Escape('**PLAY-04**: A show author can map generic MIDI Note and Control Change input to supported playback commands.') -or $requirements -notmatch [regex]::Escape('**PLAY-05**: MIDI fader mappings support soft takeover so connecting or moving a controller does not cause unintended value jumps.') -or $requirements -notmatch [regex]::Escape('**EXTN-04**: A user can install device-specific MIDI profiles after target controllers are validated.')){throw 'generic/deferred MIDI requirement drift'}; git diff --exit-code -- .planning/linear-map.json; if($LASTEXITCODE -ne 0){throw 'linear-map.json changed'}
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] PROJECT.md is updated first, then AGENTS.md is regenerated with the documented `generate-claude-md` command; the generated project block mirrors the canonical MIDI wording and every unrelated generated/profile block remains semantically unchanged.
    - [ ] PROJECT.md contains a dated Key Decision for the complete three-controller acceptance set.
    - [ ] REQUIREMENTS.md marks MIDI-HW-01 RESOLVED, adds MIDI-HW-02 OPEN, and includes the independent evidence dimensions and exact support-language rule.
    - [ ] The complete v1 requirement catalog and entire Traceability section are normalized-newline exact matches to `git show HEAD:.planning/REQUIREMENTS.md`; all 84 unique requirement bodies, their order, all 84 mappings/statuses, PLAY-04/PLAY-05 generic scope, and EXTN-04 deferred scope remain unchanged, and no REQUIREMENTS.md content outside the hardware decision/evidence-gate section changes.
    - [ ] `.planning/linear-map.json` is unchanged and contains no invented remote linkage.
  </acceptance_criteria>
  <done>The canonical project and requirements record the selected set and independent evidence gate consistently while preserving feature scope and performing no remote Linear or credential activity.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the authorized roadmap/state wording and verify Phase 1 read-only</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/research/FEATURES.md</files>
  <read_first>
    - `.planning/ROADMAP.md` - Phase 1 eight requirement IDs and the Phase 6 goal, success criteria, nine requirement IDs, and blocker text; the user-authorized edit is limited to the Phase 6 blocker line.
    - `.planning/STATE.md` - Phase 6 decision, blocker/concern list, deferred MIDI row, execution position, and quick-task append location owned by the orchestrator.
    - `.planning/research/FEATURES.md` - generic MIDI recommendation, deferred advanced profiles, and the stale open controller-target question.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/*-PLAN.md` - existing Phase 1 plan set; inspect and validate read-only, with no edits to any plan.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` - D-11 repository authority, D-14 stable identities, Phase 1 boundary, and prohibition on invented remote UUIDs.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` - existing 29-plan/33-task validation map and false Nyquist/Wave 0 state that execution, not this task, must later satisfy.
  </read_first>
  <action>The user explicitly authorized one ROADMAP exception for this quick task: replace only the Phase 6 `**Blocker:**` MIDI gate line with MIDI-HW-01 RESOLVED wording that selects Akai MIDImix, Novation Launch Control XL Mk2, and Worlde EasyControl 9 together, plus MIDI-HW-02 OPEN independent physical-evidence wording. Do not change the Phase 6 goal, success criteria, requirement list, implementation scope, or any other roadmap phase. In STATE.md, replace the existing Phase 6 Decisions bullet that still makes generic MIDI scope conditional on a pending controller choice with a complete three-device selected-set decision for generic Note/CC learn and soft takeover plus the MIDI-HW-02 independent per-device evidence gate. Also record MIDI-HW-01 as RESOLVED with all three devices and all four supplied manual filenames in Blockers/Concerns, record MIDI-HW-02 as OPEN with independent physical acceptance required for each device, and change the deferred device-profile/feedback row to v1.x gated by both MIDI-HW-02 and EXTN-04. Load `git show HEAD:.planning/STATE.md` as the preservation baseline and leave its milestone, current phase number/name, execution status, stopped position, Current focus, frontmatter and rendered progress, Phase/Plan position, and `Status: Ready to execute` semantics unchanged. Do not edit Last Activity or the quick-task ledger; the quick-task orchestrator owns those later bookkeeping changes. In FEATURES.md, replace the open controller-selection question with the complete selected three-device set and the remaining independent per-device evidence question. Preserve generic MIDI Note/CC learn and soft takeover in v1, and make device-specific profiles/feedback remain v1.x after each device's evidence passes MIDI-HW-02 and EXTN-04. Do not modify any Phase 1 plan or `01-VALIDATION.md`. Verify the existing Phase 1 plan set read-only through plan-structure validation, locked-decision coverage, Phase 1 requirement coverage, and `init.execute-phase 1`; retain `nyquist_compliant: false` and `wave_0_complete: false` because Phase 1 execution has not occurred.</action>
  <verify>
    <automated>
      $gsd='C:\Users\Lawrence\.codex\gsd-core\bin\gsd-tools.cjs'; $phase='.planning/phases/01-offline-foundation-and-delivery-traceability';
      $requirements=Get-Content -LiteralPath '.planning/REQUIREMENTS.md' -Raw; $roadmap=Get-Content -LiteralPath '.planning/ROADMAP.md' -Raw; $state=Get-Content -LiteralPath '.planning/STATE.md' -Raw; $features=Get-Content -LiteralPath '.planning/research/FEATURES.md' -Raw; $validation=Get-Content -LiteralPath "$phase/01-VALIDATION.md" -Raw;
      $baselineState=(git show HEAD:.planning/STATE.md | Out-String); if($LASTEXITCODE -ne 0){throw 'cannot load baseline STATE.md'}; $normalizeState={param($text) (($text -replace "`r`n","`n") -replace "`r","`n").TrimEnd()};
      $releaseSection=[regex]::Match($requirements,'(?s)## v1 Requirements(.*?)## v1\.x Requirements').Groups[1].Value; $reqIds=[regex]::Matches($releaseSection,'(?m)^- \[ \] \*\*([A-Z]+-\d+)\*\*:') | ForEach-Object {$_.Groups[1].Value}; $traceSection=[regex]::Match($requirements,'(?s)## Traceability(.*?)(?:\r?\n## |\r?\n---)').Groups[1].Value; $traceIds=[regex]::Matches($traceSection,'(?m)^\| ([A-Z]+-\d+) \| Phase \d+ \|') | ForEach-Object {$_.Groups[1].Value}; $roadIds=[regex]::Matches($roadmap,'(?m)^\*\*Requirements:\*\* (.+)$') | ForEach-Object {$_.Groups[1].Value -split ',\s*'}; foreach($set in @(@($reqIds),@($traceIds),@($roadIds))){if($set.Count -ne 84 -or ($set | Sort-Object -Unique).Count -ne 84){throw '84/84 traceability count or uniqueness failed'}}; if((Compare-Object ($reqIds|Sort-Object) ($traceIds|Sort-Object)) -or (Compare-Object ($reqIds|Sort-Object) ($roadIds|Sort-Object))){throw 'requirement/traceability/roadmap set mismatch'};
      $baselineRoadmap=(git show HEAD:.planning/ROADMAP.md | Out-String); if($LASTEXITCODE -ne 0){throw 'cannot load baseline ROADMAP'}; $phase6Pattern='(?s)### Phase 6:.*?(?=### Phase 7:)'; $phase6=[regex]::Match($roadmap,$phase6Pattern).Value; $baselinePhase6=[regex]::Match($baselineRoadmap,$phase6Pattern).Value; if([string]::IsNullOrWhiteSpace($phase6) -or [string]::IsNullOrWhiteSpace($baselinePhase6)){throw 'Phase 6 section missing'}; $normalize={param($text) (($text -replace "`r`n","`n").Trim())}; $roadmapOutside=& $normalize ($roadmap.Replace($phase6,'<PHASE-6>')); $baselineOutside=& $normalize ($baselineRoadmap.Replace($baselinePhase6,'<PHASE-6>')); if($roadmapOutside -ne $baselineOutside){throw 'ROADMAP change exists outside Phase 6'}; $phase6WithoutBlocker=& $normalize ([regex]::Replace($phase6,'(?m)^\*\*Blocker:\*\*.*(?:\r?\n)?','')); $baselinePhase6WithoutBlocker=& $normalize ([regex]::Replace($baselinePhase6,'(?m)^\*\*Blocker:\*\*.*(?:\r?\n)?','')); if($phase6WithoutBlocker -ne $baselinePhase6WithoutBlocker){throw 'Phase 6 change is not limited to the Blocker line'}; $phase6Ids=[regex]::Match($phase6,'(?m)^\*\*Requirements:\*\* (.+)$').Groups[1].Value -split ',\s*'; if(($phase6Ids -join ',') -ne 'PLAY-01,PLAY-02,PLAY-03,PLAY-04,PLAY-05,PLAY-06,PLAY-07,PLAY-08,PLAY-09'){throw 'Phase 6 requirement scope drift'}; if($phase6 -notmatch 'MIDI-HW-01[^\r\n]*RESOLVED' -or $phase6 -notmatch 'MIDI-HW-02[^\r\n]*OPEN'){throw 'Phase 6 MIDI gate state mismatch'};
      $devices=@('Akai MIDImix','Novation Launch Control XL Mk2','Worlde EasyControl 9'); foreach($device in $devices){if($phase6 -notmatch [regex]::Escape($device) -or $state -notmatch [regex]::Escape($device) -or $features -notmatch [regex]::Escape($device)){throw "roadmap/state/features device missing: $device"}};
      $stateInvariantPatterns=[ordered]@{'gsd_state_version'='(?m)^gsd_state_version:\s*.+\r?$';'milestone'='(?m)^milestone:\s*.+\r?$';'milestone_name'='(?m)^milestone_name:\s*.+\r?$';'current_phase'='(?m)^current_phase:\s*.+\r?$';'current_phase_name'='(?m)^current_phase_name:\s*.+\r?$';'status frontmatter'='(?m)^status:\s*.+\r?$';'stopped_at'='(?m)^stopped_at:\s*.+\r?$';'progress frontmatter'='(?ms)^progress:\r?\n(?:  [^\r\n]+\r?\n)+(?=---\r?$)';'Current focus'='(?m)^\*\*Current focus:\*\* .+\r?$';'Phase position'='(?m)^Phase: .+\r?$';'Plan position'='(?m)^Plan: .+\r?$';'Ready status'='(?m)^Status: .+\r?$';'rendered progress'='(?m)^Progress: .+\r?$'}; foreach($entry in $stateInvariantPatterns.GetEnumerator()){$current=[regex]::Match($state,$entry.Value).Value; $baseline=[regex]::Match($baselineState,$entry.Value).Value; if([string]::IsNullOrWhiteSpace($current) -or [string]::IsNullOrWhiteSpace($baseline)){throw "STATE invariant missing: $($entry.Key)"}; if((& $normalizeState $current) -cne (& $normalizeState $baseline)){throw "STATE invariant changed from HEAD: $($entry.Key)"}}; if($state -notmatch '(?m)^\*\*Current focus:\*\* Phase 1 - Offline Foundation and Delivery Traceability\r?$' -or $state -notmatch '(?m)^Status: Ready to execute\r?$'){throw 'STATE no longer preserves Phase 1 ready-to-execute semantics'};
      $stateDecisions=[regex]::Match($state,'(?ms)^### Decisions\s*$(.*?)(?=^### |^## |\z)').Value; $phase6Decision=[regex]::Match($stateDecisions,'(?im)^- \[Phase 6\]:[^\r\n]+$').Value; if([string]::IsNullOrWhiteSpace($phase6Decision) -or $phase6Decision -notmatch 'MIDI-HW-02' -or $phase6Decision -notmatch 'independent' -or $phase6Decision -notmatch 'Note/CC' -or $phase6Decision -notmatch 'soft takeover'){throw 'STATE Phase 6 decision lacks complete generic-control and independent-evidence wording'}; foreach($device in $devices){if($phase6Decision -notmatch [regex]::Escape($device)){throw "STATE Phase 6 decision omits selected-set member: $device"}};
      $stateGate=[regex]::Match($state,'(?ms)^### Blockers/Concerns\s*$(.*?)(?=^## |\z)').Value; if($stateGate -notmatch 'MIDI-HW-01[^\r\n]*RESOLVED'){throw 'STATE does not record MIDI-HW-01 RESOLVED'}; if($stateGate -notmatch 'MIDI-HW-02[^\r\n]*OPEN[^\r\n]*independent[^\r\n]*(?:each|per-device)'){throw 'STATE does not record MIDI-HW-02 OPEN with independent per-device acceptance'}; foreach($device in $devices){if($stateGate -notmatch [regex]::Escape($device)){throw "STATE resolved gate omits device: $device"}}; foreach($manual in @('Akai-MIDImix-UserGuide-v1.0.pdf','launch_control_xl_programmer_s_reference_guide.pdf','Novation-Launch Control XL GSG v2.pdf','Worlde-EasyControl-9-UserManual.pdf')){if($stateGate -notmatch [regex]::Escape($manual)){throw "STATE resolved gate omits manual: $manual"}}; $deferredMidi=[regex]::Match($state,'(?im)^\| MIDI \| Device-specific profiles and feedback \|[^\r\n]+$').Value; if([string]::IsNullOrWhiteSpace($deferredMidi) -or $deferredMidi -notmatch 'v1\.x' -or $deferredMidi -notmatch 'MIDI-HW-02' -or $deferredMidi -notmatch 'EXTN-04'){throw 'STATE deferred profiles/feedback must be v1.x and gated by MIDI-HW-02 plus EXTN-04'};
      $stateForbidden=@('until hardware is selected','initial MIDI controller is unresolved','The initial MIDI controller is not yet selected','until the target controller is selected','target controller is not yet selected','controller selection remains open','blocked pending user selection','Blocked by `MIDI-HW-01`','Select the Novation','should first use','Novation selection','Initial target','initial physical','selected acceptance target now','selected Novation','selection resolved to Novation','the Novation decision','selected hardware acceptance target','strongest fallback','fallback candidate','evaluation-only','evaluation candidate','evaluated candidates','comparison/fallback candidate','comparison/fallback','comparison-only'); foreach($literal in $stateForbidden){if($state.IndexOf($literal,[System.StringComparison]::OrdinalIgnoreCase) -ge 0){throw "stale STATE pending-selection or single-device wording remains: $literal"}}; foreach($pattern in @('(?i)(Akai|Worlde)[^\r\n]{0,100}\b(secondary|fallback|evaluation|alternative|backup|contingency|less preferred|lower priority)\b','(?i)Novation[^\r\n]{0,100}\b(primary|preferred|priority|favored|first choice|ahead of|only candidate)\b')){if($state -match $pattern){throw "STATE retains an equivalent single-device priority recommendation: $pattern"}};
      if($features -notmatch 'selected Phase 6 physical acceptance set'){throw 'FEATURES lacks the complete selected-set statement'}; foreach($stale in @('Which MIDI controllers are the initial compatibility targets','controller selection remains open','target controller is not yet selected')){if($features.IndexOf($stale,[System.StringComparison]::OrdinalIgnoreCase) -ge 0){throw "stale open-selection wording remains in FEATURES: $stale"}}; $featureV1=[regex]::Match($features,'(?ms)^### Launch With \(v1\)\s*$(.*?)(?=^### Add After)').Value; if($featureV1 -notmatch 'MIDI Note/CC learn' -or $featureV1 -notmatch 'soft takeover'){throw 'FEATURES no longer preserves generic MIDI in v1'}; $featureV1x=[regex]::Match($features,'(?ms)^### Add After Validation \(v1\.x\)\s*$(.*?)(?=^### Future Consideration)').Value; foreach($required in @('device-specific','profiles','feedback','MIDI-HW-02','EXTN-04','independent')){if($featureV1x -notmatch [regex]::Escape($required)){throw "FEATURES v1.x per-device gate wording missing: $required"}};
      if($validation -notmatch 'nyquist_compliant: false' -or $validation -notmatch 'wave_0_complete: false'){throw 'execution-only validation flags changed'}; $phasePlanStatus=git status --porcelain=v1 -- "$phase/*-PLAN.md"; if($phasePlanStatus){throw "Phase 1 plan modified: $($phasePlanStatus -join ', ')"}; $phasePlans=Get-ChildItem -LiteralPath $phase -Filter '*-PLAN.md'; foreach($planFile in $phasePlans){$front=(node $gsd query frontmatter.validate $planFile.FullName --schema plan | ConvertFrom-Json); if(-not $front.valid){throw "invalid phase-plan frontmatter: $($planFile.Name)"}; $structure=(node $gsd query verify.plan-structure $planFile.FullName | ConvertFrom-Json); if(-not $structure.valid){throw "invalid phase plan: $($planFile.Name)"}}; $coverage=node $gsd query check.decision-coverage-plan $phase "$phase/01-CONTEXT.md" | ConvertFrom-Json; if(-not $coverage.passed){throw "decision coverage failed: $($coverage.uncovered -join ', ')"}; $phase1=[regex]::Match($roadmap,'(?s)### Phase 1:.*?(?=### Phase 2:)').Value; $phase1Reqs=[regex]::Match($phase1,'(?m)^\*\*Requirements:\*\* (.+)$').Groups[1].Value -split ',\s*'; $plannedReqs=$phasePlans | ForEach-Object {[regex]::Match((Get-Content -LiteralPath $_.FullName -Raw),'(?m)^requirements:\s*\[([^\]]*)\]').Groups[1].Value -split ',\s*'} | Where-Object {$_}; $missingPhase1=@($phase1Reqs | Where-Object {$_ -notin $plannedReqs}); if($phase1Reqs.Count -ne 8 -or $missingPhase1.Count -ne 0){throw "Phase 1 requirement coverage failed: $($missingPhase1 -join ', ')"}; $init=node $gsd query init.execute-phase 1 | ConvertFrom-Json; if(-not $init.phase_found -or $init.plan_count -ne 29 -or $init.incomplete_count -ne 29 -or $init.missing_agents.Count -ne 0){throw 'Phase 1 execute initialization is not ready'};
      $quickPath='.planning/quick/260719-pgw-research-akai-midimix-novation-launch-co/260719-pgw-PLAN.md'; $quickRaw=Get-Content -LiteralPath $quickPath -Raw; $quickFront=node $gsd query frontmatter.validate $quickPath --schema plan | ConvertFrom-Json; $quickStructure=node $gsd query verify.plan-structure $quickPath | ConvertFrom-Json; if(-not $quickFront.valid -or -not $quickStructure.valid){throw 'quick plan frontmatter or structure invalid'}; $expectedMutable=@('.planning/quick/260719-pgw-research-akai-midimix-novation-launch-co/260719-pgw-RESEARCH.md','.planning/midi/README.md','.planning/PROJECT.md','AGENTS.md','.planning/REQUIREMENTS.md','.planning/ROADMAP.md','.planning/STATE.md','.planning/research/FEATURES.md'); $frontFilesBlock=[regex]::Match($quickRaw,'(?ms)^files_modified:\r?\n(.*?)(?=^autonomous:)').Groups[1].Value; $frontFiles=[regex]::Matches($frontFilesBlock,'(?m)^\s+-\s+(.+?)\s*\r?$') | ForEach-Object {$_.Groups[1].Value.Trim().Trim('"')}; $taskFileBlocks=[regex]::Matches($quickRaw,'(?ms)<task type="auto">\s*<name>.*?</name>\s*<files>(.*?)</files>'); $taskFiles=$taskFileBlocks | ForEach-Object {$_.Groups[1].Value -split ','} | ForEach-Object {$_.Trim()}; foreach($entry in @(@{Name='frontmatter';Files=@($frontFiles)},@{Name='task ownership';Files=@($taskFiles)})){if($entry.Files.Count -ne 8 -or ($entry.Files | Sort-Object -Unique).Count -ne 8 -or (Compare-Object ($expectedMutable | Sort-Object) ($entry.Files | Sort-Object))){throw "$($entry.Name) mutable scope must be exactly the eight approved documents"}; if($entry.Files -match '(?i)\.pdf$'){throw "$($entry.Name) incorrectly owns an immutable manual"}}; foreach($pdf in @('Akai-MIDImix-UserGuide-v1.0.pdf','launch_control_xl_programmer_s_reference_guide.pdf','Novation-Launch Control XL GSG v2.pdf','Worlde-EasyControl-9-UserManual.pdf')){git ls-files --error-unmatch -- ".planning/midi/$pdf" | Out-Null; if($LASTEXITCODE -ne 0){throw "manual not tracked: $pdf"}}; git diff --exit-code -- .planning/linear-map.json; if($LASTEXITCODE -ne 0){throw 'linear-map.json changed'}; git diff --check; if($LASTEXITCODE -ne 0){throw 'git diff/Markdown whitespace check failed'}
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] Per the user's explicit quick-task exception, the only ROADMAP change is the Phase 6 Blocker line: it names the three-device set, marks MIDI-HW-01 resolved, and keeps MIDI-HW-02 open; Phase 6 goal, success criteria, nine PLAY requirement IDs, implementation scope, and every other roadmap phase are byte-for-byte unchanged from HEAD.
    - [ ] STATE.md replaces its stale Phase 6 Decisions bullet with the complete three-device generic Note/CC learn and soft-takeover set plus MIDI-HW-02 independent evidence gate; Blockers/Concerns records MIDI-HW-01 RESOLVED with all three devices and four manuals, MIDI-HW-02 OPEN, and device-specific profiles/feedback as v1.x gated by MIDI-HW-02 and EXTN-04.
    - [ ] Against `git show HEAD:.planning/STATE.md`, milestone, current phase number/name, execution status, stopped position, Current focus, both progress representations, Phase/Plan position, and `Status: Ready to execute` are unchanged; only the intended MIDI decision/gate/deferred wording is changed before orchestrator-owned Last Activity/quick-task bookkeeping.
    - [ ] FEATURES.md no longer treats controller selection as open, names the complete selected set, preserves generic MIDI Note/CC learn and soft takeover in v1, and keeps device-specific profiles/feedback in v1.x after independent MIDI-HW-02 evidence and EXTN-04.
    - [ ] No Phase 1 PLAN.md file changes; all 29 existing plans pass frontmatter and structure validation, all 21 locked Phase 1 decisions remain covered, every one of Phase 1's eight ROADMAP requirement IDs appears in the existing plan frontmatter, and `init.execute-phase 1` returns 29 executable incomplete plans with no missing agents.
    - [ ] Requirement body, traceability table, and ROADMAP sets are identical and each contains 84 unique release IDs; Phase 6 remains PLAY-01 through PLAY-09.
    - [ ] All manual/research links resolve, all four manuals are tracked planning references, `.planning/linear-map.json` is unchanged, and git diff checks pass.
    - [ ] `01-VALIDATION.md` still has `nyquist_compliant: false` and `wave_0_complete: false`.
  </acceptance_criteria>
  <done>Canonical planning is internally consistent, the only ROADMAP edit is the user-authorized Phase 6 blocker wording, and deterministic read-only checks prove the unchanged Phase 1 plan set remains execution-ready.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Required outcome | Task | Status |
|---|---|---|---:|---|
| GOAL | quick-task | Research all supplied manuals, clear the controller blocker, update planning docs, and verify Phase 1 readiness | 1-3 | COVERED |
| CONTEXT | MIDI-HW-01 | Resolve the documentation gate to the complete Akai/Novation/Worlde set with supplied manuals; create no Phase 1 catalog or requirement identity | 1-3 | COVERED |
| CONTEXT | MIDI-HW-02 | Keep the documentation gate open for independent exact-revision/firmware/Windows/GOLC-build physical evidence; create no Phase 1 catalog or requirement identity | 1-3 | COVERED |
| REQ | PLAY-04 | Preserve generic MIDI Note/CC learn | 2-3 | COVERED |
| REQ | PLAY-05 | Preserve generic soft takeover | 2-3 | COVERED |
| REQ | EXTN-04 | Preserve deferred device-specific profiles | 2-3 | COVERED |
| RESEARCH | manuals | Preserve documented device differences, all unknowns, and four source hashes | 1 | COVERED |
| RESEARCH | acceptance matrix | Keep every device's physical evidence pending and independent | 1 | COVERED |
| RESEARCH | planning drift | Correct research, project, requirements, the user-authorized Phase 6 blocker line, state, feature research, and generated constraint wording | 1-3 | COVERED |
| RESEARCH | Phase 1 traceability scope | Keep MIDI-HW-01/02 as documentation gate labels only and leave the existing dynamic traceability catalog unchanged | 1 | COVERED |
| CONTEXT | LOCKED-01 | Treat all three user-owned controllers together as the selected hardware acceptance set | 1-3 | COVERED |
| CONTEXT | scope fence | Do not claim support, expand device-specific implementation, call Linear, inspect credentials, or invent remote IDs | 1-3 | COVERED |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User-supplied PDF manuals -> planning claims | Binary reference material is translated into capability and unknown statements. |
| Planning decision gates -> future support claims | Selection can be mistaken for validated compatibility unless evidence status is explicit. |
| Repository planning records -> Linear boundary | Documentation changes must not read credentials, inspect `.env`, call Linear, or create remote identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-Q-MIDI-01 | Tampering | manual references | medium | mitigate | Treat the PDFs as immutable user-supplied inputs outside executor ownership, record exact SHA-256 values, and verify every hash deterministically before the orchestrator stages them as planning references. |
| T-Q-MIDI-02 | Spoofing | compatibility/support wording | high | mitigate | Separate selection, manual evidence, physical evidence, and support states; require independent MIDI-HW-02 evidence per exact device environment. |
| T-Q-MIDI-03 | Tampering | requirements/roadmap traceability | high | mitigate | Compare the complete v1 requirement and Traceability sections to their HEAD baselines, require exactly 84 unique IDs/rows, compare the roadmap ID set, and lock Phase 6 to its nine existing PLAY IDs. |
| T-Q-MIDI-04 | Elevation of Privilege | Phase 1/Linear scope | high | mitigate | Prohibit every Phase 1 plan change, leave the schema-1 map unchanged, and prohibit `.env`, credential, or remote Linear access. |
</threat_model>

<verification>
- All task-level automated checks pass in PowerShell without credentials, network access, or physical hardware.
- All four manual hashes equal the recorded values and all relative links resolve.
- The complete v1 requirement section and Traceability section are normalized-newline exact matches to HEAD, retaining all 84 bodies, order, mappings, and statuses; roadmap mappings contain the same 84 unique IDs.
- Every existing Phase 1 plan passes frontmatter and structure validation; all 21 CONTEXT decisions and all eight Phase 1 ROADMAP requirements remain covered; `init.execute-phase 1` reports the existing 29 plans ready for dispatch.
- No Phase 1 plan differs from HEAD; `01-VALIDATION.md` retains false Nyquist and Wave 0 flags because execution has not occurred.
- The Linear seed is unchanged and Phase 1 validation flags remain false until execution.
- `git diff --check` passes; executor-owned mutable scope is exactly eight planning documents, while the four hash-verified PDFs remain immutable orchestrator-staged inputs.
</verification>

<success_criteria>
- All three controllers are selected equally for Phase 6 physical acceptance and none is called supported.
- MIDI-HW-01 is closed and MIDI-HW-02 remains an independent per-device evidence gate.
- Manual evidence is durable, hash-verifiable, and explicit about unknowns.
- Canonical planning documents agree without changing release feature counts or Phase 6 implementation scope.
- Phase 1 execution readiness remains green under read-only checks with every Phase 1 plan unchanged.
</success_criteria>

<output>
Create `.planning/quick/260719-pgw-research-akai-midimix-novation-launch-co/260719-pgw-SUMMARY.md` when done.
</output>
