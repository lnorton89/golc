---
phase: quick-desktop-views-documentation
plan: 260729-gq6
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/shell/desktopViews.json
  - frontend/src/shell/navigation.ts
  - frontend/src/shell/AppShell.navigation.test.tsx
  - internal/docgen/desktopviews.go
  - internal/docgen/docgen_test.go
  - frontend/e2e/desktop-view-docs.spec.ts
  - frontend/package.json
  - site/src/content/desktop-views.json
  - site/public/desktop-views/*.png
  - site/src/app/docs/desktop-views/page.tsx
  - site/src/components/docs/DesktopViewExplorer.tsx
  - site/src/app/docs/page.tsx
  - site/src/app/sitemap.ts
  - site/tests/visual.spec.ts
  - site/tests/visual.spec.ts-snapshots/desktop-views-light-chromium-linux.png
  - site/tests/visual.spec.ts-snapshots/desktop-views-dark-chromium-linux.png
autonomous: true
requirements: [FDUI-01, FDUI-02, FDUI-03]
must_haves:
  truths:
    - "A reader can browse every current Show, Build, Operate, and Output desktop destination, see an actual application screenshot, and understand the view's purpose and principal operator actions."
    - "The desktop navigation and published view catalog derive from one versioned, repository-owned structured catalog; adding, removing, renaming, or regrouping a destination cannot leave the documentation inventory silently stale."
    - "A documented command captures all desktop-view screenshots from the real frontend at a fixed viewport with deterministic settling and stable filenames."
    - "The generated site catalog remains machine-readable and versioned so a later GOLC workspace can ingest the same content without scraping rendered HTML."
    - "The independent golc-site repository builds, links, type-checks, lints, and visually tests the new static-export route."
  artifacts:
    - path: frontend/src/shell/desktopViews.json
      provides: "Versioned canonical catalog for all desktop destinations, groups, documentation copy, stable slugs, and screenshot paths."
    - path: internal/docgen/desktopviews.go
      provides: "Strict deterministic validation and generation of the site-owned catalog copy."
    - path: frontend/e2e/desktop-view-docs.spec.ts
      provides: "Repeatable real-browser screenshot capture for every catalog destination."
    - path: site/src/content/desktop-views.json
      provides: "Generated structured content committed inside the independently buildable site submodule."
    - path: site/src/app/docs/desktop-views/page.tsx
      provides: "Static-exported, responsive desktop views documentation route."
  key_links:
    - from: frontend/src/shell/navigation.ts
      to: frontend/src/shell/desktopViews.json
      via: "NAV_GROUPS is projected from the canonical catalog rather than maintaining a second destination list."
      pattern: "desktopViews"
    - from: internal/docgen/desktopviews.go
      to: site/src/content/desktop-views.json
      via: "The existing offline docs generator validates and writes the normalized catalog into the submodule."
      pattern: "desktop-views.json"
    - from: frontend/e2e/desktop-view-docs.spec.ts
      to: site/public/desktop-views/*.png
      via: "The capture loop reads catalog IDs/labels, opens the matching real workspace, waits for its heading, and writes the catalog-declared image."
      pattern: "desktop-views"
    - from: site/src/app/docs/desktop-views/page.tsx
      to: site/src/content/desktop-views.json
      via: "The page renders its complete inventory and copy from generated structured content."
      pattern: "desktop-views.json"
---

<objective>
Add a maintainable desktop views documentation page whose inventory, copy, and screenshots follow the real GOLC desktop application instead of a hand-maintained site-only approximation.

Purpose: The existing site docs describe five representative concepts, but the shipped shell now exposes twelve routed workspaces. A versioned catalog, deterministic generator, and real-browser capture tool make the documentation useful today and reusable by a future in-app help surface.
Output: A canonical desktop-view catalog consumed by navigation, generated site content, one screenshot per current workspace, a responsive `/docs/desktop-views` page, and submodule/parent commits that preserve repository boundaries.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.gitmodules
@.planning/STATE.md
@frontend/src/shell/navigation.ts
@frontend/src/shell/WorkspaceRouter.tsx
@frontend/src/shell/AppShell.navigation.test.tsx
@frontend/playwright.config.ts
@frontend/e2e/responsive.spec.ts
@internal/command/docs.go
@internal/docgen/docgen.go
@site/AGENTS.md
@site/README.md
@site/package.json
@site/src/app/docs/page.tsx
@site/src/components/docs/docsData.ts
@site/src/components/docs/ViewExplorer.tsx
@site/src/app/sitemap.ts
@site/playwright.config.ts
@site/tests/visual.spec.ts

The parent worktree already contains unrelated modifications in `cmd/golc-desktop/main.go`, `frontend/src/components/ArtnetConfig/*`, `frontend/src/lib/wailsBridge.ts`, `internal/command/build.go`, and `internal/wails/*`. Do not revert, format, stage, or commit those paths. Recheck both `git status --short` and `git -C site status --short` before every commit.

The site is the `https://github.com/lnorton89/golc-site.git` submodule. A site artifact is not complete until it is committed inside `site/`, then the parent repository records that exact submodule pointer. Never stage site files directly from the parent repository.

Before changing Next.js code, read the installed Next 16 guides relevant to this implementation under `site/node_modules/next/dist/docs/`, at minimum the App Router pages/layouts guide, static export guide, image guide, and Playwright guide, per `site/AGENTS.md`.

Planning assumptions (no task-specific CONTEXT.md was supplied):
- "Desktop views" means all twelve destinations currently declared by the real shell, not the five conceptual entries in the site's existing `docsData.ts`.
- The maintainable public location is a dedicated `/docs/desktop-views` route linked from `/docs`; the existing conceptual docs remain available where accurate.
- Documentation screenshots use the frontend's deterministic browser/Wails fallback state. They demonstrate view structure and controls, not a user's live show data.
- The canonical catalog belongs with the desktop shell because the app must consume it now and may consume its documentation fields more deeply later; the site receives a generated committed copy because its independent Netlify checkout cannot read parent-repository files.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the versioned desktop-view catalog and deterministic site generator</name>
  <files>frontend/src/shell/desktopViews.json, frontend/src/shell/navigation.ts, frontend/src/shell/AppShell.navigation.test.tsx, internal/docgen/desktopviews.go, internal/docgen/docgen_test.go</files>
  <behavior>
    - Test 1: the catalog contains exactly the destination IDs and group ordering exposed by the shell, with no duplicate IDs/slugs and one documentation record per destination.
    - Test 2: NAV_GROUPS is projected from the catalog and every catalog ID still mounts through WorkspaceRouter.
    - Test 3: generation rejects an unknown schema version, unknown fields, duplicate IDs/slugs, invalid screenshot paths, empty required copy, and inconsistent group/view references with stable GOLC_DOCGEN diagnostics.
    - Test 4: valid input is normalized deterministically and copied byte-identically to site/src/content/desktop-views.json on repeated runs.
  </behavior>
  <action>
Create `frontend/src/shell/desktopViews.json` as schema version 1 and make it the source of truth for the current four nav groups and twelve destinations. Each view needs a stable destination ID, URL-safe slug, nav label, documentation title, concise purpose, a non-empty list of principal operator actions, optional concepts/operating notes, and a repository-relative screenshot path under `/desktop-views/`. Keep the content factual to the implemented workspace; do not describe deferred functionality as available. Preserve the existing group/destination order and labels, including the Phase 9 Shows and Fixture Library destinations (FDUI-01/02) and the Guided First Show entry point documented from Overview (FDUI-03).

Update `navigation.ts` so `NAV_GROUPS` is derived from the catalog while retaining the closed `DestinationId` type needed by `WorkspaceRouter` and icon exhaustiveness. Strengthen `AppShell.navigation.test.tsx` to prove catalog/navigation/router parity rather than asserting a second hardcoded list. A destination addition must require catalog content plus the existing router/icon implementation, but no site-only inventory edit.

Add a focused desktop-view generator beside the existing package-reference generator. Decode with strict unknown-field rejection, validate schema version and all invariants, normalize formatting/order deterministically, and write `site/src/content/desktop-views.json` with an explicit generated/source marker or equivalent metadata. Integrate it into `GenerateAll` without resetting unrelated site content, and extend existing disposable-root tests rather than running tests against committed site output. Keep the docs route offline and do not introduce a package dependency.
  </action>
  <verify>
    <automated>go test ./internal/docgen ./internal/command -count=1 &amp;&amp; npm --prefix frontend test -- --run src/shell/AppShell.navigation.test.tsx</automated>
  </verify>
  <done>The desktop app consumes one valid schema-v1 catalog for its navigation, tests mechanically cover all router destinations, and the offline docs generator deterministically emits a strict machine-readable site copy.</done>
</task>

<task type="auto">
  <name>Task 2: Add repeatable real-workspace screenshot capture and generate site assets</name>
  <files>frontend/e2e/desktop-view-docs.spec.ts, frontend/package.json, site/src/content/desktop-views.json, site/public/desktop-views/show-overview.png, site/public/desktop-views/show-shows.png, site/public/desktop-views/show-save-recovery.png, site/public/desktop-views/show-settings.png, site/public/desktop-views/build-fixture-library.png, site/public/desktop-views/build-patch-pools.png, site/public/desktop-views/build-scenes-looks.png, site/public/desktop-views/build-scripts.png, site/public/desktop-views/operate-operator-surface.png, site/public/desktop-views/operate-midi-mapping.png, site/public/desktop-views/output-artnet.png, site/public/desktop-views/output-diagnostics.png</files>
  <action>
Add `frontend/e2e/desktop-view-docs.spec.ts` and a discoverable `npm run docs:screenshots` script using the already-installed Playwright stack. Read the canonical catalog rather than duplicating destinations. Start the existing real frontend through `frontend/playwright.config.ts`, use a fixed 1440x900 viewport and the browser-safe Wails fallback, open each nav destination by its exact catalog label, assert the matching workspace heading is visible, wait for the shell settle transition, disable animations/carets, mask or normalize only genuinely volatile telemetry, and capture the actual application shell to the catalog-declared file under `../site/public/desktop-views/`. Fail on missing/extra images, wrong dimensions, duplicate output paths, a destination that cannot be opened, or a screenshot path escaping the intended site directory.

Run the root docs command to create `site/src/content/desktop-views.json`, then run the screenshot command twice and confirm the second pass preserves the same file set and catalog linkage. Do not add a new browser/package dependency and do not fabricate screenshots with markup or image generation.

Commit the generated JSON and PNGs inside the `site/` repository first. Then commit only Task 2's root tooling plus the resulting `site` gitlink in the parent repository; use explicit pathspecs so the pre-existing parent changes remain untouched.
  </action>
  <verify>
    <automated>go run ./cmd/golc-project docs &amp;&amp; npm --prefix frontend run docs:screenshots &amp;&amp; npm --prefix frontend run docs:screenshots</automated>
  </verify>
  <done>All twelve catalog entries have correctly named, fixed-size screenshots captured from their real routed workspaces; the site submodule owns the generated content/assets in a commit and the parent points to it without absorbing unrelated changes.</done>
</task>

<task type="auto">
  <name>Task 3: Publish and verify the responsive desktop views page across the submodule boundary</name>
  <files>site/src/app/docs/desktop-views/page.tsx, site/src/components/docs/DesktopViewExplorer.tsx, site/src/app/docs/page.tsx, site/src/app/sitemap.ts, site/tests/visual.spec.ts, site/tests/visual.spec.ts-snapshots/desktop-views-light-chromium-linux.png, site/tests/visual.spec.ts-snapshots/desktop-views-dark-chromium-linux.png</files>
  <action>
Inside `site/`, create the statically exported `/docs/desktop-views` route with complete Metadata/canonical/OpenGraph/Twitter fields. Render every group and view from the generated JSON, never a second hand-authored array. Build a reusable server/client split only where interaction requires it: the page must remain readable without interaction, while a compact explorer may provide group filtering or master-detail selection. Each view must show its screenshot with intrinsic dimensions and useful alt text, purpose, principal actions, and concepts/operating notes. Use the existing GOLC site tokens, responsive patterns, focus treatment, and reduced-motion behavior; preserve semantic headings, keyboard reachability, and a sensible mobile layout.

Replace the stale `/docs` claim that the Wails UI is still Phase 6/in progress and its five-view approximation with a clear link/card to the generated desktop views guide, while retaining the existing concept/reference material where still accurate. Add the new route to `sitemap.ts` and the visual route matrix. Update the Linux Chromium light/dark baselines only after inspecting them; also exercise at a mobile viewport and verify that screenshots and text do not overflow.

Run the full submodule quality suite. Commit the page, tests, and intentional baselines inside `site/`, then make a parent commit containing only the updated `site` gitlink (plus this quick task's intended parent files if not already committed). Confirm `git diff --cached --name-only` at each boundary and leave all unrelated parent changes unstaged.
  </action>
  <verify>
    <automated>npm --prefix site run lint &amp;&amp; npm --prefix site run typecheck &amp;&amp; npm --prefix site run build &amp;&amp; npm --prefix site run test:links &amp;&amp; npm --prefix site run test:visual</automated>
    <human-check>Inspect `/docs/desktop-views` in light and dark themes at 1280x900 and 390x844. Confirm every current shell destination is present in the correct group; screenshots are legible and correspond to their titles; copy describes implemented behavior; keyboard focus and selection are clear; and no content clips or scrolls horizontally.</human-check>
  </verify>
  <done>The static site publishes a complete, accessible, visually approved desktop views guide from generated content; all site gates pass; site commits exist before their parent gitlink commits; and unrelated dirty files remain unchanged and unstaged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository catalog → generator | Repository-authored JSON crosses a strict decoder before it may write into the site submodule. |
| Catalog paths → filesystem | Screenshot paths influence cross-repository writes and must remain inside the exact generated asset directory. |
| Parent repository → site submodule | Two independent git histories must record content before the parent records the gitlink. |
| Browser-rendered app → committed images | Runtime state can introduce volatile or sensitive content into documentation assets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-gq6-01 | Tampering | `internal/docgen/desktopviews.go` | medium | mitigate | Strict schema/version validation, uniqueness and completeness checks, normalized output, and byte-stability tests prevent silent catalog manipulation or drift. |
| T-gq6-02 | Tampering | screenshot output path resolution | high | mitigate | Resolve and validate every declared path under `site/public/desktop-views`, reject traversal/absolute paths, and fail on missing or extra outputs. |
| T-gq6-03 | Information Disclosure | committed workspace screenshots | medium | mitigate | Use the browser-safe deterministic fallback data, never real show/user/provider data; inspect every generated PNG before commit and mask only declared volatile telemetry. |
| T-gq6-04 | Repudiation | submodule and parent commits | low | mitigate | Commit site content first, then record its exact SHA through the parent gitlink with explicit status/staging checks. |
| T-gq6-SC | Tampering | npm package execution | low | accept | No package install or new dependency is planned; use the exact existing lockfiles and installed Playwright/Next toolchain. |
</threat_model>

<verification>
- Root catalog/navigation/generator tests pass offline and prove exact twelve-destination completeness, strict validation, deterministic generation, and router parity.
- The documented screenshot command captures one fixed-size image per catalog destination from the real frontend and rejects unsafe/stale output.
- `site/src/content/desktop-views.json` is generated, versioned, machine-readable, and is the sole inventory consumed by the new route.
- Site lint, typecheck, static build, recursive link crawl, and Playwright visual suite pass, including intentional new light/dark baselines.
- `git -C site status --short` and parent `git status --short` show only intended task artifacts after their respective commits; the user's pre-existing parent edits remain uncommitted and untouched.
</verification>

<success_criteria>
- Every current routed desktop workspace is documented with current copy and a real screenshot.
- Navigation, docs inventory, screenshot filenames, and site rendering are linked through one schema-versioned catalog with automated drift failures.
- Structured content is suitable for later direct in-app consumption.
- The site remains independently buildable from its own committed generated assets.
- Submodule history and parent gitlink history are both complete and reviewable.
</success_criteria>

<source_coverage_audit>

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| GOAL | Maintainable desktop views documentation page in the site submodule | COVERED | Tasks 1-3 establish source data, generated assets, and the published route. |
| GOAL | Codebase metadata is the source of truth | COVERED | Task 1 makes desktop navigation consume the schema-v1 catalog and adds parity tests. |
| GOAL | Screenshot tooling | COVERED | Task 2 adds and runs the catalog-driven Playwright capture command. |
| GOAL | Reusable structured content for future in-app ingestion | COVERED | Tasks 1 and 3 preserve a versioned JSON artifact and render directly from it. |
| REQ | FDUI-01 Fixture Library front door | COVERED | Catalog/page include the implemented Fixture Library destination and screenshot. |
| REQ | FDUI-02 Show open/new/switch front door | COVERED | Catalog/page include the implemented Shows destination and screenshot. |
| REQ | FDUI-03 Guided First Show front door | COVERED | Overview documentation identifies the implemented guide entry point and purpose. |
| RESEARCH | None supplied; quick mode forbids a research phase | EXCLUDED | Existing codebase patterns and installed toolchain are sufficient Level 0 discovery. |
| CONTEXT | Preserve unrelated dirty changes | COVERED | Context plus Tasks 2-3 require explicit path staging and status checks. |
| CONTEXT | `site/` is a git submodule | COVERED | Tasks 2-3 require site-first commits followed by parent gitlink commits. |
| CONTEXT | No deferred ideas supplied | COVERED | No deferred scope is introduced. |

</source_coverage_audit>

<output>
Create `.planning/quick/260729-gq6-add-a-maintainable-desktop-views-documen/260729-gq6-SUMMARY.md` when done.
</output>
