---
phase: quick-final-powershell-removal-verification
plan: 260724-y2t
type: execute
wave: 1
depends_on: [260724-x7n]
files_modified:
  - internal/contracts/linear_plan_test.go
  - internal/command/green_subprocess_test.go
  - internal/delivery/mage_targets.go
  - internal/delivery/delivery_test.go
  - magefiles/magefile.go
  - magefiles/magefile_test.go
  - config/commands.toml
  - .github/workflows/check.yml
  - internal/command/check_test.go
  - tools/golc-mcp/protocol_test.go
  - tools/golc-mcp/README.md
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03]
must_haves:
  truths:
    - "check.yml (the real, blocking PR gate) has a real, current, all-green pull_request-triggered run -- not merely 'should work' from local testing or structural analysis."
    - "linear-sync.yml has a real, current, credential-free green run of its linear-drift job."
    - "cross-platform-mage.yml still passes on all three platforms after every Step 7/Step 9 change."
    - "Every fix in this task traces to a specific real CI failure, never guessed at speculatively."
  artifacts:
    - path: internal/delivery/mage_targets.go
      provides: "A new fixed-argument 'testquick' Mage target (Route test, Args [--quick]), closing the gap where the PR graph's bare 'test' step unconditionally required tools/linear-sync's compiled Node output that the credential-free PR bootstrap never builds"
    - path: config/commands.toml
      provides: "commands.pr.steps using 'test --quick' instead of bare 'test', matching internal/delivery's own offline-core-graph remote-failure-isolation principle"
  key_links:
    - from: config/commands.toml
      to: internal/delivery/mage_targets.go
      via: "commands.pr.steps' test --quick step resolves through the new testquick Mage target descriptor, not the full test target"
      pattern: "testquick"
    - from: internal/contracts/linear_plan_test.go
      to: internal/trace/apply/guard.go
      via: "testLinearApplyFailsWithoutFactory isolates GITHUB_EVENT_NAME so it deterministically exercises the transport-unavailable path regardless of whether it runs inside a real pull_request CI event"
      pattern: "GITHUB_EVENT_NAME"
---

<objective>
Execute PowerShell removal plan Step 9: get real, current CI evidence (not local claims) that `check.yml`, `linear-sync.yml`, and `cross-platform-mage.yml` all actually work end to end after Step 7's `golc.ps1` deletion, fixing whatever real bugs surface from `check.yml`'s literal first-ever real execution.

Purpose: `check.yml` had never run as a real `pull_request`-triggered job before this task (confirmed via `gh run list --workflow=check.yml`, zero prior runs) -- Step 7's local test suite passing, even under a simulated `GITHUB_EVENT_NAME=pull_request` environment variable, is not the same claim as a real GitHub-hosted run actually succeeding.
Output: `check.yml` green end to end on windows-latest for the first time in its history; `linear-sync.yml`'s `linear-drift` job green; `cross-platform-mage.yml` reconfirmed green on all three platforms against the final commit.
</objective>

<context>
A throwaway PR (`ci/verify-check-workflow`, an empty commit against `master`) was opened specifically to give `check.yml` a real `pull_request` trigger to run under, since it has no other trigger. The PR was rebased and re-pushed after each fix (three real runs total), then closed without merging and its branch deleted once verification was complete.

Two rounds of real bugs surfaced, both bugs that could only be observed by `check.yml` actually running for the first time — neither was reachable by local `go test ./...`, even under a simulated `GITHUB_EVENT_NAME=pull_request` environment variable set locally, because one of the two bugs (see Task 2) was a structural gap in `config/commands.toml`'s own PR-graph declaration, not a test-isolation problem at all.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: fix two real bugs found by check.yml's first-ever real run (30137427833)</name>
  <files>internal/contracts/linear_plan_test.go, internal/command/green_subprocess_test.go</files>
  <action>
1. `internal/contracts/linear_plan_test.go`'s `testLinearApplyFailsWithoutFactory` never isolated `GITHUB_EVENT_NAME`, so it only reached the real `applyRemoteClientFactory=newProcessRemoteClient` (and the `GOLC_LINEAR_TRANSPORT_UNAVAILABLE` it wraps every failure as) by accident: `runLinearApply`'s PR guard (`apply.GuardAgainstPullRequestMutation`) sits before that factory call and fires first whenever `GITHUB_EVENT_NAME=pull_request` is genuinely set, which GitHub Actions does for every `pull_request`-triggered run. `internal/command/linear_test.go` already documented and guarded against this exact failure mode elsewhere (`t.Setenv("GITHUB_EVENT_NAME", "")`); this test just never got the same treatment, invisible until `check.yml` itself finally ran for real. Added the same isolation.
2. `internal/command/green_subprocess_test.go`'s new "config set --local" subtest (added in 260724-x7n, this task's own predecessor) hit the same 8.3 short-name-aliasing bug class fixed several times earlier this session (`RUNNER~1` vs `runneradmin`): the subprocess relied on its own `os.Getwd()` fallback for `GOLC_PROJECT_ROOT` since no env var was set, and on windows-latest that can resolve to a different path form than the literal `workDir` string the test later `os.Stat`s. Fixed by setting `GOLC_PROJECT_ROOT` explicitly on the subprocess's environment so both sides agree on the exact same string.
  </action>
  <verify>
    <automated>go test ./... -count=1 (normal and GITHUB_EVENT_NAME=pull_request, both locally)</automated>
    <manual>Re-triggered the ci/verify-check-workflow PR's check.yml run (30138546263 after the follow-up fix in Task 2) -- see Task 2 for the actual green result, since this task's fix alone still left the "test" step failing for a third, different reason.</manual>
  </verify>
  <done>Commit c0b95f2 pushed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: add a mage TestQuick target -- check.yml's PR graph never worked with bare "test"</name>
  <files>internal/delivery/mage_targets.go, magefiles/magefile.go, config/commands.toml, .github/workflows/check.yml, internal/delivery/delivery_test.go, internal/command/check_test.go, magefiles/magefile_test.go, tools/golc-mcp/protocol_test.go, tools/golc-mcp/README.md</files>
  <action>After Task 1's fixes, check.yml's third real run still failed at "test": `GOLC_TEST_FAILED: node scope linear-transport-pagination failed`. Root cause: `commands.pr.steps` has always declared bare `test` (full mode), but `internal/command/test.go`'s `runTestFull` unconditionally runs every registered Node scope after the Go suite (`runAllNodeScopes`) -- which requires `tools/linear-sync`'s compiled `dist/test` output. The PR graph's `bootstrap` step can never set `GOLC_BOOTSTRAP_INCLUDE_LINEAR_SYNC=1` (D-16: no Linear credential or workspace access from untrusted pull-request CI), so that compiled output never exists. This exact combination was broken from the moment `commands.pr.steps` was written; it was simply never observed because `check.yml` itself never ran until this task. `internal/delivery/graph.go`'s own offline core graph already avoids this exact trap by using `test --quick` specifically. Applied the same fix to the PR graph: added a new `testquick` Mage target (Route `test`, Args `[--quick]`) and matching `TestQuick()` Mage wrapper, changed `commands.pr.steps` to use `test --quick`, changed `check.yml`'s test step to `run: mage TestQuick`, and updated every place enumerating the target/export list or the production PR graph shape.</action>
  <verify>
    <automated>go test ./... -count=1; go run ./cmd/golc-project check --command-parity</automated>
    <manual>Re-triggered the ci/verify-check-workflow PR's check.yml run (30138546263): fully green through every step for the first time in the workflow's history.</manual>
  </verify>
  <done>Commit 8658b32 pushed; check.yml run 30138546263 fully green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: final real-CI confirmation across all three workflows; cleanup</name>
  <files>none (verification only)</files>
  <action>Re-triggered cross-platform-mage.yml against the final commit (8658b32) to confirm the testquick addition didn't regress the 3-OS observation matrix (it doesn't touch cross-platform-mage.yml's own bare "mage Test" step, which remains correct there since that workflow's bootstrap does include linear-sync). Closed the throwaway ci/verify-check-workflow PR without merging and deleted its branch once check.yml's green run was confirmed.</action>
  <verify>
    <manual>
      - cross-platform-mage.yml run 30138724444: windows-latest, ubuntu-latest, macos-latest all green through every step.
      - check.yml run 30138546263: green through every step (first time ever).
      - linear-sync.yml run 30137404939 (linear-drift job): green, real credential-free run.
    </manual>
  </verify>
  <done>All three CI workflows have real, current, green evidence against the final PowerShell-removal commit.</done>
</task>

</tasks>

<verification>
- `check.yml` run 30138546263: every step green (Checkout, Install pinned Mage, bootstrap, generate --check, check --offline, build, test --quick, package --foundation) -- its first-ever fully green real run.
- `linear-sync.yml` run 30137404939: `linear-drift` job green end to end.
- `cross-platform-mage.yml` run 30138724444: all three platforms green through every step.
</verification>

<success_criteria>
- Every CI workflow that depends on the post-golc.ps1 build system has real, current, passing evidence -- not a local-only claim.
- Every bug found in this task traces to a specific real CI failure.
- The PowerShell removal plan (Steps 0-9) is fully complete.
</success_criteria>

<output>
Create `.planning/quick/260724-y2t-final-verification-across-all-three-c/260724-y2t-SUMMARY.md` when done.
</output>
