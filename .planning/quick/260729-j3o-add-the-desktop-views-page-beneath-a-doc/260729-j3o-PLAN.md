---
phase: quick-docs-navigation-dropdown
plan: 260729-j3o
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/components/DocsMenu.tsx
  - site/src/components/NavLinks.tsx
  - site/src/components/MobileMenu.tsx
  - site/tests/visual.spec.ts
  - site
autonomous: true
requirements: [SITE-NAV-01]
must_haves:
  truths:
    - "Desktop visitors can open a Docs dropdown in the primary navigation and reach both the Docs overview and Desktop Views."
    - "The Docs trigger and matching child link communicate the active docs route, and the dropdown closes after selection, Escape, or an outside click."
    - "Small-screen visitors see Docs overview and Desktop Views as clearly grouped links inside the existing mobile navigation dialog."
    - "Keyboard focus, expanded state, and route navigation remain understandable without relying on pointer hover."
    - "The production site builds successfully and focused desktop/mobile navigation tests pass without changing active screenshot-debug work."
  artifacts:
    - path: site/src/components/DocsMenu.tsx
      provides: "Accessible desktop Docs dropdown and its two route links."
    - path: site/src/components/NavLinks.tsx
      provides: "Primary desktop navigation wired to the Docs dropdown."
    - path: site/src/components/MobileMenu.tsx
      provides: "Responsive Docs grouping in the existing mobile dialog."
    - path: site/tests/visual.spec.ts
      provides: "Focused Playwright coverage for desktop and mobile Docs navigation."
  key_links:
    - from: site/src/components/NavLinks.tsx
      to: site/src/components/DocsMenu.tsx
      via: "The former flat Docs entry is replaced by the dedicated dropdown component."
      pattern: "DocsMenu"
    - from: site/src/components/DocsMenu.tsx
      to: site/src/app/docs/desktop-views/page.tsx
      via: "A child Next Link targets the existing /docs/desktop-views route."
      pattern: "/docs/desktop-views"
    - from: site/src/components/MobileMenu.tsx
      to: site/src/app/docs/desktop-views/page.tsx
      via: "The mobile Docs group exposes the same route and closes the dialog after activation."
      pattern: "/docs/desktop-views"
    - from: site/tests/visual.spec.ts
      to: site/src/components/DocsMenu.tsx
      via: "Role-, name-, expanded-state-, and URL-based assertions exercise the dropdown."
      pattern: "docs navigation"
---

<objective>
Expose the existing Desktop Views documentation route beneath a Docs dropdown in the primary site navigation, with an equivalent grouped destination in the mobile menu.

Purpose: Readers should discover the desktop workspace guide directly from the site header without losing the Docs overview or keyboard/mobile usability.
Output: A dedicated Docs dropdown, responsive mobile Docs grouping, focused Playwright coverage, a successful production build, and site-first commit history followed by the parent gitlink commit.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@site/AGENTS.md
@site/src/components/SiteHeader.tsx
@site/src/components/NavLinks.tsx
@site/src/components/ResourcesMenu.tsx
@site/src/components/MobileMenu.tsx
@site/src/app/docs/page.tsx
@site/src/app/docs/desktop-views/page.tsx
@site/tests/visual.spec.ts
@site/package.json

This quick task changes the independently versioned `site/` submodule. Before editing, read the relevant installed Next 16 App Router/client-component guidance under `site/node_modules/next/dist/docs/` as required by `site/AGENTS.md`; add no package.

Preserve both worktrees exactly outside the listed navigation/test paths. In particular, the parent `frontend/e2e/desktop-view-docs.spec.ts`, `.planning/debug/screenshot-error-states.md`, and the twelve modified `site/public/desktop-views/*.png` files belong to the active screenshot debug session. Do not stage, restore, regenerate, or incorporate them into navigation baselines. Recheck `git status --short` and `git -C site status --short` before every staging step and use explicit pathspecs.

Use the established `ResourcesMenu` interaction and styling pattern for the desktop Docs dropdown: a client-side `<details>` disclosure, route-aware state, outside-click closing, Escape closing, and Next links. The Docs trigger itself opens the menu; its panel contains `Docs overview` (`/docs`) and `Desktop Views` (`/docs/desktop-views`). On small screens, keep the existing modal navigation and expose those same destinations under a visible Docs group label rather than nesting a second disclosure.

Keep the closed desktop header visually stable so this navigation-only task does not require regenerating full-page route snapshots while screenshot source assets are dirty. Do not weaken or delete existing visual assertions.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add accessible desktop and mobile Docs navigation</name>
  <files>site/src/components/DocsMenu.tsx, site/src/components/NavLinks.tsx, site/src/components/MobileMenu.tsx, site/tests/visual.spec.ts</files>
  <behavior>
    - Test 1: at a desktop viewport, the Docs disclosure starts collapsed, exposes Docs overview and Desktop Views when activated, reports expanded state, and navigating through Desktop Views reaches `/docs/desktop-views`.
    - Test 2: on `/docs` and `/docs/desktop-views`, the Docs trigger is styled as the active navigation family and the exact matching child link carries `aria-current="page"`.
    - Test 3: Escape and an outside click close the open desktop Docs dropdown without navigating, and selecting either child closes it as navigation begins.
    - Test 4: at 390x844, opening the existing Site navigation dialog exposes a visible Docs group containing Docs overview and Desktop Views; selecting Desktop Views closes the dialog and reaches the route.
  </behavior>
  <action>
Write the focused Playwright behaviors first using accessible roles/names and observable URLs rather than CSS-only assertions. Create `DocsMenu.tsx` beside `ResourcesMenu.tsx`, following its repository-established `<details>` lifecycle and design tokens. Give the summary an explicit, queryable expanded state, treat both `/docs` and descendant docs routes as active, mark only the exact current child link with `aria-current="page"`, close after child activation, and retain Escape/outside-click cleanup. Render two concise panel entries: Docs overview and Desktop Views. Keep the summary's collapsed geometry aligned with the former flat Docs link and provide a visible focus treatment.

Replace only the flat Docs item in `NavLinks` with `DocsMenu`; retain Workflow, Download, Resources, GitHub, and their order. In `MobileMenu`, remove the duplicate flat Docs entry and add a visible Docs section containing the same two links before Resources. Preserve the dialog's existing initial-focus, Escape, trigger-focus restoration, and close-on-link behavior; add exact-route `aria-current` state to the Docs links. Avoid hover-only opening, a nested mobile dialog, or duplicated navigation inventories beyond the two existing desktop/mobile render surfaces.
  </action>
  <verify>
    <automated>npm --prefix site run lint &amp;&amp; npm --prefix site run typecheck &amp;&amp; npm --prefix site run test:visual -- --grep "docs navigation"</automated>
  </verify>
  <done>Desktop and mobile primary navigation expose both Docs destinations, active/expanded/current states are accessible, all close paths behave predictably, and focused Playwright coverage passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the production build and commit across the submodule boundary</name>
  <files>site/src/components/DocsMenu.tsx, site/src/components/NavLinks.tsx, site/src/components/MobileMenu.tsx, site/tests/visual.spec.ts, site</files>
  <action>
Run the focused navigation tests at their desktop and 390x844 viewports, then run lint, typecheck, and the production Next build. Confirm the build emits both `/docs` and `/docs/desktop-views`. Inspect the closed header at desktop size and the open mobile dialog at 390x844 to confirm no clipping, overlap, horizontal overflow, or loss of the Resources/GitHub destinations. Do not run snapshot-update or modify any committed baseline: the closed-header geometry must remain stable, and the active debug session's modified desktop-view screenshots must remain isolated.

Inside `site/`, inspect the diff and stage only `src/components/DocsMenu.tsx`, `src/components/NavLinks.tsx`, `src/components/MobileMenu.tsx`, and `tests/visual.spec.ts`; commit those paths first and record the site commit SHA. Return to the parent repository, verify the `site` gitlink resolves to that commit, then stage only the gitlink and this quick task's GSD artifacts for the parent commit. Never stage, restore, or clean the pre-existing parent E2E/debug files or the twelve dirty submodule screenshot assets.
  </action>
  <verify>
    <automated>npm --prefix site run lint &amp;&amp; npm --prefix site run typecheck &amp;&amp; npm --prefix site run build &amp;&amp; npm --prefix site run test:visual -- --grep "docs navigation"</automated>
  </verify>
  <done>The focused responsive navigation suite and production build pass, dirty screenshot work remains untouched and unstaged, the site implementation commit exists first, and the parent records only its resulting gitlink plus GSD artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer/keyboard input → navigation disclosure | Untrusted interaction controls expanded state, focus, and route selection. |
| Current pathname → active navigation state | Route data determines which trigger and child are announced as current. |
| Site submodule → parent repository | The parent must record an existing site commit without absorbing unrelated dirty work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-j3o-01 | Denial of Service | `DocsMenu.tsx` event listeners | low | mitigate | Register click/keyboard listeners once, clean them up on unmount, and test Escape/outside-click close behavior. |
| T-j3o-02 | Spoofing | Docs active/current state | low | mitigate | Derive family activity from `/docs` plus descendants while assigning `aria-current` only by exact child href equality. |
| T-j3o-03 | Tampering | Site submodule commit | medium | mitigate | Recheck both statuses, stage explicit navigation/test pathspecs inside site first, then record that exact SHA in the parent gitlink. |
| T-j3o-SC | Tampering | npm package execution | low | accept | No dependency or install change is planned; verification uses the committed site toolchain and lockfile. |
</threat_model>

<verification>
- Focused Playwright assertions prove the desktop Docs disclosure, active/current state, Escape/outside/link close paths, mobile grouping, dialog closure, and Desktop Views navigation.
- `npm --prefix site run lint`, `typecheck`, and `build` pass, with `/docs` and `/docs/desktop-views` emitted.
- No visual baseline or `site/public/desktop-views/*.png` file is changed by this task.
- The site implementation is committed before the parent gitlink, and explicit staging preserves all pre-existing dirty work.
</verification>

<success_criteria>
- Desktop primary navigation exposes Desktop Views beneath Docs while retaining a route to the Docs overview.
- Small-screen navigation exposes the same Docs destinations in a readable group.
- Keyboard, pointer, active-route, expanded-state, and close behavior are covered by focused automated tests.
- The production build succeeds and the submodule/parent history follows the required site-first ordering.
</success_criteria>

<source_coverage_audit>

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| GOAL | Add Desktop Views beneath a Docs dropdown in primary navigation | COVERED | Task 1 creates and wires the desktop disclosure and both docs links. |
| REQ | SITE-NAV-01 Desktop Views is discoverable from responsive primary navigation | COVERED | Task 1 covers desktop and mobile surfaces; Task 2 verifies the production build. |
| RESEARCH | No new integration or dependency | EXCLUDED | Existing ResourcesMenu, MobileMenu, and Playwright patterns make this Level 0 internal work. |
| CONTEXT | Desktop Docs dropdown child | COVERED | Task 1 adds Docs overview and Desktop Views to the desktop disclosure. |
| CONTEXT | Accessible and responsive behavior | COVERED | Task 1 specifies expanded/current state, keyboard close paths, mobile grouping, and focused tests. |
| CONTEXT | Focused tests and production build | COVERED | Tasks 1-2 run targeted Playwright coverage plus lint, typecheck, and `next build`. |
| CONTEXT | Site commit before parent gitlink | COVERED | Task 2 mandates site-first commit ordering and exact SHA verification. |
| CONTEXT | Preserve active debug screenshot work | COVERED | Context and Task 2 enumerate dirty paths, explicit staging, and the no-baseline-update constraint. |
| CONTEXT | Planning only | COVERED | This artifact specifies execution and makes no implementation change. |
</source_coverage_audit>

<output>
Create `.planning/quick/260729-j3o-add-the-desktop-views-page-beneath-a-doc/260729-j3o-SUMMARY.md` when done.
</output>
