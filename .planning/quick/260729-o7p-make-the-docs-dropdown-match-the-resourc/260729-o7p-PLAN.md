---
phase: quick-docs-resources-dropdown-parity
plan: 260729-o7p
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/app/globals.css
  - site/src/components/DesktopDropdownMenu.tsx
  - site/src/components/DocsMenu.tsx
  - site/src/components/ResourcesMenu.tsx
  - site/src/components/docs/DesktopViewExplorer.tsx
  - site/tests/visual.spec.ts
  - site
autonomous: true
requirements: [SITE-NAV-02, DOCS-VISUAL-02, DOCS-RELEASE-01]
must_haves:
  truths:
    - "At desktop widths, Docs and Resources use the same trigger geometry, typography, chevron, panel geometry/surface, item hierarchy, state styling, and disclosure behavior."
    - "Docs retains Docs overview and Desktop Views, exact-route child state, docs-family trigger state, keyboard access, focus restoration, and every established close path."
    - "Resources retains its destinations, exact-route state, descriptions, and behavior while becoming the canonical shared presentation."
    - "The mobile navigation still presents coherent Docs and Resources groups, closes after navigation or Escape, and restores trigger focus."
    - "The Desktop Views screenshot is framed by a restrained contrasting neutral stage in light and dark without adding nested cards or undoing the existing stage/detail separation."
    - "Focused non-snapshot checks and direct browser inspection cover both dropdowns and the screenshot stage across required themes and viewports."
    - "Only task-owned site files are committed; site/deno.lock and unrelated work remain untouched; site and parent are pushed in order before one production deployment."
  artifacts:
    - path: site/src/components/DesktopDropdownMenu.tsx
      provides: "Shared canonical desktop disclosure structure, styling, focus, and close behavior."
    - path: site/src/components/DocsMenu.tsx
      provides: "Docs inventory and docs-family active semantics rendered through the shared primitive."
    - path: site/src/components/ResourcesMenu.tsx
      provides: "Resources inventory and exact-route semantics rendered through the shared primitive."
    - path: site/src/app/globals.css
      provides: "Explicit light/dark neutral screenshot-stage surface token."
    - path: site/src/components/docs/DesktopViewExplorer.tsx
      provides: "Existing screenshot stage wired to the contrasting neutral surface without changing master-detail structure."
    - path: site/tests/visual.spec.ts
      provides: "Non-snapshot computed geometry/style, interaction, responsive-navigation, and stage-contrast coverage."
  key_links:
    - from: site/src/components/DocsMenu.tsx
      to: site/src/components/DesktopDropdownMenu.tsx
      via: "Docs passes its two preserved destinations and docs-family active state to the canonical shared disclosure."
      pattern: "DesktopDropdownMenu"
    - from: site/src/components/ResourcesMenu.tsx
      to: site/src/components/DesktopDropdownMenu.tsx
      via: "Resources passes its existing four destinations to the same disclosure implementation."
      pattern: "DesktopDropdownMenu"
    - from: site/src/app/globals.css
      to: site/src/components/docs/DesktopViewExplorer.tsx
      via: "The screenshot-stage wrapper consumes the theme-specific neutral stage token."
      pattern: "screenshot-stage"
    - from: site/tests/visual.spec.ts
      to: site/src/components/DesktopDropdownMenu.tsx
      via: "Stable accessible names and menu test IDs expose both instances for computed parity and interaction assertions."
      pattern: "dropdown parity"
    - from: site
      to: "parent repository gitlink"
      via: "The parent records the exact already-pushed site revision before production deployment."
      pattern: "160000"
---

<objective>
Make the desktop Docs dropdown visually and behaviorally identical to the canonical Resources dropdown, and give the Desktop Views screenshot stage a restrained neutral frame that cannot blend into the captured application's warm-gray surface.

Purpose: Site navigation should read as one consistent system, while the documentation screenshot remains clearly separated from both its stage and the following detail in every supported presentation.
Output: A shared desktop dropdown primitive with preserved route semantics, a theme-aware screenshot-stage surface, focused non-snapshot regression coverage, ordered site/parent pushes, and one verified Netlify production deployment.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260729-j3o-add-the-desktop-views-page-beneath-a-doc/260729-j3o-SUMMARY.md
@.planning/quick/260729-nfn-make-the-desktop-views-screenshot-stage-/260729-nfn-SUMMARY.md
@site/AGENTS.md
@site/src/app/globals.css
@site/src/components/DocsMenu.tsx
@site/src/components/ResourcesMenu.tsx
@site/src/components/NavLinks.tsx
@site/src/components/MobileMenu.tsx
@site/src/components/docs/DesktopViewExplorer.tsx
@site/tests/visual.spec.ts
@site/package.json
@site/playwright.config.ts

This is Windows/local work in the independently versioned `site/` submodule. Read the installed Next 16 client-component, Link, and navigation guidance under `site/node_modules/next/dist/docs/` before editing, as required by `site/AGENTS.md`; add no package.

`ResourcesMenu` is the canonical desktop design. Extract its repeated disclosure markup, presentation, and lifecycle into `DesktopDropdownMenu.tsx`; keep the route inventories and active predicates in the Docs/Resources wrappers. The primitive must render the canonical right-anchored `w-64` panel, title plus description hierarchy, and identical trigger/item states for both instances. Do not parameterize visual differences between the two menus.

Preserve `NavLinks.tsx` ordering and `MobileMenu.tsx` grouping/behavior without modifying them unless a failing focused test proves a wiring correction is necessary. Docs retains `/docs` and `/docs/desktop-views`; Resources retains `/architecture`, `/roadmap`, `/changelog`, and `/reference`.

The prior stage/detail task deliberately made the screenshot stage `bg-page` and the following detail `bg-panel`. The new requirement is narrower: replace the screenshot stage's page-like surface with a cool, restrained neutral token declared for both themes in `globals.css`, while retaining its single full-width enlarge button, inset, border, shadow, lower divider, tabpanel, and detail structure. Do not add another card, wrapper, screenshot asset, or GUI-source change.

At execution start record `git status --short`, `git -C site status --short`, and the existence/status/SHA-256 of `site/deno.lock`. Preserve that file byte-for-byte and untracked. Use explicit pathspecs for every commit. Do not create or update screenshots, Playwright pixel baselines, Linux baselines, GUI source, package manifests/locks, or Netlify configuration. Do not use Docker, WSL, or Linux containers.

<interfaces>
From `site/src/components/DocsMenu.tsx`:
- `DOCS_LINKS`: `/docs` as `Docs overview`; `/docs/desktop-views` as `Desktop Views`.
- Docs trigger is active for `/docs` and every `/docs/*` descendant; only an exact child route receives `aria-current="page"`.

From `site/src/components/ResourcesMenu.tsx`:
- `RESOURCES`: four `{ href, label, body }` entries.
- Resources trigger is active only when an exact resource route matches.
- Canonical presentation: 11px rotated chevron; right-anchored, 16rem panel; rounded-xl line border, panel surface, 0.5rem padding, shadow; items use 0.75rem horizontal and 0.625rem vertical padding with `text-sm` medium labels and `text-xs` descriptions.

From `site/src/components/docs/DesktopViewExplorer.tsx`:
- `data-testid="desktop-view-screenshot-stage"` is the single enlarge button before `data-testid="desktop-view-detail"` inside the existing tabpanel.
- The stage currently uses page surface, responsive 12/20px inset, lower line border, and one framed screenshot image.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify Docs and Resources through the canonical desktop dropdown</name>
  <files>site/src/components/DesktopDropdownMenu.tsx, site/src/components/DocsMenu.tsx, site/src/components/ResourcesMenu.tsx, site/tests/visual.spec.ts</files>
  <behavior>
    - Test 1: Docs and Resources triggers have equal computed height, font size, line height, gap, chevron box/transform behavior, focus outline, and active/inactive typography treatment.
    - Test 2: each open panel has equal width, padding, radius, border, surface, shadow, top offset, and right-edge anchoring to its own trigger; corresponding items have equal spacing and label/description hierarchy.
    - Test 3: pointer, Enter/Space, Tab, Escape, outside click, and link activation expose equivalent expanded/closed states; Escape restores focus to the originating trigger and links remain keyboard reachable.
    - Test 4: Docs family/exact-child state and Resources exact-route state remain correct, including equivalent hover, focus-visible, and current-state computed treatment.
    - Test 5: the 390x844 dialog retains visible Docs and Resources groups, exact Docs route state, Escape focus restoration, and close-on-link navigation.
  </behavior>
  <action>
Add focused Playwright tests first. Use roles, names, `aria-expanded`, `aria-current`, focus assertions, URLs, `getBoundingClientRect()`, and `getComputedStyle()`; do not assert Tailwind class strings and do not call screenshot APIs or snapshot-update mode. Open each menu separately before measuring. Compare invariant dimensions and presentation, not absolute viewport coordinates or text-dependent trigger widths.

Create a typed `DesktopDropdownMenu` client component that accepts the trigger label, items (`href`, `label`, `body`), and trigger-active predicate/result while owning one `<details>` disclosure implementation. Render the Resources geometry and visual hierarchy exactly for both menus: identical summary classes and focus treatment, identical chevron, right anchoring, 16rem panel, surface/border/shadow/radius/padding, and title/description items. Add stable instance identifiers only where required for scoped tests and ARIA relationships.

Centralize open state, outside-click cleanup, Escape closure with trigger-focus restoration, and link-activation closure. Keep native Enter/Space disclosure activation and sequential Tab access; do not add hover-only opening, a roving menu role, trapped focus, or a second navigation authority. Refactor Docs and Resources into thin inventory/route wrappers. Give both Docs entries concise descriptions so their label/body hierarchy matches Resources while retaining the exact visible labels and destinations. Preserve Resources copy and destinations. Ensure opening one menu closes the other through the established document click behavior.
  </action>
  <verify>
    <automated>npm --prefix site run lint &amp;&amp; npm --prefix site run typecheck &amp;&amp; npm --prefix site run build &amp;&amp; npm --prefix site run test:visual -- --grep "dropdown parity|docs navigation|resources dropdown|mobile dialog"</automated>
  </verify>
  <done>Both desktop dropdowns share one canonical structure and behavior, computed parity assertions pass, all route/current/focus/closure semantics are preserved, and mobile navigation remains coherent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frame the Desktop Views screenshot with a contrasting neutral stage</name>
  <files>site/src/app/globals.css, site/src/components/docs/DesktopViewExplorer.tsx, site/tests/visual.spec.ts</files>
  <behavior>
    - Test 1: at 1280x900 and 390x844 in light and dark, the computed stage surface differs from page and detail surfaces, remains behind the image inset, and retains a visible lower boundary before detail.
    - Test 2: the computed stage color has a restrained but unambiguous RGB/luminance separation from the current screenshot's sampled edge/corner background in both themes.
    - Test 3: stage precedes detail, responsive insets remain at least 12px, the image frame remains present, and neither document nor main overflows horizontally.
    - Test 4: tab selection, enlarge/lightbox behavior, Escape closure, scroll restoration, and opener-focus restoration remain unchanged.
  </behavior>
  <action>
Extend the existing non-snapshot Desktop Views computed-style test first. Sample same-origin image edge/corner pixels through an in-page canvas after the image is complete, compare them with the computed stage background, and enforce a modest documented color-distance/luminance threshold that detects the reported warm-gray blending without demanding a high-contrast or card-heavy result. Also assert the stage differs from page and detail tokens, the image remains inset/framed, the lower divider remains visible, source order is unchanged, and overflow remains absent in all four viewport/theme cases.

Add a purpose-named `--screenshot-stage` custom property in `globals.css` with explicit light and dark neutral values, expose it through Tailwind only if needed, and apply it to the existing stage button. Choose a slightly cool neutral: visibly separate from the captured warm gray and existing page/panel surfaces, but subordinate to the screenshot and content. Retain the existing button, tabpanel, responsive padding, image border/radius/shadow, enlarge label, detail surface, and all lightbox handlers. Do not add nested wrappers/cards, change screenshot files, modify GUI source, or weaken the prior stage/detail assertions.
  </action>
  <verify>
    <automated>npm --prefix site run lint &amp;&amp; npm --prefix site run typecheck &amp;&amp; npm --prefix site run build &amp;&amp; npm --prefix site run test:visual -- --grep "desktop views screenshot stage|desktop views keyboard|desktop views lightbox|dropdown parity|docs navigation|resources dropdown"</automated>
  </verify>
  <done>The screenshot has a restrained but unmistakable neutral frame in desktop/mobile light/dark, prior master-detail and lightbox behavior remains intact, and focused computed-style/contrast checks pass without baseline changes.</done>
</task>

<task type="auto">
  <name>Task 3: Inspect, release, deploy once, and verify production</name>
  <files>site/src/app/globals.css, site/src/components/DesktopDropdownMenu.tsx, site/src/components/DocsMenu.tsx, site/src/components/ResourcesMenu.tsx, site/src/components/docs/DesktopViewExplorer.tsx, site/tests/visual.spec.ts, site, .planning/STATE.md, .planning/quick/260729-o7p-make-the-docs-dropdown-match-the-resourc/260729-o7p-SUMMARY.md</files>
  <action>
Run the final focused non-snapshot Playwright set, lint, typecheck, and static production build on Windows/local tooling only. Start one task-owned local preview process and record its PID; stop only that PID after inspection. In a real browser, inspect Docs and Resources side-by-side at desktop width in light and dark, exercising pointer and keyboard opening, Tab, Escape, outside click, current routes, and link activation. Inspect the mobile dialog at 390x844. Inspect `/docs/desktop-views` at 1280x900 and 390x844 in both themes, including tab switch and lightbox open/Escape/focus restoration. Confirm the cool neutral stage clearly frames the image without reading as another card and the existing detail separation remains clear. Do not save screenshots.

Recheck both worktrees and the recorded `site/deno.lock` fingerprint. In `site/`, stage only the five task-owned source/component files plus `tests/visual.spec.ts`, commit, and push `master`; verify `origin/master` equals the site HEAD. In the parent, verify the gitlink equals that pushed site SHA, stage only `site` plus the already-created quick plan as applicable, commit the gitlink release, push `master`, and verify `origin/master` equals parent HEAD. Preserve every unrelated change.

Only after both pushes, create a task-specific deployment marker under the OS temporary directory. If it already exists, stop rather than deploy again. Write the pre-deploy site/parent SHAs to it, then invoke `npm run deploy` from `site/` exactly once. A failed deploy is terminal for this execution: record the failure and do not retry. On success, record the Netlify deploy ID/URL, then verify the canonical production home, `/docs`, `/docs/desktop-views`, and `/architecture` return 200 and repeat the desktop dropdown plus four-mode stage browser checks against production without screenshots.

Create the quick SUMMARY and update only the Quick Tasks Completed state entry with site SHA, parent gitlink/release SHA, deploy ID, canonical HTTP results, focused test results, local/production browser observations, and the unchanged `site/deno.lock` fingerprint. Commit and push this parent-only evidence update; do not amend the already-pushed gitlink release commit.
  </action>
  <verify>
    <automated>$deno = Get-FileHash -Algorithm SHA256 -LiteralPath site/deno.lock; npm --prefix site run lint; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm --prefix site run typecheck; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm --prefix site run build; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm --prefix site run test:visual -- --grep "dropdown parity|docs navigation|resources dropdown|mobile dialog|desktop views screenshot stage|desktop views keyboard|desktop views lightbox"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; if ((Get-FileHash -Algorithm SHA256 -LiteralPath site/deno.lock).Hash -ne $deno.Hash) { throw "site/deno.lock changed" }; $siteHead = git -C site rev-parse HEAD; if ((git -C site rev-parse origin/master) -ne $siteHead) { throw "site HEAD is not pushed" }; if ((git ls-tree HEAD site).Split()[2] -ne $siteHead) { throw "parent gitlink does not match site HEAD" }; if ((git rev-parse origin/master) -ne (git rev-parse HEAD)) { throw "parent HEAD is not pushed" }; foreach ($url in @("https://golc-site.netlify.app/","https://golc-site.netlify.app/docs","https://golc-site.netlify.app/docs/desktop-views","https://golc-site.netlify.app/architecture")) { if ((Invoke-WebRequest -UseBasicParsing $url).StatusCode -ne 200) { throw "production verification failed: $url" } }</automated>
    <human-check>Direct local and production browser inspection confirms desktop dropdown parity in light/dark, coherent mobile navigation, and a restrained but clear screenshot frame at desktop/mobile in light/dark, with no screenshot artifacts saved.</human-check>
  </verify>
  <done>The site revision is pushed first, the exact parent gitlink revision is pushed second, one and only one pinned Netlify deploy is attempted, production is verified, evidence is pushed, and protected/unrelated files remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Pointer/keyboard input → shared disclosure | Two route inventories depend on one interaction implementation without losing focus or current-route semantics. |
| Raster screenshot → styled stage | Computed CSS and sampled image pixels must demonstrate separation without coupling to snapshot baselines. |
| Site submodule → parent repository | The parent gitlink must reference the exact pushed site revision without absorbing unrelated work. |
| Authenticated local CLI → Netlify production | One irreversible command publishes the public site and must not be retried. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-o7p-01 | Tampering | Shared dropdown refactor | medium | mitigate | Preserve wrapper-owned inventories/active predicates and cover geometry, current state, focus, and every close path for both instances. |
| T-o7p-02 | Denial of Service | Navigation keyboard flow | medium | mitigate | Retain native disclosure activation and sequential Tab order; explicitly test Escape focus restoration and mobile dialog behavior. |
| T-o7p-03 | Tampering | Visual evidence | medium | mitigate | Use computed geometry/style and same-origin pixel sampling; prohibit screenshots and all baseline-update commands. |
| T-o7p-04 | Tampering | `site/deno.lock` and unrelated work | high | mitigate | Fingerprint the lock, inspect both worktrees, and stage only explicit task paths. |
| T-o7p-05 | Denial of Service | Local preview cleanup | medium | mitigate | Record and stop only the task-owned preview PID; do not terminate by process name or broad port match. |
| T-o7p-06 | Spoofing | Netlify target/revision | high | mitigate | Verify pushed site SHA and parent gitlink before the single deploy, then verify canonical production routes and behavior. |
| T-o7p-07 | Information Disclosure | Git/Netlify authentication | high | mitigate | Use existing authenticated contexts without printing, staging, or recording credentials. |
| T-o7p-08 | Repudiation | Production release | medium | mitigate | Record exact SHAs, deploy ID, route status, test commands, and local/production browser evidence in the summary. |
</threat_model>

<verification>
- Focused non-snapshot Playwright tests prove desktop dropdown computed parity, interaction/focus/current-route semantics, and coherent mobile navigation.
- Computed style, geometry, pixel-sampled contrast, order, inset, border, and overflow assertions pass for the Desktop Views stage at 1280x900 and 390x844 in light and dark.
- Existing focused keyboard and lightbox tests pass; lint, typecheck, and production build pass on Windows/local tooling.
- Direct local and production browser inspection covers both dropdowns in desktop light/dark, mobile navigation, and the screenshot stage in all four viewport/theme combinations without saving screenshots.
- Git diffs and commits contain only task-owned site files, the parent gitlink, plan/summary, and STATE entry; `site/deno.lock`, GUI source, screenshots, baselines, manifests/locks, Netlify config, and unrelated changes remain untouched.
- Site push precedes parent gitlink push; both precede the sole `npm run deploy` invocation; canonical production routes and behaviors are verified afterward.
</verification>

<success_criteria>
- Docs is indistinguishable from Resources as a desktop dropdown pattern except for its content and active-route semantics.
- Desktop and mobile navigation destinations, keyboard/focus behavior, closure paths, and current-route communication remain correct.
- The Desktop Views screenshot no longer blends into its outer stage in either theme, while the page remains one restrained master-detail composition.
- Focused tests, local inspection, ordered pushes, one production deploy, and production verification complete without protected-file or baseline churn.
</success_criteria>

<source_coverage_audit>

| Source | Item | Status | Plan coverage |
|---|---|---|---|
| GOAL | Docs matches Resources visually and behaviorally; responsive navigation, tests, push, and production deployment complete | COVERED | Tasks 1 and 3 implement, inspect, release, and verify the navigation outcome. |
| REQ | SITE-NAV-02 canonical Docs/Resources desktop parity with preserved routes, current state, keyboard/focus, and closure behavior | COVERED | Task 1 shares one primitive and adds computed/behavior assertions; Task 3 inspects local and production. |
| REQ | DOCS-VISUAL-02 contrasting neutral screenshot stage in desktop/mobile light/dark without card-heavy regression | COVERED | Task 2 adds the explicit theme token and non-snapshot contrast coverage; Task 3 performs four-mode inspection. |
| REQ | DOCS-RELEASE-01 site-first push, parent gitlink second, one pinned deployment, production verification | COVERED | Task 3 enforces and records the complete release order. |
| RESEARCH | Existing Resources disclosure, theme-token, Desktop Views stage/detail, Playwright, and Netlify patterns | COVERED | Tasks reuse current dependencies and established patterns; discovery level 0 requires no external research. |
| CONTEXT | Shared primitive only when it reduces real duplication without destabilizing navigation | COVERED | Task 1 extracts only repeated desktop structure/lifecycle; inventories and mobile navigation remain separate. |
| CONTEXT | Resources is canonical for all enumerated geometry, styling, hierarchy, and state treatment | COVERED | Task 1 fixes one non-parameterized shared presentation and verifies computed parity. |
| CONTEXT | Preserve Docs/Resources destinations, active semantics, mobile grouping, keyboard/focus, and all close paths | COVERED | Task 1 behavior and done criteria enumerate every preserved contract. |
| CONTEXT | Add focused non-snapshot computed parity and interaction assertions | COVERED | Tasks 1-2 prohibit class/screenshot assertions and cover computed observable behavior. |
| CONTEXT | Add cool neutral stage contrast without undoing task 260729-nfn separation | COVERED | Task 2 retains the existing structure/inset/divider/detail and changes only the stage surface token. |
| CONTEXT | Direct browser inspect desktop/mobile light/dark; no screenshots, GUI changes, Docker/WSL/Linux, or baselines | COVERED | Task 3 performs local and production inspection with explicit prohibitions. |
| CONTEXT | Preserve `site/deno.lock` and unrelated changes | COVERED | All tasks fingerprint, restrict staging, and verify the lock and worktrees. |
| CONTEXT | Commit/push site first, parent gitlink second, deploy exactly once with `npm run deploy`, verify production | COVERED | Task 3 uses a persistent deploy marker, fail-closed no-retry rule, ordered pushes, and canonical checks. |
</source_coverage_audit>

<output>
Create `.planning/quick/260729-o7p-make-the-docs-dropdown-match-the-resourc/260729-o7p-SUMMARY.md` when done.
</output>
