---
phase: 13-unified-ui-design-system-and-automated-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
autonomous: false
requirements: [D-09, D-10, D-11, UI-SPEC-ENFORCEMENT]
must_haves:
  truths:
    - "Both parser packages are independently reproduced and approved before package-manager mutation."
    - "Only the two approved exact parser pins and their lockfile resolution change in this plan."
  artifacts:
    - path: "frontend/package-lock.json"
      provides: "Exact approved parser resolutions"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/package-lock.json"
      via: "npm install --save-dev --save-exact"
      pattern: "postcss|@typescript/typescript6"
---

<objective>
Approve and install the two exact parser packages required by D-09, D-10, and D-11.

Purpose: Isolate the supply-chain mutation from manifest authority and generation.
Output: Approved exact package and lockfile pins only.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-CONTEXT.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-UI-SPEC.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-RESEARCH.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Reproduce and approve both exact parser packages</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <action>Run the read-only registry queries, then review and authorize installation only if both exact identities, repositories, integrity records, lifecycle-script status, and the Microsoft compatibility recommendation are credible.</action>
  <what-built>Read-only legitimacy evidence for postcss@8.5.22 and @typescript/typescript6@6.0.2.</what-built>
  <how-to-verify>Review the command output for registry identity, official repository, integrity, lifecycle scripts, and Microsoft recommendation. Approve both exact pins or reject either pin; no package-manager mutation may occur before approval.</how-to-verify>
  <resume-signal>Type "approved" to authorize both exact pins, or identify the rejected package.</resume-signal>
  <verify><automated>cd frontend &amp;&amp; npm view postcss@8.5.22 name version repository dist.integrity scripts --json &amp;&amp; npm view @typescript/typescript6@6.0.2 name version repository dist.integrity scripts --json &amp;&amp; cd .. &amp;&amp; git diff --exit-code -- frontend/package.json frontend/package-lock.json</automated></verify>
  <done>Both exact packages have current evidence, explicit approval, and no pre-approval package mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Install only the approved exact parser pins</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <behavior>
    - Package and lock manifests contain exact postcss@8.5.22 and @typescript/typescript6@6.0.2 dev-dependency identities.
    - No lifecycle script is executed and no unrelated dependency resolution changes.
  </behavior>
  <action>After approval, install exactly the two audited dev dependencies with lifecycle scripts disabled and commit the resulting lockfile. Reject any transitive resolution not attributable to those pins. Do not create manifests, schemas, generators, or generated sources in this plan.</action>
  <verify><automated>cd frontend &amp;&amp; npm install --save-dev --save-exact --ignore-scripts postcss@8.5.22 @typescript/typescript6@6.0.2 &amp;&amp; npm ls postcss @typescript/typescript6 --depth=0</automated></verify>
  <done>Both approved parsers are exactly pinned and no design-system authority file has been created yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Repository data to generator | Untrusted manifest paths and values enter build tooling |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-01 | Tampering | manifest.mjs | high | mitigate | Closed schemas, path containment, symlink checks, and no manifest execution |
| T-13-SC | Tampering | npm installs | high | mitigate | Exact audit plus blocking human checkpoint before install |
</threat_model>

<verification>
Re-run `npm ls` and inspect the package diff; only the approved pins and lockfile resolution may change.
</verification>

<success_criteria>
The parser dependency mutation is independently approved, exact, and bounded to two files.
</success_criteria>

<output>
Create `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-01-SUMMARY.md` when done.
</output>
