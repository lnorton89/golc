---
phase: 13-unified-ui-design-system-and-automated-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/design-system/tokens.json
  - frontend/design-system/components.json
  - frontend/design-system/runtime-geometry.json
  - frontend/design-system/exceptions.json
  - frontend/design-system/schema/**
  - frontend/scripts/design-system/manifest.mjs
  - frontend/scripts/design-system/generate.mjs
  - frontend/scripts/design-system/manifest.test.ts
  - frontend/src/design-system/tokens.generated.css
  - frontend/src/design-system/tokens.generated.ts
autonomous: false
requirements: [D-01, D-03, D-04, D-06, D-07, D-11, UI-SPEC-TOKENS, UI-SPEC-THEMES]
must_haves:
  truths:
    - "Paper/Ink and every approved theme face implement one exhaustive semantic contract per D-01, D-04, and D-06."
    - "Spacing is on the 4px grid, Guided First Show's gap is 8px, and 210px remains sizing per D-03."
    - "The audited exception manifest begins with zero entries and cannot bypass spacing per D-07 and D-11."
  artifacts:
    - path: "frontend/design-system/tokens.json"
      provides: "Inert semantic token and theme authority"
    - path: "frontend/src/design-system/tokens.generated.ts"
      provides: "Deterministic typed token surface"
  key_links:
    - from: "frontend/design-system/tokens.json"
      to: "frontend/src/design-system/tokens.generated.css"
      via: "generate.mjs"
      pattern: "generate:design-system"
---

<objective>
Establish the inert, generated token and manifest authority for D-01, D-03, D-04, D-06, D-07, and D-11.

Purpose: Freeze the semantic contract before parallel component and migration work.
Output: Approved exact parser pins, strict manifests, schemas, and byte-stable CSS/TypeScript.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-CONTEXT.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-UI-SPEC.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-RESEARCH.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Reproduce and approve both exact parser packages</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <action>Run the read-only registry queries, then review and authorize installation only if both exact identities, repositories, integrity records, lifecycle-script status, and the Microsoft compatibility recommendation are credible.</action>
  <what-built>Read-only legitimacy evidence for postcss@8.5.22 and @typescript/typescript6@6.0.2.</what-built>
  <how-to-verify>Review the command output for registry identity, official repository, integrity, lifecycle scripts, and Microsoft recommendation. Approve both exact pins or reject either pin; no package-manager mutation may occur before approval.</how-to-verify>
  <resume-signal>Type "approved" to authorize both exact pins, or identify the rejected package.</resume-signal>
  <verify><automated>cd frontend &amp;&amp; npm view postcss@8.5.22 name version repository dist.integrity scripts --json &amp;&amp; npm view @typescript/typescript6@6.0.2 name version repository dist.integrity scripts --json &amp;&amp; cd .. &amp;&amp; git diff --exit-code -- frontend/package.json frontend/package-lock.json</automated></verify>
  <done>Both exact packages have current evidence, explicit approval, and no pre-approval package mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install approved parsers and create strict inert manifests</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/design-system/**, frontend/scripts/design-system/manifest.mjs, frontend/scripts/design-system/manifest.test.ts</files>
  <behavior>
    - Malformed, unknown, duplicate, absolute, parent-traversing, or symlink-escaping records fail closed.
    - Every approved theme face has every semantic role; exceptions contain zero initial records.
  </behavior>
  <action>After approval, install both exact dev dependencies and commit the lockfile. Implement schemaVersion 1 pure-JSON manifests and strict loading by adapting the one-owner registry and synthetic-fixture patterns in `internal/projectconfig/registry.go` and `internal/projectconfig/strict_test.go`. Encode all UI-SPEC token groups, exhaustive theme faces, and typed runtime geometry. Start `exceptions.json` with zero entries.</action>
  <verify><automated>cd frontend &amp;&amp; npx vitest run scripts/design-system/manifest.test.ts</automated></verify>
  <done>Approved packages are exactly pinned and every manifest is inert, strict, exhaustive, contained, and initially exception-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate deterministic CSS and TypeScript token surfaces</name>
  <files>frontend/scripts/design-system/generate.mjs, frontend/src/design-system/tokens.generated.css, frontend/src/design-system/tokens.generated.ts</files>
  <behavior>
    - Two independent generations are byte-identical and check mode never writes.
    - CSS and TypeScript expose identical sorted semantic names for every theme face.
  </behavior>
  <action>Apply the deterministic generation and read-only drift patterns in `internal/scriptsdk/generate_test.go`: normalized LF, sorted collections, no timestamps or machine paths, one final newline, and exact repair diagnostics. Generate the checked-in CSS and frozen typed names from the strict manifests only.</action>
  <verify><automated>cd frontend &amp;&amp; npm run generate:design-system &amp;&amp; node scripts/design-system/generate.mjs --check</automated></verify>
  <done>Generated CSS/TypeScript are byte-stable, exhaustive, checked in, and fresh without check-mode mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Repository data to generator | Untrusted manifest paths and values enter build tooling |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-01 | Tampering | manifest.mjs | high | mitigate | Closed schemas, path containment, symlink checks, and no manifest execution |
| T-13-SC | Tampering | npm installs | high | mitigate | Exact audit plus blocking human checkpoint before install |
</threat_model>

<verification>
Run the Task 3 command from a clean diff and confirm generation check makes no changes.
</verification>

<success_criteria>
The semantic authority is deterministic, strict, exhaustive, Paper/Ink-preserving, 4px-grid compliant, and starts with no exceptions.
</success_criteria>

<output>
Create `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-01-SUMMARY.md` when done.
</output>
