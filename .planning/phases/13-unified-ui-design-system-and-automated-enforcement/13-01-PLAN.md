---
phase: 13-unified-ui-design-system-and-automated-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/design-system/tokens.json
  - frontend/design-system/components.json
  - frontend/design-system/runtime-geometry.json
  - frontend/design-system/exceptions.json
  - frontend/design-system/schema/tokens.schema.json
  - frontend/design-system/schema/components.schema.json
  - frontend/design-system/schema/runtime-geometry.schema.json
  - frontend/design-system/schema/exceptions.schema.json
  - frontend/scripts/design-system/manifest.mjs
  - frontend/scripts/design-system/generate.mjs
  - frontend/scripts/design-system/manifest.test.ts
  - frontend/src/design-system/tokens.generated.css
  - frontend/src/design-system/tokens.generated.ts
autonomous: false
requirements: [D-01, D-03, D-04, D-06, D-07, D-11, UI-SPEC-TOKENS, UI-SPEC-THEMES]
must_haves:
  truths:
    - "Paper/Ink and every current theme name retain light and dark faces behind one exhaustive semantic contract (D-01, D-06)."
    - "All public spacing is on the 4px grid, with Guided First Show normalized to 8px and 210px classified only as sizing (D-03)."
    - "Manifests are inert reviewed data; malformed, duplicate, unknown, escaping, or stale records fail closed (D-07)."
    - "Generated CSS and TypeScript are byte-stable on Windows and expose surfaces, text, borders, actions, statuses, typography, spacing, radii, sizing, focus, motion, and stacking (D-04)."
  artifacts:
    - path: "frontend/design-system/tokens.json"
      provides: "Single reviewed theme and foundation-token authority"
    - path: "frontend/design-system/runtime-geometry.json"
      provides: "Typed runtime geometry names, setting sites, ranges, fallbacks, and review conditions"
    - path: "frontend/design-system/exceptions.json"
      provides: "Exact audited DSX exception records with no spacing bypass"
    - path: "frontend/src/design-system/tokens.generated.ts"
      provides: "Typed exhaustive public token names"
  key_links:
    - from: "frontend/design-system/tokens.json"
      to: "frontend/src/design-system/tokens.generated.css"
      via: "deterministic generate.mjs output"
      pattern: "generate:design-system"
    - from: "frontend/design-system/runtime-geometry.json"
      to: "frontend/scripts/design-system/manifest.mjs"
      via: "strict closed-schema and contained-path validation"
      pattern: "runtime-geometry"
---

<objective>
Create the inert manifest authority and deterministic generated token surface for the complete approved design contract, honoring D-01, D-03, D-04, D-06, D-07, and D-11.

Purpose: Give all later component and migration plans one reviewed semantic vocabulary without executing configuration or preserving a legacy baseline.
Output: Exact-pinned parser tooling, strict manifests/schemas, deterministic CSS/TS generation, and freshness tests.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-CONTEXT.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-UI-SPEC.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-RESEARCH.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-PATTERNS.md
@frontend/package.json
@frontend/src/index.css
@frontend/src/lib/theme.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the two suspicious exact parser pins before installation</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <action>Re-run the read-only legitimacy evidence for postcss@8.5.22 and @typescript/typescript6@6.0.2. Present registry identity, official repository, selected-version publication, weekly adoption signal, integrity metadata, lifecycle-script status, and Microsoft's TS7 compatibility-package recommendation. Do not install either package until the user explicitly approves both exact pins; a different version requires a new legitimacy audit.</action>
  <verify>
    <automated>cd frontend; npm view postcss@8.5.22 name version repository dist.integrity scripts --json; npm view @typescript/typescript6@6.0.2 name version repository dist.integrity scripts --json</automated>
    <human-check>Confirm both exact packages are the intended official PostCSS parser and Microsoft TypeScript 6 compatibility API.</human-check>
  </verify>
  <done>The user approves both exact pins, or execution stops without changing package files.</done>
  <resume-signal>Type "approved" to install both exact pins, or identify the rejected package.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define strict inert manifests and deterministic token generation</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/design-system/tokens.json, frontend/design-system/components.json, frontend/design-system/runtime-geometry.json, frontend/design-system/exceptions.json, frontend/design-system/schema/tokens.schema.json, frontend/design-system/schema/components.schema.json, frontend/design-system/schema/runtime-geometry.schema.json, frontend/design-system/schema/exceptions.schema.json, frontend/scripts/design-system/manifest.mjs, frontend/scripts/design-system/generate.mjs, frontend/scripts/design-system/manifest.test.ts, frontend/src/design-system/tokens.generated.css, frontend/src/design-system/tokens.generated.ts</files>
  <behavior>
    - "A complete valid manifest generates byte-identical LF output twice with sorted names and no timestamp or machine path."
    - "Unknown fields, duplicate logical IDs, unsupported schema versions, missing theme roles, extra theme roles, invalid ranges, absolute/traversal paths, and symlink escape fail nonzero."
    - "Every current theme has exactly light and dark semantic faces; system remains preference resolution rather than a third face."
    - "Spacing declarations accept only 4, 8, 16, 24, 32, 48, and 64px roles; exception records cannot authorize other spacing."
    - "A drift check is read-only and reports the exact generate command."
  </behavior>
  <action>After approval, install postcss@8.5.22 and @typescript/typescript6@6.0.2 as exact dev dependencies with the lockfile committed. Implement pure JSON schemaVersion 1 manifests and a loader that rejects extra keys, duplicate public identities, unresolved files, paths outside the frontend root, and escaping symlinks without eval or dynamic manifest imports. Encode every mandatory UI-SPEC token group and all existing theme names/faces, plus typed runtime geometry records containing setter site, type, range, fallback, and review condition. Keep exceptions empty except for proven token-independent emergency/vendor/domain constructs; each admitted record must be exact and individually evidenced. Generate checked-in CSS custom properties and frozen TypeScript names deterministically, with a read-only freshness mode.</action>
  <verify>
    <automated>cd frontend; npm run generate:design-system; npx vitest run scripts/design-system/manifest.test.ts; node scripts/design-system/generate.mjs --check</automated>
  </verify>
  <done>The approved pins are exact, schemas reject every hostile/ambiguous fixture, all theme faces are exhaustive, generation is byte-stable, freshness is read-only, and no spacing exception or executable manifest path exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON manifests → generator/checker | Repository data controls generated frontend artifacts and policy decisions. |
| Manifest paths → frontend filesystem | Untrusted path strings and symlinks could escape the intended root. |
| npm registry → developer tooling | Two new parser packages enter the build toolchain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering / Elevation | manifest loader | high | mitigate | Pure JSON, closed schemas, no eval/dynamic manifest import, deterministic normalized snapshots. |
| T-13-02 | Tampering / Information disclosure | manifest path resolution | high | mitigate | Reject absolute/traversal paths and resolved symlinks outside the canonical frontend root. |
| T-13-03 | Tampering | malformed/unsupported manifests | high | mitigate | Fail closed before generation and leave checked-in outputs untouched. |
| T-13-SC | Tampering | npm installs | high | mitigate | Blocking human legitimacy checkpoint, exact pins, committed lockfile/integrity, and no lifecycle scripts. |
</threat_model>

<verification>
Run the manifest contract suite, generate twice into independent temporary roots, compare bytes, run freshness mode, and confirm package-lock records only the approved exact versions.
</verification>

<success_criteria>
The complete UI-SPEC token/theme/runtime-geometry/exception authority exists as inert data, every invalid input fails closed, and deterministic generated CSS/TS is ready for downstream consumers.
</success_criteria>

<output>
Create `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-01-SUMMARY.md` when done.
</output>
