---
context: phase
phase: 13-unified-ui-design-system-and-automated-enforcement
task: 2
total_tasks: 77
status: in_progress
last_updated: 2026-08-03T03:58:56.239Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These were discovered by actual failures. Acknowledge them before resuming.

- [ ] CONSTRAINT: Wails WebView2 CDP — `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS` is discarded by Wails v2/go-webview2; use the committed proof-only bridge, never retry the old environment-only harness.
- [ ] CONSTRAINT: migration parity — fixture and scene legacy violations must be converted to tokens/public primitives; DS001 spacing exceptions and broad exception proposals are prohibited.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Environment-only WebView2 debugging | It never reaches `msedgewebview2.exe` under Wails v2. | blocking | Preserve and run the committed proof-only local-module/overlay bridge from `82541bc4`. |
| Broad visual-policy exceptions | Fixture and scene migration initially exposed hundreds of legacy violations. | blocking | Convert directory-owned code in bounded passes; add focused tests and require scoped DS checks to pass. |
| Faking packaged evidence | Browser success is insufficient for the shipped desktop contract. | blocking | Keep `dialog-feasibility.json` fail-closed and rerun the packaged proof after seam changes. |

<current_state>

Phase 13 is executing. Completed plans: 13-01, 02, 03, 04, 05, 06, 07, 08, 09, 12, 21, 22, 23. Plan 13-10 Task 1 is complete; its Task 2 is partially converted. Plan 13-11 has a committed RED test plus interrupted broad-migration edits. Plan 13-13 was started but interrupted before any known completion. All later Wave 7 plans and validation waves remain.

Three active executors were interrupted at the user's pause request. Their shared-worktree edits are intentionally preserved; do not reset, checkout, or discard them.
</current_state>

<completed_work>

- Strict design-system manifest, schema validation, generated tokens, DS001–DS010 parser-backed checker, fixtures, and agent-facing inventory/guide/gallery are committed.
- Shared primitives (actions, fields, tabs, panel/chrome, dialog/confirm dialog, scroll/tooltip/resize) are committed and focused-tested.
- Dialog proof passes in Chromium and packaged WebView2. `82541bc4` adds the proof-only, hash-locked Wails/go-webview2 bridge; `9165038f` records passing evidence for Edg/150.0.4078.105.
- Theme authority, front-door workspaces, and Notes migrated with their reported focused tests/build checks.
- Fixture Library Task 1 committed (`e08024b6`); scoped checker/proposal parser `0fd53fe9`; shared fixture WorkspaceFrame/token foundation `4911f0d9`.
</completed_work>

<remaining_work>

- Finish 13-10 Task 2 only in its declared fixture directories. Current partial `FixturePatch.tsx` edit is uncommitted. The remaining work includes DS005 styled controls, DS006 shared classes, raw CSS policy violations, and missing focused tests. No broad exceptions.
- Finish 13-11 only in its declared scene files. `243fc2a7` is a RED SceneStack composition test. Uncommitted scene TSX/CSS changes need inspection and continuation; preserve Go timing authority and make scoped checks green.
- Inspect/continue or cleanly checkpoint Plan 13-13 Desk work; it was interrupted after dispatch.
- Execute Wave 7 plans 13-14, 15, 16, 24–29, 40, then Waves 8–16 in DAG order. Windows CI mutation at Plan 13-35 needs explicit user authority.
</remaining_work>

<decisions_made>

- Keep the Paper/Ink console identity; unify it with semantic tokens, not a rebrand.
- Treat React as a projection: migration must not move playback timing, Art-Net output, preview-safety, Deno, or live safety authority into the UI.
- Use exact direct parser pins: `postcss@8.5.22` and `@typescript/typescript6@6.0.2`.
- Keep package-script/CLI grammar repaired and tested: `generate:design-system`, `--rule`, and `--paths` were missing in reality and were added only as minimal integration fixes.
</decisions_made>

<blockers>

- No human action currently blocks work.
- The only historical blocker, packaged WebView2 CDP, is resolved by `82541bc4`; do not remove or casually refactor it without rerunning `scripts/ci/run-packaged-dialog-proof.ps1`.
- Whole-source parity is expected to remain red until the remaining Wave 7 migration directories are converted. Scoped checks should be the incremental gate.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable pause state.
1. `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-CONTEXT.md` — scope and decisions.
1. `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-UI-SPEC.md` — approved visual/safety contract.
1. `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-VALIDATION.md` — required evidence and ordering.
1. `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-10-SUMMARY.md` — exact fixture handoff.
1. `frontend/DESIGN_SYSTEM.md` and `frontend/design-system/components.json` — public surfaces agents must use.
1. `.planning/debug/webview2-cdp-wails-seam.md` — resolved root cause and seam rationale.

## Infrastructure State

- No servers, watchers, or external jobs were intentionally left running.
- Do not touch `internal/deskmidi/`, `site/`, `go.mod`, `go.sum`, `internal/projectconfig/reference_property_test.go`, `cmd/golc-desktop/rsrc_windows_amd64.syso`, or `cmd/golc-desktop/rsrc_windows_amd64.syso.wails-dev-disabled`; those are unrelated/protected worktree state.
- The root worktree has deliberate uncommitted pause state listed in `.planning/HANDOFF.json`.

<next_action>

Start by acknowledging both blocking constraints, inspect `git diff` for the uncommitted fixture/scene edits, and continue 13-10 Task 2 to a scoped-check-passing checkpoint. Do not start a new migration plan until those interrupted directory owners are resolved.
</next_action>
