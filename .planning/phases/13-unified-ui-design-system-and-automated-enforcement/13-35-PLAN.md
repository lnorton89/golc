---
phase: 13-unified-ui-design-system-and-automated-enforcement
plan: 35
type: execute
wave: 15
depends_on: [13-18, 13-20, 13-36]
files_modified:
  - .planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/windows-ci-run.json
autonomous: false
requirements: [D-09, D-10, D-11, D-13, UI-SPEC-VISUAL-MATRIX, UI-SPEC-WINDOWS-CI]
must_haves:
  truths:
    - "No push or workflow dispatch occurs without explicit authority."
    - "An immutable workflow run URL/id and head SHA identify the required Windows job."
    - "Downloaded artifacts, schemas, hashes, and embedded SHA/build identity are inspected read-only before cleanup."
    - "Later evidence/planning descendants preserve the exact CI-proven implementation tree and cannot hide a frontend, runtime, workflow, dependency, build, or configuration change."
  artifacts:
    - path: ".planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/windows-ci-run.json"
      provides: "Immutable Windows run and artifact inspection record"
  key_links:
    - from: ".github/workflows/design-system.yml"
      to: "evidence/windows-ci-run.json"
      via: "workflow run id, URL, head SHA, artifact ids and hashes"
      pattern: "runId|headSha|artifacts"
---

<objective>
Obtain explicit authority for the Windows workflow mutation, then observe and inspect its immutable run read-only.

Purpose: Do not assume push/dispatch authority and do not conflate triggering with artifact validation.
Output: Approved immutable run identity followed by downloaded semantic evidence inspection.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.github/workflows/design-system.yml
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-18-SUMMARY.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-20-SUMMARY.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-36-SUMMARY.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Authorize the workflow-triggering mutation</name>
  <files>.planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/windows-ci-run.json</files>
  <action>Perform only read-only preflight: resolve the exact local commit SHA, inspect GitHub authentication, and inspect the named workflow. Present the three options below. Do not push, dispatch, or create the evidence record until the user explicitly selects an authorized mechanism.</action>
  <decision>Choose whether the executor may push the exact current commit or dispatch the required design-system workflow for that immutable commit.</decision>
  <context>The plan runs only after Plan 13-36, so the selected SHA contains the final implementation. The plan may inspect local workflow configuration and repository/CLI authentication read-only, but push and workflow_dispatch are external mutations and are not assumed. Record the selected mechanism, exact immutable implementation SHA, deterministic non-planning implementation-tree manifest/hash, target branch/ref, repository, and approval before triggering. If authority is denied, stop at this checkpoint without claiming CI evidence.</context>
  <options>
    <option id="authorize-push">
      <name>Authorize exact commit push</name>
      <pros>Normal required-workflow path validates the actual branch commit.</pros>
      <cons>Mutates the remote branch.</cons>
    </option>
    <option id="authorize-dispatch">
      <name>Authorize workflow dispatch</name>
      <pros>Triggers the named workflow for an explicitly selected immutable ref.</pros>
      <cons>Mutates GitHub Actions state and requires workflow_dispatch support.</cons>
    </option>
    <option id="deny">
      <name>Deny external mutation</name>
      <pros>No remote state changes.</pros>
      <cons>Phase sign-off remains blocked because required Windows evidence cannot exist.</cons>
    </option>
  </options>
  <verify><automated>git rev-parse HEAD &amp;&amp; gh auth status &amp;&amp; gh workflow view design-system.yml</automated></verify>
  <resume-signal>Select authorize-push, authorize-dispatch, or deny and confirm the exact SHA/ref.</resume-signal>
  <done>The checkpoint records explicit authority or denial for one exact SHA/ref; no external mutation occurs before approval.</done>
</task>

<task type="auto">
  <name>Task 2: Perform only the explicitly authorized trigger</name>
  <files>.planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/windows-ci-run.json</files>
  <action>Execute only the mechanism approved at Task 1 for the exact approved SHA/ref. For authorize-push, push that SHA to only the approved remote ref without force. For authorize-dispatch, dispatch only `design-system.yml` with the approved immutable ref. Reject a changed local SHA, different remote/ref, absent approval, or any force flag. Resolve the resulting workflow run id/URL/head SHA and write the pending immutable identity fields to the evidence record; do not download or validate artifacts in this mutation task.</action>
  <verify><automated>gh run list --workflow design-system.yml --commit $env:GOLC_PHASE13_APPROVED_SHA --limit 1 --json databaseId,url,event,headSha,headBranch,status,conclusion,createdAt</automated></verify>
  <done>Exactly one approved push/dispatch mutation occurred and its run id/URL/head SHA are recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Wait, download, and inspect immutable Windows artifacts read-only</name>
  <files>.planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/windows-ci-run.json</files>
  <action>After the authorized trigger completes, capture `ciProvenImplementationSha`, the deterministic implementation-tree manifest/hash over all tracked paths outside `.planning/**`, immutable GitHub Actions run id/URL, workflow file path, event, repository, head SHA, head branch/ref, attempt, created/updated timestamps, conclusion, job ids, and artifact ids. Require run head SHA equality with the approved immutable implementation SHA. Also record `observedDescendantSha`, ancestry result, exact changed-path list, the sole allowlist `.planning/**`, recomputed descendant implementation-tree manifest/hash, and equality result. Wait for terminal completion using read-only `gh run watch`; download expected result, calibration, screenshot actual/expected/diff, mask-audit, zoom, offline-safety, and packaged-WebView2 artifacts by run id into a temporary contained directory. Validate each result schema, embedded SHA/build identity, artifact manifest, file count, and SHA-256; reject missing/extra artifacts, failure conclusions, non-descendants, any changed path outside `.planning/**`, any implementation manifest/hash mismatch, or any frontend/runtime/workflow/dependency/build/configuration change after the proven SHA. Write only the summarized immutable record and hashes, then remove only the verified temporary download directory.</action>
  <verify><automated>gh run view $env:GOLC_PHASE13_RUN_ID --json databaseId,url,workflowName,event,headSha,headBranch,status,conclusion,createdAt,updatedAt,jobs &amp;&amp; gh api repos/{owner}/{repo}/actions/runs/$env:GOLC_PHASE13_RUN_ID/artifacts &amp;&amp; cd frontend &amp;&amp; npm run validate:phase13-evidence -- --evidence ../.planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/windows-ci-run.json</automated></verify>
  <done>The approved SHA has a successful immutable run whose expected artifacts, hashes, schemas, and build identities were inspected read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Local commit to GitHub Actions | Unauthorized mutation or stale-run evidence could certify the wrong build |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-38 | Repudiation | workflow run | high | mitigate | Explicit trigger authority, immutable run id/URL, exact head SHA, artifact hashes/schemas |
</threat_model>
<verification>The checkpoint records authority first; the separate read-only task validates terminal run and artifact identity.</verification>
<success_criteria>Windows CI evidence is attributable to the approved immutable implementation SHA; later evidence/planning-only descendants are accepted only with identical non-planning tree content and no runtime/configuration change.</success_criteria>
<output>Create `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-35-SUMMARY.md` when done.</output>
