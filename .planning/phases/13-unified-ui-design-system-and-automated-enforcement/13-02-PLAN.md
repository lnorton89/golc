---
phase: 13-unified-ui-design-system-and-automated-enforcement
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - frontend/scripts/design-system/check.mjs
  - frontend/scripts/design-system/css-policy.mjs
  - frontend/scripts/design-system/tsx-policy.mjs
  - frontend/scripts/design-system/check.test.ts
  - frontend/scripts/design-system/fixtures
  - frontend/package.json
  - internal/command/designsystem.go
  - internal/command/designsystem_test.go
  - internal/delivery/mage_targets.go
  - internal/delivery/mage_targets_test.go
  - magefiles/magefile.go
  - magefiles/magefile_test.go
autonomous: true
requirements: [D-05, D-07, D-09, D-10, D-11, D-12, D-13, UI-SPEC-ENFORCEMENT]
must_haves:
  truths:
    - "DS001-DS010 produce stable Windows-normalized path:line:column diagnostics and fail closed on unreadable or unsupported syntax (D-09)."
    - "Every rule has an allowed and forbidden held-out fixture, preventing a checker that passes by finding nothing (D-10)."
    - "Exceptions are exact, contained, non-stale, and cannot bypass spacing, DS007, or DS008 (D-07)."
    - "The checker distinguishes feature-owned visual reinvention from native semantics inside public primitives (D-05, D-12)."
  artifacts:
    - path: "frontend/scripts/design-system/check.mjs"
      provides: "Deterministic DS001-DS010 orchestration"
    - path: "frontend/scripts/design-system/fixtures"
      provides: "Allowed and forbidden fixture repositories for every rule"
    - path: "internal/command/designsystem.go"
      provides: "Pinned-Node repository route for the policy checker"
  key_links:
    - from: "frontend/scripts/design-system/check.mjs"
      to: "frontend/design-system/exceptions.json"
      via: "exact parsed-node exception matching"
      pattern: "DS008"
    - from: "magefiles/magefile.go"
      to: "internal/command/designsystem.go"
      via: "shared Mage target registry"
      pattern: "DesignSystem"
---

<objective>
Implement the deterministic DS001-DS010 policy engine and expose it through frontend and repository-owned validation seams, per D-05, D-07, and D-09 through D-13.

Purpose: Make each later migration slice mechanically measurable without regex-only parsing, silent skips, or a permanent debt baseline.
Output: Parser-first CSS/TSX policies, adversarial fixtures, package scripts, and a pinned-Node Mage route.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-01-SUMMARY.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-UI-SPEC.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-RESEARCH.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-PATTERNS.md
@frontend/package.json
@internal/command/build.go
@magefiles/magefile.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build fail-closed DS001-DS010 parsing and exception integrity</name>
  <files>frontend/scripts/design-system/check.mjs, frontend/scripts/design-system/css-policy.mjs, frontend/scripts/design-system/tsx-policy.mjs, frontend/scripts/design-system/check.test.ts, frontend/scripts/design-system/fixtures</files>
  <behavior>
    - "DS001-DS010 each accept a canonical allowed fixture and reject a canonical forbidden fixture with stable rule ID, normalized slash path, line, column, and offending construct."
    - "Malformed CSS/TSX, unreadable manifest, unsupported syntax, absolute/traversal path, and out-of-root symlink exit nonzero instead of skipping."
    - "File enumeration and diagnostics are sorted and byte-identical across repeated Windows runs."
    - "DS005 requires a feature-owned lowercase native control plus a styling signal; public primitive implementation and semantic test scaffolding remain allowed."
    - "DS008 rejects broad, duplicate, zero-match, multi-match, stale, orphaned, spacing, DS007, and DS008 exceptions."
    - "DS010 rejects removed focus, non-token motion, color-only shared statuses, and CSS that hides named safety surfaces."
  </behavior>
  <action>Implement PostCSS declaration/selector traversal for DS001-DS004 and relevant DS006/DS010 checks, and the approved TypeScript 6 compatibility API for TSX AST checks. Build declared token/runtime-geometry sets from validated manifests. Match exceptions against one exact parsed construct and fingerprint, never a directory, glob, arbitrary regex, or line number alone. Normalize roots, resolved symlinks, paths, line endings, and sort keys before reporting. Fixture repositories must be self-contained, include both polarity cases for all ten rules, and explicitly exercise false-positive boundaries, malformed inputs, Windows separators, exception integrity, theme completeness, export parity, and safety style checks.</action>
  <verify>
    <automated>cd frontend; npx vitest run scripts/design-system/check.test.ts</automated>
  </verify>
  <done>All ten rules prove detection and acceptance behavior; parse/config/path failures are nonzero; diagnostics are deterministic; exceptions cannot become a baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Expose one checker through package scripts and the repository command graph</name>
  <files>frontend/package.json, internal/command/designsystem.go, internal/command/designsystem_test.go, internal/delivery/mage_targets.go, internal/delivery/mage_targets_test.go, magefiles/magefile.go, magefiles/magefile_test.go</files>
  <action>Add `generate:design-system`, fixture-policy, scoped-source, and final `check:design-system` scripts using only checked-in Node modules. Register one internal command that resolves the pinned Node/npm installation and delegates to these scripts; add the thin Mage target through the existing delivery registry and command-parity tests. During migration, scoped checks must accept explicit contained file roots and use the same rules/config as the final whole-source command; they cannot waive findings or maintain a baseline. Do not yet prepend the whole-source command to `build` while known source migration remains incomplete; Plan 13-11 enables the final green gate.</action>
  <verify>
    <automated>cd frontend; npm run test:design-system-policy; cd ..; go test ./internal/command ./internal/delivery ./magefiles -run 'DesignSystem|MageTarget' -count=1</automated>
  </verify>
  <done>The frontend and Mage paths invoke the same deterministic implementation with pinned Node, fixtures are green, and no second checker, waiver baseline, network access, or ambient-tool lookup exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS/TSX source → parsers | Malformed or adversarial source could evade enforcement. |
| Exception records → checker result | An overbroad/stale exception could suppress drift. |
| Mage route → Node subprocess | Tool path/environment must remain repository controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04 | Tampering | CSS/TSX parser pipeline | high | mitigate | PostCSS/TS6 AST parsing, syntax diagnostics, fail-closed reads, sorted enumeration. |
| T-13-05 | Elevation / Tampering | exceptions | high | mitigate | Exact one-node matches, contained paths, no broad expressions or rule-integrity bypass. |
| T-13-06 | Spoofing / Denial of service | DS010 safety/style policy | high | mitigate | Static named-safety hiding checks plus later rendered browser assertions. |
| T-13-07 | Tampering | Mage subprocess | medium | mitigate | Resolve pinned Node/npm through existing command infrastructure and keep a single implementation. |
| T-13-SC | Tampering | parser dependencies | high | mitigate | Consume only the approved exact lockfile pins from Plan 13-01. |
</threat_model>

<verification>
Run every fixture twice, compare diagnostic bytes, exercise malformed/escaping inputs, then execute the same fixture suite through npm and Mage.
</verification>

<success_criteria>
DS001-DS010 are deterministic, syntax-aware, fail-closed, exception-safe, and accessible through one pinned frontend/repository command path.
</success_criteria>

<output>
Create `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-02-SUMMARY.md` when done.
</output>
