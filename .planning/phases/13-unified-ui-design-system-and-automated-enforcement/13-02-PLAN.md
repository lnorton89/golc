---
phase: 13-unified-ui-design-system-and-automated-enforcement
plan: 02
type: execute
wave: 3
depends_on: [13-21]
files_modified:
  - frontend/scripts/design-system/check.mjs
  - frontend/scripts/design-system/css-policy.mjs
  - frontend/scripts/design-system/tsx-policy.mjs
  - frontend/scripts/design-system/check.test.ts
  - frontend/scripts/design-system/fixtures/**
autonomous: true
requirements: [D-05, D-07, D-09, D-10, D-11, D-12, D-13, UI-SPEC-ENFORCEMENT]
must_haves:
  truths:
    - "DS001-DS010 fail closed with stable sorted diagnostics per D-09."
    - "Every rule has allowed and forbidden fixtures per D-10."
    - "Exceptions are exact, contained, non-stale, and cannot bypass spacing or inventory parity per D-07 and D-11."
  artifacts:
    - path: "frontend/scripts/design-system/check.mjs"
      provides: "Deterministic policy orchestration"
    - path: "frontend/scripts/design-system/fixtures"
      provides: "Held-out polarity fixtures for every rule"
  key_links:
    - from: "frontend/scripts/design-system/check.mjs"
      to: "frontend/design-system/exceptions.json"
      via: "exact parsed-node matching"
      pattern: "DS008"
---

<objective>
Implement the fail-closed DS001-DS010 policy engine required by D-05, D-07, D-09, D-10, D-11, D-12, and D-13.

Purpose: Give every later slice a fast green ratchet without a debt baseline.
Output: CSS/TSX policies, orchestration, and exhaustive held-out fixtures.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-CONTEXT.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-UI-SPEC.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-RESEARCH.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-PATTERNS.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-21-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CSS and TSX policy boundaries</name>
  <files>frontend/scripts/design-system/css-policy.mjs, frontend/scripts/design-system/tsx-policy.mjs, frontend/scripts/design-system/fixtures/**</files>
  <behavior>
    - DS001-DS010 each accept a valid case and reject its forbidden counterpart.
    - Styled native reinvention is rejected while native semantics inside public primitives remain legal.
    - Malformed syntax and unresolvable source paths return nonzero stable diagnostics.
  </behavior>
  <action>Implement PostCSS traversal and the approved TypeScript compatibility AST boundary. Follow `internal/projectconfig/strict_test.go` for minimal synthetic fixtures and the explicit no-analog guidance in `13-PATTERNS.md` for the JS policy implementation. Normalize roots, symlinks, slash paths, LF, and sort keys before reporting. Cover theme completeness, raw literals, custom properties, native-control reinvention, exports/docs, exact exceptions, safety styling, and shared-state duplication.</action>
  <verify><automated>cd frontend &amp;&amp; npx vitest run scripts/design-system/check.test.ts --testNamePattern="DS00[1-9]|DS010"</automated></verify>
  <done>Every rule has both polarity fixtures and syntax/path failures are deterministic and nonzero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate scoped and whole-source checking</name>
  <files>frontend/scripts/design-system/check.mjs, frontend/scripts/design-system/check.test.ts</files>
  <behavior>
    - Scoped checking and whole-source checking share identical rules and configuration.
    - Enumeration proves production CSS Module and TSX coverage instead of passing on an empty set.
    - Exact exception records match one construct and stale, zero-match, or multi-match records fail.
  </behavior>
  <action>Build deterministic orchestration with contained explicit roots for task-level checks and full production enumeration for acceptance. Use the generated-source freshness analog in `internal/scriptsdk/generate_test.go`; check mode must report drift and the repair command without writing. Allow migration proposal manifests only as separately validated exact records; they never count as the final exception authority.</action>
  <verify><automated>cd frontend &amp;&amp; npx vitest run scripts/design-system/check.test.ts</automated></verify>
  <done>The same fail-closed engine supports fast scoped feedback and exhaustive final enumeration with no waiver baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Source tree to checker | Malformed source and paths enter developer tooling |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-02 | Tampering | check.mjs | high | mitigate | Fail-closed parsing, contained paths, sorted enumeration, exact exception fingerprints |
| T-13-03 | Spoofing | DS010 safety policy | medium | mitigate | Held-out fixtures prove labels/focus/safety visibility cannot be color-only |
</threat_model>

<verification>
Run the complete checker test file; all ten rule IDs must have positive and negative evidence.
</verification>

<success_criteria>
The checker cannot pass through omission, malformed input, broad exceptions, or nondeterministic ordering.
</success_criteria>

<output>
Create `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-02-SUMMARY.md` when done.
</output>
