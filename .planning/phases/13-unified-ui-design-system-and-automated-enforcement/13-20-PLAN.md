---
phase: 13-unified-ui-design-system-and-automated-enforcement
plan: 20
type: execute
wave: 11
depends_on: [13-18]
files_modified:
  - frontend/package.json
  - frontend/scripts/design-system/validate-phase13-evidence.mjs
  - frontend/scripts/design-system/validate-phase13-evidence.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, UI-SPEC-MIGRATION-ACCEPTANCE]
must_haves:
  truths:
    - "Validation tooling derives every task and exact normalized automated verification command from PLAN files."
    - "Command equality/hash, result schema, environment/build identity, semantic artifacts, calibration arithmetic, masks, packaged WebView2, and Windows CI evidence all fail closed."
    - "Markdown or artifact existence alone cannot produce sign-off."
    - "The validator proves 200% text-zoom and provider/daemon-offline semantics and rejects any non-planning implementation-tree change after the CI-proven SHA."
  artifacts:
    - path: "frontend/scripts/design-system/validate-phase13-evidence.mjs"
      provides: "Plan-derived fail-closed semantic evidence validator"
    - path: "frontend/scripts/design-system/validate-phase13-evidence.test.ts"
      provides: "Independent mutation coverage for every trust field"
  key_links:
    - from: "frontend/scripts/design-system/validate-phase13-evidence.mjs"
      to: ".planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-VALIDATION.md"
      via: "derived task/command/hash/evidence contract"
      pattern: "sha256|automated"
---

<objective>
Implement plan-derived validation-contract tooling deeply enough to prevent fake Phase 13 sign-off.

Purpose: Make exact commands and semantic evidence authoritative before acceptance records are written.
Output: Validator, exhaustive mutation tests, and package route.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-CONTEXT.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-UI-SPEC.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-VALIDATION.md
@internal/projectconfig/strict_test.go
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-18-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exact plan-derived command and semantic evidence validation</name>
  <files>frontend/scripts/design-system/validate-phase13-evidence.mjs, frontend/scripts/design-system/validate-phase13-evidence.test.ts, frontend/package.json</files>
  <behavior>
    - Parser discovers every `13-NN-PLAN.md` task, extracts the exact normalized automated verification element, computes SHA-256, and rejects missing/extra/stale task rows or any non-equal command/hash.
    - Completed rows require a closed result schema: task ID, plan/wave, exact command, hash, exit status zero, timestamps, repository commit SHA, environment/OS/browser/runtime identity, build identity, and typed artifact records.
    - Calibration validators recompute pairwise arithmetic and selected tolerance; mask validators inspect every rectangle and protected intersection; packaged WebView2 evidence matches executable build hash/runtime/assertions; Windows workflow run id/URL, head SHA, downloaded hashes, and result schemas agree.
    - Windows evidence identifies `ciProvenImplementationSha`, a deterministic implementation-tree manifest/hash over every tracked path outside `.planning/**`, and the observed descendant SHA; descendants are accepted only when ancestry holds, every changed path is allowlisted under `.planning/**`, and the recomputed non-planning manifest/hash is identical.
    - Text-zoom evidence proves a real 200% browser text zoom at 900x720 with exact viewport/zoom, body overflow metrics, focus traversal, and reachable navigation/live-truth/active-task/safety locators.
    - Provider-offline and daemon-offline evidence records projected status text, focus/action results for Blackout and Revoke, independent local command-path identities, and explicit playback/output truth that never infers stopped output from connectivity failure.
    - Required artifacts are parsed and semantically validated with contained paths and hashes; Markdown existence cannot satisfy a row.
    - D-01..D-14/UI-SPEC coverage and the six separately named startup, emergency fallback, specialized geometry, expanded-copy, 200%-zoom, and offline-safety backstops are exact.
  </behavior>
  <action>Parse PLAN XML instead of copying commands into code. Normalize XML entities, CRLF/LF, and surrounding whitespace once; require exact string equality plus SHA-256 and reject shell-equivalent substitutions. Derive task IDs from actual task positions. Implement typed validators for local result JSON, screenshot calibration arithmetic, mask rectangles/protected locators, packaged WebView2 executable hash, Windows workflow run/head SHA/artifacts, 200% text-zoom geometry/focus semantics, provider/daemon-offline safety semantics, and task-specific artifact contents. Define the CI implementation identity as a sorted manifest of repository-relative path, Git mode, and blob object ID for every tracked file outside `.planning/**`, hashed with SHA-256. Require the observed evidence/planning commit to descend from `ciProvenImplementationSha`; allow intervening changes only under `.planning/**`; recompute and compare the non-planning manifest/hash at both commits, and reject any frontend, runtime, workflow, build, dependency, or configuration change after the proven SHA. Add `validate:phase13-evidence` to package scripts. Follow `internal/projectconfig/strict_test.go` for stable fail-closed diagnostics.</action>
  <verify><automated>cd frontend &amp;&amp; npx vitest run scripts/design-system/validate-phase13-evidence.test.ts</automated></verify>
  <done>Every task/command is plan-derived and every evidence type is semantically validated before sign-off.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every false-sign-off mutation is rejected</name>
  <files>frontend/scripts/design-system/validate-phase13-evidence.test.ts</files>
  <behavior>
    - Independent mutations cover every required field, command character/hash, exit status, SHA identity, implementation-tree manifest/hash, descendant ancestry/allowlist, arithmetic operand/result, mask classification, WebView2 build hash, workflow artifact hash/schema, zoom metric/locator/focus result, offline projected truth/local command path, task mapping, artifact contents, and sign-off flag.
    - Malformed or ambiguous Markdown/JSON and duplicate keys fail closed with stable diagnostics.
  </behavior>
  <action>Build table-driven positive and single-fault negative fixtures. Include command whitespace/entity normalization boundaries, wrong-but-successful command substitution, stale commit/build identities, a non-descendant evidence commit, a changed frontend/runtime/config path after the proven SHA, a forged implementation-tree hash, a changed path outside the `.planning/**` allowlist, tampered calibration arithmetic, protected mask intersection, 100% mislabeled as 200% zoom, positive body overflow, an unreachable required locator, missing focus traversal, offline controls routed through provider/daemon availability, offline status falsely claiming playback/output stopped, artifact files with missing semantic content, workflow head mismatch, packaged executable hash mismatch, and premature `wave_0_complete`, `nyquist_compliant`, or approval.</action>
  <verify><automated>cd frontend &amp;&amp; npx vitest run scripts/design-system/validate-phase13-evidence.test.ts --testNamePattern="mutation|false sign-off|semantic|implementation tree|zoom|offline safety"</automated></verify>
  <done>No prose-only, existence-only, stale, mismatched, or semantically malformed evidence can pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| PLAN and execution evidence to sign-off | Stale prose or forged artifacts could falsely certify completion |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-22 | Repudiation | evidence validator | high | mitigate | Exact derived commands/hashes, closed schemas, semantic parsing, identity checks, mutation tests |
</threat_model>

<verification>Both exact mutation-test commands pass fail-fast.</verification>
<success_criteria>Phase 13 cannot be signed off from mismatched commands, prose, file existence, stale builds, or malformed semantic evidence.</success_criteria>
<output>Create `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-20-SUMMARY.md` when done.</output>
