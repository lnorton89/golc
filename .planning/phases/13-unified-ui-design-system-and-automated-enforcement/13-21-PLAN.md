---
phase: 13-unified-ui-design-system-and-automated-enforcement
plan: 21
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - frontend/design-system/tokens.json
  - frontend/design-system/components.json
  - frontend/design-system/runtime-geometry.json
  - frontend/design-system/exceptions.json
  - frontend/design-system/schema/tokens.schema.json
  - frontend/design-system/schema/components.schema.json
  - frontend/design-system/schema/runtime-geometry.schema.json
  - frontend/design-system/schema/exceptions.schema.json
  - frontend/scripts/design-system/manifest.mjs
  - frontend/scripts/design-system/manifest.test.ts
  - frontend/scripts/design-system/generate.mjs
  - frontend/src/design-system/tokens.generated.css
  - frontend/src/design-system/tokens.generated.ts
autonomous: true
requirements: [D-01, D-03, D-04, D-06, D-07, D-11, UI-SPEC-TOKENS, UI-SPEC-THEMES]
must_haves:
  truths:
    - "Paper/Ink and every approved theme face implement one exhaustive semantic contract per D-01, D-04, and D-06."
    - "Spacing is on the 4px grid, Guided First Show's gap is 8px, and 210px remains sizing per D-03."
    - "The audited exception manifest begins with zero entries and generation is byte-stable per D-07 and D-11."
  artifacts:
    - path: "frontend/design-system/tokens.json"
      provides: "Inert semantic token and theme authority"
    - path: "frontend/src/design-system/tokens.generated.ts"
      provides: "Deterministic typed token surface"
  key_links:
    - from: "frontend/design-system/tokens.json"
      to: "frontend/src/design-system/tokens.generated.css"
      via: "generate.mjs"
      pattern: "generate:design-system"
---

<objective>
Create the strict manifest authority and deterministic generated token surfaces after package approval.

Purpose: Separate supply-chain mutation from the thirteen-file semantic authority.
Output: Four manifests, four schemas, strict loader tests, generator, and byte-stable CSS/TypeScript.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-CONTEXT.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-UI-SPEC.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-PATTERNS.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create strict inert manifests and schemas</name>
  <files>frontend/design-system/tokens.json, frontend/design-system/components.json, frontend/design-system/runtime-geometry.json, frontend/design-system/exceptions.json, frontend/design-system/schema/tokens.schema.json, frontend/design-system/schema/components.schema.json, frontend/design-system/schema/runtime-geometry.schema.json, frontend/design-system/schema/exceptions.schema.json, frontend/scripts/design-system/manifest.mjs, frontend/scripts/design-system/manifest.test.ts</files>
  <behavior>
    - Malformed, unknown, duplicate, absolute, parent-traversing, or symlink-escaping records fail closed.
    - Every approved theme face has every semantic role; exceptions contain zero initial records.
  </behavior>
  <action>Implement schemaVersion 1 pure-JSON manifests and strict loading by adapting the one-owner registry and synthetic-fixture patterns in `internal/projectconfig/registry.go` and `internal/projectconfig/strict_test.go`. Encode every UI-SPEC token group, exhaustive theme face, typed runtime geometry, and zero-entry exception authority. This task owns exactly ten concrete files.</action>
  <verify><automated>cd frontend &amp;&amp; npx vitest run scripts/design-system/manifest.test.ts</automated></verify>
  <done>All four manifests are inert, strict, exhaustive, contained, and initially exception-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate deterministic CSS and TypeScript token surfaces</name>
  <files>frontend/scripts/design-system/generate.mjs, frontend/src/design-system/tokens.generated.css, frontend/src/design-system/tokens.generated.ts</files>
  <behavior>
    - Two independent generations are byte-identical and check mode never writes.
    - CSS and TypeScript expose identical sorted semantic names for every theme face.
  </behavior>
  <action>Apply `internal/scriptsdk/generate_test.go` exactly as the mapped deterministic-generation analog: normalized LF, sorted collections, no timestamps or machine paths, one final newline, and exact repair diagnostics. Generate only from the strict manifests.</action>
  <verify><automated>cd frontend &amp;&amp; npm run generate:design-system &amp;&amp; node scripts/design-system/generate.mjs --check</automated></verify>
  <done>Generated CSS/TypeScript are byte-stable, exhaustive, checked in, and fresh without check-mode mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Repository data to generator | Untrusted manifest paths and values enter build tooling |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-23 | Tampering | manifest/generator | high | mitigate | Closed schemas, contained paths, deterministic generation, no manifest execution |
</threat_model>

<verification>
Run both exact task commands and confirm generator check mode produces no working-tree change.
</verification>

<success_criteria>
The semantic authority is deterministic, strict, exhaustive, Paper/Ink-preserving, 4px-grid compliant, and starts with no exceptions.
</success_criteria>

<output>
Create `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-21-SUMMARY.md` when done.
</output>
