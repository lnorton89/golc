---
phase: 13-unified-ui-design-system-and-automated-enforcement
plan: 41
type: execute
wave: 9
depends_on: [13-15, 13-17, 13-24, 13-25]
files_modified:
  - frontend/e2e/fixtures/projectedAcceptanceStates.ts
  - frontend/e2e/design-system.text-zoom.spec.ts
  - frontend/e2e/design-system.offline-safety.spec.ts
  - .planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/text-zoom-200.json
  - .planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/offline-safety.json
autonomous: true
requirements: [D-02, D-03, D-10, D-12, D-13, D-14, UI-SPEC-VISUAL-VERIFICATION, UI-SPEC-MIGRATION-ACCEPTANCE]
must_haves:
  truths:
    - "At 200% text zoom and 900x720, navigation, live truth, active task, Blackout, Revoke Automation, and Stop/Release-All remain visible or keyboard reachable without body overflow."
    - "Provider-offline and daemon-offline projections leave Blackout and Revoke visible, keyboard reachable, and operable through independent local paths."
    - "Offline projections report connectivity truth without claiming playback or output stopped unless Go-owned state independently reports that fact."
  artifacts:
    - path: "frontend/e2e/design-system.text-zoom.spec.ts"
      provides: "Executable 200% text-zoom reachability and overflow proof"
    - path: "frontend/e2e/design-system.offline-safety.spec.ts"
      provides: "Executable provider/daemon-offline safety authority proof"
  key_links:
    - from: "frontend/e2e/fixtures/projectedAcceptanceStates.ts"
      to: "frontend/e2e/design-system.offline-safety.spec.ts"
      via: "named provider-offline and daemon-offline state/action fixtures"
      pattern: "providerOffline|daemonOffline"
---

<objective>
Make the approved UI-SPEC text-zoom and offline-safety acceptance clauses executable and semantically evidenced.

Purpose: Prove accessibility reflow and independent local safety authority under the exact failure states operators face.
Output: One typed state fixture, two focused Playwright specs, and two closed semantic evidence objects.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-UI-SPEC.md
@frontend/e2e/helpers.ts
@frontend/e2e/responsive.spec.ts
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-15-SUMMARY.md
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-24-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove 200 percent text zoom at compact desktop size</name>
  <files>frontend/e2e/design-system.text-zoom.spec.ts, .planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/text-zoom-200.json</files>
  <behavior>
    - Chromium renders the seeded active-show fixture at exactly 900x720 with browser text zoom set to 200%, not viewport scaling or a mislabeled CSS transform.
    - Named locators for current navigation, Go-projected live truth, active task/dominant next action, Blackout, Revoke Automation, and Stop/Release-All are visible or reachable through a recorded keyboard focus traversal.
    - `documentElement` and body scroll widths do not exceed client widths; bounded workspace regions retain their declared scroll ownership and no overlay intercepts a required control.
  </behavior>
  <action>Use a deterministic seeded active-show state and set Chromium text zoom through the browser-supported text-zoom mechanism before assertions. Record viewport width/height, requested and computed zoom, root/body clientWidth and scrollWidth, each required locator role/name/bounding box/visibility, ordered Tab traversal with focused role/name, intended scroll-owner metrics, overlay hit-test results, theme, build SHA, browser/runtime identity, and assertion booleans in `text-zoom-200.json`. Fail if computed zoom is not exactly 2, either root/body overflows horizontally, navigation/live truth/active task/safety controls are neither visible nor keyboard reachable, or keyboard focus cannot operate each safety control.</action>
  <verify><automated>cd frontend &amp;&amp; npx playwright test e2e/design-system.text-zoom.spec.ts --project=chromium --workers=1</automated></verify>
  <done>The exact 200%-zoom 900x720 fixture proves navigation, live truth, active task, and every persistent safety control remain reachable with zero body overflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove provider-offline and daemon-offline safety authority</name>
  <files>frontend/e2e/fixtures/projectedAcceptanceStates.ts, frontend/e2e/design-system.offline-safety.spec.ts, .planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/offline-safety.json</files>
  <behavior>
    - The fixture declares separate provider-offline and daemon-offline projections with explicit Go-owned playback/output truth and spies for the independent local Blackout and Revoke paths.
    - In each state Blackout and Revoke remain visible, keyboard focusable, and operable; each action invokes only its named independent local path once and does not wait for provider, daemon, script, or queued application-command completion.
    - Connectivity copy states only the unavailable dependency and preserves the supplied playback/output truth; it never infers that playback or output stopped.
  </behavior>
  <action>Create typed named states with connectivity, playback, output, controlling source, active task, safety-command spies, and expected accessible status text. For each state assert the projected provider/daemon status, unchanged playback/output truth, visible Blackout/Revoke roles and names, deterministic Tab reachability, keyboard activation through the independent local-priority path, exactly one matching dispatch, zero provider/daemon-dependent dispatches, and no copy or accessible status that claims stopped playback/output unless the fixture explicitly supplies stopped Go-owned truth. Record state ID, inputs, projected status strings, control boxes, focus order, activation method, local path identity, dispatch counts, before/after playback-output truth, build SHA, browser/runtime identity, and assertions in `offline-safety.json`.</action>
  <verify><automated>cd frontend &amp;&amp; npx playwright test e2e/design-system.offline-safety.spec.ts --project=chromium --workers=1</automated></verify>
  <done>Both offline projections preserve truthful live state and prove Blackout/Revoke visibility, keyboard reachability, and independent local operation without false stopped-output claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser accessibility projection to operator | Text zoom or dependency failure could hide controls or falsify live truth |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-44 | Denial of service | compact zoom layout | high | mitigate | Exact 200% zoom, overflow, locator, focus-order, and hit-test evidence |
| T-13-45 | Spoofing | offline live projection | critical | mitigate | Explicit Go-owned truth fixture and rejection of inferred stopped state |
| T-13-46 | Denial of service | offline safety controls | critical | mitigate | Independent local-path spies and keyboard activation in both offline states |
</threat_model>

<verification>Both exact Playwright commands pass and the validator accepts both closed semantic evidence objects while rejecting their single-fault mutations.</verification>
<success_criteria>Approved UI-SPEC zoom and offline-safety clauses are executable, attributable, and fail closed.</success_criteria>
<output>Create `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-41-SUMMARY.md` when done.</output>
