---
phase: 13-unified-ui-design-system-and-automated-enforcement
plan: 18
type: execute
wave: 10
depends_on: [13-02, 13-17, 13-30, 13-31, 13-32, 13-33, 13-34]
files_modified:
  - frontend/package.json
  - internal/command/designsystem.go
  - internal/command/designsystem_test.go
  - internal/delivery/mage_targets.go
  - internal/delivery/mage_targets_test.go
  - magefiles/magefile.go
  - magefiles/magefile_test.go
  - config/commands.toml
  - .github/workflows/design-system.yml
autonomous: true
requirements: [D-09, D-10, D-11, D-13, D-14, UI-SPEC-ENFORCEMENT, UI-SPEC-VISUAL-MATRIX]
must_haves:
  truths:
    - "Normal frontend validation exposes fast checker/unit commands and build starts with the green whole-source gate."
    - "A registry-backed Mage route runs the serialized browser matrix with pinned tooling."
    - "Windows CI provisions Chromium explicitly and uploads actual/expected/diff evidence on failure."
  artifacts:
    - path: "internal/command/designsystem.go"
      provides: "Pinned Node design-system routes"
    - path: ".github/workflows/design-system.yml"
      provides: "Required Windows browser gate"
  key_links:
    - from: ".github/workflows/design-system.yml"
      to: "magefiles/magefile.go"
      via: "registry-backed target"
      pattern: "DesignSystem"
---

<objective>
Integrate static/unit/browser design-system enforcement into package scripts, Mage, and required Windows CI.

Purpose: Make D-09/D-10 enforcement normal, pinned, offline-aware, and artifact-producing.
Output: Package routes, internal command, Mage registry entry, command graph, and Windows workflow.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-PATTERNS.md
@frontend/package.json
@internal/command/build.go
@magefiles/magefile.go
@.github/workflows/check.yml
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-17-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose package and pinned internal command routes</name>
  <files>frontend/package.json, internal/command/designsystem.go, internal/command/designsystem_test.go</files>
  <behavior>
    - check/generate/unit commands use checked-in dependencies; build begins with whole-source checking and excludes Playwright.
    - Browser execution resolves pinned Node/npm and lockfile-matched Playwright, not ambient tools.
  </behavior>
  <action>Follow package composition in `frontend/package.json` and the pinned subprocess seam in `internal/command/build.go` mapped by `13-PATTERNS.md`. Add static/unit/browser internal routes with focused tests; do not call ambient npx from Go.</action>
  <verify><automated>go test ./internal/command -run DesignSystem -count=1</automated></verify>
  <done>Static/unit checks join normal build and internal design-system execution is pinned and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register delivery, Mage, and command-graph routes</name>
  <files>internal/delivery/mage_targets.go, internal/delivery/mage_targets_test.go, magefiles/magefile.go, magefiles/magefile_test.go, config/commands.toml</files>
  <behavior>
    - The serialized browser target is registry-backed, command-parity tested, and discoverable through Mage.
  </behavior>
  <action>Follow the thin Mage adapter in `magefiles/magefile.go` and declarative graph in `config/commands.toml` mapped by `13-PATTERNS.md`. Preserve one execution graph and command-parity tests.</action>
  <verify><automated>go test ./internal/delivery ./magefiles -run 'DesignSystem|MageTarget' -count=1 &amp;&amp; mage -l</automated></verify>
  <done>The browser target is registered, serialized, discoverable, and parity-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Add required Windows visual CI and artifact evidence</name>
  <files>.github/workflows/design-system.yml</files>
  <action>Follow `.github/workflows/check.yml`: checkout, install pinned Mage, bootstrap, explicitly provision the lockfile-matched Chromium in the network step, run the registry-backed Mage target, and upload test-results plus actual/expected/diff/calibration artifacts on failure. Keep canonical snapshots out of cross-platform CI and browser work out of `npm run build`.</action>
  <verify><automated>mage CheckOffline &amp;&amp; git diff --check -- .github/workflows/design-system.yml</automated></verify>
  <done>The required Windows job is least-privilege, pinned, serialized, and preserves complete failure evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CI/tool registry to package scripts | Ambient or network tooling could bypass reproducibility |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-20 | Tampering | CI/Mage route | high | mitigate | Pinned Node/npm/browser, registry-backed command, exact lockfile, artifact upload |
</threat_model>

<verification>
Focused Go registry tests, Mage listing, offline check, and workflow diff check all pass.
</verification>

<success_criteria>
Design-system enforcement is part of normal validation and required Windows CI with reproducible browser evidence.
</success_criteria>

<output>
Create `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-18-SUMMARY.md` when done.
</output>
