---
phase: 13-unified-ui-design-system-and-automated-enforcement
plan: 30
type: execute
wave: 9
depends_on: [13-17, 13-24, 13-16]
files_modified:
  - frontend/e2e/fixtures/startupProbe.ts
  - frontend/e2e/design-system.startup.spec.ts
  - .planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/startup-theme-font.json
  - frontend/e2e/fixtures/emergencyFallback.ts
  - frontend/e2e/design-system.emergency-fallback.spec.ts
  - .planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/error-boundary-fallback.json
autonomous: true
requirements: [D-01, D-06, D-10, D-12, D-13, UI-CONSIDERATIONS-BACKSTOP-STARTUP, UI-CONSIDERATIONS-BACKSTOP-ERROR]
must_haves:
  truths:
    - "Theme/font startup observation begins before navigation and before fonts or theme settle, proving no unreadable flash."
    - "ErrorBoundary remains readable and recoverable when theme CSS is unavailable."
  artifacts:
    - path: "frontend/e2e/design-system.startup.spec.ts"
      provides: "Pre-settle startup-frame assertions"
    - path: "frontend/e2e/design-system.emergency-fallback.spec.ts"
      provides: "Token-independent emergency fallback assertions"
  key_links:
    - from: "frontend/e2e/design-system.startup.spec.ts"
      to: "frontend/src/lib/theme.ts"
      via: "pre-navigation instrumentation and sampled frames"
      pattern: "addInitScript|MutationObserver"
---

<objective>
Implement the startup and emergency-fallback UI backstops as separately named executable tasks and evidence records.

Purpose: Prevent font waiting from concealing startup flash and prove fallback readability before theme CSS.
Output: Two independent fixtures, specs, and semantic evidence files.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-UI-SPEC.md
@frontend/src/lib/theme.ts
@frontend/src/shell/ErrorBoundary.tsx
@frontend/e2e/helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backstop theme and font initialization before settle</name>
  <files>frontend/e2e/fixtures/startupProbe.ts, frontend/e2e/design-system.startup.spec.ts, .planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/startup-theme-font.json</files>
  <behavior>
    - Instrumentation is installed with `page.addInitScript` before navigation and records initial DOM/style samples before `document.fonts.ready` or settled theme state.
    - Light and dark persisted themes never expose an unreadable foreground/background pair, transparent content flash, wrong-theme frame, or missing safety labels during the pre-settle window.
    - Font readiness is awaited only after pre-settle observations are captured, then final Archivo/JetBrains Mono and layout stability are asserted.
  </behavior>
  <action>Create a deterministic pre-navigation probe that timestamps document creation, first styled frame, theme attribute changes, font status transitions, computed foreground/background pairs, visible safety labels, and bounding boxes. Start observation before navigation; do not call a font/theme settle helper until the pre-settle sample window is complete. Test cold light and dark persistence independently and write semantic JSON containing samples, contrast calculations, theme sequence, font sequence, build SHA, browser/runtime, and assertions.</action>
  <verify><automated>cd frontend &amp;&amp; npx playwright test e2e/design-system.startup.spec.ts --project=chromium --workers=1</automated></verify>
  <done>Pre-settle and post-settle evidence proves there is no unreadable font/theme startup flash.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backstop token-independent ErrorBoundary before theme CSS</name>
  <files>frontend/e2e/fixtures/emergencyFallback.ts, frontend/e2e/design-system.emergency-fallback.spec.ts, .planning/phases/13-unified-ui-design-system-and-automated-enforcement/evidence/error-boundary-fallback.json</files>
  <behavior>
    - Blocking the generated theme stylesheet and forcing a primitive render failure still produces readable fallback copy and a keyboard-operable recovery action.
    - Assertions prove the fallback uses its token-independent emergency style contract, not computed custom-property values.
    - The fallback remains inside the viewport at 900x720 and 1280x720 and does not obscure persistent safety actions.
  </behavior>
  <action>Route/intercept the generated token stylesheet before navigation, induce the deterministic ErrorBoundary fixture failure, and assert exact emergency fallback foreground/background contrast, accessible name/role, focus-visible recovery, activation, viewport containment, and safety reachability. Record blocked stylesheet identity, computed styles, contrast arithmetic, viewport results, build SHA, and assertion outcomes in semantic JSON.</action>
  <verify><automated>cd frontend &amp;&amp; npx playwright test e2e/design-system.emergency-fallback.spec.ts --project=chromium --workers=1</automated></verify>
  <done>The emergency fallback is demonstrably readable, operable, and token-independent before theme CSS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Startup/theme CSS to first visible frame | Settling helpers could hide unsafe transient frames |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-13-32 | Spoofing | startup frame | high | mitigate | Pre-navigation instrumentation and pre-settle sample assertions |
| T-13-33 | Denial of service | ErrorBoundary | high | mitigate | CSS-blocked browser proof and token-independent recovery |
</threat_model>

<verification>Run each backstop independently; each writes and validates its own semantic evidence schema.</verification>
<success_criteria>Startup flash and emergency fallback are independently executable, attributable, and impossible to conceal behind font/theme settling.</success_criteria>
<output>Create `.planning/phases/13-unified-ui-design-system-and-automated-enforcement/13-30-SUMMARY.md` when done.</output>
