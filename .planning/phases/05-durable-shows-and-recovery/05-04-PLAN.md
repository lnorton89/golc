---
phase: 05-durable-shows-and-recovery
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - internal/show/diagnose.go
  - internal/show/diagnose_test.go
  - internal/command/show_diagnose.go
  - internal/command/show_diagnose_test.go
autonomous: true
requirements: [SHOW-06]
must_haves:
  truths:
    - "`golc show diagnose --show <path>` runs BOTH SQLite-level `PRAGMA integrity_check` (file/page corruption) and structural `show.validate()` (logical show invariants) and prints a combined DiagnosticReport (D-11)."
    - "`golc show export --show <path>` prints the versioned human-readable JSON that is byte-identical to `strictjson.CanonicalEncode(State)` — the full canonical document, NOT the allowlisted `show inspect` projection (D-13)."
    - "Diagnostics run on-demand only via explicit commands — neither `show diagnose` nor `show export` runs automatically on `show open` (D-12)."
    - "`show diagnose` and `show export` tolerate a newer-than-supported schema_version via `LoadForRead` and still produce output (read-only inspect is allowed even when editing is refused, D-10) — the file is never rewritten."
    - "`show diagnose` on a corrupted `.golc` reports the corruption (non-'ok' integrity_check lines and/or a failed structural validate) in its report rather than crashing or reporting healthy (SHOW-06 corrupted-file probe)."
  artifacts:
    - internal/show/diagnose.go
    - internal/show/diagnose_test.go
    - internal/command/show_diagnose.go
    - internal/command/show_diagnose_test.go
  key_links:
    - "diagnose.go wraps the existing whole-State validate() plus a new PRAGMA integrity_check; it does not duplicate validate()."
    - "show export calls strictjson.CanonicalEncode(state) directly (via LoadForRead), NOT buildShowInspectView's field-filtered projection."
---

<objective>
Deliver the on-demand troubleshooting and interchange surface (SHOW-06): integrity diagnostics that combine SQLite-level `PRAGMA integrity_check` with the existing structural `show.validate()` (D-11), and a versioned JSON export that is byte-identical to today's canonical State document (D-13). Both are read-only and tolerate a newer-than-supported file via `LoadForRead` (D-10), and neither runs automatically on open (D-12). This is an independent vertical slice depending only on the Plan 01 store.

Purpose: Give operators a deliberate way to prove a `.golc` file's health and to export a portable, human-readable, round-trippable representation for interchange and debugging.
Output: `internal/show/diagnose.go` (+ test) with `Diagnose`/`DiagnosticReport`, and `internal/command/show_diagnose.go` (+ test) registering `show diagnose` and `show export`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/05-durable-shows-and-recovery/05-CONTEXT.md
@.planning/phases/05-durable-shows-and-recovery/05-RESEARCH.md
@.planning/phases/05-durable-shows-and-recovery/05-PATTERNS.md
@internal/command/deployment.go
</context>

## Artifacts this phase produces (Plan 04 scope)

New symbols this plan creates (exclude from drift verification):
- File `internal/show/diagnose.go`: `DiagnosticReport` struct (`FileLevelIssues []string`, `StructuralOK bool`, `StructuralError error`/string, `SchemaVersion int`, `Revision int`), `Diagnose(root, path string) (DiagnosticReport, error)` (PRAGMA integrity_check + LoadForRead/validate).
- File `internal/command/show_diagnose.go`: routes `show diagnose`, `show export`; handlers `runShowDiagnose`, `runShowExport`; arg parsers `parseShowDiagnoseArgs`, `parseShowExportArgs`.
- Error/status codes: `GOLC_SHOW_DIAGNOSE_FAILED`, `GOLC_SHOW_EXPORT_ENCODE_FAILED`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Diagnose (PRAGMA integrity_check + structural validate)</name>
  <files>internal/show/diagnose.go, internal/show/diagnose_test.go</files>
  <read_first>
    - internal/show/store.go (Plan 01: openStore, Load, LoadForRead, ErrSchemaTooNew)
    - internal/show/state.go (validate; State shape)
    - .planning/phases/05-durable-shows-and-recovery/05-RESEARCH.md (Code Examples "Diagnose combining structural + file-level checks"; Common Pitfalls 4 — integrity_check is sub-second at this scale; Anti-Patterns — do not run integrity_check on every open)
    - .planning/phases/05-durable-shows-and-recovery/05-PATTERNS.md (diagnose.go pattern assignment)
  </read_first>
  <behavior>
    - Diagnose runs `PRAGMA integrity_check` and collects every returned line that is not "ok" into FileLevelIssues; then runs the structural check via LoadForRead (reusing validate()) recording StructuralOK/StructuralError; returns SchemaVersion + Revision from the meta row.
    - A healthy file returns empty FileLevelIssues and StructuralOK=true.
    - A file-level corrupted `.golc` returns the non-'ok' integrity lines; a structurally-invalid (but readable) `.golc` returns StructuralOK=false with the validate error — Diagnose never crashes and never reports healthy for a bad file.
    - Diagnose tolerates a newer-than-supported schema_version (LoadForRead), so a newer file can still be inspected read-only (D-10).
    - Use the full `integrity_check` (not `quick_check`) synchronously — at this app's KB-to-low-MB scale it completes well under a second (Pitfall 4); no async/streaming/cancellation machinery.
  </behavior>
  <action>
    Create `internal/show/diagnose.go` and `internal/show/diagnose_test.go` from RESEARCH's Diagnose Code Example verbatim in shape, reusing `openStore` and `LoadForRead`. Follow the `{DOMAIN}_{CONDITION}` convention (GOLC_SHOW_DIAGNOSE_FAILED for a diagnostics-execution failure). Add a benchmark or a timing assertion confirming integrity_check on a representative saved show completes under one second (empirically confirming Pitfall 4's scale assumption for this phase, rather than assuming it).

    Tests:
    - `TestDiagnoseHealthyFile`: a freshly saved valid show returns empty FileLevelIssues and StructuralOK=true, with the right SchemaVersion/Revision.
    - `TestDiagnoseStructurallyInvalid`: a `.golc` whose blob decodes but fails validate returns StructuralOK=false with the validate error (does not crash).
    - `TestDiagnoseFileCorruption`: a `.golc` with injected page/file corruption returns non-'ok' FileLevelIssues (does not report healthy).
    - `BenchmarkDiagnose` (or a bounded timing test): integrity_check + validate over a representative show completes under 1s.
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" 2>/dev/null; go test ./internal/show/... -run 'TestDiagnose' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/show/... -run TestDiagnose` passes.
    - `grep -c 'integrity_check' internal/show/diagnose.go` >= 1 (the thorough integrity check per D-11; the corruption test confirms it surfaces real file-level corruption rather than the lighter quick variant).
    - `internal/show/diagnose.go` defines `Diagnose` and `DiagnosticReport` and reuses `validate()` rather than reimplementing structural checks.
    - The corruption test proves a bad file is reported, not silently healthy; the timing check confirms sub-second execution.
  </acceptance_criteria>
  <done>`Diagnose` reports both file-level and structural health in one report, tolerates a newer format read-only, and provably surfaces a corrupted file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: show diagnose / show export CLI routes</name>
  <files>internal/command/show_diagnose.go, internal/command/show_diagnose_test.go</files>
  <read_first>
    - internal/command/deployment.go (existing `show` scope + runShowInspect read-only Load->encode->print shape + exit-code convention; buildShowInspectView is the projection to AVOID for export)
    - internal/command/router.go (MustDeclareRoute / CommandRegistration / Request / Result)
    - internal/show/diagnose.go (Task 1: Diagnose / DiagnosticReport)
    - internal/show/store.go (Plan 01: LoadForRead; ErrSchemaTooNew tolerance)
    - internal/strictjson (CanonicalEncode — the export payload)
    - .planning/phases/05-durable-shows-and-recovery/05-PATTERNS.md (show diagnose/export pattern assignment; D-10 read-only routing note; scope-not-re-declared note)
  </read_first>
  <behavior>
    - `show diagnose --show <path>`: call Diagnose, print the DiagnosticReport as deterministic JSON; exit 0 when healthy, exit 1 when issues are found (so scripts can gate on it), never a crash.
    - `show export --show <path>`: LoadForRead then print `strictjson.CanonicalEncode(state)` directly — the FULL canonical document (D-13), NOT buildShowInspectView's allowlisted projection. The output is byte-identical to what the whole `.golc` file was before this phase's SQLite move, and round-trips back into a fresh `.golc` via the Load path.
    - Both routes register on the ALREADY-declared `show` scope (deployment.go) and must NOT re-declare it.
    - Both tolerate a newer-than-supported file (LoadForRead), consistent with D-10 read-only inspect.
    - Exit codes: 2 for usage/arg errors, 1 for runtime/diagnostic-fail, 0 on success.
  </behavior>
  <action>
    Create `internal/command/show_diagnose.go` (a separate file in package `command`, so it does not collide with `internal/command/show.go`) registering `show diagnose` and `show export` on the existing `show` scope (do NOT call MustDeclareScope for "show"). Model `runShowDiagnose`/`runShowExport` on `runShowInspect`'s read-only Load->encode->print shape, but `runShowExport` must call `strictjson.CanonicalEncode(state)` — not `buildShowInspectView` — so the export is the complete document (D-13).

    Create `internal/command/show_diagnose_test.go`:
    - `TestShowExportMatchesCanonicalEncode`: export output is byte-identical to `strictjson.CanonicalEncode(state)` for a loaded show, and re-importing it via Load reproduces the same State (round-trip).
    - `TestShowDiagnoseHealthyExitZero`: diagnose on a healthy file exits 0.
    - `TestShowDiagnoseCorruptExitOne`: diagnose on a corrupted/invalid file exits 1 and prints the issues.
    - `TestShowExportTooNewReadOnly`: export/diagnose on a file with schema_version > SchemaVersion still succeeds (read-only) and does not rewrite the file.
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" 2>/dev/null; go test ./internal/command/... -run 'TestShowExport|TestShowDiagnose' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/command/... -run 'TestShowExport|TestShowDiagnose'` passes.
    - `internal/command/show_diagnose.go` registers `show diagnose` and `show export` and reuses the already-declared `show` scope — proven by `go test ./internal/command/...` passing (a duplicate scope registration panics the router at init).
    - `grep -c 'CanonicalEncode' internal/command/show_diagnose.go` >= 1 (export encodes the full canonical document); `TestShowExportMatchesCanonicalEncode` proves the output is byte-identical to the full document and therefore not the field-filtered inspect projection.
    - The export-round-trip test proves byte-identity + re-import; the too-new test proves read-only tolerance without rewrite.
    - `go build ./...` and `go test ./...` pass.
  </acceptance_criteria>
  <done>Operators can run on-demand integrity diagnostics and export the full versioned canonical JSON, both read-only and both tolerant of a newer format, without either running automatically on open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk -> internal/show | A `.golc` opened for diagnose/export is untrusted; diagnostics must surface corruption rather than crash or falsely pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering / Denial of Service | `Diagnose`/`show export` on an untrusted or corrupted `.golc` | high | mitigate | `PRAGMA integrity_check` surfaces file-level corruption and `validate()` surfaces structural invalidity in a report; LoadForRead runs DecodeStrict + validate before trusting content — a bad file is reported, never silently trusted or crashed. |
| T-05-04 | Tampering / Information Disclosure | `--show` path | medium | mitigate | Reuse `resolvePath` via the store's Load/LoadForRead — no new path rule. |
</threat_model>

<verification>
- `go test ./internal/show/... -run TestDiagnose` and `go test ./internal/command/... -run 'TestShowExport|TestShowDiagnose'` green.
- `go test ./...` green.
- `show export` output re-imports via Load to an identical State (round-trip proven in test).
</verification>

<success_criteria>
`show diagnose` reports combined file-level + structural health on demand and exits non-zero on problems; `show export` prints the full canonical, round-trippable JSON document; both are read-only, tolerate a newer format, and never run automatically on open.
</success_criteria>

<output>
Create `.planning/phases/05-durable-shows-and-recovery/05-04-SUMMARY.md` when done.
</output>
