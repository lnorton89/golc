---
phase: 09-front-door-ui-completion
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - frontend/src/workspaces/show/GuidedFirstShow/readiness.ts
  - frontend/src/workspaces/show/GuidedFirstShow/stages/FixturesStage.tsx
  - frontend/src/workspaces/show/GuidedFirstShow/stages/PatchStage.tsx
  - frontend/src/workspaces/show/GuidedFirstShow/stages/ProgramStage.tsx
  - frontend/src/workspaces/show/GuidedFirstShow/stages/AssignStage.tsx
  - frontend/src/workspaces/show/GuidedFirstShow/stages/VerifyStage.tsx
  - frontend/src/workspaces/show/GuidedFirstShow/GuidedFirstShow.tsx
  - frontend/src/workspaces/show/GuidedFirstShow/GuidedFirstShow.test.tsx
autonomous: true
requirements: [FDUI-03]

must_haves:
  truths:
    - "A first-time operator can walk the guide from an empty show all the way to a patched fixture and a scene on screen — the Program, Assign, and Verify stages are real, not placeholders (FDUI-03, ROADMAP success criterion 3)."
    - "Each stage renders its own evidence independently and a later stage can always be viewed even while an earlier stage still reports a blocker — only the transition to Perform is gated (UI-SPEC partial state, locked interaction contract)."
    - "The Verify stage reports blockers, warnings, and evidence as three independently pluralized counts — '1 blocker' versus '2 blockers', and a zero count reads as zero rather than being hidden — and zero blockers is the only condition that unlocks the Perform transition (UI-SPEC zero-one-many, locked design)."
    - "Readiness is never rendered as a percentage, score, or progress bar anywhere in the guide (locked design)."
    - "Absent MIDI hardware never produces a blocker; physical MIDI evidence appears only as optional evidence, because it is required solely for named hardware compatibility claims (locked interaction contract)."
    - "Every stage's status is recomputed from a live domain read whenever it renders — the Verify stage re-derives all four upstream stage statuses rather than reading a stored result (locked interaction contract)."
    - "A blocker anywhere in the guide never prevents editing in another workspace — the guide's blocking effect is limited to disabling its own Perform transition (locked interaction contract)."
  artifacts:
    - path: "frontend/src/workspaces/show/GuidedFirstShow/readiness.ts"
      provides: "Pure per-stage status derivations plus the aggregate readiness rollup"
      contains: "export function aggregateReadiness"
    - path: "frontend/src/workspaces/show/GuidedFirstShow/stages/VerifyStage.tsx"
      provides: "Aggregate readiness stage and the Perform transition gate"
      min_lines: 60
    - path: "frontend/src/workspaces/show/GuidedFirstShow/stages/ProgramStage.tsx"
      provides: "Scene-programming readiness stage"
    - path: "frontend/src/workspaces/show/GuidedFirstShow/stages/AssignStage.tsx"
      provides: "Operator-surface assignment readiness stage"
  key_links:
    - from: "frontend/src/workspaces/show/GuidedFirstShow/stages/VerifyStage.tsx"
      to: "frontend/src/workspaces/show/GuidedFirstShow/readiness.ts"
      via: "aggregateReadiness over live re-derived upstream stage statuses"
      pattern: "aggregateReadiness"
    - from: "frontend/src/workspaces/show/GuidedFirstShow/stages/ProgramStage.tsx"
      to: "frontend/src/lib/wailsBridge.ts"
      via: "listProgramming live read"
      pattern: "listProgramming"
    - from: "frontend/src/workspaces/show/GuidedFirstShow/stages/AssignStage.tsx"
      to: "internal/wails/svc_surface.go"
      via: "window.go.wails.SurfaceService.ListSurfaces — the documented no-helper-yet escape hatch in wailsBridge.ts"
      pattern: "SurfaceService"
  prohibitions:
    - statement: "MUST NOT gate anything other than the guide's own Perform transition on a blocker — a blocker never disables a rail item, never prevents viewing a later stage, and never restricts editing in any other workspace."
      category: values
    - statement: "MUST NOT collapse blockers, warnings, and evidence into a single readiness number or bar — each category keeps its own label, its own count, and its own explanation of what is unmet."
      category: transparency
---

## Phase Goal

**FLAGGED — MVP user-story format discrepancy** (see 09-01-PLAN.md). ROADMAP goal, verbatim:

> A new operator can go from a fresh checkout to a patched fixture and a scene on screen using only
> the UI, with Guided First Show onboarding as the on-ramp — no CLI required for the happy path.

This plan's slice: the operator can finish the guide — programming a scene, assigning it to a
surface, and seeing an evidence-based readiness verdict.

<objective>
Complete the Guided First Show flow with its Program, Assign, and Verify stages and the
evidence-based Perform gate.

Purpose: FDUI-03's second half. Plan 09-03 delivered the overlay, entry points, and the first two
stages; this plan finishes the walk from an empty show to a scene on screen and implements the
locked "readiness is evidence, never a percentage" contract.

Output: `readiness.ts` (pure derivations + rollup), three new stage components, and the wired Perform
transition.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-front-door-ui-completion/09-CONTEXT.md
@.planning/phases/09-front-door-ui-completion/09-RESEARCH.md
@.planning/phases/09-front-door-ui-completion/09-PATTERNS.md
@.planning/phases/09-front-door-ui-completion/09-UI-SPEC.md
@.planning/sketches/references/onboarding-readiness-impact.md
@.planning/phases/09-front-door-ui-completion/09-03-SUMMARY.md
</context>

## Artifacts this phase produces (plan 04)

New symbols created by this plan:

- TypeScript functions `deriveFixturesStatus`, `derivePatchStatus`, `deriveProgramStatus`,
  `deriveAssignStatus`, `aggregateReadiness` (all in `readiness.ts`)
- TypeScript type `ReadinessRollup` (`{ blockers, warnings, evidence, items }`)
- TypeScript components `ProgramStage`, `AssignStage`, `VerifyStage`
- New files `frontend/src/workspaces/show/GuidedFirstShow/readiness.ts`,
  `stages/ProgramStage.tsx`, `stages/AssignStage.tsx`, `stages/VerifyStage.tsx`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing stage and readiness-rollup tests</name>
  <files>frontend/src/workspaces/show/GuidedFirstShow/GuidedFirstShow.test.tsx</files>
  <read_first>
    - `frontend/src/workspaces/show/GuidedFirstShow/GuidedFirstShow.test.tsx` — the harness and mocking conventions established by plan 09-03, which this task extends rather than replaces
    - `frontend/src/workspaces/show/GuidedFirstShow/stages.ts` — the `GuideStageStatus` / `GuideEvidenceItem` / `GuideEvidenceTone` contract every stage implements
    - `frontend/src/workspaces/show/GuidedFirstShow/stages/FixturesStage.tsx` and `PatchStage.tsx` — the derivation logic being extracted into `readiness.ts`
    - `frontend/src/lib/wailsBridge.ts` — `listProgramming` / `offlineProgrammingView` / `ProgrammingView` / `ProgSceneView`, and the `SurfaceServiceBinding.ListSurfaces(): Promise<unknown[]>` declaration plus the file's own documented "for a service without a helper yet, cast through window.go?.wails?.<Service>" convention
    - `.planning/sketches/references/onboarding-readiness-impact.md` — the readiness contract, the optional-MIDI-evidence rule, and the blocker scope rule
  </read_first>
  <behavior>
    - `deriveProgramStatus` with zero scenes yields exactly one `blocker` item; with one scene yields an `evidence` item using singular language; with three yields plural.
    - `deriveAssignStatus` with zero surfaces yields a `blocker`; with at least one surface yields `evidence`; in every case it also yields exactly one optional MIDI-hardware row whose tone is `evidence`, never `blocker` and never `warning`.
    - `aggregateReadiness` over a mixed set returns independent `blockers` / `warnings` / `evidence` counts and preserves every item.
    - `VerifyStage` with zero blockers renders an enabled Perform action; with one or more blockers renders it disabled together with the blocker list.
    - `VerifyStage` renders each category count with correct singular/plural agreement and renders a zero count explicitly rather than omitting the category.
    - `VerifyStage` renders no percent sign and no element with role `progressbar`.
    - `VerifyStage` re-derives from live reads: mounting it twice produces two `ListProgramming` calls.
    - `a later stage is viewable while an earlier one reports a blocker`: with a mocked empty fixture library, selecting the Verify rail item still renders Verify's own content and the rail item is not disabled.
    - `no MIDI-related blocker is ever produced`: with every MIDI mock absent, no rendered `blocker` row mentions MIDI.
  </behavior>
  <action>
Extend `GuidedFirstShow.test.tsx` with the cases above. Test the four `derive*` functions and `aggregateReadiness` as pure functions against hand-built view objects — no rendering needed for those — and test the three new stage components through the existing render harness.

For the "no percentage" assertions reuse the same two checks plan 09-03 established: no `progressbar` role and no percent sign in the rendered text.

For the blocker-scope assertion, assert that every rail item remains enabled (not `aria-disabled`, not `disabled`) while a blocker is present, and that selecting the last rail item renders that stage's own heading.

All new cases must fail at this point. Commit red.
  </action>
  <acceptance_criteria>
    - `npm --prefix frontend test -- GuidedFirstShow` reports failures naming the missing `readiness` module or the missing stage components.
    - `grep -c "aggregateReadiness" frontend/src/workspaces/show/GuidedFirstShow/GuidedFirstShow.test.tsx` is at least 1.
    - `grep -c "progressbar" frontend/src/workspaces/show/GuidedFirstShow/GuidedFirstShow.test.tsx` is at least 1.
    - Every test case that passed before this task still passes (no pre-existing case was deleted).
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend test -- GuidedFirstShow 2>&1 | grep -q -E "FAIL|Cannot find module" &amp;&amp; echo RED_CONFIRMED</automated>
  </verify>
  <done>Readiness-rollup and remaining-stage tests exist and fail for the right reason, committed as a `test(09-04):` commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract pure readiness derivations and implement the Program and Assign stages</name>
  <files>frontend/src/workspaces/show/GuidedFirstShow/readiness.ts, frontend/src/workspaces/show/GuidedFirstShow/stages/FixturesStage.tsx, frontend/src/workspaces/show/GuidedFirstShow/stages/PatchStage.tsx, frontend/src/workspaces/show/GuidedFirstShow/stages/ProgramStage.tsx, frontend/src/workspaces/show/GuidedFirstShow/stages/AssignStage.tsx, frontend/src/workspaces/show/GuidedFirstShow/GuidedFirstShow.tsx</files>
  <read_first>
    - `frontend/src/workspaces/show/GuidedFirstShow/stages/FixturesStage.tsx` and `PatchStage.tsx` — the exact derivation behaviour to move without changing it
    - `frontend/src/workspaces/show/GuidedFirstShow/stages.ts` — the shared status contract
    - `frontend/src/lib/wailsBridge.ts` — `listProgramming`, `offlineProgrammingView`, and the `SurfaceServiceBinding` declaration plus the documented cast-through convention for a service with no helper yet
    - `frontend/src/workspaces/operate/OperatorSurfaceWorkspace.tsx` — how an existing component reads surfaces today, so the Assign stage reads the same source rather than inventing a second one
    - `.planning/sketches/references/onboarding-readiness-impact.md` — the optional-MIDI-evidence rule and the one-dominant-action-per-stage rule
  </read_first>
  <action>
Create `readiness.ts` holding pure, render-free derivations that take an already-fetched view and return a `GuideStageStatus`:
- `deriveFixturesStatus(view)` — moved verbatim in behaviour from `FixturesStage.tsx`.
- `derivePatchStatus(view)` — moved verbatim in behaviour from `PatchStage.tsx`.
- `deriveProgramStatus(view)` — zero scenes is a `blocker` stating the show has no scene to perform yet; one or more is `evidence` naming the count with singular/plural agreement.
- `deriveAssignStatus(surfaceCount, programming)` — zero operator surfaces is a `blocker`; at least one is `evidence` naming the count. In both cases it appends exactly one MIDI row with tone `evidence` whose detail states that physical MIDI hardware evidence is optional for on-screen and keyboard operation and is required only for a named hardware compatibility claim. This row must never be emitted as a `blocker` or a `warning`.
- `aggregateReadiness(statuses)` returning `{ blockers, warnings, evidence, items }` — three independent counts plus the flattened item list, with no combined score, ratio, or percentage anywhere in the module.

Rewrite `FixturesStage.tsx` and `PatchStage.tsx` to fetch as before and delegate their derivation to the extracted functions. Their observable behaviour and their existing tests must be unchanged.

Create `ProgramStage.tsx`: reads `listProgramming()` on mount, derives via `deriveProgramStatus`, and exposes one primary action that calls `navigateTo("build-scenes-looks")`. It performs no programming mutation of its own.

Create `AssignStage.tsx`: reads the operator surfaces on mount via `window.go?.wails?.SurfaceService?.ListSurfaces()` wrapped in a try/catch that falls back to an empty array — the escape hatch `wailsBridge.ts`'s own doc comment sanctions for a service with no helper yet — and reads `listProgramming()` for the scene context. Derives via `deriveAssignStatus` and exposes one primary action that calls `navigateTo("operate-operator-surface")`. It performs no assignment mutation of its own; assigning is the Operator Surface workspace's job.

Both new stages disable their primary action and show a busy label while their read is in flight. Wire both into `GuidedFirstShow.tsx`'s stage-content switch, replacing their placeholders.
  </action>
  <acceptance_criteria>
    - `npm --prefix frontend test -- GuidedFirstShow` passes the `deriveProgramStatus`, `deriveAssignStatus`, and `aggregateReadiness` cases and all pre-existing Fixtures/Patch cases.
    - `grep -c "export function aggregateReadiness" frontend/src/workspaces/show/GuidedFirstShow/readiness.ts` is 1.
    - `grep -v '^//' frontend/src/workspaces/show/GuidedFirstShow/readiness.ts | grep -c -E "percent|%|progress"` is 0.
    - `grep -v '^//' frontend/src/workspaces/show/GuidedFirstShow/stages/ProgramStage.tsx | grep -c -E "createScene|createTheme|createChase|createMotion|createBlend|recordPreset|programmerSet"` is 0.
    - `grep -v '^//' frontend/src/workspaces/show/GuidedFirstShow/stages/AssignStage.tsx | grep -c -E "AssignItem|UnassignItem|CreateSurface|RemoveSurface|AuthorizeControl"` is 0.
    - `npm --prefix frontend run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend test -- GuidedFirstShow &amp;&amp; npm --prefix frontend run build</automated>
  </verify>
  <done>The Program and Assign stages report live readiness and hand off to the real workspaces; every stage's derivation is a pure function the Verify stage can reuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the Verify stage and the evidence-based Perform gate</name>
  <files>frontend/src/workspaces/show/GuidedFirstShow/stages/VerifyStage.tsx, frontend/src/workspaces/show/GuidedFirstShow/GuidedFirstShow.tsx</files>
  <read_first>
    - `frontend/src/workspaces/show/GuidedFirstShow/readiness.ts` — the four derivations and `aggregateReadiness` from Task 2
    - `frontend/src/workspaces/show/GuidedFirstShow/GuidedFirstShow.tsx` — the shared evidence aside and footer rendering the Verify stage feeds
    - `frontend/src/lib/wailsBridge.ts` — `listLocalFixtures`, `listPatch`, `listProgramming`, and the `SurfaceService` cast-through convention
    - `.planning/sketches/references/onboarding-readiness-impact.md` — "'Ready to perform' is based on explicit gates and evidence, not a generic completion percentage" and "A blocker prevents the transition to Perform but not editing in other workspaces"
    - `.planning/phases/09-front-door-ui-completion/09-UI-SPEC.md` — the locked "Blocker" / "Warning" / "Evidence" status labels
  </read_first>
  <action>
Create `VerifyStage.tsx`. On mount it performs all four live reads in parallel (`listLocalFixtures`, `listPatch`, `listProgramming`, and the surface read), re-derives all four upstream stage statuses through the pure functions in `readiness.ts`, and rolls them up with `aggregateReadiness`. It stores no result across mounts and reads no persisted progress record.

Render, in the stage content: one row per category using the locked status words "Blocker", "Warning", and "Evidence", each with its own count pluralized independently ("1 blocker", "2 blockers", "0 warnings") and each expanding to the individual items with their own explanations of what is unmet. Render no percentage, no ratio, no score, and no progress bar.

The stage's single primary action is the Perform transition: it calls `navigateTo("operate-operator-surface")` and is `disabled` when the blocker count is greater than zero, with an adjacent explanation naming the outstanding blockers. This disabled state is the ONLY thing a blocker gates — rail items stay enabled, every other stage stays viewable, and nothing in any other workspace is restricted.

In `GuidedFirstShow.tsx`, replace the Verify placeholder with the real stage. Confirm that the rail renders all five stages as individually selectable at all times, including while blockers exist, so the operator can visit stages non-linearly as the locked contract requires.
  </action>
  <acceptance_criteria>
    - `npm --prefix frontend test -- GuidedFirstShow` passes in full, including the Perform-gate, pluralization, and later-stage-viewable cases.
    - `grep -c "aggregateReadiness" frontend/src/workspaces/show/GuidedFirstShow/stages/VerifyStage.tsx` is at least 1.
    - `grep -v '^//' frontend/src/workspaces/show/GuidedFirstShow/stages/VerifyStage.tsx | grep -c -E 'role="progressbar"|toFixed|Math.round'` is 0.
    - `grep -rn -E "localStorage|sessionStorage" frontend/src/workspaces/show/GuidedFirstShow/ | wc -l` is 0.
    - `grep -c "Blocker" frontend/src/workspaces/show/GuidedFirstShow/stages/VerifyStage.tsx` is at least 1.
    - `npm --prefix frontend run build` succeeds and `go test ./...` is unaffected.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend test -- GuidedFirstShow &amp;&amp; npm --prefix frontend run build</automated>
    <human-check>Walk the whole guide on a brand-new show: import a fixture, create a pool and activate a deployment, create a scene, create an operator surface, then open Verify. Confirm it reports zero blockers and enables the Perform action, that no percentage appears anywhere, and that with a deliberately missing scene the Perform action is disabled while every other workspace remains fully editable.</human-check>
  </verify>
  <done>A first-time operator can complete the guide end to end and receive an evidence-based, percentage-free readiness verdict whose only gating effect is the guide's own Perform transition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go backend -> webview | Fixture, patch, programming, and surface state cross into the readiness rollup |
| guide -> domain workspaces | The Perform transition hands control to the Operator Surface workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04-01 | Spoofing | Readiness verdict that does not reflect real show state | high | mitigate | `VerifyStage` re-runs all four live reads and re-derives every upstream status on each mount; no result is cached across mounts and a repository-wide storage-API assertion pins that no progress record exists |
| T-09-04-02 | Elevation of Privilege | A blocker silently restricting work outside the guide | medium | mitigate | The blocker count gates exactly one control — the Verify stage's own Perform action. Rail items stay enabled and no other workspace is touched, asserted by a test that keeps every rail item selectable while a blocker is present |
| T-09-04-03 | Repudiation | A green verdict produced by an unreachable bridge | medium | mitigate | Every read falls back to the documented offline/empty shape, which produces blockers rather than evidence — an unreachable bridge can only ever look less ready, never more |
| T-09-04-04 | Tampering | Guide-initiated programming or surface mutation | high | mitigate | Source assertions pin that no programming or surface mutation call exists in the new stages; every stage hands off by navigation only |
| T-09-SC | Tampering | npm/go package installs | high | accept | No new packages (`09-RESEARCH.md` Package Legitimacy Audit: not applicable). No legitimacy checkpoint applies |
</threat_model>

<verification>
- `npm --prefix frontend run build` green.
- All five guide stages are real; none is a placeholder.
- No percentage, score, or progress bar exists anywhere under `GuidedFirstShow/`.
- No storage API and no domain mutation call exists anywhere under `GuidedFirstShow/`.
</verification>

<success_criteria>
- FDUI-03 is fully delivered: an operator can complete Guided First Show from an empty application to a patched fixture and a scene on screen.
- Readiness is evidence-based with three independently counted categories.
- Absent MIDI hardware never blocks on-screen readiness.
</success_criteria>

<output>
Create `.planning/phases/09-front-door-ui-completion/09-04-SUMMARY.md` when done.
</output>