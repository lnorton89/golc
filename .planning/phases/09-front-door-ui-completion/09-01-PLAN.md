---
phase: 09-front-door-ui-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/fixture/directory.go
  - internal/fixture/directory_test.go
  - internal/command/artnet.go
  - internal/wails/svc_fixturelibrary.go
  - internal/wails/svc_fixturelibrary_test.go
  - cmd/golc-desktop/main.go
  - frontend/src/lib/wailsBridge.ts
  - frontend/src/workspaces/build/FixtureLibraryWorkspace.tsx
  - frontend/src/workspaces/build/FixtureLibraryWorkspace.test.tsx
  - frontend/src/workspaces/build/FixtureLibraryWorkspace.module.css
autonomous: true
requirements: [FDUI-01]

must_haves:
  truths:
    - "An operator opening Build -> Fixture Library sees every valid fixture definition in their local library as a row showing model name, manufacturer, and a validation-status chip — not a Coming Soon stub pointing at the CLI (FDUI-01, D-01 local half)."
    - "A library with zero fixtures renders the 'No fixtures yet' heading plus the 'Import a fixture from the Open Fixture Library or add your own YAML definition to get started.' body (UI-SPEC empty state)."
    - "A library with exactly one fixture renders singular count language ('1 fixture') and a library with several renders plural, with an identical row layout at every count (UI-SPEC zero-one-many)."
    - "Selecting a row renders that fixture's inspect detail inline in the workspace — stable key, content hash, revision, source, validation result, and any lossy-import warnings — never a separate modal or wizard (D-02)."
    - "A fixture file that fails to decode or pin renders as a row with a revoked-tone 'Invalid' chip and, when selected, the 'This fixture definition has {N} error(s) and can't be added. Fix them and try again.' copy — one bad file never blanks the whole list (UI-SPEC error state)."
    - "Typing in the search input filters the visible rows by case-insensitive substring match against manufacturer and model, with no faceted filter controls (D-03)."
    - "A long fixture or manufacturer name truncates with an ellipsis at the row's fixed width and exposes the full value via the row's title attribute (UI-SPEC long-text)."
    - statement: "The Fixture Library list shows an explicit initial-load treatment while ListLocal is in flight rather than an indistinguishable blank panel."
      verification: backstop
    - statement: "The Fixture Library list scrolls within its bounded panel when the library holds many fixtures, with no unbounded canvas growth."
      verification: backstop
    - statement: "ASSUMPTION (unclassified edge, FDUI-01 — flagged, not silently resolved): the local fixture library is exactly one non-recursive directory, resolved as GOLC_DESKTOP_FIXTURES when set and <projectRoot>/fixtures otherwise; subdirectories, non-.yaml/.yml files, and files outside that directory are never listed, and a directory that does not exist yet lists as an empty library rather than an error."
      verification: backstop
  artifacts:
    - path: "internal/fixture/directory.go"
      provides: "Exported ListDirectory + DirectoryEntry — the single local-fixture-directory scan implementation"
      contains: "func ListDirectory"
    - path: "internal/wails/svc_fixturelibrary.go"
      provides: "FixtureLibraryService.ListLocal / .Inspect Wails read projections"
      contains: "func NewFixtureLibraryService"
    - path: "frontend/src/workspaces/build/FixtureLibraryWorkspace.tsx"
      provides: "Real browse + inline-inspect workspace replacing the stub"
      min_lines: 120
  key_links:
    - from: "internal/command/artnet.go"
      to: "internal/fixture/directory.go"
      via: "loadFixtureDirectory delegates to fixture.ListDirectory — never a second scan implementation"
      pattern: "fixture\\.ListDirectory"
    - from: "cmd/golc-desktop/main.go"
      to: "internal/wails/svc_fixturelibrary.go"
      via: "NewFixtureLibraryService constructed and added to options.App{Bind}"
      pattern: "NewFixtureLibraryService"
    - from: "frontend/src/workspaces/build/FixtureLibraryWorkspace.tsx"
      to: "frontend/src/lib/wailsBridge.ts"
      via: "listLocalFixtures / inspectFixtureFile bridge calls"
      pattern: "listLocalFixtures"
    - from: "internal/wails/svc_fixturelibrary.go"
      to: "internal/command/fixture.go"
      via: "Inspect forwards to the registered 'fixture inspect' route via the registry execute helper"
      pattern: "\"fixture\", \"inspect\""
  prohibitions:
    - statement: "MUST NOT present a fixture definition as usable when its decode or pin failed — an unreadable or invalid definition is always visibly marked invalid in the list and its diagnostics are shown, never skipped silently so the operator believes their library is complete."
      category: safety
    - statement: "MUST NOT re-implement fixture decode, pin, or validation inside the Wails layer — every validation verdict the workspace shows originates from internal/fixture.Decode/Pin or the registered fixture inspect route, so a definition the CLI rejects is never accepted on screen."
      category: transparency
---

## Phase Goal

**FLAGGED — MVP user-story format discrepancy.** `MVP_MODE` is enabled for this phase, but
`ROADMAP.md`'s Phase 9 `**Goal:**` line is not in `As a / I want to / so that` form. Per MVP-mode
rules the planner does not invent a user story. The ROADMAP goal is reproduced verbatim; run
`/gsd mvp-phase 9` to convert it before this phase seals if the user-story form is required.

> A new operator can go from a fresh checkout to a patched fixture and a scene on screen using only
> the UI, with Guided First Show onboarding as the on-ramp — no CLI required for the happy path.

This plan's slice: the operator can see and inspect the fixtures they already have, on screen.

<objective>
Replace the Fixture Library `ComingSoon` stub with a real browse-and-inspect surface backed by a new
local-fixture-directory listing capability (D-01's local half, D-02, D-03).

Purpose: FDUI-01's first user-visible slice. Today no backend route lists fixtures at all, and the
Build -> Fixture Library destination renders a stub telling the operator to use the CLI. After this
plan an operator can open the workspace, see every fixture in their library with its validation
status, search it, and read one fixture's identity/provenance/warnings inline.

Output: `internal/fixture/directory.go` (the single extracted directory scan),
`internal/wails/svc_fixturelibrary.go` (`FixtureLibraryService`), new `wailsBridge.ts` exports, and a
real `FixtureLibraryWorkspace.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-front-door-ui-completion/09-CONTEXT.md
@.planning/phases/09-front-door-ui-completion/09-RESEARCH.md
@.planning/phases/09-front-door-ui-completion/09-PATTERNS.md
@.planning/phases/09-front-door-ui-completion/09-UI-SPEC.md
</context>

## Artifacts this phase produces (plan 01)

New symbols created by this plan — these do not exist anywhere in the codebase yet, so a
source-grounding drift check must treat them as newly created, not as unacknowledged references:

- Go type `fixture.DirectoryEntry` (fields `FileName`, `Definition`, `Identity`, `Err`)
- Go function `fixture.ListDirectory(dir string) ([]DirectoryEntry, error)`
- Go diagnostic code `GOLC_FIXTURE_DIR_READ_FAILED`
- Go type `wails.FixtureLibraryService` and constructor `wails.NewFixtureLibraryService`
- Go types `wails.FixtureLibraryRowView`, `wails.FixtureLibraryView`, `wails.FixtureInspectView`,
  `wails.FixtureWarningView`
- Go methods `FixtureLibraryService.ListLocal`, `FixtureLibraryService.Inspect`
- Go diagnostic code `GOLC_WAILS_FIXTURE_INSPECT_DECODE_FAILED`
- TypeScript interface `FixtureLibraryServiceBinding` and `window.go.wails.FixtureLibraryService`
- TypeScript types `FixtureLibraryRowView`, `FixtureLibraryView`, `FixtureInspectView`,
  `FixtureWarningView`
- TypeScript exports `listLocalFixtures`, `inspectFixtureFile`, `offlineFixtureLibraryView`
- New file `frontend/src/workspaces/build/FixtureLibraryWorkspace.module.css`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing tests for local fixture listing, projection, and workspace rendering</name>
  <files>internal/fixture/directory_test.go, internal/wails/svc_fixturelibrary_test.go, frontend/src/workspaces/build/FixtureLibraryWorkspace.test.tsx</files>
  <read_first>
    - `internal/command/artnet.go` (lines 300-400) — the private `loadFixtureDirectory` being extracted, its decode/pin/skip discipline, and `newArtnetFixtureResolver`, its only caller
    - `internal/fixture/decode.go` and `internal/fixture/identity.go` — `Decode`/`Pin` signatures and the `Identity` fields (`StableKey`, `ContentHash`, `Revision`)
    - `internal/wails/svc_show_test.go` — the established table/temp-dir test shape for a Wails service in this package
    - `frontend/src/workspaces/build/FixtureLibraryWorkspace.test.tsx` — the current stub assertions being replaced
    - `frontend/src/workspaces/build/ScriptsWorkspace.test.tsx` — the established `window.go.wails` mock + `@testing-library/react` shape for a bridge-backed workspace test
    - `.planning/phases/09-front-door-ui-completion/09-UI-SPEC.md` — Copywriting Contract rows for the Fixture Library empty/error copy the tests assert
  </read_first>
  <behavior>
    - `TestListDirectorySkipsNonFixtureFiles`: a temp dir holding `a.yaml`, `b.yml`, `notes.txt`, and a subdirectory returns exactly the two YAML entries, in `os.ReadDir` filename order.
    - `TestListDirectoryRecordsPerEntryFailureWithoutAbortingScan`: a dir holding one valid and one malformed YAML returns two entries; the malformed one carries a non-nil `Err` and the valid one a populated `Identity`.
    - `TestListDirectoryMissingDirectoryReturnsNotExistError`: a nonexistent dir returns an error satisfying `errors.Is(err, fs.ErrNotExist)` and whose message carries `GOLC_FIXTURE_DIR_READ_FAILED`.
    - `TestFixtureLibraryServiceListLocalProjectsRowsSortedByStableKey`: two valid fixtures project into rows sorted ascending by `StableKey`, each with `source` `"local"` and `status` `"valid"`.
    - `TestFixtureLibraryServiceListLocalMissingDirectoryIsEmptyNotError`: a service pointed at a nonexistent directory returns a view whose `Rows` is a non-nil empty slice and a nil error.
    - `TestFixtureLibraryServiceListLocalNeverMarshalsRowsAsNull`: `json.Marshal` of the returned view contains `"rows":[]`, never `"rows":null` (the svc_show.go nil-slice contract).
    - `TestFixtureLibraryServiceInspectReportsInvalidWithDiagnostics`: inspecting a malformed YAML file returns `valid:false` with a non-empty `Errors` slice; inspecting a valid one returns `valid:true` with the pinned stable key and content hash.
    - Frontend `renders local library rows with manufacturer and validation chip`: a mocked `FixtureLibraryService.ListLocal` returning two rows renders both model names and both manufacturer values.
    - Frontend `renders the no-fixtures empty state`: a mocked empty view renders the "No fixtures yet" heading and the import-or-add body copy.
    - Frontend `filters rows by search text`: typing into the search input narrows the rendered rows to substring matches on manufacturer or model.
    - Frontend `renders inline inspect detail for the selected row`: clicking a row renders that fixture's stable key and content hash in the workspace body (not a dialog/modal role).
  </behavior>
  <action>
Create `internal/fixture/directory_test.go` in package `fixture` (internal test package is fine — `Decode`/`Pin` are same-package) covering the three `ListDirectory` behaviors above. Build fixture YAML content from the same minimal valid shape `internal/fixture/decode_test.go` already uses (schema_version, manufacturer, model, modes, capabilities) rather than inventing a new one; write files with `os.WriteFile` under `t.TempDir()`.

Create `internal/wails/svc_fixturelibrary_test.go` in package `wails` covering `ListLocal` and `Inspect`. Construct the service via `NewFixtureLibraryService(pipeName, root, fixturesDir)` where `root` is `t.TempDir()` and `fixturesDir` a subdirectory of it. Include the `json.Marshal` nil-slice assertion — that is the exact contract `TestShowServiceDiagnosticReportNeverOmitsOrNullsFileLevelIssues` pins for `ShowService`, and it must be pinned here too because `null.length` throws with no error boundary anywhere in this app.

Rewrite `frontend/src/workspaces/build/FixtureLibraryWorkspace.test.tsx` completely — its current assertions describe the stub and are meaningless once the real workspace lands. Mock the bridge by assigning `window.go = { wails: { FixtureLibraryService: { ListLocal: vi.fn(), Inspect: vi.fn() } } }` in `beforeEach` and deleting it in `afterEach`, following the exact shape `ScriptsWorkspace.test.tsx` already uses. Use `await screen.findBy...` for post-effect assertions since the workspace loads in a `useEffect`.

Assert the empty-state and error copy against the exact strings in the UI-SPEC Copywriting Contract, not paraphrases.

All tests in this task must FAIL at this point — `fixture.ListDirectory`, `NewFixtureLibraryService`, and the real workspace do not exist yet. Commit them red.
  </action>
  <acceptance_criteria>
    - `go test ./internal/fixture/... -run TestListDirectory` fails with a compile error naming `ListDirectory` (the symbol does not exist yet).
    - `go test ./internal/wails/... -run TestFixtureLibraryService` fails with a compile error naming `NewFixtureLibraryService`.
    - `npm --prefix frontend test -- FixtureLibraryWorkspace` reports failing assertions (not a passing stub test).
    - `grep -c "No fixtures yet" frontend/src/workspaces/build/FixtureLibraryWorkspace.test.tsx` is at least 1.
    - `grep -v '^//' frontend/src/workspaces/build/FixtureLibraryWorkspace.test.tsx | grep -c "golc fixture"` is 0 — the CLI-hint stub assertion is gone.
    - `grep -c "\"rows\":\[\]" internal/wails/svc_fixturelibrary_test.go` is at least 1.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/fixture/... ./internal/wails/... -run 'TestListDirectory|TestFixtureLibraryService' 2>&1 | grep -q -E 'undefined|FAIL' &amp;&amp; echo RED_CONFIRMED</automated>
  </verify>
  <done>Three test files exist and fail for the right reason (missing symbols / unimplemented behavior), committed as a `test(09-01):` commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship the end-to-end local library list — extracted directory scan through to rendered rows</name>
  <files>internal/fixture/directory.go, internal/command/artnet.go, internal/wails/svc_fixturelibrary.go, cmd/golc-desktop/main.go, frontend/src/lib/wailsBridge.ts, frontend/src/workspaces/build/FixtureLibraryWorkspace.tsx, frontend/src/workspaces/build/FixtureLibraryWorkspace.module.css</files>
  <read_first>
    - `internal/command/artnet.go` (lines 300-400) — `loadFixtureDirectory` and `newArtnetFixtureResolver`
    - `internal/wails/svc_show.go` — the whole file: `execute` helper, read-projection shape, camelCase JSON tags, and the nil-slice normalization doc comment
    - `internal/wails/svc_fixturepatch.go` — the sibling `execute` helper and constructor signature convention
    - `cmd/golc-desktop/main.go` — service construction order and the `options.App{Bind: [...]}` list
    - `frontend/src/lib/wailsBridge.ts` (lines 540-620 and 1081-1140) — `ShowServiceBinding`, the single `declare global` block, `bridgeUnavailableResult`, `errorMessage`, `assertOk`, and the `showService()` / `offlineShowInspectView()` accessor+fallback+wrapper trio
    - `frontend/src/workspaces/show/SaveRecoveryWorkspace.tsx` — the Toolbar -> canvas -> layout -> Panel/PanelHeader/ScrollRegion/EmptyState skeleton and its load/refresh/error `useCallback`+`useEffect` shape
    - `frontend/src/workspaces/show/SaveRecoveryWorkspace.module.css` — the module-CSS conventions (`.workspace`, `.canvas`, `.layout`, `.errorText`, `.loading`) to mirror
    - `frontend/src/components/primitives/ListRow/ListRow.tsx` and `.../Chip/Chip.tsx` — `ListRow` props (`label`, `icon`, `meta`, `selected`, `onSelect`; it already sets `title={label}`) and the `ChipTone` union
  </read_first>
  <action>
Create `internal/fixture/directory.go` in package `fixture`. Define `DirectoryEntry` with fields `FileName string`, `Definition FixtureDefinition`, `Identity Identity`, `Err error`, and `ListDirectory(dir string) ([]DirectoryEntry, error)`. `ListDirectory` reads `dir` non-recursively with `os.ReadDir`, skips directories and any file whose lowercased `filepath.Ext` is not `.yaml` or `.yml`, and for each remaining file reads it, calls `Decode`, then `Pin`. Per-entry read/decode/pin failure is recorded on that entry's `Err` and the scan continues — it does not abort. Only the `os.ReadDir` failure returns a top-level error, wrapped with `%w` under diagnostic code `GOLC_FIXTURE_DIR_READ_FAILED` so callers can test it with `errors.Is(err, fs.ErrNotExist)`. Entry order is `os.ReadDir`'s own filename order (already deterministic).

Rewrite `internal/command/artnet.go`'s `loadFixtureDirectory` as a thin delegation to `fixture.ListDirectory` that preserves its existing fail-fast contract and its existing diagnostic strings byte-for-byte: a top-level error becomes `GOLC_ARTNET_FIXTURES_DIR_READ_FAILED: %v`, the first entry carrying a non-nil `Err` becomes `GOLC_ARTNET_FIXTURES_DIR_READ_FAILED: %s: %v` with that entry's `FileName`, and successful entries are indexed into the same `map[string]fixture.FixtureDefinition` keyed by `Identity.StableKey`. There must be exactly one directory-scan implementation after this change.

Create `internal/wails/svc_fixturelibrary.go` in package `wails`, following `svc_show.go`'s file-level doc-comment convention (state what is reused vs genuinely new, and explicitly disclaim any duplicate implementation). Define:
- `FixtureLibraryService` with `pipeName`, `root`, `fixturesDir` fields, and `NewFixtureLibraryService(pipeName, root, fixturesDir string) *FixtureLibraryService`. When `fixturesDir` is empty the constructor resolves it to `filepath.Join(root, "fixtures")`.
- An `execute(args []string) Result` helper copied verbatim in shape from `svc_show.go` (`command.NewDefaultCommandRegistry`, `GOLC_WAILS_REGISTRY_BUILD_FAILED` on build failure, `registry.Execute(command.Request{Root: s.root, Args: args})`).
- `FixtureLibraryRowView` with camelCase JSON tags `stableKey`, `manufacturer`, `model`, `fileName`, `source`, `status`, `detail`, and `FixtureLibraryView` with `directory` and `rows`.
- `ListLocal() (FixtureLibraryView, error)`: calls `fixture.ListDirectory(s.fixturesDir)`; when the error satisfies `errors.Is(err, fs.ErrNotExist)` it returns a view with a non-nil empty `Rows` slice and a nil error (a library the operator has not created yet is empty, not broken); any other error is returned. Each entry projects to a row with `source` `"local"`; an entry with a non-nil `Err` gets `status` `"invalid"`, `detail` set to that error's message, and `manufacturer`/`model` left empty with `stableKey` falling back to the file name; a clean entry gets `status` `"valid"` and its pinned `StableKey`. Rows are sorted ascending by `StableKey`. `Rows` is never nil. `directory` is projected repo-relative with forward slashes when `s.fixturesDir` is under `s.root`, and as `"external:" + filepath.Base(...)` otherwise — the same discipline `internal/command/fixture.go`'s `fixtureInspectSource` uses; never the resolved absolute path.
- `FixtureWarningView` (`severity`, `capabilityType`, `detail`) and `FixtureInspectView` (`path`, `valid`, `errors`, `schemaVersion`, `stableKey`, `contentHash`, `revision`, `source`, `validationResult`, `warnings`) — both slice fields always non-nil.
- `Inspect(path string) (FixtureInspectView, error)`: forwards to `s.execute([]string{"fixture", "inspect", path})`. On exit code 0 it unmarshals the route's stdout (the existing allowlisted `fixtureInspectView` JSON envelope, snake_case) into the camelCase `FixtureInspectView` with `valid: true`; a JSON unmarshal failure returns an error carrying `GOLC_WAILS_FIXTURE_INSPECT_DECODE_FAILED`. On a non-zero exit code it returns `valid: false` with `errors` set to the non-empty trimmed lines of stderr and a nil error — a rejected fixture is a renderable state, not a thrown exception. This method must not decode, validate, or pin anything itself.

In `cmd/golc-desktop/main.go`, construct `fixtureLibraryService := golcwails.NewFixtureLibraryService(cfg.PipeName, cfg.ProjectRoot, cfg.FixturesDir)` alongside the existing services and add it to the `options.App{Bind: [...]}` slice. Do not change `cfg.FixturesDir` itself or the `defaultSpawn` daemon arguments — the daemon's own fixture-directory behavior stays exactly as it is today.

In `frontend/src/lib/wailsBridge.ts` add, following the existing three-part contract exactly: exported types `FixtureLibraryRowView`, `FixtureLibraryView`, `FixtureWarningView`, `FixtureInspectView`; a `FixtureLibraryServiceBinding` interface declaring `ListLocal(): Promise<FixtureLibraryView>` and `Inspect(path: string): Promise<FixtureInspectView>`; a `FixtureLibraryService?: FixtureLibraryServiceBinding` property inside the single existing `declare global` `window.go.wails` block (do not add a second `declare global`); a `fixtureLibraryService()` accessor; an exported `offlineFixtureLibraryView(): FixtureLibraryView` returning `{ directory: "", rows: [] }`; and `listLocalFixtures()` / `inspectFixtureFile(path)` wrappers that return the offline fallback when the service is absent and swallow a rejected call into the same fallback rather than throwing.

Replace `frontend/src/workspaces/build/FixtureLibraryWorkspace.tsx` in full with the real workspace, and add `FixtureLibraryWorkspace.module.css`. This task delivers the LIST only — no inline inspect panel yet. Structure: a file-level doc comment stating what this replaces and which backend routes it wires against; `Toolbar title="Fixture Library" icon={Lightbulb}`; a `useState`/`useCallback`/`useEffect` load-refresh-error skeleton mirroring `SaveRecoveryWorkspace.tsx`; a `Panel` + `PanelHeader` whose label carries the row count with singular/plural agreement ("1 fixture" / "{n} fixtures"); a `ScrollRegion` wrapping either an `EmptyState` with the "No fixtures yet" heading and the "Import a fixture from the Open Fixture Library or add your own YAML definition to get started." body, or a `<ul aria-label="Fixture library">` of `ListRow`s. Each row uses `label` = the model name (falling back to the file name for an invalid entry), `meta` = the manufacturer plus a `Chip` whose tone is `frame-lock` for `"valid"` and `revoked` for `"invalid"`. Row truncation and the full-value tooltip come from `ListRow`'s existing fixed-width label and `title` behavior — do not re-implement them. Render the load error with the same inline `<p className={styles.errorText}>` pattern `SaveRecoveryWorkspace.tsx` uses.
  </action>
  <acceptance_criteria>
    - `go build ./...` succeeds.
    - `go test ./internal/fixture/... -run TestListDirectory` passes.
    - `go test ./internal/wails/... -run TestFixtureLibraryServiceListLocal` passes.
    - `go test ./internal/command/...` passes unchanged — the artnet delegation preserves the existing diagnostic strings.
    - `grep -c "func loadFixtureDirectory" internal/command/artnet.go` is 1 and `grep -c "fixture.ListDirectory" internal/command/artnet.go` is at least 1.
    - `grep -v '^//' internal/command/artnet.go | grep -c "fixture.Decode(data)"` is 0 — the duplicate decode loop is gone from the command layer.
    - `grep -c "NewFixtureLibraryService" cmd/golc-desktop/main.go` is 2 (construction plus the Bind entry).
    - `grep -c "declare global" frontend/src/lib/wailsBridge.ts` is 1.
    - `npm --prefix frontend test -- FixtureLibraryWorkspace` passes the list-rendering and empty-state cases.
    - `npm --prefix frontend run build` succeeds (tsc + vitest + vite).
  </acceptance_criteria>
  <verify>
    <automated>go build ./... &amp;&amp; go test ./internal/fixture/... ./internal/wails/... ./internal/command/... &amp;&amp; npm --prefix frontend run build</automated>
    <human-check>Launch golc-desktop, open Build -> Fixture Library, and confirm the panel lists the fixture YAML files in the resolved fixtures directory with correct manufacturer text and status chips (and the "No fixtures yet" empty state when the directory is absent).</human-check>
  </verify>
  <done>An operator opening Build -> Fixture Library sees their real local fixture library, not a Coming Soon stub. The directory scan exists in exactly one place and the artnet resolver delegates to it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add inline inspect and search to the Fixture Library workspace</name>
  <files>frontend/src/workspaces/build/FixtureLibraryWorkspace.tsx, frontend/src/workspaces/build/FixtureLibraryWorkspace.module.css</files>
  <read_first>
    - `frontend/src/workspaces/build/FixtureLibraryWorkspace.tsx` — the list-only workspace from Task 2 being extended
    - `frontend/src/workspaces/build/ScriptsWorkspace.tsx` — the selection-state + detail-panel + validation-gates-action composition this workspace mirrors
    - `frontend/src/workspaces/show/OverviewWorkspace.tsx` (lines 91-114) — the technical-readout treatment (`Schema {v} · Revision {r}`) and `Chip` status usage the inspect panel reuses
    - `.planning/phases/09-front-door-ui-completion/09-UI-SPEC.md` — the Copywriting Contract search placeholder and validation-failure copy, the Typography "Technical readout" row, and the accent-reserved-for list
  </read_first>
  <behavior>
    - Typing "chauvet" into the search input with rows for two different manufacturers leaves only the matching row rendered.
    - Search matching is case-insensitive and matches on manufacturer OR model.
    - Clicking a row calls `Inspect` with that row's file path and renders the returned stable key and content hash in the workspace body.
    - Selecting a row whose `Inspect` returns `valid:false` with two error strings renders the "This fixture definition has 2 error(s) and can't be added. Fix them and try again." copy.
    - Selecting a row whose inspect result carries warnings renders the "This import has {N} unsupported or approximated attribute(s) — review before adding." warning with an `armed`-tone chip, distinct from the invalid state.
    - With no row selected the inspect region renders nothing prominent (it stays visually quiet until a selection exists).
  </behavior>
  <action>
Extend the workspace with a search input and an inline inspect panel. Add `search`, `selectedStableKey`, `inspecting`, and `inspectView` state.

Search: render a text input inside the `Toolbar` region or directly above the list with `aria-label="Search fixtures"` and `placeholder="Search fixtures by name or manufacturer…"` (exact UI-SPEC copy, including the ellipsis character). Filtering is client-side over the already-fetched rows — a case-insensitive substring test against `manufacturer` and `model`. Do not add a backend round-trip and do not add faceted filter controls (D-03 scopes this to basic text search).

Inline inspect: selecting a `ListRow` sets `selected` on that row and calls `inspectFixtureFile(<row.fileName resolved against the library directory as the workspace already knows it>)`. Render the result as a second `Panel` inside the same `styles.layout` grid — never a dialog, modal, or wizard step (D-02: one on-screen flow). The panel renders: the stable key, content hash, revision, and source in the Technical readout treatment (the same 14px JetBrains Mono class `OverviewWorkspace.module.css` uses for `Schema … · Revision …`); a validation `Chip` with tone `frame-lock` when valid and `revoked` when not; on invalid, the exact "This fixture definition has {N} error(s) and can't be added. Fix them and try again." copy with `{N}` = `errors.length`, followed by the individual diagnostics as a list; on warnings, the exact "This import has {N} unsupported or approximated attribute(s) — review before adding." copy with an `armed`-tone chip and the per-warning details itemized (FIXT-06's own unsupported/lossy detail).

Keep the inspect panel visually quiet with no selection — render an `EmptyState` prompting the operator to select a fixture rather than a large empty container. Reserve the accent (`--accent`) in this workspace for the selected row and the search input's focus ring only; no primary-variant button is introduced by this plan.

Add the corresponding CSS-module classes (`.layout`, `.searchInput`, `.inspectPanel`, `.techReadout`, `.diagnosticList`, `.errorText`, `.warningText`) using the declared spacing scale: 8px row internal padding, 16px search-bar padding, 24px inspect-panel padding, 32px gap between the list and the inspect panels. Do not introduce a new font size or a third font weight.
  </action>
  <acceptance_criteria>
    - `npm --prefix frontend test -- FixtureLibraryWorkspace` passes, including the search-filter and inline-inspect cases.
    - `grep -c "Search fixtures by name or manufacturer" frontend/src/workspaces/build/FixtureLibraryWorkspace.tsx` is at least 1.
    - `grep -c "error(s) and can't be added" frontend/src/workspaces/build/FixtureLibraryWorkspace.tsx` is at least 1.
    - `grep -v '^//' frontend/src/workspaces/build/FixtureLibraryWorkspace.tsx | grep -c -E 'role="dialog"|<Modal|Dialog'` is 0 — inspect is inline, not a modal (D-02).
    - `grep -c -E '^\s*(gap|padding):\s*(8|16|24|32)px' frontend/src/workspaces/build/FixtureLibraryWorkspace.module.css` is at least 3 — spacing comes from the declared scale.
    - `npm --prefix frontend run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix frontend test -- FixtureLibraryWorkspace &amp;&amp; npm --prefix frontend run build</automated>
    <human-check>In the running app, type a partial manufacturer name and confirm the list narrows; click a fixture and confirm its stable key, content hash, and validation status render inline in the workspace (not in a popup); confirm a deliberately broken YAML file shows the "{N} error(s)" copy.</human-check>
  </verify>
  <done>An operator can search their fixture library and read one fixture's full identity, provenance, validation verdict, and lossy-import warnings inline in the same workspace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> Go backend | Fixture YAML files in the library directory are operator-supplied, untrusted content decoded by `internal/fixture.Decode` |
| Go backend -> webview | Every projected view crosses into the webview as JSON; a nil slice or an absolute path leaks across this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | `FixtureLibraryView.directory` | medium | mitigate | Project the library directory repo-relative with forward slashes, or as `external:<basename>` when outside the root — the same discipline `internal/command/fixture.go`'s `fixtureInspectSource` applies for T-01-23; never emit the resolved absolute path |
| T-09-01-02 | Denial of Service | `fixture.ListDirectory` over a large or hostile directory | low | mitigate | Non-recursive single-level scan; only `.yaml`/`.yml` files are read; a per-entry decode/pin failure is recorded on that entry and the scan continues, so one malformed file can neither abort the listing nor blank the workspace |
| T-09-01-03 | Tampering | Duplicate fixture validation logic in the Wails layer | high | mitigate | `ListLocal` decodes only through `internal/fixture.Decode`/`Pin` via the single extracted `ListDirectory`; `Inspect` forwards to the registered `fixture inspect` route through `command.NewDefaultCommandRegistry` and only projects its output — no second accept/reject implementation exists that could diverge from the CLI |
| T-09-01-04 | Denial of Service | `null.length` unmount from a nil-marshalled slice | medium | mitigate | `Rows`, `Errors`, and `Warnings` are normalized to non-nil empty slices before return, pinned by a `json.Marshal` assertion — the app has no React error boundary, so a `null.length` throw blanks the entire window |
| T-09-SC | Tampering | npm/go package installs | high | accept | This phase installs no new packages (`09-RESEARCH.md` Package Legitimacy Audit: not applicable — every capability is an already-vendored dependency or stdlib). No `[ASSUMED]`/`[SUS]`/`[SLOP]` package enters the tree, so no legitimacy checkpoint applies |
</threat_model>

<verification>
- `go build ./...`, `go test ./...` green.
- `npm --prefix frontend run build` green (tsc --noEmit, vitest run, vite build).
- Exactly one local-fixture-directory scan implementation exists in the repository.
- The Build -> Fixture Library destination renders a real list-and-inspect workspace.
</verification>

<success_criteria>
- FDUI-01's local-browse half is delivered: browse, search, and inspect a local fixture library entirely on screen.
- `internal/command/artnet.go` and `internal/wails/svc_fixturelibrary.go` share one directory-scan implementation.
- No fixture validation verdict shown on screen originates anywhere other than `internal/fixture` or the registered `fixture inspect` route.
</success_criteria>

<output>
Create `.planning/phases/09-front-door-ui-completion/09-01-SUMMARY.md` when done.
</output>
