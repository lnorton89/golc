---
phase: 06-wails-authoring-and-operator-surface
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/midi/takeover.go
  - internal/midi/takeover_test.go
  - internal/midi/learn.go
  - internal/midi/learn_test.go
autonomous: true
requirements: [PLAY-04, PLAY-05]
must_haves:
  truths:
    - "Soft takeover uses cross-to-catch: a fader arms only when its physical value reaches or crosses the app's current value in the direction of travel, then its value becomes the controlling value (D-11/PLAY-05)"
    - "Soft takeover logic applies only to continuous CC/fader controls; Note/button controls act immediately with no pickup/arming state (D-12)"
    - "MIDI learn is a bounded per-control capture session: the next Note-on or CC message received is the mapping candidate (D-05)"
    - "A learn candidate whose (channel, kind, number) tuple already maps a different control on the current surface is rejected outright; the existing mapping is left untouched and never overwritten (D-06)"
  prohibitions:
    - "Soft takeover must NOT arm on proximity/threshold (abs(physical - appValue) < tolerance); it must arm only on a true directional crossing of appValue (D-11) — a tunable sensitivity/threshold constant is the wrong mode"
    - "A learn conflict must NOT silently overwrite or prompt-to-reassign; it is rejected outright, leaving the prior mapping intact (D-06)"
  artifacts:
    - path: internal/midi/takeover.go
      provides: "cross-to-catch soft-takeover state machine (direction-aware, continuous-only)"
      contains: "func (t *TakeoverState) Update"
    - path: internal/midi/learn.go
      provides: "bounded learn capture + conflict rejection against a surface's ControlKey set"
      contains: "ControlKey"
  key_links:
    - from: internal/midi/learn.go
      to: internal/midi/takeover.go
      via: "shared ControlKey (channel, kind, number) tuple type"
      pattern: "ControlKey"
---

<phase_goal>
**As a** playback operator, **I want to** run a prepared show through on-screen, keyboard, and constrained generic MIDI controls with independent local safety actions, **so that** I can operate reliably without learning the authoring system and without the frontend ever owning playback.

This plan delivers the two pure, device-independent MIDI algorithms (PLAY-04 learn-conflict + PLAY-05 soft takeover) as fully unit-tested Go logic with no external dependency — the correctness core that 06-08 wires to a live gomidi driver and the frontend sliders.
</phase_goal>

<objective>
Create the pure, dependency-free logic of `internal/midi`: the cross-to-catch soft-takeover state machine (D-09..D-12) and the bounded learn-capture + conflict-rejection logic (D-05/D-06). Neither file imports gomidi or any external MIDI library — they operate on our own `ControlKey`/value types — so they build and test with zero new dependencies and zero device access, isolating the phase's hardest correctness question (crossing vs. proximity) into fast unit tests.

Purpose: RESEARCH.md flags soft-takeover crossing (A4) and the learn/conflict rule (D-06) as the phase's error-prone logic. Building them as pure functions first (before the gomidi driver in 06-08) guarantees the crossing/conflict rules are proven independent of live hardware, which stays gated by the open MIDI-HW-02 blocker.

Output: `internal/midi/takeover.go` and `internal/midi/learn.go` (+ tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-wails-authoring-and-operator-surface/06-CONTEXT.md
@.planning/phases/06-wails-authoring-and-operator-surface/06-RESEARCH.md
@.planning/phases/06-wails-authoring-and-operator-surface/06-PATTERNS.md

Analog sources to read for convention (small, focused reads):
@internal/artnet/worker.go

<interfaces>
From RESEARCH.md Pattern 4 (the crossing algorithm to implement — direction-aware, NOT proximity):
```go
func (t *TakeoverState) Update(physical float64) (armed bool, controlValue float64) {
    if !t.Armed {
        crossedUp := t.LastPhysical <= t.AppValue && physical >= t.AppValue
        crossedDown := t.LastPhysical >= t.AppValue && physical <= t.AppValue
        if crossedUp || crossedDown { t.Armed = true }
    }
    t.LastPhysical = physical
    if t.Armed { t.AppValue = physical }
    return t.Armed, t.AppValue
}
```

From 06-01 (operatorsurface, do NOT import it here — keep midi decoupled): the persisted mapping tuple is (Channel int, Kind {Note|ControlChange}, Number int). This plan defines the equivalent live-side `ControlKey` value type; the command/host layer maps between operatorsurface.MidiMapping and midi.ControlKey.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: takeover.go — cross-to-catch soft-takeover state machine</name>
  <files>internal/midi/takeover.go, internal/midi/takeover_test.go</files>
  <read_first>
    - .planning/phases/06-wails-authoring-and-operator-surface/06-RESEARCH.md (Pattern 4 example + Pitfall 2 proximity-vs-crossing; Assumption A4 on jitter)
    - .planning/phases/06-wails-authoring-and-operator-surface/06-CONTEXT.md (D-09..D-12)
    - internal/artnet/worker.go lines 259-269 (nextSeq — the small mutex/atomic-guarded pure-transform helper shape to mirror)
  </read_first>
  <behavior>
    - Not armed, appValue=0.5, physical rising 0.2 -> 0.4 -> 0.6: arms on the message that reaches/crosses 0.5, and from then on controlValue tracks physical.
    - Not armed, appValue=0.5, physical falling 0.9 -> 0.6 -> 0.4: arms on the crossing-down message.
    - Not armed, physical hovering below appValue without ever crossing: never arms; controlValue stays appValue (the ghost/target) while LastPhysical still updates (D-09 live position published separately).
    - An exact landing on appValue counts as a crossing (arms).
    - Note/button controls do NOT use TakeoverState at all: expose a way for the caller to treat a control as immediate (a helper or documented rule) so buttons act on press with no arming (D-12).
    - No tunable proximity/threshold constant exists anywhere in the file.
  </behavior>
  <action>
    Create `internal/midi/takeover.go` (package midi). Define `TakeoverState struct { Armed bool; AppValue float64; LastPhysical float64 }` and `func (t *TakeoverState) Update(physical float64) (armed bool, controlValue float64)` implementing the direction-aware crossing check from RESEARCH.md Pattern 4 verbatim (crossedUp OR crossedDown; never `abs(physical-appValue) < threshold`). Add a `func NewTakeoverState(appValue float64) TakeoverState` seeding AppValue and leaving Armed=false. Document at the top that this applies only to continuous CC/fader controls (D-12) and that Note/button controls bypass it entirely (act immediately). Add a small `SetAppValue` to re-seed the target when the app value changes from another source while not armed (so the ghost marker follows the new app value). Write `takeover_test.go` covering every behavior above, explicitly including a rising-cross, falling-cross, never-cross, and exact-landing case.
  </action>
  <verify>
    <automated>go test ./internal/midi/... -run TestTakeover</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/midi/... -run TestTakeover` passes.
    - Tests cover rising-cross, falling-cross, never-cross, and exact-landing.
    - Source review confirms no proximity/threshold constant (crossing only).
  </acceptance_criteria>
  <done>The soft-takeover crossing state machine exists, direction-aware and continuous-only, with exhaustive unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: learn.go — bounded capture candidate + conflict rejection</name>
  <files>internal/midi/learn.go, internal/midi/learn_test.go</files>
  <read_first>
    - internal/artnet/daemon.go lines 353-404 (handleConfigure — the "validate candidate against existing set, reject-or-accept" shape; adapt ONLY the lookup/validate halves, NOT the replace-by-key branch, per 06-PATTERNS.md learn.go section)
    - .planning/phases/06-wails-authoring-and-operator-surface/06-CONTEXT.md (D-05 per-control learn, D-06 conflict rejected outright, D-07 per-surface scope)
  </read_first>
  <behavior>
    - Given an existing set of ControlKeys for a surface, proposing a candidate ControlKey not in the set returns the candidate accepted.
    - Proposing a candidate whose (channel, kind, number) already exists returns GOLC_MIDI_MAPPING_CONFLICT and does not mutate the existing set (existing mapping untouched, D-06).
    - The check is scoped to the provided (per-surface) set only — the same tuple may be free on a different surface (D-07 is enforced by passing that surface's set).
    - A Note candidate and a ControlChange candidate with the same number/channel are distinct keys (kind is part of identity).
  </behavior>
  <action>
    Create `internal/midi/learn.go` (package midi). Define `ControlKey struct { Channel int; Kind MessageKind; Number int }` and `MessageKind` (`Note`, `ControlChange`). Implement `func ProposeMapping(existing []ControlKey, candidate ControlKey) error` returning `GOLC_MIDI_MAPPING_CONFLICT: note/CC %v is already mapped on this surface` when the candidate collides, else nil — never mutating `existing` (D-06: reject outright, existing untouched). Implement a bounded capture helper `func CaptureCandidate(next <-chan ControlKey, timeout <-chan struct{}) (ControlKey, error)` representing the D-05 capture window: it returns the first ControlKey received, or `GOLC_MIDI_LEARN_TIMEOUT` if the timeout fires first (this decouples the pure capture-session logic from the live gomidi driver, which 06-08 supplies as the channel source). Use the `GOLC_MIDI_{CONDITION}` diagnostic convention. Write `learn_test.go` covering accept, conflict-reject (existing untouched), per-set scoping, kind-distinct keys, and capture-timeout.
  </action>
  <verify>
    <automated>go test ./internal/midi/... -run TestLearn</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/midi/... -run TestLearn` passes.
    - A test proves a conflicting candidate is rejected AND the existing set is unchanged.
    - A test proves the same tuple is accepted against a different (empty) surface set.
    - A capture-timeout test returns GOLC_MIDI_LEARN_TIMEOUT.
    - `go build ./internal/midi/...` succeeds with no new go.mod dependency.
  </acceptance_criteria>
  <done>Bounded learn capture and outright conflict rejection exist as pure logic, per-surface scoped, with no external MIDI dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MIDI message stream -> learn/takeover logic | Physical-control values (later, from a device) are untrusted input to arming/mapping decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | learn conflict check (D-06) | medium | mitigate | Conflict is rejected outright and the existing set is never mutated, so a stray/replayed control movement cannot silently steal an existing mapping (Task 2). |
| T-06-08 | Spoofing / unintended action | soft-takeover arming (D-11) | medium | mitigate | Cross-to-catch requires a directional crossing before a fader controls, preventing a connecting/moving controller from causing an unintended value jump (Task 1). |
| T-06-09 | Denial of Service | capture window bound (D-05) | low | accept | Capture is bounded by a timeout channel; an idle learn session ends with GOLC_MIDI_LEARN_TIMEOUT rather than hanging. Rationale: no unbounded wait path. |
</threat_model>

<verification>
- `go test ./internal/midi/... -run 'TestTakeover|TestLearn'` green.
- `go test -race ./internal/midi/...` green at Wave merge.
- No new go.mod/go.sum entries introduced by this plan (device driver is 06-08's scope).
</verification>

<success_criteria>
- Cross-to-catch soft takeover (D-09..D-12) and per-surface learn conflict rejection (D-05/D-06) are proven correct in isolation, ready for the live driver wiring in 06-08.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (created by this plan)

- New package `internal/midi` (partial): `takeover.go` with `TakeoverState`, `NewTakeoverState`, `SetAppValue`, `Update`; `learn.go` with `ControlKey`, `MessageKind` (`Note`/`ControlChange`), `ProposeMapping`, `CaptureCandidate`.
- No new external dependency; the gomidi driver + device I/O is added by 06-08.
</artifacts_produced>

<output>
Create `.planning/phases/06-wails-authoring-and-operator-surface/06-03-SUMMARY.md` when done.
</output>
