---
phase: 06-wails-authoring-and-operator-surface
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/wails/svc_midi.go
  - internal/wails/svc_midi_test.go
autonomous: true
gap_closure: true
requirements: [PLAY-04, PLAY-05]
must_haves:
  truths:
    - "A MIDI Note mapped to a scene control switches the show's active scene when the button is pressed — the persisted show's active scene changes, not only the on-screen armed/ghost feedback marker."
    - "A MIDI Note mapped to a layer control flips that layer's Enabled flag when pressed, and preserves the layer's existing Ref (mirrors PlaybackService.SetLayerEnabled's WR-01/WR-03 discipline)."
    - "A MIDI CC mapped to a group or grand master forwards `artnet master set` to the daemon with the crossed control value once the fader crosses to armed (cross-to-catch, D-11), and never before arming."
    - "A MIDI Note mapped to a safety control forwards the corresponding `artnet safety blackout|stop-all|revoke-automation --source manual` route to the daemon when pressed."
    - "A message matching no mapping on the active surface still dispatches nothing and changes no state (unchanged from the prior feedback-only behavior)."
    - "PLAY-04 idempotency/concurrency edges (repeated capture, parallel learn) remain addressed by the already-executed 06-03/06-08 learn+takeover plans — no re-resolution needed this round."
  artifacts:
    - "internal/wails/svc_midi.go — dispatchToActiveSurface builds and executes the command implied by a matched mapping's ControlRef Target"
    - "internal/wails/svc_midi_test.go — tests asserting a mapped MIDI event changes playback/safety state, not only feedback state"
  key_links:
    - "svc_midi.go dispatchToActiveSurface -> internal/command default registry (scene switch, layer set) via an in-process execute, mirroring PlaybackService.execute"
    - "svc_midi.go dispatchToActiveSurface -> daemon IPC dial (artnet master set, artnet safety *) via a dialForwardFunc, mirroring SafetyService.toggle"
  prohibitions:
    - "MIDI master and safety dispatch must reach the daemon over the same IPC dial path SafetyService already uses; it must not route master or safety actions through show.Save disk writes."
    - "A discrete action (scene switch, safety trigger, layer toggle) fires on the control's activation edge only; a held or repeatedly-sampled armed control must not re-fire the same discrete action on every subsequent message."
    - "Dispatch must not introduce a second, independent MIDI-mappable control list; the only dispatchable controls are the mappings already persisted on the active surface."
---

<objective>
Close VERIFICATION.md Gap B[1] (the FAILED "MIDI dispatch" truth): `internal/wails/svc_midi.go`'s `dispatchToActiveSurface` currently computes cross-to-catch arming state and calls `emitMidiFeedback` only — it never builds or executes an `internal/command` Request for the matched mapping, so a mapped fader/button updates the on-screen marker but does not switch scenes, toggle layers, change master levels, or trigger safety actions. This plan wires the dispatch step so an armed/pressed mapping actually operates the show, mirroring how `PlaybackService`/`SafetyService` already dispatch the same four action kinds.

Purpose: PLAY-04 ("map MIDI Note/CC input to supported playback commands") and PLAY-05 (soft takeover) cannot be considered functionally complete until a triggered mapping changes real playback/safety state. This is an amendment to the already-executed 06-03/06-08 learn+takeover work, not new requirement scope.
Output: `dispatchToActiveSurface` that executes the ControlRef-implied command, plus tests proving state change (not only feedback).

## Phase Goal (MVP vertical slice)

**As a** playback operator, **I want to** have a learned MIDI control actually run the show when I press or move it, **so that** the constrained generic MIDI surface operates the rig, not just the on-screen markers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wails-authoring-and-operator-surface/06-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing dispatch tests — a mapped MIDI event must change playback/safety state, not only feedback</name>
  <files>internal/wails/svc_midi_test.go</files>
  <acceptance_criteria>
    - Five tests named TestMidiServiceDispatchSceneNoteSwitchesActiveScene, TestMidiServiceDispatchLayerNoteTogglesEnabledPreservingRef, TestMidiServiceDispatchMasterCcForwardsOnlyAfterCrossing, TestMidiServiceDispatchSafetyNoteForwardsDaemonRoute, and TestMidiServiceDispatchUnmappedEventDoesNothing exist.
    - `go test ./internal/wails/... -run TestMidiServiceDispatch` reports FAIL (RED).
    - internal/wails/svc_midi.go is unmodified by this task.
  </acceptance_criteria>
  <read_first>
    - internal/wails/svc_midi.go (the whole file — `dispatchToActiveSurface` ~lines 211-233 is the target; note it currently only calls `emitMidiFeedback`)
    - internal/wails/svc_midi_test.go (existing table/fixture conventions: how it constructs a MidiService, seeds a show via show.Save, feeds `midi.Event`s, and asserts Result/feedback — reuse verbatim)
    - internal/wails/svc_playback.go (`execute`, `SwitchScene`, `SetLayerEnabled`, `currentLayerRef` — the exact in-process registry dispatch and Ref-preservation this task's scene/layer expectations must match)
    - internal/wails/svc_safety.go (`dial`, `dialFn`, `toggle`, `Blackout`/`StopReleaseAll`/`RevokeAutomation` — the daemon dial+forward pattern and the `dialForwardFunc` injection point tests use)
    - internal/wails/hotkey.go (`dialForwardFunc`, `defaultDialForward` — the injectable dial type and its production default)
    - internal/midi/driver.go (`Event{Key ControlKey; Value float64}`; Note-on Value is velocity/127, Note-off Value is 0, CC Value is value/127)
    - internal/operatorsurface/model.go (`ControlRef`, `MasterRef`, `SafetyControl`, `MidiMapping.Target`)
    - internal/command/artnet.go `runArtnetMasterSet` (~lines 819-860 — the exact `artnet master set` grand vs group/level daemon arg grammar the master expectation must assert against)
  </read_first>
  <behavior>
    - Test: a Note mapping whose Target is a scene, fed a Note-on event (Value>0), causes the loaded show's active scene to become that scene (assert via show.Load after dispatch). A following Note-off (Value 0) does NOT re-switch or error.
    - Test: a Note mapping whose Target is a layer, fed a Note-on, flips that layer's Enabled flag while leaving its Ref unchanged (seed a layer with a non-nil Ref; assert Ref survives the toggle).
    - Test: a CC mapping whose Target is a grand or group master, fed CC events that cross the seeded app value, forwards exactly one `artnet master set` daemon Request (captured via an injected dialForwardFunc) with the crossed value, and forwards NOTHING for pre-arm messages that have not yet crossed.
    - Test: a Note mapping whose Target is a safety control (blackout/stop-all/revoke-automation), fed a Note-on, forwards the matching `artnet safety ...` Request with `--source manual` (captured via the injected dial). 
    - Test: an event matching no active-surface mapping forwards nothing and changes no show state.
  </behavior>
  <action>
    Add failing tests to internal/wails/svc_midi_test.go named TestMidiServiceDispatchSceneNoteSwitchesActiveScene, TestMidiServiceDispatchLayerNoteTogglesEnabledPreservingRef, TestMidiServiceDispatchMasterCcForwardsOnlyAfterCrossing, TestMidiServiceDispatchSafetyNoteForwardsDaemonRoute, and TestMidiServiceDispatchUnmappedEventDoesNothing.
    Construct the MidiService with an injected dialForwardFunc capturing every ipc.Request (copy svc_safety_test.go's fake-dial approach). Seed the show via show.Save with at least one scene (one layer carrying a non-nil Ref), one group, and an operator surface whose SceneRefs/LayerRefs/MasterRefs/SafetyRefs include the targets, plus MidiMappings binding a Note to the scene, a Note to the layer, a CC to a master, and a Note to a safety control. Call SetActiveSurface(surfaceName) so activeMappings is populated, then feed events by calling the same routing entrypoint the driver uses (route/dispatchToActiveSurface).
    These tests MUST fail against the current feedback-only dispatchToActiveSurface (scene/layer assertions fail because state never changes; master/safety assertions fail because the fake dial captures zero Requests). Do not modify svc_midi.go in this task.
  </action>
  <verify>
    <automated>go test ./internal/wails/... -run TestMidiServiceDispatch 2>&1 | grep -qE 'FAIL|--- FAIL' && echo RED-OK</automated>
  </verify>
  <done>Five new dispatch tests exist and fail for the documented reason (state/daemon-forward assertions), proving the gap is captured before the fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement command dispatch in dispatchToActiveSurface (scene/layer in-process, master/safety over daemon IPC)</name>
  <files>internal/wails/svc_midi.go, internal/wails/svc_midi_test.go</files>
  <acceptance_criteria>
    - `go test ./internal/wails/... -run TestMidiServiceDispatch` passes (all five Task-1 tests GREEN).
    - `go test ./internal/wails/...` and `go vet ./internal/wails/...` stay green.
    - NewMidiService's signature is unchanged (cmd/golc-desktop/main.go untouched).
    - dispatchToActiveSurface still calls emitMidiFeedback for every event AND now dispatches the ControlRef-implied command; master/safety go through the dial, scene/layer through the in-process registry.
  </acceptance_criteria>
  <read_first>
    - internal/wails/svc_midi.go (target file; `dispatchToActiveSurface`, `takeoverStateFor`, `emitMidiFeedback`, `controlRefLabel`, `sceneNameByID`, `groupNameByID`)
    - internal/wails/svc_playback.go (`execute` — build via command.NewDefaultCommandRegistry and Execute with Request{Root: s.root, Args: ...}; `SwitchScene` args `playback switch <scene> --show <path>`; `SetLayerEnabled` args `scene layer set <scene> --kind <kind> [--ref <uuid>] [--disable] --show <path>`; `currentLayerRef` for reading the existing Ref and Enabled flag)
    - internal/wails/svc_safety.go (`toggle` — dial `dialFn()(s.pipeName, ipc.Request{Route, Args})`; safety routes forward `--on <bool> --source manual`)
    - internal/command/artnet.go `runArtnetMasterSet` (exact grand vs group/level arg grammar; whether --group takes a name or id — bind to whatever it expects)
    - internal/command/scene.go `scene layer set` route (~lines 46, 247-367 — the `--kind`/`--ref`/`--disable`/`--show` grammar the layer toggle must build)
    - internal/artnet/ipc (Request/Result shapes for the dial)
  </read_first>
  <action>
    Add a settable `dial dialForwardFunc` field to MidiService (default via a `dialFn()` helper returning defaultDialForward when nil, exactly like SafetyService) — do NOT change NewMidiService's signature, so cmd/golc-desktop/main.go and its call site stay untouched (this plan owns only svc_midi.go + its test).
    In dispatchToActiveSurface, after computing `armed`/`controlValue` and emitting feedback (keep the existing emitMidiFeedback call — feedback is additive, not replaced), add a dispatch step keyed on `mapping.Target.Kind`:
      - ControlScene: on the ACTIVATION EDGE only (a Note whose Value>0, or a CC transitioning from not-armed to armed this message) run the in-process registry with args `playback switch <sceneNameByID(state, ref.Scene)> --show <showPath>`. Build a small `execute(args ...string)` helper mirroring svc_playback.go's `execute` (command.NewDefaultCommandRegistry + registry.Execute(Request{Root: s.root, Args: args})). Ignore Note-off (Value 0) and non-edge armed CC repeats — do not re-switch.
      - ControlLayer: on the activation edge, read the target layer's current Enabled + Ref via a show.Load (mirror svc_playback.go.currentLayerRef, extended to also return Enabled), then run `scene layer set <sceneName> --kind <ref.Layer.Kind> [--ref <currentRef>] [--disable if currently enabled] --show <showPath>` to FLIP Enabled, re-supplying the existing Ref so it is never nulled (WR-01/WR-03).
      - ControlMaster: only when `armed` is true, dial the daemon (dialFn) with Route "artnet master set" and the grand-or-group/level args runArtnetMasterSet expects, carrying `controlValue` as the level and appending `--source manual`. For a Note-kind master mapping (armed immediately, D-12), use Value>0 as level 1.0 / Value==0 as level 0.0. For a CC, forward on each armed update (continuous — this is the one deliberately-repeating dispatch).
      - ControlSafety: on the activation edge (Note Value>0), dial the daemon with the route mapped from ref.Safety (blackout -> "artnet safety blackout", stop_release_all -> "artnet safety stop-all", revoke_automation -> "artnet safety revoke-automation") and Args `--on true --source manual` (reuse svc_safety.go's route constants routeBlackout/routeStopAll/routeRevokeAutomation).
    Track the per-mapping activation edge with a small `lastArmed`/`lastValue` map (or reuse the takeovers map) so discrete actions fire once per press/arm, not per message. Resolve scene/group names through the existing sceneNameByID/groupNameByID helpers; a target whose scene/group was deleted resolves to "" and dispatches nothing (matching the file's read-only projection tolerance).
    Document the discrete-vs-continuous and Note-on-master-level=1.0 choices as a `flagged_assumptions` block in the plan/summary: they are reasonable defaults for controls the existing routes support, not new command semantics.
  </action>
  <verify>
    <automated>go test ./internal/wails/... -run TestMidiServiceDispatch && go test ./internal/wails/... && go vet ./internal/wails/...</automated>
  </verify>
  <done>All five Task-1 tests pass; the full internal/wails suite and go vet stay green; a mapped Note switches scenes/toggles layers/triggers safety and a mapped CC sets master level only after crossing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Edge coverage — release/no-op, double-press idempotency, and unassigned-target tolerance</name>
  <files>internal/wails/svc_midi.go, internal/wails/svc_midi_test.go</files>
  <acceptance_criteria>
    - TestMidiServiceDispatchSceneEdgeFiresPerPressNotPerMessage, TestMidiServiceDispatchMasterCcContinuesWhileArmed, and TestMidiServiceDispatchDeletedTargetIsSilentNoOp pass.
    - `go build ./...` is green.
    - Discrete actions fire once per activation edge; continuous master updates persist while armed; a deleted target dispatches nothing and does not panic; emitMidiFeedback still fires on release/no-op events.
  </acceptance_criteria>
  <read_first>
    - internal/wails/svc_midi.go (post-Task-2 dispatch code)
    - internal/wails/svc_midi_test.go (Task-1 tests, to extend)
    - internal/midi/takeover.go (`TakeoverState.Update` — arming edge semantics the CC edge detection reuses)
  </read_first>
  <behavior>
    - Test: a scene Note-on immediately followed by a second Note-on (repeated press without release) dispatches the scene switch each press but never errors; a Note-off between them dispatches nothing.
    - Test: a CC master mapping held past the crossing keeps forwarding master-set updates (continuous is intended), while a scene CC mapping crossing once dispatches the switch once and does not re-switch on subsequent armed messages.
    - Test: a mapping whose Target scene/group was deleted from the show dispatches nothing and does not panic.
  </behavior>
  <action>
    Extend svc_midi_test.go with TestMidiServiceDispatchSceneEdgeFiresPerPressNotPerMessage, TestMidiServiceDispatchMasterCcContinuesWhileArmed, and TestMidiServiceDispatchDeletedTargetIsSilentNoOp. Adjust dispatchToActiveSurface only if a test surfaces a real edge defect (e.g. an off-by-one in edge detection); otherwise these tests certify the Task-2 behavior. Keep the emitMidiFeedback call firing for every event including release/no-op events so the on-screen slider still tracks the physical position (D-09) regardless of whether a command was dispatched.
  </action>
  <verify>
    <automated>go test ./internal/wails/... -run TestMidiServiceDispatch && go build ./...</automated>
  </verify>
  <done>Edge tests pass; discrete actions are per-activation-edge, continuous master updates persist while armed, deleted targets are silent no-ops, and feedback still emits on every event.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| physical MIDI device -> Go host dispatch | Untrusted physical control input crosses into command dispatch |
| Go host -> daemon (named-pipe IPC) | Master/safety actions cross into the daemon-resident local-priority path |
| Go host -> show document | Scene/layer actions cross into the persisted ShowState via the in-process registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-24 | Elevation of Privilege | dispatchToActiveSurface dispatching a mapping whose target was unassigned from the surface after learn | low | mitigate | Mappings are only creatable for assigned controls (StartLearn's command.Authorize, D-04); dispatch acts only on mappings persisted on the active surface, and a target deleted from the show dispatches nothing (Task 3). Documented as a bounded trust-on-provenance assumption. |
| T-06-25 | Denial of Service | Fast fader sweep flooding `artnet master set` daemon forwards | low | mitigate | CC master forwards only while armed (post-crossing); master-set is idempotent and the daemon already coalesces per-tick; no unbounded queue is introduced Go-side. |
| T-06-26 | Tampering | Master/safety action taking a slow path (show.Save) instead of the daemon local-priority path | low | mitigate | Master/safety dispatch uses the SafetyService dial+forward IPC path exclusively; only scene/layer (non-safety) uses the in-process registry, matching the existing service split. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs in this plan (Go-only edit to an existing file reusing already-pinned deps); no supply-chain surface added this round. |
</threat_model>

<verification>
- `go test ./internal/wails/...` green (including the new TestMidiServiceDispatch* tests).
- `go build ./...` and `go vet ./...` clean.
- Source review: dispatchToActiveSurface still calls emitMidiFeedback for every event (feedback preserved) AND now dispatches the ControlRef-implied command; master/safety go through the dial, scene/layer through the in-process registry.
</verification>

<success_criteria>
- A mapped MIDI Note switches scenes / toggles layers / triggers safety, and a mapped CC sets a master level only after cross-to-catch arming — proven by automated tests asserting real state change and captured daemon Requests, not feedback state.
- No regression in existing learn/takeover/feedback tests.
- NewMidiService signature and cmd/golc-desktop/main.go remain unchanged (this plan is isolated to svc_midi.go + its test).
</success_criteria>

## Artifacts this phase produces

- `MidiService.dial dialForwardFunc` field + `dialFn()` helper (new, in svc_midi.go)
- `MidiService.execute(args ...string) Result` helper (new, mirrors PlaybackService.execute)
- Dispatch branch logic inside `dispatchToActiveSurface` (scene/layer/master/safety)
- Per-mapping activation-edge tracking state (new field/map in MidiService)
- Tests: TestMidiServiceDispatchSceneNoteSwitchesActiveScene, TestMidiServiceDispatchLayerNoteTogglesEnabledPreservingRef, TestMidiServiceDispatchMasterCcForwardsOnlyAfterCrossing, TestMidiServiceDispatchSafetyNoteForwardsDaemonRoute, TestMidiServiceDispatchUnmappedEventDoesNothing, TestMidiServiceDispatchSceneEdgeFiresPerPressNotPerMessage, TestMidiServiceDispatchMasterCcContinuesWhileArmed, TestMidiServiceDispatchDeletedTargetIsSilentNoOp

<output>
Create `.planning/phases/06-wails-authoring-and-operator-surface/06-09-SUMMARY.md` when done.
</output>
