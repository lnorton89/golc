---
context: phase
phase: 07-versioned-external-control-api
task: 14
total_tasks: 14
status: gaps_found
last_updated: 2026-07-25T12:04:00.000Z
---

<current_state>
Phase 7 (Versioned External Control API) has 14 plans executed and merged (07-01 through 07-14 — the original 9 plus a 5-plan gap-closure round). Code review and phase-goal verification both re-ran after gap closure; verification now scores 3/4 must-haves (up from 2/4), status `gaps_found`. The phase is still NOT marked complete in ROADMAP.md/STATE.md/REQUIREMENTS.md — one gap remains.
</current_state>

<completed_work>
Original 9 plans (see prior git history 07-01..07-09) plus gap-closure round:
- 07-10 (Wave 9): Re-scoped Success Criterion #1 / API-01 to delivered breadth; filed remaining ~61 routes under new deferral owner `EXTN-05` — commit 6b6f9f6+.
- 07-11 (Wave 9): Fixed CR-01 — decoupled SSE id/replay key from `show.State.Revision` via `ringEvent.Seq`, plus the daemon-restart resync split — commit fc849c3+.
- 07-12 (Wave 9): Fixed WR-02/WR-03 for the pre-flight loop — commit 2156d21+.
- 07-13 (Wave 10): Fixed WR-01 (idempotency scoping) + IN-02 (comma escaping) — commit e6d1e6d+.
- 07-14 (Wave 11): Fixed WR-04 (deprecation middleware wiring) + IN-01/IN-02 (key lifetime cap) — commit 55b3da8+.

Post-execution pipeline (second pass):
- Gap-closure code review (07-REVIEW-gaps.md, commit 3c3eb2d): confirmed CR-01/WR-01/WR-04/IN-01/IN-02 genuinely fixed with solid tests. Found ONE NEW warning, WR-05, during a fresh-eyes pass (see open_gaps below).
- Regression gate: full `go test ./...` green after every wave merge; regenerated pre-existing `linear-map.json` drift again after 07-10 (commit 7076008, same recurring pattern as before).
- Re-verification (07-VERIFICATION.md, commit 0ee3c50): 3/4 must-haves verified, gaps_found. Criteria #1 and #3 now CLOSED. Criterion #4 remains blocked by WR-05.
</completed_work>

<open_gaps>
See `.planning/phases/07-versioned-external-control-api/07-VERIFICATION.md` and `07-REVIEW-gaps.md` for full detail. One gap remains, blocking Success Criterion #4 ("every result is auditable"):

**WR-05 — `runBatch`'s locked/transactional section still writes no audit row on failure.** `internal/api/batch.go`'s pre-flight loop (lines ~202-235, fixed by 07-12) is fully audited. But the LOCKED section — everything from `mutationMutex.Lock()` (line ~237) through the final `show.Save` (line ~299) — has 8 distinct failure-return paths that never call `fireMutationObservers`:
- `show.CurrentRevision` failure
- `parseIfMatch` failure
- **the batch-level If-Match precondition mismatch (412)** — most consequential: routine, expected client failure mode, has dedicated tests (`TestBatchIfMatch`, `TestBatchIfMatchExternalRace`) that assert status/revision but never audit rows
- `show.NewTempCopy` failure
- a per-sub-request execution failure against the throwaway copy
- `show.Load` failure on the copy
- **the pre-commit external-write race (412)** — same audit gap, same test coverage blind spot
- `show.Save` failure on the real show

Fix (07-REVIEW-gaps.md's suggested approach): add a `fireMutationObservers` call (outcome `"failure"`) immediately before each of the returns above, mirroring `mutate.go`'s unconditional-fire discipline — likely one observer event per sub-request for the batch-level branches, matching how a rejected batch's audit rows are otherwise reported elsewhere in this file. Add regression tests analogous to `TestBatchScopeRejectionIsAudited` for at minimum the If-Match-mismatch and external-race paths, since those already have dedicated behavioral tests that could trivially gain an audit-row assertion.

Two info-level items also on record, non-blocking (07-REVIEW-gaps.md): IN-03 (`expires_in` has no lower bound — negative/zero durations pass through unrejected), IN-04 (`int64`→`int` narrowing for the SSE `id:` line, harmless on current 64-bit targets but not portable).

Next step: `/gsd-plan-phase 07 --gaps` to create a new gap-closure plan for WR-05, then `/gsd-execute-phase 07 --gaps-only`.
</open_gaps>

<infrastructure_state>
- Working tree: clean as of this note (all phase-7 execution, review, verification, and hygiene fixes committed to master).
- `master` is well ahead of `origin/master` — nothing has been pushed this session.
- Security enforcement (`workflow.security_enforcement`) is active and no `07-SECURITY.md` exists yet — run `/gsd-secure-phase 07` at some point before shipping, independent of the gap-closure work above.
</infrastructure_state>
