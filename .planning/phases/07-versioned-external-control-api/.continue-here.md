---
context: phase
phase: 07-versioned-external-control-api
task: 6
total_tasks: 9
status: paused
last_updated: 2026-07-25T07:56:32.298Z
---

<current_state>
Phase 7 (Versioned External Control API) execution paused after Wave 5 of 8. Plans 07-01 through 07-05 are complete, verified, and merged to master. Waves 6-8 (plans 07-06, 07-07, 07-08, 07-09) have not started. Paused deliberately — not blocked — to fix an unrelated, real user-reported crash (golc-desktop's Wails build-tags bug) while context budget allowed a clean stopping point between waves.
</current_state>

<completed_work>

Completed Tasks (each is one plan, one wave):
- 07-01 (Wave 1): Pin chi v5.3.1, huma/v2 v2.39.0, golang.org/x/time v0.15.0 — Done, commit 9c71a72. Included a blocking human-verify checkpoint (supply-chain approval), already approved.
- 07-02 (Wave 2): `/v1` Chi+Huma server hosted inside the Art-Net daemon, HTTP→command translation, capability-coverage gate. Resolved a real import-cycle risk (internal/api never imports internal/command; internal/artnet never imports internal/api) via an Executor interface + artnet.Subsystem interface. Done, commit b633b3a.
- 07-03 (Wave 3): `api` config concern (config/api.toml), enforced loopback-by-default bind. Done, commit f82f199.
- 07-04 (Wave 4): Scoped/expiring/revocable API-key auth, per-key rate limiting, `/v1/keys` REST. Done, commit eeeb292.
- 07-05 (Wave 5): Serialized mutation pipeline — If-Match/412 revision check, `?dry_run=true`, Idempotency-Key dedupe, post-mutation observer seam (audit/SSE attach point). Only "pool create" wired as a concrete mutating operation — the pipeline itself is generic and reusable. Done, commit 4e3c730.

Also completed, unrelated to Phase 7 but done in the same session:
- Fixed `internal/wails/hotkey.go`'s unrecoverable panic on CGo-free Linux/OpenBSD builds (golang.design/x/hotkey's nocgo variant panics instead of returning an error) — commit 3a1cdee.
- Wired `midicat` auto-provisioning into `mage Bootstrap` (config/toolchain.toml's new `[go_install.midicat]`, internal/bootstrap/engine.go's `installGoInstallTools`) — commit 60edcf3.
- Added `mage Run` dev-loop launcher (`internal/command/run.go`) that execs the built golc-desktop with the go-install bin directory prepended onto PATH, closing the "midicat provisioned but not on PATH at process-exec-time" gap for the one entrypoint that can control it — commit 8d9ab38. Added companion test-parity updates — commit 04cc49d.
- Fixed a real capability-coverage conflict between the above "run" route and Phase 7's own coverage gate — commit 1a1abc8.
</completed_work>

<remaining_work>

- 07-06 (Wave 6, depends on 07-05): Atomic `/v1/batch` — the phase's highest-risk item (07-RESEARCH.md Pitfall 1). Resolved via copy-to-throwaway + single aggregated Save, NOT the 15-file State-in/State-out refactor. Not started.
- 07-07 (Wave 7, depends on 07-05, 07-06): `audit_log` SQLite table + redacting post-mutation writer, attaching to 07-05's observer seam. Runs in PARALLEL with 07-08 (zero file overlap, both declared Wave 7). Not started.
- 07-08 (Wave 7, depends on 07-05, 07-06): Revisioned global SSE (`GET /v1/events`) with Last-Event-ID replay buffer and resync-on-gap. Runs in parallel with 07-07. Not started.
- 07-09 (Wave 8, depends on 07-04, 07-05, 07-06, 07-08): Generated OpenAPI 3.1 from Go, drift check, compatibility/deprecation docs, and closes the capability-coverage gate (07-05 left ~40 mutation routes in the exclusion set — 07-09 needs to either wire them or explicitly re-confirm the exclusion reasons still apply). Not started.
</remaining_work>

<decisions_made>

- Each wave is executed as one (or, for Wave 7, two parallel) `gsd-executor` Agent call(s) with `isolation="worktree"`, using the exact worktree-branch-check guard block from `gsd-core/references/worktree-branch-check.md` with `{EXPECTED_BASE}` = the orchestrator's `git rev-parse HEAD` captured immediately before dispatch.
- After each executor returns, check `git worktree list`: if it shows only the main tree, Claude Code's `isolation="worktree"` already auto-merged and cleaned up — nothing to do. If a `worktree-agent-*` entry with a `<worktree>` tag in the notification is still present, merge manually (`git merge --ff-only <branch>` if master hasn't diverged, `git merge --no-ff -m "merge: bring 0X-0Y into master" <branch>` if it has), then `git worktree remove --force` + `git branch -d`.
- STATE.md's `state.begin-phase`/`state.record-session` writes must be committed separately by the orchestrator after each wave (worktree-mode executors skip STATE.md/ROADMAP.md automatically).
</decisions_made>

<blockers>
None. This is a deliberate pause between waves, not a stuck state.
</blockers>

## Required Reading (in order)
1. `.planning/phases/07-versioned-external-control-api/07-RESEARCH.md` — especially Pitfall 1 (batch atomicity) before starting 07-06
2. `.planning/phases/07-versioned-external-control-api/07-05-SUMMARY.md` — the observer seam 07-07/07-08 both attach to, and the coverage-gate scope note affecting 07-09
3. `.planning/phases/07-versioned-external-control-api/07-06-PLAN.md` through `07-09-PLAN.md` — the actual next work

## Infrastructure State
- Working tree is clean (`git status --porcelain` empty as of pause).
- `master` is 36+ commits ahead of `origin/master` — nothing has been pushed this session.
- No running servers, watchers, or background processes.

<context>
Was mid-execution of Phase 7 via `/gsd-execute-phase 7`, wave-by-wave, with real-time worktree merge/cleanup handled by the orchestrator after each wave. Two unrelated user-reported bugs (a Linux user's golc-desktop crashes) came in during execution and were fixed in parallel via forked agents on non-overlapping files, which is why several non-Phase-7 commits are interleaved in the log above. A third bug (Wails build-tags crash, `GOLC_WAILS_RUN_FAILED`) was diagnosed but not yet fixed when this pause was triggered — that's the next thing being worked on, separately from Phase 7 resumption.
</context>

<next_action>
Start with: `/gsd-execute-phase 7` (or `/gsd-progress --next`) to resume at Wave 6. First step inside that: spawn a `gsd-executor` for `07-06-PLAN.md` with `isolation="worktree"`, `EXPECTED_BASE` = current `git rev-parse HEAD`.
</next_action>
