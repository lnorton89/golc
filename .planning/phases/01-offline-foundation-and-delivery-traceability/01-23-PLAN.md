---
phase: 01-offline-foundation-and-delivery-traceability
plan: 23
type: execute
wave: 8
depends_on: [01-10, 01-21]
files_modified:
  - internal/trace/transport/contract.go
  - internal/trace/transport/fake.go
  - internal/trace/reconcile/diff.go
  - internal/trace/reconcile/reconcile_test.go
  - internal/command/linear.go
  - tests/fixtures/linear/remote-complete.json
  - tests/fixtures/linear/remote-conflict.json
  - tests/fixtures/linear/remote-ambiguous.json
  - tests/fixtures/linear/explicit-archive.json
autonomous: true
requirements: [LINR-03, LINR-04]
must_haves:
  truths:
    - "Same-field dual changes block with field-by-field base/repository/Linear preview, per D-13."
    - "Incomplete, partial, ambiguous, or rate-limited snapshots cannot produce an applyable preview, per D-21."
    - "Local absence creates no delete; only exact explicit archive/unlink routes create reviewable operations, per D-15."
    - "Preview/archive/unlink routes are self-registered and reachable through fake transport."
  artifacts:
    - path: internal/trace/transport/contract.go
      provides: "Transport-neutral complete snapshot/mutation interface"
    - path: internal/trace/reconcile/diff.go
      provides: "Three-way authority/conflict preview"
    - path: internal/command/linear.go
      provides: "Self-registered fake preview/archive/unlink routes"
  key_links:
    - from: internal/command/linear.go
      to: internal/trace/reconcile/diff.go
      via: "fake-snapshot preview handlers"
      pattern: "MustDeclareRoute|Preview"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Preview complete fake snapshots with conflict, ambiguity, incompleteness, and inferred removal blocked.

Purpose: Make the reconciliation policy executable without credentials or live mutation.
Output: Transport contract/fake, three-way diff, exact routes, tests, and four fixtures.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/linear-map.json
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-10-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-21-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/trace/transport/contract.go`, `internal/trace/transport/fake.go`, `internal/trace/reconcile/diff.go`, `internal/trace/reconcile/reconcile_test.go`, `internal/command/linear.go`, `tests/fixtures/linear/remote-complete.json`, `tests/fixtures/linear/remote-conflict.json`, `tests/fixtures/linear/remote-ambiguous.json`, `tests/fixtures/linear/explicit-archive.json`.

- Created/modified all nine transport/diff/route/test/fixture paths listed in frontmatter.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preview complete snapshots with conflicts and ambiguity blocked</name>
  <files>internal/trace/transport/contract.go, internal/trace/transport/fake.go, internal/trace/reconcile/diff.go, internal/trace/reconcile/reconcile_test.go, internal/command/linear.go, tests/fixtures/linear/remote-complete.json, tests/fixtures/linear/remote-conflict.json, tests/fixtures/linear/remote-ambiguous.json, tests/fixtures/linear/explicit-archive.json</files>
  <read_first>
    - All nine owned paths — new transport/diff/fixtures and existing Linear route/test files.
    - `internal/trace/reconcile/model.go`, `canonical.go`, and `marker.go` — Plan 10 contracts.
    - `internal/trace/catalog/model.go` and `.planning/linear-map.json` — owned fields/mapping/base.
    - `internal/command/router.go` and `test.go` — exact route/scope registration.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-11 through D-15/D-21.
  </read_first>
  <behavior>
    - Three-way comparison enforces field ownership and blocks dual changes.
    - Zero markers may create, one expected kind/parent match may adopt/update, multiple block; titles are diagnostic only.
    - Incomplete/partial/cursor/rate inputs block; absence never deletes; explicit archive/unlink is reviewable.
  </behavior>
  <acceptance_criteria>
    - [ ] `ValidateCompleteSnapshot` blocks incomplete, partial, cursor-anomalous, ambiguous, or rate-limited inputs.
    - [ ] `ThreeWayField` emits blocking conflicts with base/repository/Linear values when both sides changed.
    - [ ] Marker discovery follows zero/one/multiple rules and never adopts by title.
    - [ ] `internal/command/linear.go` self-registers exact routes `linear preview --snapshot --out`, `linear archive --local-id --preview-out`, and `linear unlink --local-id --preview-out`.
    - [ ] `reconcile_test.go` registers exact scope `linear-reconcile`, defines `TestScopeLinearReconcile`, and all four fixtures pass without live transport.
  </acceptance_criteria>
  <action>Implement preview over the complete transport-neutral snapshot. Enforce D-11 through D-15 and D-21. Self-register the three exact Linear routes and exact test scope `linear-reconcile` through the Plan 17 contract before verification; never modify `router.go`. Use fake snapshots only.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-reconcile</automated>
  </verify>
  <done>Complete fake snapshots yield deterministic previews; conflict, ambiguity, incompleteness, and inferred removal block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| remote snapshot -> preview | Incomplete/hostile remote state enters policy. |
| remote descriptions -> identity | Forged/duplicate markers may spoof managed objects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-28 | Spoofing | marker discovery | high | mitigate | Exact footer/kind/parent and ambiguity block. |
| T-01-29 | Tampering | field conflicts | high | mitigate | Three-way owned-field comparison. |
| T-01-30 | Tampering | removals | high | mitigate | No delete; explicit archive/unlink only. |
</threat_model>

<verification>
Run the registered reconcile scope over all four fixtures and repeat the complete preview bytes.
</verification>

<success_criteria>
- Preview is complete-snapshot bound, conflict-aware, ambiguity-safe, and removal-safe.
- Exact routes/scopes are registered before invocation.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-23-SUMMARY.md` when done.
</output>
