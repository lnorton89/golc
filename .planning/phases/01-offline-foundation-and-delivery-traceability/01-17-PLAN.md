---
phase: 01-offline-foundation-and-delivery-traceability
plan: 17
type: execute
wave: 3
depends_on: [01-16]
files_modified:
  - cmd/golc-project/main.go
  - internal/command/router.go
  - internal/command/router_test.go
  - internal/projectconfig/load.go
  - golc.project.toml
  - config/runtime.toml
  - golc.ps1
autonomous: true
requirements: [CONF-01, CONF-03]
must_haves:
  truths:
    - "Normal root commands resolve through a compile-safe self-registration contract rather than repeated central-router edits, per D-03."
    - "Duplicate routes/scopes fail deterministically and route lookup/listing is byte-stable."
    - "The green walking skeleton inspects a contained committed runtime concern without network or Linear, per D-02 and D-05."
  artifacts:
    - path: internal/command/router.go
      provides: "CommandHandler/CommandRegistry self-registration contract"
    - path: internal/command/router_test.go
      provides: "duplicate-route/scope and deterministic lookup tests"
    - path: internal/projectconfig/load.go
      provides: "contained root/concern inspection"
  key_links:
    - from: cmd/golc-project/main.go
      to: internal/command/router.go
      via: "NewDefaultCommandRegistry then deterministic lookup"
      pattern: "NewDefaultCommandRegistry|Lookup"
    - from: internal/projectconfig/load.go
      to: golc.project.toml
      via: "fixed contained concern index"
      pattern: "golc\\.project\\.toml"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Complete the walking skeleton and establish the route/scope self-registration contract used by every later command.

Purpose: Make later route additions reachable without shared central-router ownership.
Output: Go entrypoint, tested `CommandRegistry`, contained TOML inspection, root index/runtime concern, and green acceptance.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-16-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/SKELETON.md
</context>

## Artifacts this phase produces

- Created `cmd/golc-project/main.go` with stable result-to-exit mapping.
- Created `internal/command/router.go` with `Request`, `Result`, `CommandHandler`, `CommandRegistration`, `ScopeRegistration`, `CommandRegistry`, `NewCommandRegistry`, `RegisterRoute`, `RegisterScope`, `MustDeclareRoute`, `MustDeclareScope`, `NewDefaultCommandRegistry`, `Lookup`, `Routes`, `Scopes`, `Execute`, and `WriteResult`.
- Created `internal/command/router_test.go` for duplicate rejection, deterministic route/scope ordering, and self-registered lookup.
- Created `internal/projectconfig/load.go`, `golc.project.toml`, and `config/runtime.toml`; modified `golc.ps1` for normal delegation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route and inspect a real indexed concern through a self-registering command registry</name>
  <files>cmd/golc-project/main.go, internal/command/router.go, internal/command/router_test.go, internal/projectconfig/load.go, golc.project.toml, config/runtime.toml, golc.ps1</files>
  <read_first>
    - `cmd/golc-project/main.go`, `internal/command/router.go`, `internal/command/router_test.go` — new entrypoint/registry contract and tests.
    - `internal/projectconfig/load.go`, `golc.project.toml`, `config/runtime.toml` — new contained inspection path.
    - `golc.ps1` — Plan 16 shim modified only for normal delegation.
    - `tests/acceptance/walking-skeleton.ps1` — green-mode output contract.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-03/D-05 authority.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/SKELETON.md` — developer-tooling boundary.
  </read_first>
  <behavior>
    - `config inspect runtime --format json` is served by the project-local executable.
    - File-local `MustDeclareRoute`/`MustDeclareScope` calls populate `NewDefaultCommandRegistry` without editing a central switch.
    - Duplicate route or scope names fail with stable codes; routes/scopes list and lookup in deterministic normalized order.
    - Root concern paths cannot escape through absolute, parent, symlink, or reparse paths.
  </behavior>
  <acceptance_criteria>
    - [ ] `internal/command/router.go` exports the exact registry identifiers listed in the artifact inventory and uses normalized exact route/scope keys.
    - [ ] `RegisterRoute` and `RegisterScope` reject duplicates with stable diagnostics; `Routes`, `Scopes`, and `Lookup` are deterministic regardless of declaration order.
    - [ ] `internal/command/router_test.go` registers a fixture route/scope through the same declaration entrypoint that later command files use and proves it is reachable from `NewDefaultCommandRegistry`.
    - [ ] The config-inspect handler explicitly self-registers route `config inspect`; no later plan needs to modify `internal/command/router.go` to become reachable.
    - [ ] `golc.project.toml` contains only schema/index metadata, `config/runtime.toml` alone owns `runtime.log_level`, and green acceptance exits 0 with byte-identical JSON.
  </acceptance_criteria>
  <action>Implement the D-03 route contract before adding further commands. Each later command file must call `MustDeclareRoute` for its exact route and each owning test/command graph must call `MustDeclareScope` for its exact scope before verification. Build a registry from declarations, reject duplicate normalized routes/scopes, sort introspection output, and make lookup deterministic. The root config-inspect file self-registers `config inspect`; `cmd/golc-project/main.go` builds the default registry and never contains a growing route switch. Complete D-05 contained strict root/concern inspection and keep PowerShell limited to bootstrap/delegation.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\walking-skeleton.ps1 -Mode green</automated>
  </verify>
  <done>The walking skeleton is green and a tested compile-safe route/scope registration entrypoint makes future handlers reachable without router edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| command declarations -> registry | Duplicate or ambiguous routes could select the wrong handler. |
| root/concern TOML -> Go CLI | Repository paths and values are untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-02 | Tampering / Spoofing | command registry | high | mitigate | Normalized exact keys, duplicate rejection, deterministic lookup, and registry tests. |
| T-01-03 | Tampering / Information Disclosure | concern paths | high | mitigate | Fixed relative paths and final repository-containment checks. |
| T-01-SC | Tampering | dependencies | high | mitigate | Existing exact Go lock only; no new package install. |
</threat_model>

<verification>
Run registry unit tests through the bootstrapped local Go tool and then green walking-skeleton acceptance.
</verification>

<success_criteria>
- The adapted root-command -> registry -> TOML slice is usable offline.
- Duplicate/ambiguous route and scope declarations fail before handler execution.
- Every later route can self-register without owning `internal/command/router.go`.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-17-SUMMARY.md` when done.
</output>
