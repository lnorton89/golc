---
phase: 01-offline-foundation-and-delivery-traceability
plan: 32
type: execute
wave: 2
depends_on: [01-30, 01-31]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [LINR-03, LINR-04]
must_haves:
  truths:
    - "REQUIREMENTS.md checks off LINR-01 and LINR-02 in BOTH the checkbox list (a checked box) and the Traceability table (Phase 1 | Complete), reflecting the delivered, tested, verified internal/trace/catalog implementation that Plan 01-22's SUMMARY already declared complete."
    - statement: "LINR-03 and LINR-04 remain 'Complete' in REQUIREMENTS.md only with evidence that CR-01 and CR-02 are resolved, so no unresolved release-blocking finding contradicts them (Definition of Done)."
      verification: "This plan runs in wave 2 after 01-30 and 01-31; the certification is gated on source evidence that both fixes landed — internal/command/linear.go invokes apply.RunApply and tools/linear-sync/src/adapter.ts's readOperation has a try/catch — recorded as the gating check before the marks are confirmed rather than reverted."
  artifacts:
    - ".planning/REQUIREMENTS.md (LINR-01/LINR-02 checked off; LINR-03/LINR-04 certified Complete against the resolved findings)"
  key_links:
    - ".planning/REQUIREMENTS.md -> internal/trace/catalog (LINR-01/LINR-02 tracking now matches delivered code)"
    - ".planning/REQUIREMENTS.md LINR-03/LINR-04 -> 01-30/01-31 fixes (certification depends on both fix plans completing first)"
---

<objective>
Close Gap 3 (Warning, Definition-of-Done clause): reconcile REQUIREMENTS.md with the actual, now-corrected implementation state. LINR-01 and LINR-02 have never been checked off despite delivered, tested, verified `internal/trace/catalog` implementation (Plan 01-22's SUMMARY declares `requirements-completed: [LINR-01, LINR-02]`). LINR-03 and LINR-04 were marked "Complete" by commit 6e8c88c (Plan 01-13) BEFORE the CR-01/CR-02 defects were introduced by later plans and before the code review found them; REQUIREMENTS.md's own Definition of Done states a requirement is complete only when "no unresolved release-blocking finding contradicts the requirement," so those marks were premature until this gap-closure round resolves CR-01/CR-02.

Purpose: Make the phase-closing REQUIREMENTS.md state match the actual verified implementation in both directions — check off what was silently delivered (LINR-01/LINR-02) and certify (not silently retain) what was prematurely marked (LINR-03/LINR-04), only once the fixes that justify it are in place.

Output: REQUIREMENTS.md with LINR-01/LINR-02 checked off and LINR-03/LINR-04 certified Complete against resolved findings.

This plan MUST run LAST (wave 2), strictly after 01-30 (CR-01) and 01-31 (CR-02), because it certifies their completion. It performs no code changes — only the requirements-tracker reconciliation. The phase's COVERAGE.md capability matrix (ai-integration checkpoint, enforced at verify:pre) was produced during planning and already exists at `.planning/phases/01-offline-foundation-and-delivery-traceability/COVERAGE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check off LINR-01/LINR-02 and certify LINR-03/LINR-04 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 132-137 (Linear Traceability checkbox list: LINR-01/LINR-02 currently unchecked, LINR-03/LINR-04 currently checked)
    - .planning/REQUIREMENTS.md lines 192-194 (Definition of Done — the "no unresolved release-blocking finding" clause that gates certification)
    - .planning/REQUIREMENTS.md lines 280-283 (Traceability table rows for LINR-01..04: LINR-01/LINR-02 Pending, LINR-03/LINR-04 Complete)
    - .planning/REQUIREMENTS.md lines 293 (the "Last updated" footer stamp to refresh)
    - .planning/phases/01-offline-foundation-and-delivery-traceability/01-VERIFICATION.md gaps[] Gap 3 (the authoritative description of the required reconciliation and its two directions)
    - .planning/phases/01-offline-foundation-and-delivery-traceability/01-30-SUMMARY.md and 01-31-SUMMARY.md (the completed-fix evidence this certification depends on) — confirm both exist and report their fixes landed before touching LINR-03/LINR-04
  </read_first>
  <action>
    Use Edit (scoped replacements) on .planning/REQUIREMENTS.md — never Write/overwrite the whole file.

    First, confirm the gating evidence that CR-01 and CR-02 are resolved (this is what makes LINR-03/LINR-04 legitimately Complete rather than premature): verify internal/command/linear.go now invokes apply.RunApply (CR-01 closed by 01-30) and tools/linear-sync/src/adapter.ts's readOperation now has a try/catch (CR-02 closed by 01-31). If either is absent, STOP and do not finalize the certification — the DoD clause is not yet satisfied.

    Then make exactly these edits:
    1. Checkbox list (Linear Traceability section): change the LINR-01 line from an unchecked to a checked box, and likewise LINR-02. Leave LINR-03 and LINR-04 checked (unchanged).
    2. Traceability table: change the LINR-01 row status from Pending to Complete, and likewise LINR-02. Leave LINR-03 and LINR-04 as Complete (now legitimately certified against the resolved CR-01/CR-02).
    3. Refresh the "Last updated" footer stamp to 2026-07-21 with a short note that LINR-01/LINR-02 were checked off and LINR-03/LINR-04 certified after the CR-01/CR-02 gap-closure fixes (plans 01-30/01-31).

    Do not alter any non-LINR row, the coverage totals (still 84/84), or any other section. Do not revert LINR-03/LINR-04 — the fixes in 01-30/01-31 resolve the findings that made them premature, so certification (retain Complete, backed by evidence) is the correct outcome per Gap 3's stated options.
  </action>
  <verify>
    <automated>grep -Eq '^- \[x\] \*\*LINR-01' .planning/REQUIREMENTS.md && grep -Eq '^- \[x\] \*\*LINR-02' .planning/REQUIREMENTS.md && grep -Eq '\| LINR-01 \| Phase 1 \| Complete \|' .planning/REQUIREMENTS.md && grep -Eq '\| LINR-02 \| Phase 1 \| Complete \|' .planning/REQUIREMENTS.md && grep -q 'apply.RunApply(' internal/command/linear.go && sed -n '/async function readOperation/,/^}/p' tools/linear-sync/src/adapter.ts | grep -q 'catch' && echo REQUIREMENTS_SYNCED</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md's Linear Traceability checkbox list shows LINR-01 and LINR-02 as checked boxes; LINR-03 and LINR-04 remain checked.
    - The Traceability table shows LINR-01 and LINR-02 as "Phase 1 | Complete"; LINR-03 and LINR-04 remain "Phase 1 | Complete".
    - The gating evidence holds: internal/command/linear.go contains apply.RunApply( and adapter.ts's readOperation region contains a catch (CR-01 and CR-02 resolved); if either were absent the certification would not have been finalized.
    - The coverage totals remain 84/84 and no non-LINR row was modified.
    - Changes were applied with scoped Edits, not a whole-file overwrite; the "Last updated" footer notes the 2026-07-21 reconciliation.
    - The structural gate above prints REQUIREMENTS_SYNCED.
  </acceptance_criteria>
  <done>REQUIREMENTS.md reflects the true implementation state: LINR-01/LINR-02 checked off to match delivered/tested code, and LINR-03/LINR-04 certified Complete against the now-resolved CR-01/CR-02 findings — closing Gap 3's two-directional inconsistency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification record <-> requirements tracker | REQUIREMENTS.md is the release-gating source of truth; a mismatch between it and the verified code state can let a release proceed on inaccurate completion claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-32-01 | Repudiation | .planning/REQUIREMENTS.md completion marks | low | mitigate | The check-off/certification is gated on source evidence that CR-01/CR-02 are resolved (apply.RunApply present, readOperation try/catch present) and on the existence of 01-30/01-31 SUMMARYs, so a completion mark cannot be recorded without the implementation state that justifies it. |
| T-01-32-02 | Tampering | non-LINR rows / coverage totals | low | accept | Scoped Edits touch only the four LINR rows and the footer; the verify asserts 84/84 coverage is unchanged. No code or other requirement is altered. |
| T-01-32-SC | Tampering | package installs | low | accept | Documentation-only plan; no npm/go dependency changes. No [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- REQUIREMENTS.md shows LINR-01/LINR-02 checked and Complete; LINR-03/LINR-04 remain Complete.
- Gating evidence present: internal/command/linear.go invokes apply.RunApply; adapter.ts readOperation has a catch.
- Coverage remains 84/84; only LINR rows + footer changed.
</verification>

<success_criteria>
The requirement-to-phase mapping is current for every requirement Phase 1 claims: silently-delivered LINR-01/LINR-02 are checked off, and prematurely-marked LINR-03/LINR-04 are certified against resolved findings rather than left as unbacked bookkeeping. Gap 3 flips from partial to verified.
</success_criteria>

<notes>
Specless-probe-fallback scoping: per the planning brief, the generic edge-probe gate was deliberately NOT run mechanically for this gap-closure round (it would generate predicates for the already-verified CONF-01..04 / LINR-01 / LINR-02). This plan's must_haves predicates are sourced from 01-VERIFICATION.md's Gap 3, applying the same lift discipline; the LINR-03/LINR-04 certification predicate is expressed as a structured statement+verification backstop because its truth cannot be proven at plan time (it depends on 01-30/01-31 landing first).
</notes>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-32-SUMMARY.md` when done.
</output>
