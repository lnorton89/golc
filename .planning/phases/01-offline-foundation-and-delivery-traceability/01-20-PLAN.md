---
phase: 01-offline-foundation-and-delivery-traceability
plan: 20
type: execute
wave: 10
depends_on: [01-06]
files_modified:
  - internal/command/package.go
  - internal/delivery/foundation.go
  - internal/delivery/delivery_test.go
  - tests/acceptance/offline.ps1
  - tests/golden/foundation-manifest.json
  - .gitignore
  - docs/development.md
autonomous: true
requirements: [CONF-01, CONF-03]
must_haves:
  truths:
    - "Exact route `package --foundation` is self-registered and reachable, per D-03."
    - "Repeated identical inputs produce byte-identical Windows AMD64 foundation ZIP, manifest, and checksum."
    - "The package is developer tooling only and makes no Wails/NSIS product claim."
  artifacts:
    - path: internal/delivery/foundation.go
      provides: "Deterministic foundation bundle"
    - path: tests/golden/foundation-manifest.json
      provides: "Canonical sorted inventory"
  key_links:
    - from: internal/command/package.go
      to: internal/delivery/foundation.go
      via: "self-registered package handler"
      pattern: "MustDeclareRoute|BuildFoundationBundle"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Produce the deterministic Phase 1 foundation package through a reachable root route.

Purpose: Complete the phase-bounded delivery command without pulling product packaging forward.
Output: Package handler, deterministic bundler, golden/acceptance, ignores, and contributor docs.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-06-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/command/package.go`, `internal/delivery/foundation.go`, `internal/delivery/delivery_test.go`, `tests/acceptance/offline.ps1`, `tests/golden/foundation-manifest.json`, `.gitignore`, `docs/development.md`.

- Created/modified all seven listed package implementation, test, acceptance, golden, ignore, and documentation paths.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Produce the deterministic foundation ZIP</name>
  <files>internal/command/package.go, internal/delivery/foundation.go, internal/delivery/delivery_test.go, tests/acceptance/offline.ps1, tests/golden/foundation-manifest.json, .gitignore, docs/development.md</files>
  <read_first>
    - All seven owned paths — package route/implementation/tests/acceptance/golden/ignore/docs.
    - `internal/delivery/graph.go` and `config/commands.toml` — authoritative file inventory/network policy.
    - `internal/command/router.go` — self-registration API; read only.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-03/D-08/D-10.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md` — resolved Windows AMD64 foundation package.
  </read_first>
  <behavior>
    - Identical inputs yield identical archive, manifest, and SHA-256 bytes.
    - Files and ZIP metadata are normalized; machine paths/timestamps are absent.
    - Product UI, NSIS, lighting source, and runtime claims are excluded.
  </behavior>
  <acceptance_criteria>
    - [ ] `internal/command/package.go` calls `MustDeclareRoute` for exact route `package --foundation` before verification.
    - [ ] `BuildFoundationBundle` and `CanonicalManifest` sort files, normalize ZIP metadata, and omit machine paths/timestamps.
    - [ ] Bundle contents come from the authoritative graph allowlist and preserve the phase boundary.
    - [ ] `.gitignore` ignores exact distribution/transient outputs without hiding locks, schemas, maps, or goldens; docs name the exact outputs/boundary.
    - [ ] `offline.ps1 -Mode package` repeats packaging, compares bytes, and exits 0.
  </acceptance_criteria>
  <action>Implement the resolved deterministic Windows AMD64 developer-tool ZIP, canonical manifest, and checksum. Self-register exact route `package --foundation` through the Plan 17 registry and drive contents from the command graph. Normalize all metadata and document the explicit non-product boundary.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\offline.ps1 -Mode package</automated>
  </verify>
  <done>Foundation packaging is reachable, byte-reproducible, checksumed, and phase-bounded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository files -> foundation ZIP | Unintended or nondeterministic files may enter deliverables. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-16 | Tampering | foundation ZIP | high | mitigate | Sorted allowlist, normalized metadata, canonical manifest, repeat hashes. |
| T-01-SC | Tampering | dependencies | high | mitigate | Existing exact cache/locks only. |
</threat_model>

<verification>
Run package acceptance twice with no network or `.env`.
</verification>

<success_criteria>
- Package route is registered and output is deterministic and phase-bounded.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-20-SUMMARY.md` when done.
</output>
