---
phase: 01-offline-foundation-and-delivery-traceability
plan: 06
type: execute
wave: 9
depends_on: [01-19, 01-28]
files_modified:
  - config/commands.toml
  - internal/command/build.go
  - internal/command/test.go
  - internal/command/check.go
  - internal/delivery/graph.go
  - internal/delivery/delivery_test.go
  - tests/acceptance/offline.ps1
autonomous: true
requirements: [CONF-01, CONF-03]
must_haves:
  truths:
    - "Generate, check, build, and test share one root graph and exact self-registered routes, per D-03 and D-10."
    - "After bootstrap, registered core steps use local caches with network denied, per D-02."
    - "Missing cache or network attempts produce named failures rather than fallback downloads."
  artifacts:
    - path: internal/delivery/graph.go
      provides: "One offline-enforced command graph"
    - path: tests/acceptance/offline.ps1
      provides: "Network-deny root-command acceptance"
  key_links:
    - from: config/commands.toml
      to: internal/delivery/graph.go
      via: "single authoritative step/network inventory"
      pattern: "NetworkPolicy|LoadGraph"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Deliver the shared offline generate/check/build/test command graph.

Purpose: Make contributor and CI core delivery executable after bootstrap.
Output: Self-registered core routes, graph engine/tests, and offline acceptance.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-19-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-28-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `config/commands.toml`, `internal/command/build.go`, `internal/command/check.go`, `internal/command/test.go`, `internal/delivery/graph.go`, `internal/delivery/delivery_test.go`, `tests/acceptance/offline.ps1`.

- Modified `config/commands.toml`, `internal/command/test.go`, and `internal/command/check.go`; created build route, graph engine/tests, and offline acceptance at the listed paths.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run the complete contributor core graph offline</name>
  <files>config/commands.toml, internal/command/build.go, internal/command/test.go, internal/command/check.go, internal/delivery/graph.go, internal/delivery/delivery_test.go, tests/acceptance/offline.ps1</files>
  <read_first>
    - All seven owned paths — command inventory/routes/graph/tests/acceptance.
    - `internal/bootstrap/cache.go` — Plan 28 offline environment/cache validation.
    - `internal/contracts/generate.go` and `internal/projectconfig/registry.go` — graph steps.
    - `internal/command/router.go` — self-registration API; read only.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-02/D-03/D-10.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — quick/full/offline commands.
  </read_first>
  <behavior>
    - Root generate/check/build/test steps use project-local tools and cache-only environment.
    - Any network attempt or missing cache returns a named diagnostic.
    - The graph has one declarative owner and exact routes/scopes are registered before use.
  </behavior>
  <acceptance_criteria>
    - [ ] `LoadGraph`, `Run`, `RunOffline`, and `ValidateParity` consume only `config/commands.toml` for the command inventory.
    - [ ] Offline mode sets `GOTOOLCHAIN=local`, `GOPROXY=off`, local Go/build/npm caches, and an injected deny transport.
    - [ ] `build`, `test`, and `check --offline` are exact `MustDeclareRoute` registrations; existing generate/config registrations remain reachable.
    - [ ] `internal/command/test.go` preserves safe names, exact marker listing, and fail-on-zero while adding full/quick graph orchestration and registered Node-scope lookup.
    - [ ] `offline.ps1 -Mode core` runs generate/check/build/test with network denied and exits 0.
  </acceptance_criteria>
  <action>Implement D-02/D-03/D-10 as one executable graph. Self-register exact routes `build`, `test`, and `check --offline` through the Plan 17 contract before verification; never modify `router.go`. Preserve exact Go marker discovery and add Node scope execution only for scopes explicitly registered from `config/commands.toml`. Fail closed on missing cache or any network attempt. Local Linear trace integration remains in the final cross-capability plan.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\offline.ps1 -Mode core</automated>
  </verify>
  <done>Generate, check, build, and test run through registered root routes and one graph with network denied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| warmed caches -> offline graph | Missing/poisoned state could trigger downloads or wrong tools. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-15 | Denial of Service / Tampering | offline graph | high | mitigate | Network-deny transport, local-only environment, cache validation. |
| T-01-17 | Repudiation | command routing/parity | medium | mitigate | Duplicate-safe route registration and one parsed graph. |
| T-01-SC | Tampering | cached dependencies | high | mitigate | Exact manifests/sums; no new install. |
</threat_model>

<verification>
Run core acceptance without `.env` and with every network attempt denied.
</verification>

<success_criteria>
- Core routes are registered, reachable, offline-enforced, and graph-owned once.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-06-SUMMARY.md` when done.
</output>
