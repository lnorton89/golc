---
phase: 01-offline-foundation-and-delivery-traceability
plan: 09
type: execute
wave: 6
depends_on: [01-08]
files_modified:
  - internal/strictjson/decode.go
  - internal/strictjson/decode_test.go
  - internal/trace/catalog/migrate.go
  - internal/trace/catalog/migrate_test.go
  - .planning/linear-map.json
  - tests/fixtures/linear/map-schema1.json
  - tests/golden/linear-map-schema2.json
autonomous: true
requirements: [LINR-01, LINR-02]
must_haves:
  truths:
    - "Schema 1 migrates without changing `project:golc`, `milestone:v1`, pending status, or null remote identity, per D-14."
    - "The schema-2 map dynamically contains the complete local catalog and remains credential-free, per D-11/D-12."
    - "Duplicate JSON names, unknown fields, and multiple values fail before typed decoding."
  artifacts:
    - path: internal/strictjson/decode.go
      provides: "Duplicate-safe strict JSON and canonical encoding"
    - path: internal/trace/catalog/migrate.go
      provides: "Lossless atomic schema-1-to-2 migration"
    - path: .planning/linear-map.json
      provides: "Credential-free complete local identity map"
  key_links:
    - from: internal/trace/catalog/migrate.go
      to: .planning/linear-map.json
      via: "strict decode, dynamic catalog merge, atomic replacement"
      pattern: "MigrateV1ToV2"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Migrate the existing seed to a canonical complete schema-2 map without identity or credential drift.

Purpose: Preserve established IDs while adding every dynamically discovered Phase 1 plan/task.
Output: Strict JSON guard, migration, canonical map, fixture, golden, and tests.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/linear-map.json
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-08-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/strictjson/decode.go`, `internal/strictjson/decode_test.go`, `internal/trace/catalog/migrate.go`, `internal/trace/catalog/migrate_test.go`, `.planning/linear-map.json`, `tests/fixtures/linear/map-schema1.json`, `tests/golden/linear-map-schema2.json`.

- Created/modified all seven strict JSON/migration/map/fixture/golden paths in frontmatter.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the seed without identity or credential drift</name>
  <files>internal/strictjson/decode.go, internal/strictjson/decode_test.go, internal/trace/catalog/migrate.go, internal/trace/catalog/migrate_test.go, .planning/linear-map.json, tests/fixtures/linear/map-schema1.json, tests/golden/linear-map-schema2.json</files>
  <read_first>
    - All seven owned paths — strict decoder/tests, migration/tests, map, legacy fixture, canonical golden.
    - `internal/trace/catalog/model.go`, `parse.go`, and `validate.go` — dynamic complete catalog.
    - `internal/command/router.go` and `test.go` — exact scope registration/dispatch.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-11/D-12/D-14.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-PATTERNS.md` — exact seed identity.
  </read_first>
  <behavior>
    - Duplicate/unknown/multiple JSON values fail before typed decode.
    - Legacy project/milestone/display/pending/null linkage remains exact.
    - New dynamically discovered entities are pending/null; check is read-only and write is atomic/idempotent.
  </behavior>
  <acceptance_criteria>
    - [ ] `ValidateSingleValueNoDuplicateNames`, `DecodeStrict`, and `CanonicalEncode` reject malformed/ambiguous JSON and produce canonical output.
    - [ ] `MigrateV1ToV2` preserves the two seed IDs/display values/pending/null linkage exactly and uses the dynamic catalog rather than a fixed plan count.
    - [ ] Check is read-only; write validates the complete target, replaces atomically, and is byte-idempotent.
    - [ ] Both owned test files call `MustDeclareScope` for exact scope `linear-map` and define `TestScopeLinearMap` before verification.
    - [ ] The registered map scope exits 0 without network or Node.
  </acceptance_criteria>
  <action>Implement the explicit D-14 migration with token-level duplicate rejection. Merge Plan 08's dynamically discovered catalog without synthesizing workspace/team IDs, GraphQL UUIDs, identifiers, URLs, credentials, or sync bases. Enforce D-11/D-12 owned fields/comment exclusion. Explicitly register exact scope `linear-map` through the Plan 17 contract in the owned tests.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-map</automated>
  </verify>
  <done>The seed is losslessly migrated to a complete canonical credential-free map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| legacy/current JSON -> map | Duplicate or unknown members may redefine identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-24 | Tampering | strict JSON/migration | high | mitigate | Duplicate-safe decode, full-target validation, atomic replacement. |
| T-01-25 | Spoofing | remote identity | high | mitigate | Nullable UUID-only links; no synthesis. |
| T-01-26 | Information Disclosure | map | high | mitigate | Credential-free schema/model and canonical allowlisted content. |
</threat_model>

<verification>
Run the registered migration scope without `.env`, Node, or network.
</verification>

<success_criteria>
- Stable seed identity is exact and every current plan/task is present dynamically.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-09-SUMMARY.md` when done.
</output>
