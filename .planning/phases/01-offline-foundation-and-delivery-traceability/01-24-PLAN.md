---
phase: 01-offline-foundation-and-delivery-traceability
plan: 24
type: execute
wave: 10
depends_on: [01-11, 01-22]
files_modified:
  - internal/contracts/linear_plan.go
  - internal/contracts/linear_plan_test.go
  - internal/command/linear.go
  - schemas/linear-plan.schema.json
  - schemas/linear-report.schema.json
autonomous: true
requirements: [LINR-03, LINR-04]
must_haves:
  truths:
    - "Strict deterministic plan/report schemas register exactly once as `linear-plan` and `linear-report`, join stable global generation/check traversal, and reject malformed or illegal state before apply."
    - "Exact route `linear apply {plan-file} --plan-id` is self-registered and requires matching plan ID."
    - "Before the process transport exists, remote apply fails `LINEAR_TRANSPORT_UNAVAILABLE` before credential/subprocess/mutation access."
  artifacts:
    - path: internal/contracts/linear_plan.go
      provides: "Plan/report projections plus exactly-once `linear-plan` and `linear-report` SchemaDescriptor registrations"
    - path: internal/command/linear.go
      provides: "Self-registered explicit apply route with injected factory"
    - path: schemas/linear-plan.schema.json
      provides: "Strict canonical preview contract"
    - path: schemas/linear-report.schema.json
      provides: "Strict apply report contract"
  key_links:
    - from: internal/contracts/linear_plan.go
      to: internal/contracts/generate.go
      via: "package initialization calls `RegisterSchema` exactly once for `linear-plan` and once for `linear-report`"
      pattern: "RegisterSchema.*linear-(plan|report)"
    - from: internal/contracts/generate.go
      to: schemas/linear-plan.schema.json
      via: "unchanged `GenerateAll`/`CheckDrift` traverse both descriptors from `RegisteredSchemas`"
      pattern: "RegisteredSchemas|GenerateAll|CheckDrift"
    - from: internal/contracts/generate.go
      to: schemas/linear-report.schema.json
      via: "unchanged `GenerateAll`/`CheckDrift` traverse the `linear-report` descriptor from `RegisteredSchemas`"
      pattern: "RegisteredSchemas|GenerateAll|CheckDrift"
    - from: internal/command/linear.go
      to: internal/trace/apply/engine.go
      via: "injected RemoteClientFactory"
      pattern: "MustDeclareRoute|RemoteClientFactory"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Generate strict plan/report contracts and expose the exact apply route through an injected fake-only transport boundary.

Purpose: Make malformed, unbound, or unwired remote requests fail before mutation.
Output: Schema registrations/tests, two schemas, and self-registered apply handler.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-04-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-11-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-22-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/contracts/linear_plan.go`, `internal/contracts/linear_plan_test.go`, `internal/command/linear.go`, `schemas/linear-plan.schema.json`, `schemas/linear-report.schema.json`.

- Created/modified all five contract/test/route/schema paths listed in frontmatter; `internal/contracts/linear_plan.go` contributes exactly one `linear-plan` and one `linear-report` descriptor through the Plan 04 API while leaving `internal/contracts/generate.go` untouched.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate strict plan/report contracts and expose apply</name>
  <files>internal/contracts/linear_plan.go, internal/contracts/linear_plan_test.go, internal/command/linear.go, schemas/linear-plan.schema.json, schemas/linear-report.schema.json</files>
  <read_first>
    - All five owned paths — schema source/tests, existing route file, generated schemas.
    - `internal/contracts/generate.go` and `.planning/phases/01-offline-foundation-and-delivery-traceability/01-04-SUMMARY.md` — existing `SchemaDescriptor`, `RegisterSchema`, `RegisteredSchemas`, `GenerateAll`, and `CheckDrift` contracts; read only and do not modify the central generator.
    - `internal/strictjson/decode.go` — strict decode contract.
    - `internal/trace/apply/model.go`, `engine.go`, and `guard.go` — apply consumers.
    - `internal/trace/transport/contract.go` and `fake.go` — injected fake boundary.
    - `internal/command/router.go` and `test.go` — exact route/scope registration.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-16 through D-18.
  </read_first>
  <behavior>
    - Duplicate/unknown/unsorted/bad-digest/invalid-conflict/illegal-transition inputs fail before apply.
    - `internal/contracts/linear_plan.go` registers descriptors named `linear-plan` and `linear-report`; global generation/check reaches each once in stable order and writes/checks the two declared schema paths.
    - CLI requires explicit plan file/matching ID and an injected factory; missing factory fails before transport access.
  </behavior>
  <acceptance_criteria>
    - [ ] `internal/contracts/linear_plan.go` invokes `RegisterSchema` exactly once with `SchemaDescriptor{Name: "linear-plan", OutputPath: "schemas/linear-plan.schema.json", ...}` and exactly once with `SchemaDescriptor{Name: "linear-report", OutputPath: "schemas/linear-report.schema.json", ...}`; registration failure stops initialization with a stable diagnostic.
    - [ ] `RegisteredSchemas` contains each new name exactly once in stable sorted position, and unchanged `GenerateAll`/`CheckDrift` generate/check both deterministic strict Draft 2020-12 outputs.
    - [ ] Malformed ordering/digest/conflict/report transitions fail before typed apply.
    - [ ] `internal/command/linear.go` calls `MustDeclareRoute` for exact route `linear apply {plan-file} --plan-id` and requires a matching ID.
    - [ ] No `ProcessClient` reference exists; absent factory returns `LINEAR_TRANSPORT_UNAVAILABLE` before credential/subprocess/mutation access.
    - [ ] `internal/contracts/generate.go` is unchanged by this plan; `linear_plan_test.go` proves both descriptors are present exactly once, registers exact scope `linear-plan-contract`, defines `TestScopeLinearPlanContract`, and the chained global generation/scope command exits 0.
  </acceptance_criteria>
  <action>Implement the plan/report projections and factories in `internal/contracts/linear_plan.go`, then invoke the Plan 04 `RegisterSchema` API exactly once for stable descriptor `linear-plan` targeting `schemas/linear-plan.schema.json` and exactly once for `linear-report` targeting `schemas/linear-report.schema.json`; fail initialization deterministically on any registration error. Do not modify `internal/contracts/generate.go`: unchanged `GenerateAll` and `CheckDrift` must discover both descriptors through `RegisteredSchemas`. Make strict canonical validation precede typed use. Self-register exact route `linear apply {plan-file} --plan-id` through the Plan 17 registry and wire it to an injected `RemoteClientFactory` whose tests return `FakeClient`. Keep concrete process selection absent. Explicitly register exact scope `linear-plan-contract` before verification and assert both schema descriptors are reachable exactly once.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 generate --check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-plan-contract</automated>
  </verify>
  <done>The unchanged global generator reaches exactly one `linear-plan` and one `linear-report` descriptor; strict contracts, exact route registration, fake injection, and unavailable-transport behavior prevent unsafe apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| plan/report JSON -> apply | Malformed or unbound files may attempt mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-31 | Tampering / Repudiation | apply route/binding | high | mitigate | Strict schema/canonical validation and exact route/ID. |
| T-01-34 | Tampering | contracts/registration | medium | mitigate | Source-owned deterministic schemas, exactly-once named descriptor registration, and unchanged sorted global traversal. |
| T-01-SC | Tampering | dependencies | high | mitigate | Existing exact Go lock; no npm install. |
</threat_model>

<verification>
Run global generation drift and the registered plan-contract scope against fake transport only; require both descriptors exactly once with no central-generator edit.
</verification>

<success_criteria>
- Apply route is registered, exact-plan-bound, fake-injected, and safely unavailable without a factory.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-24-SUMMARY.md` when done.
</output>
