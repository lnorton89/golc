---
phase: 01-offline-foundation-and-delivery-traceability
plan: 15
type: execute
wave: 18
depends_on: [01-07, 01-24, 01-27]
files_modified:
  - internal/trace/transport/process.go
  - internal/trace/transport/process_test.go
  - internal/command/linear.go
  - tests/acceptance/linear-transport.ps1
  - config/commands.toml
  - internal/delivery/graph.go
  - internal/delivery/foundation.go
  - .github/workflows/linear-sync.yml
  - .env.example
autonomous: true
requirements: [CONF-03, CONF-04, LINR-03, LINR-04]
must_haves:
  truths:
    - "Go launches only the project-local compiled adapter over strict bounded JSON-RPC; transport never owns identity/merge/apply policy."
    - "A fake-SDK project -> milestone -> parent/requirement issue -> task sub-issue preview/apply/replay creates each object once and replay is all no-op."
    - "Missing adapter/credential/network fails only the explicit remote command; core offline build/test/package/trace stays green, per D-21."
    - "Remote mutation is explicit exact-plan-bound and PR-blocked, per D-16, D-17, and D-18."
    - "Protected jobs materialize `.env` from secrets only inside the command and remove it in `finally`, with no value in argv/logs/artifacts, per D-19 and D-20."
  artifacts:
    - path: internal/trace/transport/process.go
      provides: "bounded strict Go-to-Node RemoteClient"
    - path: tests/acceptance/linear-transport.ps1
      provides: "complete fake-SDK hierarchy preview/apply/replay and offline isolation"
    - path: .github/workflows/linear-sync.yml
      provides: "protected explicit remote drift/apply with ephemeral cleanup"
  key_links:
    - from: internal/trace/transport/process.go
      to: tools/linear-sync/dist/cli.js
      via: "strict newline-delimited JSON-RPC with deadlines"
      pattern: "linear-sync|dist.*cli\\.js"
    - from: internal/command/linear.go
      to: internal/trace/apply/engine.go
      via: "SDK transport feeds Go exact preview/apply policy"
      pattern: "ProcessClient|Apply"
    - from: .github/workflows/linear-sync.yml
      to: golc.ps1
      via: "protected preview/apply and finally cleanup"
      pattern: "plan_id|confirm_apply|finally"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Connect Go policy to the compiled SDK adapter, prove the full hierarchy preview/apply/replay against a fake SDK, and protect optional remote execution.

Purpose: Finish LINR-03/LINR-04 at the real adapter contract without requiring credentials or a live workspace.
Output: ProcessClient, full acceptance, completed offline graph/package, and protected manual Linear workflow.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-07-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-24-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-27-SUMMARY.md
</context>

## Artifacts this phase produces

- Created `internal/trace/transport/process.go`: `ProcessClient`, `ProcessConfig`, `RPCError`, `NewProcessClient`, `Call`, and `Close`; strict stdout protocol, allowlisted stderr, context deadline/process-tree termination.
- Created `internal/trace/transport/process_test.go`: executable path, request/response discriminants, protocol noise/unknown fields, cancellation, termination, redaction, and missing adapter tests.
- Modified `internal/command/linear.go`: new routes `linear drift --remote --read-only` and remote `linear preview`; the Plan 24 exact `linear apply` registration is retained while its injected factory gains the process implementation; flags `--read-only`, `--plan-id`, `--snapshot-report`, `--timeout`; missing integration isolation.
- Created `tests/acceptance/linear-transport.ps1`: compiled fake-SDK Go<->Node contract, complete hierarchy preview/apply/replay, page/error/rate/timeout/resume cases, secret canary, and offline-core isolation.
- Modified `config/commands.toml`: final approved adapter build/test/fake/remote nodes and PR/protected/network policies.
- Modified `internal/delivery/graph.go`: final offline adapter build/test/cache nodes and remote-failure isolation.
- Modified `internal/delivery/foundation.go`: deterministic inclusion of `tools/linear-sync/dist/cli.js` and required runtime files in foundation ZIP.
- Created `.github/workflows/linear-sync.yml`: no PR trigger; protected/manual `linear-drift` and `linear-apply`, exact `plan_file`/`plan_id`/`confirm_apply`, ephemeral `.env` try/finally cleanup.
- Modified `.env.example`: final supported names and notes that external reviewed taxonomy/credentials are required only by explicit remote commands.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan(s) | Status |
|---|---|---|---|---|
| GOAL | Phase 1 | Configure, validate, build, and trace from durable repository sources without Linear/secrets | 01-29 (all plans) | COVERED |
| REQ | CONF-01 | Root config discovers pins/setup/generate/check/build/test/package/defaults/runtime | 01-06, 13, 16-20, 25, 28-29 | COVERED |
| REQ | CONF-02 | Separated independently validatable concerns and one authority | 02-04, 18-19 | COVERED |
| REQ | CONF-03 | Same root commands for contributor/CI and exact-lock adapter bootstrap | 01-07, 12-13, 15-17, 19-20, 25, 28-29 | COVERED |
| REQ | CONF-04 | Secret/machine values external with safe examples | 02-03, 07, 14-15, 18, 26-27 | COVERED |
| REQ | LINR-01 | Durable project/milestone/phase/requirement/plan/task IDs | 08-09, 21-22 | COVERED |
| REQ | LINR-02 | Credential-free nullable immutable UUID mapping | 08-09, 21-22 | COVERED |
| REQ | LINR-03 | Idempotent Project/Milestone/Issue/sub-issue preview/apply/replay | 10-15, 23-27 | COVERED |
| REQ | LINR-04 | Ambiguity/partial/pagination/rate/offline isolation | 07, 10-12, 14-15, 23-24, 26-27 | COVERED |
| CONTEXT | D-01 | One clean Windows bootstrap provisions complete local tools | 01, 05, 13, 16, 25, 28 | COVERED |
| CONTEXT | D-02 | Post-bootstrap core work succeeds offline from local caches | 01, 06, 13, 15-16, 25, 28 | COVERED |
| CONTEXT | D-03 | One root command and required subcommands | 01-02, 06-07, 13, 15, 17, 19-25, 29 | COVERED |
| CONTEXT | D-04 | Explicit check/write updates cover toolchain, Go, and exact npm package/lock authorities; bootstrap and update modes never install | 01, 13, 16, 25, 28-29 | COVERED |
| CONTEXT | D-05 | Small root index points to separated concerns | 02-03, 17 | COVERED |
| CONTEXT | D-06 | Exact five-layer precedence | 02, 18 | COVERED |
| CONTEXT | D-07 | Effective value plus source layer inspection | 02, 18 | COVERED |
| CONTEXT | D-08 | Committed stable generated contracts; transient state ignored | 04, 09, 11, 19-20, 22, 24 | COVERED |
| CONTEXT | D-09 | Strict failures and deprecation migration guidance | 03 | COVERED |
| CONTEXT | D-10 | Local/CI same commands and independent concern validation | 03-04, 06-07, 18-20 | COVERED |
| CONTEXT | D-11 | Repository/Linear owned-field split | 08-10, 13, 21-23 | COVERED |
| CONTEXT | D-12 | Linear comments are not mirrored | 08-10, 21-23 | COVERED |
| CONTEXT | D-13 | Same-field dual change blocks with field preview | 10-11, 23-24 | COVERED |
| CONTEXT | D-14 | Stable local/UUID identity across renames | 08-10, 21-23 | COVERED |
| CONTEXT | D-15 | No inferred delete; reviewed archive/unlink only | 11, 23-24 | COVERED |
| CONTEXT | D-16 | Explicit mutation milestones; PR read-only | 07, 11, 15, 24 | COVERED |
| CONTEXT | D-17 | Deterministic preview then separate exact apply | 10-11, 15, 23-24 | COVERED |
| CONTEXT | D-18 | Apply rejects changed source/remote state | 11, 15, 24 | COVERED |
| CONTEXT | D-19 | Safe `.env.example`, real `.env` untracked | 07, 15 | COVERED |
| CONTEXT | D-20 | Ephemeral CI secret cleanup and no leakage | 07, 14-15, 26-27 | COVERED |
| CONTEXT | D-21 | Sync failures/pages/errors/rates do not block local work | 07, 10-15, 23-27 | COVERED |
| RESEARCH | Bootstrap | PowerShell shim, exact archives/hashes, project-local caches | 01, 05, 13, 16, 25, 28 | COVERED |
| RESEARCH | Configuration | Strict TOML, ownership/reference graph, provenance, path safety | 02-04, 17-19 | COVERED |
| RESEARCH | Contracts | Go-owned deterministic config/map/plan/report schemas | 04, 09, 11, 19, 22, 24 | COVERED |
| RESEARCH | Catalog | Lossless seed migration and stable dynamic hierarchy | 08-09, 21-22 | COVERED |
| RESEARCH | Reconciliation | Three-way preview, footer, stale-safe resumable apply | 10-11, 23-24 | COVERED |
| RESEARCH | SDK transport | Exact SDK, reviewable exact package/lock updates, entity operations, exhaustive pages/errors/rates/redaction | 12-15, 25-27, 29 | COVERED |
| RESEARCH | Packaging/cache | Deterministic foundation ZIP; warmed caches without vendored trees | 06, 13, 15, 20, 25, 28 | COVERED |
| RESEARCH | Validation/security | Nyquist fakes/goldens/canary/CI and ASVS L1 mitigations | 01-29 | COVERED |
| RESEARCH | Boundary | No Wails UI, SQLite show DB, NSIS product installer, or live automated mutation | SKELETON, 01-29 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bridge Go to the adapter and prove the complete hierarchy replay</name>
  <files>internal/trace/transport/process.go, internal/trace/transport/process_test.go, internal/command/linear.go, tests/acceptance/linear-transport.ps1</files>
  <read_first>
    - `internal/trace/transport/process.go` — new process transport; does not exist before this task.
    - `internal/trace/transport/process_test.go` — new process-boundary tests; does not exist before this task.
    - `internal/command/linear.go` — existing Go policy command surface modified to install/select `ProcessClient`.
    - `tests/acceptance/linear-transport.ps1` — new cross-process acceptance; does not exist before this task.
    - `internal/trace/transport/contract.go` — `RemoteClient` signatures implemented by `ProcessClient`.
    - `internal/trace/apply/engine.go` and `internal/trace/reconcile/diff.go` — Go-owned exact preview/apply policy consumers.
    - `tools/linear-sync/src/protocol.ts`, `src/cli.ts`, and `dist/cli.js` — Plan 01-13/14 discriminated NDJSON protocol and compiled executable contract.
    - `tools/linear-sync/test/*.test.ts` and `tests/fixtures/linear/*.json` — fault/call-counter corpus exercised through the real boundary.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-11/D-13/D-17/D-18/D-21 authority and failure isolation.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md` — bounded process transport, timeout, redaction, and postcondition rules.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — hierarchy/page/error/rate/timeout/replay acceptance.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-PATTERNS.md` — real adapter boundary with Go retaining policy.
  </read_first>
  <behavior>
    - ProcessClient launches only project-local `dist/cli.js`, enforces strict newline JSON, deadlines/cancellation, process-tree kill, and safe stderr.
    - Empty fake SDK -> preview creates Project, Project Milestone, parent Issue, requirement Issue, task sub-issue in dependency order -> apply reads back each -> replay performs zero creates/updates.
    - Create/update/readback call counters prove each required SDK operation route is used and no duplicate occurs.
    - Page-two marker, data-plus-errors, rate limit, timeout-after-create, partial resume, and canary cases pass through the real process boundary.
  </behavior>
  <acceptance_criteria>
    - [ ] `NewProcessClient`, `Call`, and `Close` launch only project-local `tools/linear-sync/dist/cli.js`, use strict one-line discriminated JSON, enforce bounds/deadlines, kill the process tree, and allowlist stderr.
    - [ ] Protocol noise, unknown fields, missing adapter, cancellation, timeout, and secret-canary output return typed safe failures without changing local state.
- [ ] `internal/command/linear.go` self-registers exact new remote routes `linear drift --remote --read-only` and `linear preview --remote`, retains the single Plan 24 registration for `linear apply {plan-file} --plan-id`, and binds that apply route to the process factory without redeclaring it.
    - [ ] Empty fake SDK preview/apply creates Project, Project Milestone, parent Issue, requirement Issue, and task sub-issue in dependency order, reads each back, and replay records zero creates/updates.
    - [ ] Acceptance call counters prove every required discriminant once and one remote object per local ID across page/error/rate/timeout/resume cases.
    - [ ] `powershell -NoProfile -File .\tests\acceptance\linear-transport.ps1 -Mode hierarchy` exits 0 with no live credential or service.
  </acceptance_criteria>
<action>Implement the real process transport against the Plan 13/14/26/27 protocol, with Go retaining D-11/D-13/D-17/D-18 policy. In `internal/command/linear.go`, self-register exact new remote routes `linear drift --remote --read-only` and `linear preview --remote` through the Plan 17 registry. Preserve the single Plan 24 registration for `linear apply {plan-file} --plan-id` and bind its injected factory to the process transport; do not redeclare it because the registry rejects duplicates. Never edit `router.go`. The acceptance harness injects a fake official SDK into the compiled adapter, runs complete preview/apply/replay, and asserts exact per-discriminant calls and one-object-per-local-ID state. Reject protocol noise/unknown fields and isolate cancellation/startup failures.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\linear-transport.ps1 -Mode hierarchy</automated>
  </verify>
  <done>The real Go/Node boundary completes and replays the required hierarchy against a fake SDK with no duplicates or live service.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete offline graph and foundation package integration</name>
  <files>config/commands.toml, internal/delivery/graph.go, internal/delivery/foundation.go, tests/acceptance/linear-transport.ps1</files>
  <read_first>
    - `config/commands.toml` — existing authoritative graph modified with final adapter/fake/remote policies.
    - `internal/delivery/graph.go` — existing offline graph modified with final adapter nodes/failure isolation.
    - `internal/delivery/foundation.go` — existing deterministic packager modified with compiled adapter runtime files.
    - `tests/acceptance/linear-transport.ps1` — existing Task 1 acceptance modified with offline mode.
    - `tools/linear-sync/package-lock.json`, `tsconfig.json`, and `dist/cli.js` — approved exact adapter build/runtime inputs.
    - `internal/bootstrap/cache.go` — validated project-local npm cache/offline reinstall contract.
    - `internal/trace/transport/process.go` — Task 1 startup/network/credential failure surface.
    - `tests/golden/foundation-manifest.json` — existing deterministic package inventory golden.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-02/D-03/D-10/D-21 offline and remote isolation.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — offline check/package and remote-failure acceptance.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-PATTERNS.md` — adapter packaging within developer-tool boundary.
  </read_first>
  <behavior>
    - Full build/test/package includes approved compiled adapter using only warmed cache offline.
    - Missing adapter/credential/network changes only explicit remote command status; core command results stay green.
    - Foundation ZIP deterministically includes `dist/cli.js` and required license/runtime files.
  </behavior>
  <acceptance_criteria>
    - [ ] `config/commands.toml` remains the single owner for adapter build/test/fake/remote nodes, exact registered scopes, and offline/network/PR/protected policies.
    - [ ] Offline build/test/package uses only Plan 01-13 warmed cache and compiled adapter; missing adapter, credential, or network affects only the explicit remote result.
    - [ ] `BuildFoundationBundle` deterministically includes `dist/cli.js` and required license/runtime files with canonical manifest/checksum and no Node product-runtime claim.
    - [ ] Generate/check/build/test/package/local trace remain green under injected adapter startup, credential, and network failures.
    - [ ] The chained `linear-transport.ps1 -Mode offline` plus `golc.ps1 check --offline` command exits 0.
  </acceptance_criteria>
  <action>Register final adapter nodes and their exact scopes through the Plan 17 registry from the single D-03/D-10 graph, then include the compiled adapter in the deterministic package. Run adapter build/test offline from the Plan 13 cache. Inject startup/network/credential failures and assert D-21 isolation from generate/check/build/test/package/local trace. Keep Node absent from product runtime claims; this is developer tooling.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\linear-transport.ps1 -Mode offline; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 check --offline</automated>
  </verify>
  <done>The completed toolchain builds/tests/packages offline and remote failures cannot poison local results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Protect optional remote drift/apply and run final phase gates</name>
  <files>internal/command/linear.go, .github/workflows/linear-sync.yml, .env.example, tests/acceptance/linear-transport.ps1</files>
  <read_first>
    - `internal/command/linear.go` — existing command surface modified with final remote drift/preview/apply flags and runtime guards.
    - `.github/workflows/linear-sync.yml` — new protected/manual workflow; does not exist before this task.
    - `.env.example` — existing safe variable-name example modified with final optional-remote notes.
    - `tests/acceptance/linear-transport.ps1` — existing acceptance modified with protected workflow/cleanup mode.
    - `internal/trace/apply/guard.go`, `engine.go`, and `journal.go` — exact-plan, PR-block, stale/replay/resume policy.
    - `internal/security/redact.go` and `tools/linear-sync/src/redact.ts` — Go/Node canary and allowlisted-output contracts.
    - `.github/workflows/check.yml` — PR workflow that must remain secret-free/read-only.
    - `config/commands.toml` and `internal/delivery/graph.go` — final command/policy graph used by all phase gates.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-16 through D-21 protected exact apply, ephemeral secret, and offline isolation decisions.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md` — protected/manual workflow and `finally` cleanup contract.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — workflow mode plus final generate/check/test/build/package/map gates.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-PATTERNS.md` — no-PR mutation, safe `.env`, and pending real-smoke behavior.
  </read_first>
  <behavior>
    - Workflow has no PR trigger; protected apply requires plan file, matching plan ID, confirmation, and protected environment.
    - `.env` is created from protected secret only inside try and removed in finally on success/failure/cancellation; no value enters argv/logs.
    - No automated test contacts or mutates a live workspace; absent external taxonomy/credentials records real smoke pending without failing offline acceptance.
  </behavior>
  <acceptance_criteria>
    - [ ] `.github/workflows/linear-sync.yml` has no PR trigger and separates protected/manual read-only drift from apply requiring `plan_file`, matching `plan_id`, `confirm_apply`, and protected environment.
    - [ ] The workflow creates `.env` from protected secrets only inside the command body, removes it in `finally` on success/failure/cancellation, and emits no secret in argv/log/artifact bytes.
    - [ ] Runtime PR guard, exact self-registered remote routes, and exact plan engine remain authoritative even if workflow YAML is bypassed; no automated test contacts a live workspace.
    - [ ] Unavailable external taxonomy/credential records the reviewed real smoke pending and does not fail offline acceptance or invent a remote ID.
    - [ ] Workflow acceptance and all final generation, offline check, full test, build, foundation package, and offline map validation commands exit 0 with fake credentials only.
  </acceptance_criteria>
  <action>Complete D-16 through D-21 protected execution. Confirm the exact remote routes registered in Task 1 remain discoverable and duplicate-free; remote drift is explicit/read-only and apply routes through the runtime PR guard/exact plan engine. Materialize supported external names only in protected/manual jobs and remove `.env` in `finally`. Test cleanup with fake canaries on success and forced failure. External taxonomy remains reviewed configuration and no remote ID is invented. Then run generation, offline check, full tests, build, package, and offline map validation.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\linear-transport.ps1 -Mode workflow; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 generate --check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 check --offline; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 package --foundation; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 linear validate --offline</automated>
  </verify>
  <done>Protected optional remote routes are exact, secret-safe, PR-blocked, and the full offline phase gate is green without live Linear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Node stdio -> Go engine | Transport output must be complete, typed, bounded, and redacted. |
| protected workflow/secret -> remote command | Credential and mutation authority cross CI boundaries. |
| adapter availability -> offline graph | Integration failure must not contaminate local results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-42 | Tampering | Go/Node protocol | high | mitigate | Strict discriminated JSON, project-local executable, deadlines, unknown-field/noise rejection, and contract tests. |
| T-01-43 | Tampering / Repudiation | hierarchy apply/replay | high | mitigate | Exact plan/readback, per-operation counters, and full fake-SDK replay acceptance. |
| T-01-44 | Information Disclosure | workflow/env/logs | high | mitigate | Env-only secret, allowlisted output, canary tests, and `finally` cleanup. |
| T-01-45 | Elevation of Privilege | protected/PR mutation | high | mitigate | No PR trigger/secret, protected environment, exact plan/hash/confirmation, and runtime guard. |
| T-01-46 | Denial of Service | remote integration failure | high | mitigate | Independent result boundary and offline graph acceptance. |
| T-01-SC | Tampering | compiled adapter/packages | high | mitigate | Plan 12-approved exact lock, Plan 13 cache verification, deterministic inclusion, no new install. |
</threat_model>

<verification>
Run hierarchy, offline, and workflow acceptance modes, then all final root-command gates. Use fake SDK/credentials only; leave real-service smoke pending unless separately authorized.
</verification>

<success_criteria>
- The real process boundary proves the full required hierarchy preview/apply/replay with one remote object per local ID.
- All core commands and foundation packaging pass offline after the complete bootstrap.
- PR cannot mutate; protected manual apply is exact and secret-clean.
- Multi-source audit has no missing GOAL, REQ, RESEARCH, or CONTEXT item.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-15-SUMMARY.md` when done.
</output>
