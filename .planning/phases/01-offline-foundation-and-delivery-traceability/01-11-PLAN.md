---
phase: 01-offline-foundation-and-delivery-traceability
plan: 11
type: execute
wave: 9
depends_on: [01-23]
files_modified:
  - internal/trace/apply/model.go
  - internal/trace/apply/engine.go
  - internal/trace/apply/guard.go
  - internal/trace/apply/apply_test.go
  - tests/fixtures/linear/remote-stale.json
  - tests/fixtures/linear/remote-timeout-after-create.json
  - internal/trace/apply/journal.go
  - tests/fixtures/linear/remote-partial-apply.json
  - tests/golden/linear-apply-report.json
autonomous: true
requirements: [LINR-03, LINR-04]
must_haves:
  truths:
    - "Apply consumes the exact reviewed plan/hash and rejects repository/relevant remote drift, per D-17/D-18."
    - "Exact achieved postcondition is replay no-op; exact before/updatedAt allows one mutation/readback."
    - "Unknown outcomes are discovered before retry and partial apply resumes an exact prefix without duplicates, per D-21."
    - "PR events cannot mutate and only explicit archive/unlink may alter removal state, per D-15/D-16."
  artifacts:
    - path: internal/trace/apply/engine.go
      provides: "Stale-safe resumable apply state machine"
    - path: internal/trace/apply/guard.go
      provides: "Exact-plan and PR mutation guards"
    - path: internal/trace/apply/journal.go
      provides: "Atomic achieved-prefix journal"
    - path: tests/golden/linear-apply-report.json
      provides: "Canonical credential-free apply report"
  key_links:
    - from: internal/trace/apply/engine.go
      to: internal/trace/transport/contract.go
      via: "read-before-write, one mutation, readback"
      pattern: "RemoteClient"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Implement exact-plan apply, stale rejection, safe replay, atomic journaling, and exact-prefix resume.

Purpose: Complete Go-owned mutation safety before exposing the apply route or real SDK process.
Output: Apply model/engine/guard/journal/tests, three fixtures, and report golden.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/linear-map.json
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-23-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/trace/apply/model.go`, `internal/trace/apply/engine.go`, `internal/trace/apply/guard.go`, `internal/trace/apply/apply_test.go`, `tests/fixtures/linear/remote-stale.json`, `tests/fixtures/linear/remote-timeout-after-create.json`, `internal/trace/apply/journal.go`, `tests/fixtures/linear/remote-partial-apply.json`, `tests/golden/linear-apply-report.json`.

- Created all nine apply implementation/test/fixture/golden paths listed in frontmatter.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exact-plan apply, stale rejection, and safe replay</name>
  <files>internal/trace/apply/model.go, internal/trace/apply/engine.go, internal/trace/apply/guard.go, internal/trace/apply/apply_test.go, tests/fixtures/linear/remote-stale.json, tests/fixtures/linear/remote-timeout-after-create.json</files>
  <read_first>
    - The six owned Task 1 paths — apply model/engine/guard/tests/stale/timeout fixtures.
    - `internal/trace/reconcile/model.go`, `canonical.go`, and `marker.go` — exact plan/digest/identity.
    - `internal/trace/transport/contract.go` and `fake.go` — remote/fault counters.
    - `internal/command/router.go` and `test.go` — exact scope registration/dispatch.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-13/D-15 through D-18/D-21.
  </read_first>
  <behavior>
    - Invalid/bound-to-different state fails before mutation.
    - Exact postcondition is no-op; exact before/updatedAt permits one mutation/readback.
    - Timeout-after-create discovers the achieved object before retry.
    - PR mutation is blocked independently of workflow YAML.
  </behavior>
  <acceptance_criteria>
    - [ ] Plan binding rejects bad schema/hash/digests, changed intent/map/relevant remote state, conflict, ambiguity, or incompleteness before mutation.
    - [ ] Apply returns no-op only for exact achieved postcondition plus matching UUID/footer; otherwise exact before/updatedAt permits one mutation and readback.
    - [ ] Timeout-after-create discovers by immutable UUID/exact footer before retry and leaves one object.
    - [ ] PR guard blocks independently; only explicit archive/unlink operations can change removal state.
    - [ ] `apply_test.go` registers exact scope `linear-apply-core`, defines `TestScopeLinearApplyCore`, and passes against fake transport only.
  </acceptance_criteria>
  <action>Implement D-16/D-17/D-18 state binding. Re-read complete relevant state before each operation; accept only exact postcondition or exact before-state. Mutate once, read back, and reconcile uncertain outcomes by UUID/footer. Enforce conflict/ambiguity/removal rules. Explicitly register exact scope `linear-apply-core` through the Plan 17 contract before verification.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-apply-core</automated>
  </verify>
  <done>Stale/forged plans fail before mutation, exact replay no-ops, and timeout recovery creates no duplicate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Journal partial apply and resume an exact achieved prefix</name>
  <files>internal/trace/apply/journal.go, internal/trace/apply/engine.go, internal/trace/apply/apply_test.go, tests/fixtures/linear/remote-partial-apply.json, tests/golden/linear-apply-report.json</files>
  <read_first>
    - The five owned Task 2 paths — journal plus existing engine/tests, partial fixture, report golden.
    - `internal/trace/apply/model.go` and `guard.go` — Task 1 states/guards.
    - `internal/trace/transport/contract.go` — rate/outcome metadata.
    - `.planning/linear-map.json` — atomic mapping target.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-17/D-18/D-21.
  </read_first>
  <behavior>
    - Rate/partial error stops writes and reports completed/no-op/pending/blocked plus safe retry metadata.
    - Same plan resumes only an exact achieved prefix and never replays completed operations.
    - Map/journal/report update is atomic; unrelated changes invalidate resume.
  </behavior>
  <acceptance_criteria>
    - [ ] `CommitResultAtomically` updates map/journal/report as one validated result or leaves prior state intact.
    - [ ] Partial/rate failure stops writes and reports all operation states plus safe retry metadata.
    - [ ] `ResumePrefix` requires the same plan ID/exact achieved current postconditions and rejects unrelated changes.
    - [ ] Timing/rate fields remain outside canonical plan bytes and credentials remain absent.
    - [ ] `apply_test.go` registers exact scope `linear-apply-resume`, defines `TestScopeLinearApplyResume`, and matches the report golden.
  </acceptance_criteria>
  <action>Implement D-21 partial execution/reporting and atomic persistence. Bind journals to plan ID and achieved postconditions, validate current state before resume, and keep timing/rate outside canonical plan bytes. Explicitly register exact scope `linear-apply-resume` through the Plan 17 contract before verification.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-apply-resume</automated>
  </verify>
  <done>Partial work stops safely and resumes the exact plan without replay or mapping corruption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| preview file -> apply engine | A plan may be forged, stale, conflicted, or bound elsewhere. |
| apply -> remote/map/journal | Unknown outcomes may duplicate objects or corrupt linkage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-31 | Tampering / Repudiation | plan binding | high | mitigate | Strict hash/digests/state rereads. |
| T-01-32 | Tampering / Denial of Service | unknown/partial outcome | high | mitigate | Postcondition discovery, one mutation, atomic journal/map, prefix resume. |
| T-01-33 | Elevation of Privilege | PR apply | high | mitigate | Runtime event guard and explicit plan binding. |
</threat_model>

<verification>
Run apply-core then apply-resume against fake transport only.
</verification>

<success_criteria>
- Apply is exact, stale-safe, replayable, resumable, and PR-blocked.
- Timeout/partial/rate scenarios do not duplicate operations.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-11-SUMMARY.md` when done.
</output>
