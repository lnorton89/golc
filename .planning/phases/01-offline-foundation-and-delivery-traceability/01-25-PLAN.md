---
phase: 01-offline-foundation-and-delivery-traceability
plan: 25
type: execute
wave: 14
depends_on: [01-13, 01-29]
files_modified:
  - internal/bootstrap/bootstrap_test.go
  - internal/delivery/graph.go
  - tools/linear-sync/src/cli.ts
  - tools/linear-sync/test/operations.test.ts
  - tests/fixtures/linear/hierarchy-operations.json
  - tests/acceptance/bootstrap-node.ps1
autonomous: true
requirements: [CONF-01, CONF-03, LINR-03]
must_haves:
  truths:
    - "A clean first SDK bootstrap compiles; immediate second bootstrap and isolated cache-only reinstall/compile succeed with network denied, per D-02."
    - "Package/lock/pins remain unchanged by bootstrap, per D-04."
    - "Fake SDK contract tests cover create/update/readback for the complete hierarchy without live credentials or mutation, per D-21."
  artifacts:
    - path: tools/linear-sync/src/cli.ts
      provides: "Strict NDJSON adapter entrypoint"
    - path: tools/linear-sync/test/operations.test.ts
      provides: "Complete fake-SDK hierarchy contract"
    - path: tests/acceptance/bootstrap-node.ps1
      provides: "Online first/offline second/cache-only reinstall acceptance"
  key_links:
    - from: tools/linear-sync/test/operations.test.ts
      to: tests/fixtures/linear/hierarchy-operations.json
      via: "exact SDK method/input/output transcript"
      pattern: "TestScopeLinearSdkOperations"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Contract-test the hierarchy after provisioning and prove network-free repeat/bootstrap-cache rebuild.

Purpose: Demonstrate exact cache completeness and fake-only SDK behavior after the workspace is registered.
Output: NDJSON CLI, fake tests/fixture, bootstrap-node acceptance, and graph/test extensions.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-13-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-29-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/bootstrap/bootstrap_test.go`, `internal/delivery/graph.go`, `tools/linear-sync/src/cli.ts`, `tools/linear-sync/test/operations.test.ts`, `tests/fixtures/linear/hierarchy-operations.json`, `tests/acceptance/bootstrap-node.ps1`.

- Created/modified all six test/graph/CLI/fixture/acceptance paths listed in frontmatter.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract-test the hierarchy and prove offline repeat bootstrap</name>
  <files>internal/bootstrap/bootstrap_test.go, internal/delivery/graph.go, tools/linear-sync/src/cli.ts, tools/linear-sync/test/operations.test.ts, tests/fixtures/linear/hierarchy-operations.json, tests/acceptance/bootstrap-node.ps1</files>
  <read_first>
    - All six owned paths — bootstrap tests/graph/CLI/Node test/fixture/acceptance.
    - `tools/linear-sync/src/protocol.ts`, `adapter.ts`, package/lock/tsconfig — Plan 13 authority.
    - `config/commands.toml` and `internal/command/test.go` — pre-registered scopes and dispatch.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-01 through D-04/D-21.
  </read_first>
  <behavior>
    - First clean bootstrap installs/compiles, second bootstrap with network denied is no-op, and isolated prefix reinstalls/compiles from cache only.
    - Package files/lock/pins remain byte-identical.
    - Fake SDK records exact method/input for every hierarchy variant.
  </behavior>
  <acceptance_criteria>
    - [ ] Acceptance performs clean exact-lock install/compile, network-denied no-op bootstrap, and temporary-prefix offline reinstall/compile.
    - [ ] Second bootstrap records zero network calls; cache probe does not reuse existing `node_modules`; package/lock/pin bytes remain unchanged.
    - [ ] `runCLI` accepts strict NDJSON discriminated requests only and contains no HTTP server/reconciliation policy.
    - [ ] `operations.test.ts` defines marker `TestScopeLinearSdkOperations` matching the exact pre-registered `linear-sdk-operations` scope and asserts one create/update/readback per entity.
    - [ ] Bootstrap-node acceptance plus the registered Node scope exits 0 with no live credential/mutation.
  </acceptance_criteria>
  <action>After Plan 13 provisioning/registration, add strict NDJSON CLI and fake-SDK hierarchy tests. The cache acceptance must include an isolated prefix so existing dependencies cannot certify cache completeness. Bootstrap verifies but never rewrites manifests per D-04. Invoke only the exact scope already registered in `config/commands.toml`; fail if marker and registration differ or no marker exists.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\bootstrap-node.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-sdk-operations</automated>
  </verify>
  <done>First bootstrap compiles, repeat/cache rebuild are offline, and every hierarchy operation passes against a fake SDK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local cache -> offline reinstall | Incomplete cache may hide network fallback. |
| fake RPC -> SDK adapter | Contract mismatch may call wrong SDK operation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-36 | Tampering | npm cache/bootstrap | high | mitigate | Exact-lock isolated offline reinstall and byte-unchanged manifests. |
| T-01-37 | Spoofing / Tampering | SDK operation mapping | high | mitigate | Exact discriminants, parents/footer, fake call assertions. |
| T-01-38 | Repudiation | hierarchy operations | high | mitigate | Recorded typed outcomes/readbacks and complete sequence. |
</threat_model>

<verification>
Run bootstrap-node acceptance before invoking the registered fake-SDK scope.
</verification>

<success_criteria>
- Cache-only rebuild is proven independently of existing install state.
- Complete hierarchy operations are fake-SDK contract tested.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-25-SUMMARY.md` when done.
</output>
