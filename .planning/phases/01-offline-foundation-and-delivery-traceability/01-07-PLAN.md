---
phase: 01-offline-foundation-and-delivery-traceability
plan: 07
type: execute
wave: 11
depends_on: [01-20]
files_modified:
  - internal/security/redact.go
  - internal/security/redact_test.go
  - tests/acceptance/command-parity.ps1
  - .env.example
  - .gitignore
  - .github/workflows/check.yml
  - internal/command/check.go
  - config/commands.toml
autonomous: true
requirements: [CONF-03, CONF-04, LINR-04]
must_haves:
  truths:
    - "Safe examples commit supported variable names and empty placeholders while `.env` and machine values stay untracked, per D-19."
    - "Contributors and Windows PR CI invoke the same root graph, per D-03 and D-10."
    - "PR CI has no credential or mutation route, per D-16, and integration failures cannot block local work, per D-21."
    - "Fake-secret bytes never appear in diagnostics, generated contracts, maps, reports, or packages, per D-20."
  artifacts:
    - path: internal/security/redact.go
      provides: "allowlisted diagnostics and canary scan"
    - path: .env.example
      provides: "safe supported integration names"
    - path: .github/workflows/check.yml
      provides: "least-privilege Windows PR root-command validation"
  key_links:
    - from: .github/workflows/check.yml
      to: golc.ps1
      via: "bootstrap/generate/check/build/test/package"
      pattern: "golc\\.ps1"
    - from: tests/acceptance/command-parity.ps1
      to: config/commands.toml
      via: "parsed authoritative command graph"
      pattern: "commands\\.toml"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Make contributor/PR delivery secret-free and prove CI uses the same root command graph.

Purpose: Close the committed-secret and PR-mutation boundaries without requiring Linear.
Output: Safe example, centralized redaction/canary tests, command parity acceptance, and least-privilege Windows PR workflow.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-20-SUMMARY.md
</context>

## Artifacts this phase produces

- Created `internal/security/redact.go`: `SafeDiagnostic`, `Redact`, `SetState`, `ScanCanary`, and allowlisted output fields.
- Created `internal/security/redact_test.go`: stdout/stderr/file/report/schema/map/package canary tests and set/unset rendering.
- Created `tests/acceptance/command-parity.ps1`: parses the command graph and workflow, proving local/CI parity and absence of PR mutation/secret references.
- Created `.env.example`: empty `LINEAR_API_KEY` and `LINEAR_TEAM_ID` declarations with optional-remote comments only.
- Modified `.gitignore`: retains `.env` exclusion and explicitly keeps `.env.example` trackable.
- Created `.github/workflows/check.yml`: least-privilege Windows PR job running root bootstrap, generation drift, offline check, build, test, and foundation package; no secret or remote apply.
- Modified `internal/command/check.go`: route `check --command-parity` and canary/repository artifact scan integration.
- Modified `config/commands.toml`: parity metadata and explicit `pr_allowed`/network/mutation policies.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce safe examples and secret-free output</name>
  <files>internal/security/redact.go, internal/security/redact_test.go, .env.example, .gitignore, internal/command/check.go</files>
  <read_first>
    - `internal/security/redact.go` — new redaction implementation; does not exist before this task.
    - `internal/security/redact_test.go` — new secret/scope test path; does not exist before this task.
    - `.env.example` — new safe example; does not exist before this task.
    - `.gitignore` — existing secret boundary modified to keep `.env.example` visible.
    - `internal/command/check.go` — existing Plan 01-04/01-06 check route modified with canary scans.
    - `internal/delivery/graph.go` and `internal/delivery/foundation.go` — upstream output-surface inventory and package contracts.
    - `internal/command/test.go` — generic scope dispatcher that must discover `TestScopeSecrets`.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-19/D-20 secret isolation decisions.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — canary/repository scan behavior and `secrets` command.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-PATTERNS.md` — safe error and `.env` handling pattern.
  </read_first>
  <behavior>
    - Committed example contains names and empty values only; real `.env` remains ignored.
    - Sensitive declarations render only set/unset.
    - A unique fake token is absent from every emitted/artifact byte scanned by the command graph.
  </behavior>
  <acceptance_criteria>
    - [ ] `.env.example` contains only empty `LINEAR_API_KEY` and `LINEAR_TEAM_ID` declarations plus optional-remote comments; `.gitignore` still ignores real `.env`.
    - [ ] `SafeDiagnostic`, `Redact`, `SetState`, and `ScanCanary` expose allowlisted fields and never serialize generic environment/header/config/exception objects.
    - [ ] Canary coverage scans stdout, stderr, schemas, maps, reports, manifests, and ZIP inventory bytes configured by the root graph.
    - [ ] `internal/security/redact_test.go` calls `MustDeclareScope` for exact scope `secrets` and defines `TestScopeSecrets` before verification.
    - [ ] `powershell -NoProfile -File .\golc.ps1 test --quick --scope secrets` lists the marker, finds no fake-secret bytes, and exits 0.
  </acceptance_criteria>
  <action>Implement D-19/D-20 without opening any local `.env`. Centralize allowlisted diagnostics and canary scans; prohibit generic environment, header, exception, or configuration-object serialization. Scan stdout, stderr, schemas, maps, reports, manifests, and ZIP inventories addressed by the root graph. Preserve exact ignores and committed example visibility. Explicitly register scope `secrets` through the Plan 17 contract in `internal/security/redact_test.go` beside marker `TestScopeSecrets` before invoking the dispatcher.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope secrets</automated>
  </verify>
  <done>Safe names are documented and fake-secret bytes are absent from all configured output surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove root-command parity and prohibit PR mutation</name>
  <files>tests/acceptance/command-parity.ps1, .github/workflows/check.yml, internal/command/check.go, config/commands.toml</files>
  <read_first>
    - `tests/acceptance/command-parity.ps1` — new parity acceptance path; does not exist before this task.
    - `.github/workflows/check.yml` — new Windows PR workflow; does not exist before this task.
    - `internal/command/check.go` — existing check route modified with `check --command-parity`.
    - `config/commands.toml` — existing authoritative graph modified with PR/network/mutation policy.
    - `golc.ps1` and `internal/delivery/graph.go` — supported root entrypoint and one-owner execution graph.
    - `.env.example` and `internal/security/redact.go` — Task 1 secret boundary/output rules.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-03/D-10/D-16/D-21 CI and remote isolation decisions.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — command-parity acceptance and no-PR-mutation gate.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-PATTERNS.md` — least-privilege Windows workflow pattern.
  </read_first>
  <behavior>
    - Windows PR CI invokes the same root graph as contributors.
    - The workflow has no Linear secret reference, remote trigger, or apply-capable route.
    - Command parity is derived from the canonical graph, not a duplicate hardcoded inventory.
  </behavior>
  <acceptance_criteria>
    - [ ] `.github/workflows/check.yml` runs root bootstrap, generate drift, offline check, build, test, and foundation package on Windows PR events.
    - [ ] The PR workflow contains no Linear secret reference, remote trigger, or apply-capable command.
    - [ ] `config/commands.toml` owns `pr_allowed`, network, and mutation policy; `tests/acceptance/command-parity.ps1` parses it rather than duplicating the inventory.
    - [ ] `internal/command/check.go` calls `MustDeclareRoute` for exact route `check --command-parity` and reports stable mismatched/missing step diagnostics.
    - [ ] `powershell -NoProfile -File .\tests\acceptance\command-parity.ps1` proves parity and mutation unreachability, then exits 0 without credentials.
  </acceptance_criteria>
  <action>Create least-privilege PR validation per D-03/D-10/D-16. Self-register exact route `check --command-parity` through the Plan 17 registry, then run only root bootstrap/generate/check/build/test/package routes. Parse workflow steps against `config/commands.toml`; also assert PR policy marks remote mutation unreachable. Plan 15 adds the final local Linear trace gate after the route and adapter graph are available. Linear absence/failure remains isolated per D-21.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\command-parity.ps1</automated>
  </verify>
  <done>Contributor and PR CI delivery are graph-identical, secret-free, and incapable of remote mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| environment/protected values -> output | Secret material could enter logs or committed artifacts. |
| pull-request branch -> CI router | Untrusted branches must not gain mutation authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-18 | Information Disclosure | diagnostics/artifacts | high | mitigate | Allowlisted fields, set/unset rendering, and cross-artifact canary scans in Task 1. |
| T-01-19 | Elevation of Privilege / Tampering | PR workflow | high | mitigate | No secret/mutation route and graph-parity enforcement in Task 2. |
| T-01-20 | Repudiation | CI/local parity | medium | mitigate | One canonical graph plus executable comparison report. |
| T-01-SC | Tampering | action/tool dependencies | high | mitigate | Exact pinned bootstrap and no npm install in this plan. |
</threat_model>

<verification>
Run the secrets scope and command-parity acceptance; inspect no local `.env` and require no Linear credential.
</verification>

<success_criteria>
- Safe variable names are documented without values.
- Fake-secret scans cover every root-graph output class.
- Windows PR CI uses root commands only and cannot reach remote mutation.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-07-SUMMARY.md` when done.
</output>
