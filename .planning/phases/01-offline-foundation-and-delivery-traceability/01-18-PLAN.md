---
phase: 01-offline-foundation-and-delivery-traceability
plan: 18
type: execute
wave: 6
depends_on: [01-03]
files_modified:
  - internal/projectconfig/registry.go
  - internal/projectconfig/resolve.go
  - internal/projectconfig/path.go
  - internal/projectconfig/resolve_test.go
  - internal/projectconfig/path_test.go
  - tests/golden/config-explain.json
autonomous: true
requirements: [CONF-01, CONF-02, CONF-04]
must_haves:
  truths:
    - "Values resolve committed -> user -> project-local -> environment -> CLI with safe provenance, per D-06 and D-07."
    - "Locked keys reject every higher-layer override and indexed paths remain inside the repository."
    - "Explain output is deterministic and sensitive declarations reveal only set/unset."
  artifacts:
    - path: internal/projectconfig/registry.go
      provides: "One-owner canonical key/reference graph"
    - path: internal/projectconfig/resolve.go
      provides: "Five-layer typed resolution with provenance"
    - path: internal/projectconfig/path.go
      provides: "Lexical/final repository containment"
    - path: tests/golden/config-explain.json
      provides: "Credential-free deterministic provenance"
  key_links:
    - from: internal/projectconfig/resolve.go
      to: internal/projectconfig/registry.go
      via: "ownership/reference checks before precedence merge"
      pattern: "Registry"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Resolve the strict concerns through all five layers with contained paths and deterministic provenance.

Purpose: Make local and CI differences fully explainable without exposing secrets.
Output: Ownership/reference registry, layered resolver, path checks, tests, and provenance golden.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-03-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/projectconfig/registry.go`, `internal/projectconfig/resolve.go`, `internal/projectconfig/path.go`, `internal/projectconfig/resolve_test.go`, `internal/projectconfig/path_test.go`, `tests/golden/config-explain.json`.

- Created the six files listed in frontmatter for registry, resolution, path containment, focused tests, and deterministic explain output.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve all layers with contained paths and safe provenance</name>
  <files>internal/projectconfig/registry.go, internal/projectconfig/resolve.go, internal/projectconfig/path.go, internal/projectconfig/resolve_test.go, internal/projectconfig/path_test.go, tests/golden/config-explain.json</files>
  <read_first>
    - All six owned paths — new registry/resolver/path/test/golden files.
    - `internal/projectconfig/model.go` and `decode.go` — Plan 03 types/diagnostics.
    - `internal/projectconfig/local.go` and `internal/command/config.go` — local persistence and handler interfaces.
    - `internal/command/router.go` and `test.go` — exact scope registration/dispatch contracts.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-06/D-07 exact precedence/provenance.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-PATTERNS.md` — final-path and secret-safe output pattern.
  </read_first>
  <behavior>
    - Every pair in committed/user/project-local/environment/CLI precedence resolves in required order.
    - Locked keys reject every higher-layer attempt.
    - Explain output lists winning/shadowed safe origins; sensitive declarations show set/unset only.
    - Indexed paths reject lexical, symlink, and reparse escapes.
  </behavior>
  <acceptance_criteria>
    - [ ] `ResolveAll` and `ResolveKey` apply the five-layer order for every pair and reject all locked-key override attempts.
    - [ ] `ExplainRecord` contains deterministic winning/shadowed safe origins and renders sensitive declarations as set/unset only.
    - [ ] `ResolveContainedPath` and `ValidateConcernPath` reject absolute, parent, symlink, and reparse escapes.
    - [ ] An owned test file calls `MustDeclareScope` for exact scope `config` and defines `TestScopeConfig` before verification.
    - [ ] The registered config scope exits 0 and `tests/golden/config-explain.json` is byte-stable and credential-free.
  </acceptance_criteria>
  <action>Implement D-06 and D-07 using `%APPDATA%\GOLC\config.toml`, root `golc.local.toml`, an explicit environment allowlist, and typed CLI overrides. Validate ownership, references, and contained paths before merging. Never serialize environment/header/config objects. Explicitly self-register exact test scope `config` through the Plan 17 contract in an owned test file; do not modify the central router.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope config</automated>
  </verify>
  <done>Five-layer resolution, locked-key enforcement, path safety, and deterministic provenance pass through a registered root scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user/local/env/CLI -> resolver | Higher layers may tamper with authority or disclose secrets. |
| concern path -> filesystem | References may escape repository scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-07 | Tampering | resolver | high | mitigate | One-owner registry, locked classes, and complete pairwise tests. |
| T-01-08 | Tampering / Information Disclosure | paths | high | mitigate | Lexical and final containment checks. |
| T-01-09 | Information Disclosure | provenance | high | mitigate | Allowlisted origins and sensitive set/unset output. |
</threat_model>

<verification>
Run the registered config scope and compare its safe explain output with the golden.
</verification>

<success_criteria>
- Exact precedence and provenance are automated.
- Unsafe paths and secret-bearing output fail closed.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-18-SUMMARY.md` when done.
</output>
