---
phase: 01-offline-foundation-and-delivery-traceability
plan: 29
type: execute
wave: 13
depends_on: [01-28, 01-13]
files_modified:
  - config/toolchain.toml
  - go.mod
  - go.sum
  - tools/linear-sync/package.json
  - tools/linear-sync/package-lock.json
  - internal/command/tools.go
  - internal/command/tools_test.go
autonomous: true
requirements: [CONF-01, CONF-03]
must_haves:
  truths:
    - "`tools update --check` and `--write` are explicitly registered, review-only update modes, per D-03 and D-04."
    - "Repeated fake-source checks produce byte-identical toolchain, Go, and npm package/lock proposals; check changes nothing and both modes perform zero install/extract calls."
    - "Write changes only the five declared toolchain, Go manifest/lock, and npm package/lock authorities; bootstrap does not adopt changes until reviewed bytes exist."
    - "The proposed npm package manifest and lock are exact and mutually consistent, with no cache, dependency-tree, or compiled-output changes."
  artifacts:
    - path: internal/command/tools.go
      provides: "Deterministic explicit update routes"
    - path: internal/command/tools_test.go
      provides: "Executable D-04 fake-source/read-only/write-only/no-install proof"
    - path: tools/linear-sync/package-lock.json
      provides: "Exact npm dependency integrity authority covered by explicit updates"
  key_links:
    - from: internal/command/tools.go
      to: config/toolchain.toml
      via: "canonical proposed/written pin and checksum changes"
      pattern: "UpdateProposal|ManifestChange"
    - from: internal/command/tools.go
      to: tools/linear-sync/package.json
      via: "canonical mutually consistent package/lock proposal and write"
      pattern: "package\.json|package-lock\.json"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Deliver executable deterministic `tools update --check|--write` behavior without installing anything.

Purpose: Prove D-04 across toolchain, Go, and npm authorities as a reviewable manifest/lock mutation contract distinct from bootstrap.
Output: Self-registered update handlers and a named fake-source scope covering both modes.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-28-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-13-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `config/toolchain.toml`, `go.mod`, `go.sum`, `tools/linear-sync/package.json`, `tools/linear-sync/package-lock.json`, `internal/command/tools.go`, `internal/command/tools_test.go`.

- Created `internal/command/tools.go` and `tools_test.go`; update write mode owns only `config/toolchain.toml`, `go.mod`, `go.sum`, `tools/linear-sync/package.json`, and `tools/linear-sync/package-lock.json`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove deterministic check/write update proposals with zero installation</name>
  <files>config/toolchain.toml, go.mod, go.sum, tools/linear-sync/package.json, tools/linear-sync/package-lock.json, internal/command/tools.go, internal/command/tools_test.go</files>
  <read_first>
    - All seven owned paths — exact writable toolchain/Go/npm manifests and locks plus new command/tests.
    - `internal/command/router.go` and `test.go` — route/scope registration contract.
    - `internal/bootstrap/bootstrap.go` and `cache.go` — bootstrap read-only input and install-call interfaces.
    - `tools/linear-sync/tsconfig.json` — read-only workspace compiler contract that update modes must not modify.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-03/D-04.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — required `tools-update` scope and fake-source assertions.
  </read_first>
  <behavior>
    - Two `--check` runs against identical fake metadata emit byte-identical proposals.
    - `--check` changes no file; `--write` changes only the five declared reviewable manifest/lock paths.
    - Proposed npm package and lock bytes agree on exact direct versions, resolved package records, integrity, and lockfile metadata.
    - Neither mode calls archive download, extraction, cache warm, tool/package install, dependency-tree creation, or build output generation.
    - Bootstrap still uses the reviewed bytes present on disk and does not silently adopt an uncommitted proposal.
  </behavior>
  <acceptance_criteria>
    - [ ] `internal/command/tools.go` calls `MustDeclareRoute` for exact routes `tools update --check` and `tools update --write` before verification.
    - [ ] `internal/command/tools_test.go` calls `MustDeclareScope` for exact scope `tools-update` and defines `TestScopeToolsUpdate`.
    - [ ] Repeated `--check` runs against the same fake source produce byte-identical proposal/diff bytes for `config/toolchain.toml`, `go.mod`, `go.sum`, `tools/linear-sync/package.json`, and `tools/linear-sync/package-lock.json`, change no file, and record zero installation/extraction calls.
    - [ ] The npm proposal pins exact `@linear/sdk` and TypeScript versions and produces package/lock bytes that agree on direct dependencies, lockfile package entries, resolved versions, integrity, and lockfile version.
    - [ ] `--write` changes only the five declared paths, writes bytes identical to the reviewed proposal, and records zero installation/extraction calls.
    - [ ] Neither mode invokes `npm install`, `npm ci`, archive extraction, cache warming, dependency-tree creation, TypeScript compilation, or any other install/build operation; cache, `node_modules`, and `dist` remain unchanged.
    - [ ] A subsequent bootstrap reads only the resulting reviewed files; proposal output alone cannot change the selected pins, hashes, dependencies, or cache state.
  </acceptance_criteria>
  <action>Implement D-04 with a fake metadata source and injected call counters after the Plan 13 npm workspace exists. Self-register the two exact update routes and exact test scope `tools-update` through the Plan 17 contract. Build one canonical proposal containing exact bytes for the five declared authorities; derive npm manifest and lock changes together so direct dependency pins, resolved package records, integrity fields, and lockfile metadata are mutually consistent. Run check twice and compare canonical proposal/diff bytes while snapshotting the worktree to prove read-only behavior. Run write in a temporary fixture with an exact five-path allowlist and assert every resulting byte equals the reviewed proposal. Keep download, extraction, cache-warm, package-manager, dependency-tree, and build/compile call counters at zero in both modes, and assert cache, `node_modules`, and `dist` bytes are unchanged. Prove bootstrap remains reviewed-file-driven and cannot silently adopt proposal-only state.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope tools-update</automated>
  </verify>
  <done>Both update modes are reachable, deterministic, reviewable, path-bounded, and incapable of installation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| remote metadata -> review proposal | Untrusted release metadata may alter executable pins. |
| proposal -> manifest/lock write | Write mode must keep toolchain, Go, and npm authorities mutually consistent without touching caches or undeclared files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14 | Tampering / Repudiation | update proposal/write | high | mitigate | Canonical repeat diff, exact path allowlist, and zero-install counters. |
| T-01-SC | Tampering | dependency metadata | high | mitigate | Fake-source tests only; reviewed exact versions/locks remain bootstrap inputs. |
</threat_model>

<verification>
Run the named `tools-update` scope; it invokes both modes and checks deterministic/read-only/write-only/no-install behavior.
</verification>

<success_criteria>
- D-04 check and write behavior is executable, deterministic, and installation-free.
- Bootstrap never becomes an updater.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-29-SUMMARY.md` when done.
</output>
