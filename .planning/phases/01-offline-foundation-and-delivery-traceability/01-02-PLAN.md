---
phase: 01-offline-foundation-and-delivery-traceability
plan: 02
type: execute
wave: 4
depends_on: [01-17]
files_modified:
  - internal/command/test.go
  - internal/projectconfig/local.go
  - internal/projectconfig/local_test.go
  - internal/command/config.go
  - golc.ps1
  - .gitignore
  - docs/development.md
  - tests/acceptance/walking-skeleton.ps1
autonomous: true
requirements: [CONF-01, CONF-02, CONF-04]
must_haves:
  truths:
    - "A contributor writes an ignored local runtime value and reads it back through a new CLI process."
    - "Committed then project-local resolution reports the winning layer, safe source, and shadowed origin, per D-06 and D-07."
    - "The root index stays the single discovery point while concern data and machine-local state remain separate, per D-05."
  artifacts:
    - path: internal/command/test.go
      provides: "compile-safe generic test --quick --scope dispatcher with fail-on-zero scope discovery"
    - path: internal/projectconfig/local.go
      provides: "contained atomic local write and two-layer provenance"
    - path: internal/command/config.go
      provides: "config set/explain routes"
    - path: docs/development.md
      provides: "repeatable walking-skeleton contributor sequence"
  key_links:
    - from: internal/command/config.go
      to: internal/projectconfig/local.go
      via: "config set/explain handlers"
      pattern: "WriteLocal|Explain"
    - from: internal/projectconfig/local.go
      to: golc.local.toml
      via: "fixed contained atomic destination"
      pattern: "golc\\.local\\.toml"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Extend the walking skeleton with a real local write and deterministic provenance readback.

Purpose: Give contributors a useful offline configuration interaction before the full precedence/validation refinements.
Output: Safe ignored local persistence, `config set --local`, `config explain`, quick tests, and contributor documentation.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-17-SUMMARY.md
</context>

## Artifacts this phase produces

- Created `internal/command/test.go`: generic `test --quick --scope {scope-name}` dispatcher that accepts safe scope names, resolves exact `TestScope{PascalName}` markers, fails when no marker exists, and invokes only project-local Go tests.
- Created `internal/projectconfig/local.go`: `WriteLocal`, `ResolveRuntime`, `Explain`, and fixed `golc.local.toml` atomic replacement.
- Created `internal/projectconfig/local_test.go`: persistence across processes, locked/unknown-key rejection, safe provenance, and deterministic JSON tests.
- Created `internal/command/config.go`: Task 2 adds `config set --local` and `config explain {key} --format json` through the same handler surface.
- `internal/command/test.go` and `internal/command/config.go` self-register exact routes/scopes through the Plan 17 `CommandRegistry`; the central router is not modified.
- Modified `golc.ps1`: delegates the new routes to the project-local Go command.
- Modified `.gitignore`: preserves `.env`, adds exact `.tools/` and `golc.local.toml` ignores, and keeps examples/contracts/maps/locks trackable.
- Created `docs/development.md`: canonical bootstrap, set, explain, inspect, and quick-test sequence plus explicit Phase 1 adaptation/out-of-scope boundary.
- Modified `tests/acceptance/walking-skeleton.ps1`: green mode now asserts local write, cross-process readback, provenance, and deterministic output.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist and explain one safe local value</name>
  <files>internal/command/test.go, internal/projectconfig/local.go, internal/projectconfig/local_test.go, internal/command/config.go, golc.ps1, .gitignore</files>
  <read_first>
    - `internal/command/test.go` — new generic dispatcher path; does not exist before this task.
    - `internal/projectconfig/local.go` — new local persistence path; does not exist before this task.
    - `internal/projectconfig/local_test.go` — new local/scope test path; does not exist before this task.
    - `internal/command/config.go` — new config command path; does not exist before this task.
    - `internal/command/router.go` — Plan 17 `CommandRegistry` contract; read only, never modified by this plan.
    - `golc.ps1` — existing root shim from Plan 16/17; modified only to preserve delegation.
    - `.gitignore` — existing repository secret boundary; modified narrowly.
    - `internal/projectconfig/load.go` — upstream root/concern signatures used by local resolution.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-05 through D-07 authority, precedence, and provenance.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — generic scope route must precede all named-scope checks.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-PATTERNS.md` — local state, secret isolation, and root-command patterns.
  </read_first>
  <behavior>
    - `runtime.log_level=debug` is written only to repository-root `golc.local.toml` via atomic replacement.
    - A new process resolves `debug` above the committed default and returns layer `project-local`, safe filename, and shadowed committed origin.
    - Unknown/locked keys, path redirection, and attempts to target `.env` fail.
  </behavior>
  <acceptance_criteria>
    - [ ] `internal/command/test.go` self-registers route `test --quick --scope` through `MustDeclareRoute`, rejects unsafe names, derives exact `TestScope{PascalName}` markers, lists them before execution, and returns nonzero when no marker exists.
    - [ ] `internal/projectconfig/local_test.go` registers exact scope `config-local` through `MustDeclareScope` and defines marker `TestScopeConfigLocal` before invocation.
    - [ ] `WriteLocal` writes only repository-root `golc.local.toml` through contained atomic replacement and rejects unknown/locked keys, redirection, and `.env` targets.
    - [ ] `.gitignore` continues to ignore `.env` and newly ignores only `.tools/` and `golc.local.toml`; committed contracts/maps/locks remain trackable.
    - [ ] `powershell -NoProfile -File .\golc.ps1 test --quick --scope config-local` discovers at least one marker and exits 0.
  </acceptance_criteria>
  <action>Implement the first real local mutation per D-06 and D-07. Use a fixed allowlist, deterministic TOML, a contained temporary file, and atomic replacement. Provenance may expose canonical key, value for non-sensitive keys, layer, safe source name, and ordered shadowed origins; it must not serialize environment or credentials. Before invoking verification, create the generic `test --quick --scope {scope-name}` dispatcher in `internal/command/test.go`, self-register its exact route through the Plan 17 `CommandRegistry`, and make it accept only safe scope names, translate a scope to an exact `TestScope{PascalName}` marker, list matching tests first, and fail when zero markers exist. Register exact scope `config-local` through `MustDeclareScope` in `internal/projectconfig/local_test.go` beside `TestScopeConfigLocal`. Preserve `golc.ps1` delegation and never edit the central router. Update ignores narrowly so local/tool state is untracked while committed artifacts remain visible.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope config-local</automated>
  </verify>
  <done>The local value persists safely, resolves with deterministic safe provenance, and its Task 1 unit suite is executable through the registered root route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose and document the local write/read capability</name>
  <files>internal/command/config.go, docs/development.md, tests/acceptance/walking-skeleton.ps1</files>
  <read_first>
    - `internal/command/config.go` — existing Task 1 handler modified with set/explain routes.
    - `docs/development.md` — new contributor document; does not exist before this task.
    - `tests/acceptance/walking-skeleton.ps1` — existing Plan 01-01 acceptance modified for local write/readback.
    - `internal/command/test.go` — upstream generic quick/scope dispatcher used by the documented route.
    - `internal/projectconfig/local.go` — Task 1 `WriteLocal`, `ResolveRuntime`, and `Explain` signatures.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-03/D-05/D-07 user-facing command and provenance contract.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-PATTERNS.md` — developer CLI/data-layer boundary and excluded product scope.
  </read_first>
  <behavior>
    - Root commands set and explain the local value without ecosystem-specific commands.
    - Two unchanged explain calls are byte-identical.
    - Documentation reproduces bootstrap/read/write/readback/quick-test from repository root.
  </behavior>
  <acceptance_criteria>
    - [ ] `internal/command/config.go` exposes and self-registers exact routes `config set --local` and `config explain {key} --format json` through the existing registry.
    - [ ] Two unchanged explain calls are byte-identical and report `project-local`, safe source filename, and ordered shadowed committed origin without environment or credential fields.
    - [ ] `docs/development.md` documents repository-root bootstrap, inspect, set, explain, and quick-test commands plus the explicit Wails/SQLite/NSIS exclusions.
    - [ ] `powershell -NoProfile -File .\tests\acceptance\walking-skeleton.ps1 -Mode green` proves cross-process persistence and exits 0.
  </acceptance_criteria>
<action>Add exact self-registered routes `config set --local` and `config explain {key} --format json` to the config handler, per D-03, while preserving the root-index/concern split in D-05. Reuse the existing root shim and bounded project-local quick-test route. Document the contributor interaction and explain that CLI -> Go registry -> TOML is this walking skeleton's user-interaction/routing/data-layer adaptation. Explicitly exclude Wails UI, SQLite show storage, and NSIS product packaging.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\walking-skeleton.ps1 -Mode green</automated>
  </verify>
  <done>A contributor can set, explain, and retest the value through the documented root command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI value -> ignored local TOML | Contributor input becomes persisted machine-local state. |
| local TOML -> provenance output | Local values and source metadata become observable output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-04 | Tampering | local writer | high | mitigate | Fixed file/key allowlist, containment, and atomic replacement in Task 1. |
| T-01-05 | Information Disclosure | explain output | high | mitigate | Allowlisted provenance fields and no environment/config-object dumping in Tasks 1-2. |
| T-01-06 | Repudiation | provenance | medium | mitigate | Deterministic ordered origins covered by unit and acceptance tests. |
| T-01-SC | Tampering | dependencies | high | mitigate | No package install; all execution uses the verified project-local toolchain from Plan 16. |
</threat_model>

<verification>
Run the local config scope and green walking-skeleton acceptance with no `.env` and no network.
</verification>

<success_criteria>
- A local value survives process restart and is explainable through one root command.
- Local state remains ignored; committed examples/contracts remain trackable.
- The walking skeleton remains developer CLI/data-layer only.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-02-SUMMARY.md` when done.
</output>
