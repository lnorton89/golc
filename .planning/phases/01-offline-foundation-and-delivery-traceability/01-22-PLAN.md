---
phase: 01-offline-foundation-and-delivery-traceability
plan: 22
type: execute
wave: 9
depends_on: [01-09, 01-19, 01-21, 01-23]
files_modified:
  - internal/contracts/linear.go
  - internal/command/linear.go
  - internal/command/linear_validate.go
  - schemas/linear-map.schema.json
autonomous: true
requirements: [LINR-01, LINR-02]
must_haves:
  truths:
    - "The generated map schema is strict, deterministic, registered exactly once as `linear-map`, and traversed by global generation/drift, per D-08."
    - "Exact offline map migrate/status routes are self-registered, while the existing Plan 21 offline validate route is extended without duplicate registration."
    - "Pending/null remote mappings are valid incomplete linkage, not local failure."
  artifacts:
    - path: internal/contracts/linear.go
      provides: "Linear map projection plus exactly-once `linear-map` SchemaDescriptor registration"
    - path: internal/command/linear.go
      provides: "Self-registered offline map migrate/status routes"
    - path: internal/command/linear_validate.go
      provides: "Plan 21-owned offline validation route extended with strict map/schema checks"
    - path: schemas/linear-map.schema.json
      provides: "Strict deterministic map contract"
  key_links:
    - from: internal/contracts/linear.go
      to: internal/contracts/generate.go
      via: "package initialization calls `RegisterSchema(SchemaDescriptor{Name: \"linear-map\", ...})` exactly once"
      pattern: "RegisterSchema.*linear-map"
    - from: internal/contracts/generate.go
      to: schemas/linear-map.schema.json
      via: "`GenerateAll` and `CheckDrift` traverse the sorted registered descriptor snapshot"
      pattern: "RegisteredSchemas|GenerateAll|CheckDrift"
    - from: internal/contracts/linear.go
      to: schemas/linear-map.schema.json
      via: "`linear-map` descriptor factory and repository-relative output path"
      pattern: "LinearMapSchema|linear-map.schema.json"
    - from: internal/command/linear_validate.go
      to: schemas/linear-map.schema.json
      via: "existing validation handler extended with strict schema/catalog/authority checks"
      pattern: "LinearMapSchema|DecodeStrict|BuildCatalog"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Generate the map contract and expose reachable offline migrate/validate/status routes.

Purpose: Make credential-free linkage reviewable and usable without remote availability.
Output: Map schema registration/output, exact self-registered migrate/status routes, and the extended Plan 21 validation handler.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/linear-map.json
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-04-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-09-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-19-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-21-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-23-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/contracts/linear.go`, `internal/command/linear.go`, `internal/command/linear_validate.go`, `schemas/linear-map.schema.json`.

- Created/modified the four schema registration/route/validation/output paths listed in frontmatter; `internal/contracts/linear.go` contributes exactly one `linear-map` descriptor through the Plan 04 API while preserving Plan 21's single validation-route declaration and leaving `internal/contracts/generate.go` untouched.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate the map contract and report pending linkage offline</name>
  <files>internal/contracts/linear.go, internal/command/linear.go, internal/command/linear_validate.go, schemas/linear-map.schema.json</files>
  <read_first>
    - All four owned paths — schema registration, existing Linear command file, Plan 21 validation-route owner, and generated map schema.
    - `internal/command/linear_validate.go` — Plan 21's existing exact route declaration and catalog-only handler; preserve the declaration and extend only the handler.
    - `internal/contracts/generate.go` and `.planning/phases/01-offline-foundation-and-delivery-traceability/01-04-SUMMARY.md` — existing `SchemaDescriptor`, `RegisterSchema`, `RegisteredSchemas`, `GenerateAll`, and `CheckDrift` contracts; read only and do not modify the central generator.
    - `internal/strictjson/decode.go` — strict decode contract.
    - `internal/trace/catalog/migrate.go` and `.planning/linear-map.json` — canonical map.
    - `internal/command/router.go` — exact self-registration API; read only.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-08/D-11/D-12/D-14.
  </read_first>
  <behavior>
    - `internal/contracts/linear.go` registers one descriptor named `linear-map`; global generation/check reaches it once in stable order and writes/checks `schemas/linear-map.schema.json`.
    - The existing offline validate handler now also checks strict JSON/schema/map/catalog/graph/source/authority/credential absence without adding another route declaration.
    - Offline status accepts pending/null linkage while exposing safe counts/local IDs.
  </behavior>
  <acceptance_criteria>
    - [ ] `internal/contracts/linear.go` declares the Linear map projection/factory and invokes `RegisterSchema` exactly once with `SchemaDescriptor{Name: "linear-map", OutputPath: "schemas/linear-map.schema.json", ...}`; registration failure stops initialization with a stable diagnostic.
    - [ ] `RegisteredSchemas` contains `linear-map` exactly once in stable sorted position, and unchanged `GenerateAll`/`CheckDrift` generate/check `schemas/linear-map.schema.json` through that descriptor.
    - [ ] `internal/command/linear.go` calls `MustDeclareRoute` for exact routes `linear map migrate --check`, `linear map migrate --write`, and `linear status --offline`.
    - [ ] `internal/command/linear_validate.go` preserves the single Plan 21 declaration for exact route `linear validate --offline`, extends its existing handler with strict map/schema/catalog/authority validation, and adds no second declaration.
    - [ ] The extended offline validate handler checks all local contract/authority constraints; status emits allowlisted safe fields and treats pending/null linkage as valid.
    - [ ] Registry lookup reaches all exact routes without `.env`, network, Node, or `router.go` modification.
    - [ ] `internal/contracts/generate.go` is unchanged by this plan; generation drift plus offline validate exits 0 for the canonical pending map, proving the registered descriptor is reachable without a second generator list or switch.
  </acceptance_criteria>
  <action>Implement D-08 by declaring the Linear map projection and schema factory in `internal/contracts/linear.go`, then invoke the Plan 04 `RegisterSchema` API exactly once during package initialization with stable name `linear-map` and output `schemas/linear-map.schema.json`; fail initialization deterministically if registration is rejected. Do not modify `internal/contracts/generate.go`: unchanged `GenerateAll` and `CheckDrift` must discover this descriptor through `RegisteredSchemas`. In `internal/command/linear.go`, self-register only exact routes `linear map migrate --check`, `linear map migrate --write`, and `linear status --offline` through the Plan 17 registry. In `internal/command/linear_validate.go`, preserve Plan 21's existing single route declaration and extend only its handler to add strict JSON, generated-schema, canonical-map, catalog-correspondence, authority, source-containment, and credential-absence checks; do not declare that route again because the registry rejects duplicates. Output only safe counts, local IDs, sources, mapping status, and public identifier/URL when present. Missing/malformed/stale local entries fail; absent remote linkage does not.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 generate --check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 linear validate --offline</automated>
  </verify>
  <done>The unchanged global generator reaches the exactly-once `linear-map` descriptor, and the complete pending map validates through the preserved Plan 21 route while map migrate/status routes remain separately registered and reachable offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| map -> validation/status | Malformed linkage or credentials may enter output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-24 | Tampering | map contract/registration | high | mitigate | Strict generated schema, exactly-once named descriptor registration, unchanged sorted global traversal, and canonical validation. |
| T-01-26 | Information Disclosure | status | high | mitigate | Credential-free model and allowlisted output. |
| T-01-SC | Tampering | dependencies | high | mitigate | Existing exact Go lock only. |
</threat_model>

<verification>
Run global generation drift and registered offline map validation/status; require `linear-map` to be discovered exactly once from `internal/contracts/linear.go` with no central-generator edit.
</verification>

<success_criteria>
- Map routes are reachable offline and pending linkage remains valid.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-22-SUMMARY.md` when done.
</output>
