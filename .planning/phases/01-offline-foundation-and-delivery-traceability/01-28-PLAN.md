---
phase: 01-offline-foundation-and-delivery-traceability
plan: 28
type: execute
wave: 8
depends_on: [01-05]
files_modified:
  - golc.ps1
  - internal/bootstrap/bootstrap.go
  - internal/bootstrap/cache.go
  - internal/bootstrap/bootstrap_test.go
  - tests/acceptance/bootstrap.ps1
autonomous: true
requirements: [CONF-01, CONF-03]
must_haves:
  truths:
    - "Pinned Go modules and Wails are warmed into project-local caches, per D-01 and D-02."
    - "A matching second bootstrap performs zero transport/install calls and changes no file."
    - "Bootstrap consumes immutable manifest/lock inputs and never upgrades them, per D-04."
  artifacts:
    - path: internal/bootstrap/cache.go
      provides: "Project-local cache layout and offline environment"
    - path: tests/acceptance/bootstrap.ps1
      provides: "Corrupt retry, warm cache, and idempotent rerun acceptance"
  key_links:
    - from: golc.ps1
      to: internal/bootstrap/cache.go
      via: "project-local environment and cache validation"
      pattern: "GOMODCACHE|GOCACHE|GOTOOLCHAIN"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Warm and validate project-local Go/Wails caches with measurable bootstrap idempotence.

Purpose: Satisfy the post-bootstrap offline boundary without coupling bootstrap to updates.
Output: Cache layout/orchestration and self-contained bootstrap acceptance.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-05-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `golc.ps1`, `internal/bootstrap/bootstrap.go`, `internal/bootstrap/cache.go`, `internal/bootstrap/bootstrap_test.go`, `tests/acceptance/bootstrap.ps1`.

- Modified the root shim/bootstrap tests/orchestrator and created cache handling plus full bootstrap acceptance at the five listed paths.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Warm project-local Go/Wails caches and prove idempotent bootstrap</name>
  <files>golc.ps1, internal/bootstrap/bootstrap.go, internal/bootstrap/cache.go, internal/bootstrap/bootstrap_test.go, tests/acceptance/bootstrap.ps1</files>
  <read_first>
    - All five owned paths — shim/orchestrator/cache/tests/acceptance.
    - `config/toolchain.toml` — Plan 05 exact sources, pins, hashes, and cache rules.
    - `go.mod` and `go.sum` — immutable Go manifest/lock inputs.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-01 through D-04.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — corrupt/retry/second-bootstrap scenarios.
  </read_first>
  <behavior>
    - Project Go installs exact Wails 2.13.0 with local bin/module/build caches and `GOTOOLCHAIN=local`.
    - Installed/cache manifests are validated before no-op.
    - Corrupt first attempt fails; correct retry succeeds; identical rerun performs zero transport/install calls.
  </behavior>
  <acceptance_criteria>
    - [ ] `ProjectCacheLayout` and `OfflineEnvironment` set repository-local `GOBIN`, `GOMODCACHE`, and `GOCACHE` plus `GOTOOLCHAIN=local` and exact Wails 2.13.0.
    - [ ] Installed/cache manifests are validated before no-op; a matching second bootstrap records zero transport/install calls and no file changes.
    - [ ] Bootstrap never invokes an update route and never changes `config/toolchain.toml`, `go.mod`, or `go.sum`.
    - [ ] npm installation remains absent until the legitimacy checkpoint and SDK workspace plan.
    - [ ] `powershell -NoProfile -File .\tests\acceptance\bootstrap.ps1` proves corrupt rejection, correct retry, cache warm, and idempotent rerun.
  </acceptance_criteria>
  <action>Complete the Go/Node archive and Go-module cache portion of D-01/D-02. Validate exact installed/cache manifests before allowing no-op. Assert bootstrap treats the committed pin/checksum manifest and Go manifest/lock as read-only inputs per D-04. Keep every source injected and checksum-controlled; do not install npm packages or call an update feed.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\bootstrap.ps1</automated>
  </verify>
  <done>Clean cache warming and an exact no-op second bootstrap pass without coupling setup to updates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| warmed cache -> offline execution | Cache completeness controls network fallback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-13 | Tampering / Denial of Service | cache manifests | high | mitigate | Exact installed hashes and required-entry validation before no-op. |
| T-01-SC | Tampering | Go/Wails cache | high | mitigate | Exact pins and project-local caches. |
</threat_model>

<verification>
Run the self-contained bootstrap acceptance and confirm zero calls/diff on the second run.
</verification>

<success_criteria>
- Post-bootstrap Go/Wails work has validated local caches.
- Ordinary bootstrap cannot mutate reviewed pins or locks.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-28-SUMMARY.md` when done.
</output>
