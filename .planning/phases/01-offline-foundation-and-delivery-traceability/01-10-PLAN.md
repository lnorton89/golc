---
phase: 01-offline-foundation-and-delivery-traceability
plan: 10
type: execute
wave: 7
depends_on: [01-09]
files_modified:
  - internal/trace/reconcile/model.go
  - internal/trace/reconcile/canonical.go
  - internal/trace/reconcile/marker.go
  - internal/trace/reconcile/reconcile_test.go
  - tests/golden/linear-preview.json
  - tests/golden/linear-conflict-preview.json
autonomous: true
requirements: [LINR-03, LINR-04]
must_haves:
  truths:
    - "Identical intent/map/remote-scope inputs produce byte-stable previews and plan IDs, per D-17."
    - "Visible exact local-ID footers identify objects across renames; titles never do, per D-14."
    - "Operations are dependency-ordered and contain exact pre/postconditions without time/randomness/credentials."
  artifacts:
    - path: internal/trace/reconcile/canonical.go
      provides: "Hash-bound canonical preview"
    - path: internal/trace/reconcile/marker.go
      provides: "Visible parser-stable local-ID footer"
    - path: tests/golden/linear-preview.json
      provides: "Canonical hierarchy preview"
  key_links:
    - from: internal/trace/reconcile/canonical.go
      to: .planning/linear-map.json
      via: "mapping/intent digest inputs"
      pattern: "DigestMapping|PlanID"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Define canonical reconciliation operations, plan hashing, ordering, and visible identity markers.

Purpose: Make remote intent exactly reviewable before snapshot comparison or mutation.
Output: Plan model/canonicalizer/marker, tests, and two goldens.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/linear-map.json
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-09-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/trace/reconcile/model.go`, `internal/trace/reconcile/canonical.go`, `internal/trace/reconcile/marker.go`, `internal/trace/reconcile/reconcile_test.go`, `tests/golden/linear-preview.json`, `tests/golden/linear-conflict-preview.json`.

- Created all six model/canonical/marker/test/golden files listed in frontmatter.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical operations and visible identity markers</name>
  <files>internal/trace/reconcile/model.go, internal/trace/reconcile/canonical.go, internal/trace/reconcile/marker.go, internal/trace/reconcile/reconcile_test.go, tests/golden/linear-preview.json, tests/golden/linear-conflict-preview.json</files>
  <read_first>
    - All six owned paths — new preview contracts/tests/goldens.
    - `internal/trace/catalog/model.go` and `.planning/linear-map.json` — local IDs/hierarchy/owned fields/mapping.
    - `internal/strictjson/decode.go` — canonical strict JSON.
    - `internal/command/router.go` and `test.go` — exact scope registration/dispatch.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-13 through D-18.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md` — operation/footer/hash/order contract.
  </read_first>
  <behavior>
    - Canonical body excludes time/randomness and binds SHA-256 digests.
    - Operations order Project -> Project Milestone -> parent/requirement Issue -> task sub-issue, then local ID.
    - Footer contains exact local ID/mapping schema and round-trips for every entity kind.
  </behavior>
  <acceptance_criteria>
    - [ ] Plan/operation/conflict/intent/marker types encode exact owned fields/preconditions/dependencies without sensitive or nondeterministic fields.
    - [ ] Canonical/digest/plan-ID functions produce stable bytes for identical inputs.
    - [ ] Operations use the required hierarchy/topological order and stable tie-break.
    - [ ] Marker render/parse round-trips exact local ID/schema and validates kind/parent preservation.
    - [ ] `reconcile_test.go` calls `MustDeclareScope` for exact scope `linear-preview-contract`, defines `TestScopeLinearPreviewContract`, and both goldens remain byte-stable.
  </acceptance_criteria>
  <action>Define the D-17 exact preview and visible footer contracts. Each operation carries local ID, remote type/UUID or marker, exact before/after/owned fields/updatedAt/parent preconditions/dependencies. Hash only canonical content. Explicitly register exact scope `linear-preview-contract` through the Plan 17 registry before verification.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-preview-contract</automated>
  </verify>
  <done>Canonical operations, ordering, digests, and footer identity are test-defined and byte-stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| catalog/map -> canonical plan | Multiple authorities become reviewed mutation intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-27 | Tampering / Repudiation | plan binding | high | mitigate | Canonical digests, exact fields, deterministic goldens. |
| T-01-28 | Spoofing | identity marker | high | mitigate | Exact visible footer, kind/parent validation, no title identity. |
</threat_model>

<verification>
Run the registered preview-contract scope and compare repeated golden bytes/plan IDs.
</verification>

<success_criteria>
- Preview format and marker identity are deterministic and exact.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-10-SUMMARY.md` when done.
</output>
