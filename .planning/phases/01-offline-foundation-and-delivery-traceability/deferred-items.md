# Phase 01 Deferred Items

Out-of-scope discoveries logged during plan execution. Not fixed inline per executor scope boundary.

| Found During | Item | Notes |
|--------------|------|-------|
| 01-08 execution (2026-07-20) | Untracked `README.md` at repository root | Appeared outside plan 01-08's task (not created by the executor; 8.5 KB project README describing GOLC and its phases). Content looks intentional and accurate. Needs an owner decision: commit via a docs quick task or its owning plan. Left untracked deliberately — committing it under 01-08 would misattribute authorship. |
| 01-30 execution (2026-07-21) | `TestScopeLinearMap/real_repository_seed_migrates_end_to_end_offline` (internal/trace/catalog/migrate_test.go) fails with `GOLC_MIGRATE_DRIFT`: the committed `.planning/linear-map.json` does not match `catalog.CheckMigration`'s freshly recomputed schema-2 output. Root cause: `.planning/linear-map.json` was last regenerated at commit `4a6a73d` (plan 01-09), but gap-closure plans 01-30/01-31/01-32 were added afterward at commit `3e27be4` — `catalog.BuildCatalog` now dynamically discovers those three new `NN-MM-PLAN.md` files (and their tasks) and expects matching entries in `linear-map.json` that were never written. Confirmed unrelated to this plan's CR-01 change (`internal/command/linear.go`/`runLinearApply`) — `git log --oneline -- .planning/linear-map.json` shows no commit since 4a6a73d, and this plan modifies neither `internal/trace/catalog` nor `.planning/linear-map.json`. Not fixed inline per executor scope boundary; needs an explicit `linear map migrate --write` (or equivalent) run once all of phase 01's gap-closure plans have landed. |
