# Phase 01 Deferred Items

Out-of-scope discoveries logged during plan execution. Not fixed inline per executor scope boundary.

| Found During | Item | Notes |
|--------------|------|-------|
| 01-08 execution (2026-07-20) | Untracked `README.md` at repository root | Appeared outside plan 01-08's task (not created by the executor; 8.5 KB project README describing GOLC and its phases). Content looks intentional and accurate. Needs an owner decision: commit via a docs quick task or its owning plan. Left untracked deliberately — committing it under 01-08 would misattribute authorship. |
| 01-30 execution (2026-07-21) | ~~`TestScopeLinearMap/real_repository_seed_migrates_end_to_end_offline` (internal/trace/catalog/migrate_test.go) fails with `GOLC_MIGRATE_DRIFT`~~ **RESOLVED** (2026-07-21, orchestrator post-Wave-1 gate): root cause was `.planning/linear-map.json` last regenerated at commit `4a6a73d` (plan 01-09) while gap-closure plans 01-30/01-31/01-32 (committed at `3e27be4`) added three new `NN-MM-PLAN.md` files `catalog.BuildCatalog` now discovers. Confirmed unrelated to 01-30/01-31's actual code changes. Fixed by running the offline, credential-free `go run ./cmd/golc-project linear map migrate --write` after Wave 1 merged (all three gap-closure PLAN.md files were already committed, so this did not need to wait for Wave 2/01-32 to execute). `linear map migrate --check` now reports no drift; `go test ./...` passes clean. |
| Post-execution code review (2026-07-21), see 01-REVIEW.md | WR-01: `commitApplyResults` (`internal/command/linear.go:1300-1373`) reimplements `apply.RunApply`'s achieved-prefix rule from scratch instead of using the `Journal` `RunApply` already returns — the two copies could silently diverge if `engine.go`'s rule is ever extended. | Not fixed — correctness risk is latent, not active (both copies are currently identical by construction). Fix: thread `RunApply`'s returned `Journal` through to `commitApplyResults` and delete `achievedApplyPrefix`. Left for a future refactor plan, not this gap-closure round. |
| Post-execution code review (2026-07-21), see 01-REVIEW.md | WR-02: CR-02's regression tests (`mutation.test.ts`, `linear-transport.ps1 -Mode readfailure`) only exercise the Issue-shaped SDK read path, never `project()`/`projectMilestone()`. | Not fixed — the try/catch fix itself wraps the whole `readOperation` switch uniformly, so residual risk is low, but coverage has a gap. Fix: add a project/project_milestone throwing-read test case. Left for a future test-hardening plan. |
| Post-execution code review (2026-07-21), see 01-REVIEW.md | IN-01: `runLinearApply` runs the PR guard and plan-integrity checks twice (once explicitly, once inside `apply.RunApply`) with no comment explaining the intentional fail-fast duplication. | Not fixed — cosmetic/documentation only, no functional impact. Fix: add a one-line comment. Trivial; left for whoever next touches this function. |
