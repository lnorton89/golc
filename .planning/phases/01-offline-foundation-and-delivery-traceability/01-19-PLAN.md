---
phase: 01-offline-foundation-and-delivery-traceability
plan: 19
type: execute
wave: 8
depends_on: [01-04]
files_modified:
  - internal/command/generate.go
  - internal/command/check.go
  - schemas/golc-project.schema.json
  - schemas/config-toolchain.schema.json
  - schemas/config-commands.schema.json
  - schemas/config-generation.schema.json
  - schemas/config-application-defaults.schema.json
  - schemas/config-runtime.schema.json
  - schemas/config-linear.schema.json
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03]
must_haves:
  truths:
    - "Seven committed schemas are deterministic strict review artifacts, per D-08."
    - "Exact root routes `generate`, `generate --check`, and `check --concern project` are self-registered and reachable, per D-03 and D-10."
    - "Generation drift and concern validation run offline with stable results."
  artifacts:
    - path: internal/command/generate.go
      provides: "Self-registered generate routes"
    - path: internal/command/check.go
      provides: "Self-registered project-check route"
    - path: schemas/golc-project.schema.json
      provides: "Strict root-index contract"
  key_links:
    - from: internal/command/generate.go
      to: internal/contracts/generate.go
      via: "GenerateAll/CheckDrift handlers"
      pattern: "MustDeclareRoute|CheckDrift"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Commit the configuration schemas and expose reachable offline generation/project-check commands.

Purpose: Turn the generator into contributor and CI drift gates.
Output: Seven schemas and self-registered `generate`/`check` command files.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-04-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/command/generate.go`, `internal/command/check.go`, `schemas/golc-project.schema.json`, `schemas/config-toolchain.schema.json`, `schemas/config-commands.schema.json`, `schemas/config-generation.schema.json`, `schemas/config-application-defaults.schema.json`, `schemas/config-runtime.schema.json`, `schemas/config-linear.schema.json`.

- Created both command files and all seven schema paths listed in frontmatter.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit schemas and expose offline generation/check routes</name>
  <files>internal/command/generate.go, internal/command/check.go, schemas/golc-project.schema.json, schemas/config-toolchain.schema.json, schemas/config-commands.schema.json, schemas/config-generation.schema.json, schemas/config-application-defaults.schema.json, schemas/config-runtime.schema.json, schemas/config-linear.schema.json</files>
  <read_first>
    - All nine owned paths — new handlers and generated outputs.
    - `internal/contracts/generate.go` and `model.go` — Plan 04 generator contracts.
    - `internal/command/router.go` — Plan 17 self-registration API; read only.
    - `golc.project.toml` and all concern files — authoritative validation inputs.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-03/D-08/D-10.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — drift and concern-check commands.
  </read_first>
  <behavior>
    - `generate --check` returns a stable changed-path diagnostic without writing.
    - `check --concern project` validates root, concerns, authority/reference graph, paths, and generated bytes.
    - All exact routes are present in `NewDefaultCommandRegistry` with network denied.
  </behavior>
  <acceptance_criteria>
    - [ ] All seven listed schemas are generated by Plan 04 code, committed, strict, and source/version marked.
    - [ ] `internal/command/generate.go` calls `MustDeclareRoute` for exact routes `generate` and `generate --check`.
    - [ ] `internal/command/check.go` calls `MustDeclareRoute` for exact route `check --concern project` and composes every strict project check.
    - [ ] Registry lookup reaches all three routes without modifying `internal/command/router.go`; duplicate declarations fail.
    - [ ] The chained generated-drift/project-check command exits 0 clean and nonzero for drift or invalid configuration.
  </acceptance_criteria>
  <action>Generate and commit every listed contract. Self-register exact routes `generate`, `generate --check`, and `check --concern project` through the Plan 17 registry before verification; do not add a central router switch. Per D-10, contributor and CI handlers share structured results. Check mode is read-only and neither route opens network or `.env`.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 generate --check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 check --concern project</automated>
  </verify>
  <done>All configuration contracts are committed and all exact root generation/check routes are registered and green offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated files -> project check | Hand-edited schemas must not become authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10 | Tampering | generated contracts | high | mitigate | Exact source regeneration and byte comparison. |
| T-01-11 | Repudiation | route/drift results | medium | mitigate | Duplicate-safe registration and stable changed-path output. |
| T-01-SC | Tampering | dependencies | high | mitigate | Existing exact lock only; no install. |
</threat_model>

<verification>
Run generation drift then project concern validation with network denied.
</verification>

<success_criteria>
- Seven schemas are reviewable deterministic artifacts.
- Every introduced root route is explicitly registered and reachable.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-19-SUMMARY.md` when done.
</output>
