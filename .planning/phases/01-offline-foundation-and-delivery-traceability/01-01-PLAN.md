---
phase: 01-offline-foundation-and-delivery-traceability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/acceptance/walking-skeleton.ps1
  - tests/fixtures/config/walking-skeleton/golc.project.toml
  - tests/fixtures/config/walking-skeleton/config/toolchain.toml
  - tests/fixtures/config/walking-skeleton/config/runtime.toml
autonomous: true
requirements: [CONF-01, CONF-03]
must_haves:
  truths:
    - "The first Phase 1 deliverable is a failing end-to-end contributor test for the adapted walking skeleton, per D-01 and D-03."
    - "The test distinguishes a missing root command from fixture, copy, or PowerShell failures."
    - "The eventual green path is checksum-first bootstrap followed by deterministic offline runtime inspection, per D-02 and D-04."
  artifacts:
    - path: tests/acceptance/walking-skeleton.ps1
      provides: "Red/bootstrap/green clean-checkout acceptance contract"
    - path: tests/fixtures/config/walking-skeleton/golc.project.toml
      provides: "Contained root-index fixture"
  key_links:
    - from: tests/acceptance/walking-skeleton.ps1
      to: tests/fixtures/config/walking-skeleton/golc.project.toml
      via: "temporary fixture-repository copy"
      pattern: "FixtureRoot|golc\\.project\\.toml"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Define the failing end-to-end test for the adapted Phase 1 walking skeleton.

Purpose: Lock the clean-checkout contributor outcome before bootstrap and routing implementation.
Output: A self-contained red/bootstrap/green acceptance harness and strict TOML fixtures.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/SKELETON.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `tests/acceptance/walking-skeleton.ps1`, `tests/fixtures/config/walking-skeleton/golc.project.toml`, `tests/fixtures/config/walking-skeleton/config/toolchain.toml`, `tests/fixtures/config/walking-skeleton/config/runtime.toml`.

- Created `tests/acceptance/walking-skeleton.ps1`: `Mode=red|bootstrap|green`, `FixtureRoot`, `Invoke-Golc`, `New-ChecksumToolchainFixture`, and failure classification.
- Created the three contained TOML fixtures under `tests/fixtures/config/walking-skeleton/` for root index, exact toolchain archive metadata, and committed runtime value.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing clean-checkout contributor test</name>
  <files>tests/acceptance/walking-skeleton.ps1, tests/fixtures/config/walking-skeleton/golc.project.toml, tests/fixtures/config/walking-skeleton/config/toolchain.toml, tests/fixtures/config/walking-skeleton/config/runtime.toml</files>
  <read_first>
    - `tests/acceptance/walking-skeleton.ps1` — new acceptance path.
    - `tests/fixtures/config/walking-skeleton/golc.project.toml` — new root fixture.
    - `tests/fixtures/config/walking-skeleton/config/toolchain.toml` — new checksum-controlled source fixture.
    - `tests/fixtures/config/walking-skeleton/config/runtime.toml` — new committed runtime fixture.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-01 through D-05 contract.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — red/bootstrap/green sampling contract.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/SKELETON.md` — adapted vertical-MVP boundary.
  </read_first>
  <behavior>
    - Red mode succeeds only because the documented root command is absent.
    - Bootstrap mode constructs a local archive and its expected SHA-256 before invoking bootstrap.
    - Green mode will inspect `runtime.log_level` twice and require byte-identical JSON.
  </behavior>
  <acceptance_criteria>
    - [ ] The harness exposes `-Mode red|bootstrap|green`, `-FixtureRoot`, `Invoke-Golc`, and `New-ChecksumToolchainFixture`.
    - [ ] Red mode exits 0 only for the specifically absent `golc.ps1` implementation and rejects unrelated shell, fixture-copy, and syntax failures.
    - [ ] Bootstrap mode calculates the fixture archive SHA-256 before invoking bootstrap; green mode compares two runtime JSON reads byte-for-byte.
    - [ ] The fixtures contain no credential or `.env` read, and `powershell -NoProfile -File .\tests\acceptance\walking-skeleton.ps1 -Mode red` exits 0.
  </acceptance_criteria>
  <action>Create the first MVP failing end-to-end test per D-01 through D-04. Copy the fixture repository to a temporary directory, construct a checksum-controlled local archive, invoke only the documented root command, and clean all state in `finally`. Keep the boundary strictly contributor CLI -> Go routing -> TOML; do not introduce Wails UI, SQLite show storage, Linear access, credentials, or product packaging.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\walking-skeleton.ps1 -Mode red</automated>
  </verify>
  <done>The live black-box test fails for the missing root implementation while fully defining the checksum-first bootstrap/inspect outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixture input -> temporary checkout | Test-controlled paths and archive metadata are untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | acceptance fixture | high | mitigate | Contained temporary checkout, checksum-first source contract, and exact failure classification in Task 1. |
| T-01-SC | Tampering | package/tool install | high | mitigate | This red plan installs no package and contacts no registry. |
</threat_model>

<verification>
Run red mode from the repository root and confirm no project-local executable, network, `.env`, or Linear state is required.
</verification>

<success_criteria>
- The failing test is specific, self-contained, and expresses the full adapted walking-skeleton outcome.
- The first plan modifies four files and introduces no implementation or external mutation.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-01-SUMMARY.md` when done.
</output>
