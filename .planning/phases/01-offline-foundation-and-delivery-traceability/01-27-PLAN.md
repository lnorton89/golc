---
phase: 01-offline-foundation-and-delivery-traceability
plan: 27
type: execute
wave: 17
depends_on: [01-26]
files_modified:
  - config/commands.toml
  - tools/linear-sync/src/protocol.ts
  - tools/linear-sync/src/redact.ts
  - tools/linear-sync/src/adapter.ts
  - tools/linear-sync/test/redact.test.ts
  - tools/linear-sync/test/mutation.test.ts
  - tests/fixtures/linear/mutation-uncertain.json
autonomous: true
requirements: [CONF-04, LINR-03, LINR-04]
must_haves:
  truths:
    - "Mutation partial/error/timeout returns unknown outcome immediately for Go reconciliation and TypeScript never retries writes, per D-21."
    - "Fake credential/header bytes are absent from every adapter output, per D-20."
    - "Exact scope `linear-transport-node` is registered before invocation."
  artifacts:
    - path: tools/linear-sync/src/redact.ts
      provides: "Allowlisted transport diagnostics/canary scan"
    - path: tests/fixtures/linear/mutation-uncertain.json
      provides: "Commit/timeout unknown-outcome transcript"
  key_links:
    - from: tools/linear-sync/src/adapter.ts
      to: tools/linear-sync/src/redact.ts
      via: "safe uncertain outcome/error emission"
      pattern: "safeError|scanCanary"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Redact transport output and return uncertain writes without adapter retry.

Purpose: Keep credentials out of every byte and preserve Go-owned postcondition recovery.
Output: Redaction helper, protocol/adapter integration, tests, fixture, and exact scope registration.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-26-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `config/commands.toml`, `tools/linear-sync/src/protocol.ts`, `tools/linear-sync/src/redact.ts`, `tools/linear-sync/src/adapter.ts`, `tools/linear-sync/test/redact.test.ts`, `tools/linear-sync/test/mutation.test.ts`, `tests/fixtures/linear/mutation-uncertain.json`.

- Modified graph/protocol/adapter and created redaction helper/two tests/fixture at all seven paths listed.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redact transport output and return uncertain writes without retry</name>
  <files>config/commands.toml, tools/linear-sync/src/protocol.ts, tools/linear-sync/src/redact.ts, tools/linear-sync/src/adapter.ts, tools/linear-sync/test/redact.test.ts, tools/linear-sync/test/mutation.test.ts, tests/fixtures/linear/mutation-uncertain.json</files>
  <read_first>
    - All seven owned paths — graph/protocol/redact/adapter/tests/fixture.
    - `tools/linear-sync/src/errors.ts` — safe normalized errors.
    - `internal/security/redact.go` and `internal/trace/apply/engine.go` — Go canary/postcondition boundaries.
    - `internal/command/test.go` — registered Node scope dispatcher.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-20/D-21.
  </read_first>
  <behavior>
    - Mutation partial/error/timeout returns typed unknown outcome immediately.
    - SDK create/update is never retried by TypeScript.
    - Fake secret/header bytes never appear in emitted results/errors/streams.
  </behavior>
  <acceptance_criteria>
    - [ ] Mutation uncertainty returns typed unknown outcome immediately with zero automatic create/update retry.
    - [ ] Redaction excludes clients, environments, request bodies, generic exceptions, credentials, and non-allowlisted headers from every byte.
    - [ ] Go retains postcondition discovery/reconciliation authority; TypeScript remains transport-only.
    - [ ] `config/commands.toml` registers exact Node scope `linear-transport-node` before invocation.
    - [ ] Owned tests define matching marker `TestScopeLinearTransportNode`, and the registered scope proves zero leakage/write retry.
  </acceptance_criteria>
  <action>Implement D-20/D-21 uncertain-write/redaction behavior. Return typed uncertainty without SDK retry and allow Go to discover postconditions. Register exact scope `linear-transport-node` in the authoritative graph through the Plan 17 registry before invocation and require matching Node markers.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-transport-node</automated>
  </verify>
  <done>Uncertain writes are non-retrying and every transport surface is secret-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SDK errors/headers -> output | Credentials and sensitive metadata may leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-41 | Information Disclosure | errors/headers/streams | high | mitigate | Allowlisted normalization and canary scans. |
| T-01-40 | Tampering | uncertain writes | high | mitigate | No adapter mutation retry; Go postcondition recovery. |
| T-01-SC | Tampering | npm dependencies | high | mitigate | Approved exact lock/cache only. |
</threat_model>

<verification>
Run the registered Node transport scope against fake transcripts only.
</verification>

<success_criteria>
- Uncertain writes are isolated and every transport byte is secret-clean.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-27-SUMMARY.md` when done.
</output>
