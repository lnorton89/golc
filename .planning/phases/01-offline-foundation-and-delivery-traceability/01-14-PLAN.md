---
phase: 01-offline-foundation-and-delivery-traceability
plan: 14
type: execute
wave: 15
depends_on: [01-25]
files_modified:
  - config/commands.toml
  - tools/linear-sync/src/protocol.ts
  - tools/linear-sync/src/pagination.ts
  - tools/linear-sync/src/adapter.ts
  - tools/linear-sync/test/pagination.test.ts
  - tests/fixtures/linear/paginated-51.json
  - tests/fixtures/linear/cursor-loop.json
autonomous: true
requirements: [CONF-04, LINR-03, LINR-04]
must_haves:
  truths:
    - "Every Relay page is exhausted with repeated/null cursor detection and explicit complete/page/object diagnostics."
    - "A page-two exact footer prevents duplicate create; ambiguity/incompleteness never reaches preview."
    - "Exact Node scope `linear-transport-pagination` is registered before invocation."
  artifacts:
    - path: tools/linear-sync/src/pagination.ts
      provides: "Exhaustive cursor-safe Relay reads"
    - path: tests/fixtures/linear/paginated-51.json
      provides: "Page-two marker completeness oracle"
  key_links:
    - from: tools/linear-sync/src/adapter.ts
      to: tools/linear-sync/src/pagination.ts
      via: "all connection reads"
      pattern: "fetchAllPages"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Exhaust every Linear page and block incomplete identity discovery through a registered test scope.

Purpose: Ensure later previews never infer absence or uniqueness from a first page.
Output: Pagination protocol/helper/adapter integration, tests, fixtures, and exact scope registration.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-25-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `config/commands.toml`, `tools/linear-sync/src/protocol.ts`, `tools/linear-sync/src/pagination.ts`, `tools/linear-sync/src/adapter.ts`, `tools/linear-sync/test/pagination.test.ts`, `tests/fixtures/linear/paginated-51.json`, `tests/fixtures/linear/cursor-loop.json`.

- Modified the command graph/protocol/adapter and created pagination helper/test/two fixtures at all seven paths listed in frontmatter.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exhaust every page and block incomplete identity discovery</name>
  <files>config/commands.toml, tools/linear-sync/src/protocol.ts, tools/linear-sync/src/pagination.ts, tools/linear-sync/src/adapter.ts, tools/linear-sync/test/pagination.test.ts, tests/fixtures/linear/paginated-51.json, tests/fixtures/linear/cursor-loop.json</files>
  <read_first>
    - All seven owned paths — scope graph, protocol/pagination/adapter/test/fixtures.
    - `internal/trace/reconcile/marker.go` and `internal/trace/transport/contract.go` — Go marker/completeness shapes.
    - `internal/command/test.go` — exact registered Node scope dispatcher.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-14/D-21.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — 51-object/page-two/cursor cases.
  </read_first>
  <behavior>
    - Fifty-one objects consume two pages and find the exact footer on page two.
    - Repeated/null cursors produce complete=false and never permit create/preview.
    - Snapshot cannot mark complete until all intended connections end.
  </behavior>
  <acceptance_criteria>
    - [ ] `fetchAllPages` consumes both pages, records counts, and finds the exact page-two footer.
    - [ ] Cursor anomalies produce safe complete=false diagnostics and block identity decisions.
    - [ ] Adapter completeness covers every intended connection and preserves one/multiple marker semantics.
    - [ ] `config/commands.toml` registers exact Node scope `linear-transport-pagination` through the Plan 17 registry contract before invocation.
    - [ ] `pagination.test.ts` defines matching marker `TestScopeLinearTransportPagination`, and the registered scope exits 0.
  </acceptance_criteria>
  <action>Route every SDK connection through exhaustive pagination. Track cursor history, reject anomalies, narrow intended scopes, and expose completeness metrics. Register exact Node scope `linear-transport-pagination` in the authoritative command graph before verification and require the marker to match; never rely on implicit marker discovery alone.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-transport-pagination</automated>
  </verify>
  <done>Remote reads are exhaustive and identity decisions cannot use incomplete pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Linear pagination -> snapshot | Hidden later pages may spoof absence/uniqueness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-39 | Spoofing / Tampering | pagination/marker discovery | high | mitigate | Exhaust pages, cursor anomaly block, completeness diagnostics. |
| T-01-SC | Tampering | npm dependencies | high | mitigate | Approved exact lock/cache only. |
</threat_model>

<verification>
Run the registered pagination scope against fake fixtures only.
</verification>

<success_criteria>
- All pagination is exhaustive, explicit, and scope-registered.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-14-SUMMARY.md` when done.
</output>
