---
phase: 01-offline-foundation-and-delivery-traceability
plan: 12
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [CONF-03, LINR-03, LINR-04]
must_haves:
  truths:
    - "No npm install runs until the user verifies the two exact recently published packages against official registry/source evidence."
    - "Approval is exact-version scoped and never auto-approved."
  artifacts: []
  key_links: []
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Obtain the mandatory human legitimacy approval for the exact Linear SDK and TypeScript pins before any npm installation.

Purpose: Enforce the package supply-chain gate recorded by research.
Output: A blocking approval or a safe stop with no package-manager execution.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
</context>

## Artifacts this phase produces

- No repository file is created or modified. The execution summary records the exact human approval text or the rejected package and safe stop.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the two exact recent npm packages</name>
  <files>none</files>
  <action>Before any npm command, present `@linear/sdk@88.1.0` and `typescript@7.0.2`, their official Linear/Microsoft source repositories, registry integrity metadata recorded in research, absence of lifecycle/postinstall scripts, and registry version pages `https://www.npmjs.com/package/@linear/sdk/v/88.1.0` and `https://www.npmjs.com/package/typescript/v/7.0.2`. Require explicit confirmation of both exact pins. This checkpoint cannot be auto-approved; rejection or uncertainty stops Plans 13-15 and 25-27 without installation.</action>
  <verify>
    <automated>$p = Get-Content -Raw -LiteralPath '.planning\phases\01-offline-foundation-and-delivery-traceability\01-RESEARCH.md'; if ($p -match '@linear/sdk@88\.1\.0' -and $p -match 'typescript@7\.0\.2' -and $p -match 'no postinstall') { exit 0 }; exit 1</automated>
    <human-check>Open both exact npm registry version pages; confirm official source ownership, exact versions, recorded integrity, and no install lifecycle script.</human-check>
  </verify>
  <done>The user explicitly approves both exact pins, or execution stops with no npm install.</done>
  <resume-signal>Type "approved @linear/sdk@88.1.0 and typescript@7.0.2" or name the rejected package.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry/source evidence -> approval | Recently published package identity must be human-verified before executable code enters caches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-35 | Spoofing / Tampering | npm package identity | high | mitigate | Blocking exact-version registry/source/integrity verification with no auto-approval. |
| T-01-SC | Tampering | npm install | high | mitigate | Plans 13-15 depend on this checkpoint; no install is permitted before approval. |
</threat_model>

<verification>
Automated research-audit presence passes and the human gives exact-version approval.
</verification>

<success_criteria>
- Both exact pins are approved against official evidence before npm executes.
- Rejection leaves package/cache/worktree state unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-12-SUMMARY.md` when done.
</output>
