---
phase: 01-offline-foundation-and-delivery-traceability
plan: 13
type: execute
wave: 12
depends_on: [01-06, 01-07, 01-23, 01-12]
files_modified:
  - golc.ps1
  - internal/bootstrap/cache.go
  - config/toolchain.toml
  - config/commands.toml
  - tools/linear-sync/package.json
  - tools/linear-sync/package-lock.json
  - tools/linear-sync/tsconfig.json
  - tools/linear-sync/src/protocol.ts
  - tools/linear-sync/src/adapter.ts
autonomous: true
requirements: [CONF-01, CONF-03, LINR-03]
must_haves:
  truths:
    - "After exact package approval, root bootstrap uses project-local Node/npm, exact lock integrity/cache, and compiles the adapter, per D-01/D-03."
    - "Bootstrap never rewrites package pins/lock; after this workspace exists, Plan 01-29 extends the explicit D-04 route to propose exact npm package/lock updates."
    - "Every required Linear entity has discriminated read/create/update/readback transport shapes while Go retains policy."
    - "Exact build/test scopes are registered before later SDK tests invoke them."
  artifacts:
    - path: tools/linear-sync/src/protocol.ts
      provides: "Discriminated complete Linear operation contract"
    - path: tools/linear-sync/src/adapter.ts
      provides: "Official SDK mapping without reconciliation policy"
    - path: tools/linear-sync/package-lock.json
      provides: "Approved exact npm integrity lock"
  key_links:
    - from: golc.ps1
      to: tools/linear-sync/package-lock.json
      via: "project-local exact-lock npm ci and compile"
      pattern: "npm.*ci|package-lock"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Provision, compile, and register the exact approved Linear SDK workspace and hierarchy protocol.

Purpose: Close the exact-lock bootstrap/build boundary before SDK contract and offline-repeat tests.
Output: Private workspace/lock/compiler config, protocol/adapter, and registered graph scopes.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-12-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-23-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `golc.ps1`, `internal/bootstrap/cache.go`, `config/toolchain.toml`, `config/commands.toml`, `tools/linear-sync/package.json`, `tools/linear-sync/package-lock.json`, `tools/linear-sync/tsconfig.json`, `tools/linear-sync/src/protocol.ts`, `tools/linear-sync/src/adapter.ts`.

- Modified root/cache/toolchain/command graph and created the five workspace/protocol/adapter paths listed in frontmatter.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provision, compile, and register the exact SDK workspace</name>
  <files>golc.ps1, internal/bootstrap/cache.go, config/toolchain.toml, config/commands.toml, tools/linear-sync/package.json, tools/linear-sync/package-lock.json, tools/linear-sync/tsconfig.json, tools/linear-sync/src/protocol.ts, tools/linear-sync/src/adapter.ts</files>
  <read_first>
    - All nine owned paths — root/cache/manifests/graph/workspace/protocol/adapter.
    - `internal/trace/transport/contract.go` — Go transport shapes.
    - `internal/delivery/graph.go`, `internal/command/router.go`, and `test.go` — graph/route/scope consumers.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-12-SUMMARY.md` — exact-pin human approval.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-01 through D-04/D-11/D-17.
  </read_first>
  <behavior>
    - Only project-local Node/npm runs exact-lock `npm ci` with lifecycle scripts disabled and compiles protocol/adapter.
    - Exact build scope `linear-sdk` and Node test scope `linear-sdk-operations` are registered before invocation.
    - Each required entity has explicit read/create/update request and normalized result variants.
    - Unknown operation/kind/result shapes fail strict decoding.
  </behavior>
  <acceptance_criteria>
    - [ ] Private package manifest pins exactly `@linear/sdk` 88.1.0 and TypeScript 7.0.2, has build/test only, and lock integrity matches approval.
    - [ ] Bootstrap uses only project-local Node/npm, exact lock/cache, disabled lifecycle scripts, and never rewrites package/lock/pins.
    - [ ] Operation union contains explicit read/create/update variants for Project, Project Milestone, parent Issue, requirement Issue, and task sub-issue; mutations require readback.
    - [ ] `config/commands.toml` registers exact scopes `linear-sdk` and `linear-sdk-operations` through the Plan 17 registry contract before Plan 25 tests.
    - [ ] `bootstrap --include linear-sync` then `build --scope linear-sdk` produces compiled protocol/adapter output and exits 0 after approval.
  </acceptance_criteria>
  <action>Only after Plan 12 approval, create the exact private workspace and lock using project-local Node/npm. Define discriminated read/create/update variants for all required entity roles and require readback outcomes. Run exact-lock install with lifecycle scripts disabled, warm/validate the npm cache, and compile protocol/adapter. In `config/commands.toml`, explicitly register exact build scope `linear-sdk` and test scope `linear-sdk-operations` through the command registry before any later invocation. Go retains D-11/D-13/D-17 policy.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 bootstrap --include linear-sync; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 build --scope linear-sdk</automated>
  </verify>
  <done>The approved exact workspace is installed, cache-warmed, compiled, and its exact scopes are registered before SDK tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| approved npm registry/lock -> local executable cache | Packages enter developer tooling. |
| discriminated RPC -> SDK methods | Generic/malformed operations could bypass hierarchy contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-36 | Tampering | npm install/cache | high | mitigate | Exact approved lock, project npm, disabled lifecycle scripts, integrity validation. |
| T-01-37 | Spoofing / Tampering | SDK protocol | high | mitigate | Exhaustive discriminated variants and strict decoding. |
| T-01-SC | Tampering | npm supply chain | high | mitigate | Blocking Plan 12 approval plus exact lock/integrity. |
</threat_model>

<verification>
Run bootstrap/build only after exact approval; confirm registered scopes and unchanged manifests.
</verification>

<success_criteria>
- Exact approved workspace compiles through project-local tooling and exposes no merge policy.
- Required scopes are registered before downstream tests.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-13-SUMMARY.md` when done.
</output>
