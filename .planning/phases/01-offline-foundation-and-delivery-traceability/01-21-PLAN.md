---
phase: 01-offline-foundation-and-delivery-traceability
plan: 21
type: execute
wave: 6
depends_on: [01-08]
files_modified:
  - internal/command/linear.go
  - internal/command/linear_validate.go
  - internal/trace/catalog/catalog_test.go
  - tests/fixtures/linear/map-duplicate-id.json
  - tests/fixtures/linear/map-invalid-parent.json
  - tests/fixtures/linear/map-cycle.json
  - tests/fixtures/linear/map-unsafe-source.json
  - tests/fixtures/linear/catalog-rename-before.json
  - tests/fixtures/linear/catalog-rename-after.json
autonomous: true
requirements: [LINR-01, LINR-02]
must_haves:
  truths:
    - "Exact route `linear catalog --offline --format json` is self-registered and deterministic."
    - "Exact route `linear validate --offline` is self-registered once in a compile-safe dedicated command file before this plan invokes it."
    - "Rename fixtures preserve local IDs/mapped UUIDs while invalid fixtures receive specific diagnostics."
    - "Catalog inspection needs no `.env`, network, Node, SDK, or remote mapping."
  artifacts:
    - path: internal/command/linear.go
      provides: "Self-registered offline catalog route"
    - path: internal/command/linear_validate.go
      provides: "Initial self-registered offline validation route over the repository catalog"
    - path: internal/trace/catalog/catalog_test.go
      provides: "Executable adversarial and rename fixture coverage"
    - path: tests/fixtures/linear/catalog-rename-before.json
      provides: "Rename-stability fixture pair"
  key_links:
    - from: internal/command/linear.go
      to: internal/trace/catalog/parse.go
      via: "offline catalog handler"
      pattern: "MustDeclareRoute|BuildCatalog"
    - from: internal/command/linear_validate.go
      to: internal/trace/catalog/validate.go
      via: "single self-registered offline validation handler over the complete catalog"
      pattern: "MustDeclareRoute|BuildCatalog|Validate"
    - from: internal/trace/catalog/catalog_test.go
      to: tests/fixtures/linear
      via: "table-driven loading of every adversarial and rename fixture"
      pattern: "map-duplicate-id|map-invalid-parent|map-cycle|map-unsafe-source|catalog-rename"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Expose deterministic offline catalog inspection and exact rename/adversarial fixtures.

Purpose: Make the local identity catalog directly useful to contributors before mapping.
Output: Reachable Linear catalog/validation routes, executable catalog fixture tests, and six fixture files.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/linear-map.json
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-08-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/command/linear.go`, `internal/command/linear_validate.go`, `internal/trace/catalog/catalog_test.go`, `tests/fixtures/linear/map-duplicate-id.json`, `tests/fixtures/linear/map-invalid-parent.json`, `tests/fixtures/linear/map-cycle.json`, `tests/fixtures/linear/map-unsafe-source.json`, `tests/fixtures/linear/catalog-rename-before.json`, `tests/fixtures/linear/catalog-rename-after.json`.

- Modified the catalog route/test, created the dedicated offline-validation route owner, and created the six exact fixture files listed in frontmatter.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove rename stability and expose offline catalog inspection</name>
  <files>internal/command/linear.go, internal/command/linear_validate.go, internal/trace/catalog/catalog_test.go, tests/fixtures/linear/map-duplicate-id.json, tests/fixtures/linear/map-invalid-parent.json, tests/fixtures/linear/map-cycle.json, tests/fixtures/linear/map-unsafe-source.json, tests/fixtures/linear/catalog-rename-before.json, tests/fixtures/linear/catalog-rename-after.json</files>
  <read_first>
    - All nine owned paths — catalog route, dedicated offline-validation route owner, executable catalog test, and adversarial/rename fixtures.
    - `internal/command/linear_validate.go` — new compile-safe owner of the exact `linear validate --offline` declaration and catalog-only handler that Plan 22 will extend.
    - `internal/trace/catalog/model.go`, `parse.go`, and `validate.go` — Plan 08 interfaces.
    - `internal/command/router.go` — self-registration API; read only.
    - `.planning/linear-map.json` — nullable mapping fields preserved across rename.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-14/offline authority.
  </read_first>
  <behavior>
    - Rename fixtures preserve local IDs and mapped UUID fields.
    - Invalid fixtures return specific local-ID/parent/source diagnostics.
    - `linear validate --offline` is reachable in this wave and validates catalog identity, parent, cycle, source-containment, and repository-authority constraints without map-schema dependencies.
    - Offline output is deterministic and safe.
  </behavior>
  <acceptance_criteria>
    - [ ] `internal/trace/catalog/catalog_test.go` loads all six fixture paths: duplicate ID emits `CATALOG_DUPLICATE_ID`, invalid parent emits `CATALOG_INVALID_PARENT`, cycle emits `CATALOG_CYCLE`, and unsafe source/anchor emits `CATALOG_UNSAFE_SOURCE`.
    - [ ] The same test loads rename-before/after and proves every durable local ID and mapped immutable UUID is identical while display text changes.
    - [ ] `internal/command/linear.go` calls `MustDeclareRoute` for exact route `linear catalog --offline --format json`.
    - [ ] `internal/command/linear_validate.go` is the single owner that calls `MustDeclareRoute` for exact route `linear validate --offline` before Task 1 verification; its initial handler compiles against Plan 08 catalog interfaces and validates catalog identity, graph, source, and authority constraints.
    - [ ] Registry lookup reaches the route without importing Node/SDK/remote transport or modifying `router.go`.
    - [ ] The registered `linear-catalog` scope runs the fixture assertions before the Plan 21-owned deterministic offline validation route, which contains only allowlisted catalog fields.
  </acceptance_criteria>
  <action>Self-register exact route `linear catalog --offline --format json` through the Plan 17 registry. Create `internal/command/linear_validate.go` as the sole route-owning file for exact route `linear validate --offline`: declare it through `MustDeclareRoute` and implement a compile-safe initial handler using only the Plan 08 catalog model/parser/validator so the route is reachable before this task verifies it. The initial handler validates durable identity, parent/cycle topology, contained sources, and repository authority, then emits only allowlisted catalog fields; Plan 22 will extend this same handler with map/schema checks without redeclaring the route. Add exact fixtures for every graph failure plus display renames. Extend `internal/trace/catalog/catalog_test.go` with table-driven cases that open every new fixture: assert duplicate-ID, invalid-parent, cycle, and unsafe source/anchor diagnostics individually, then compare both rename fixtures to prove stable local IDs and mapped immutable UUIDs with display-only changes. Preserve the existing `linear-catalog` scope declaration/marker and keep all validation offline. Run verification in the specified order: the `linear-catalog` scope must pass before the offline validation route is invoked.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-catalog; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File .\golc.ps1 linear validate --offline</automated>
  </verify>
  <done>Every adversarial/rename fixture is consumed by the catalog test, diagnostics are specific, mapped identity survives renames, and the Plan 21-owned validation route is registered and validates the complete catalog offline after the catalog scope passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source refs -> repository navigation | Stored paths/anchors may escape or disclose host paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-21 | Spoofing / Tampering | route/fixtures | high | mitigate | Exact registration plus ID/rename/adversarial validation. |
| T-01-23 | Information Disclosure | catalog output | high | mitigate | Contained sources and allowlisted fields. |
| T-01-SC | Tampering | dependencies | high | mitigate | No package installation. |
</threat_model>

<verification>
Run the exact offline route without external state.
</verification>

<success_criteria>
- Offline catalog inspection is reachable and rename/invalid cases are automated.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-21-SUMMARY.md` when done.
</output>
