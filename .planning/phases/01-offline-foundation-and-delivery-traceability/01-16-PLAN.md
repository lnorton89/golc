---
phase: 01-offline-foundation-and-delivery-traceability
plan: 16
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - golc.ps1
  - go.mod
  - go.sum
  - internal/bootstrap/bootstrap.go
  - internal/bootstrap/bootstrap_test.go
  - config/toolchain.toml
  - tests/acceptance/walking-skeleton.ps1
autonomous: true
requirements: [CONF-01, CONF-03]
must_haves:
  truths:
    - "One root bootstrap provisions a checksum-verified project-local command before tests invoke it, per D-01."
    - "Bootstrap uses exact pins, local caches, and `GOTOOLCHAIN=local`; it never silently updates, per D-02 and D-04."
    - "The successful online bootstrap caches `github.com/BurntSushi/toml@v1.6.0`, `github.com/invopop/jsonschema@v0.14.0`, and their required transitive modules so a subsequent network-denied schema-generation probe resolves without download or lock mutation."
  artifacts:
    - path: golc.ps1
      provides: "PowerShell 5.1 bootstrap/delegation shim"
    - path: internal/bootstrap/bootstrap.go
      provides: "Verified staged local tool installation"
    - path: go.mod
      provides: "Pinned TOML v1.6.0 and JSON Schema v0.14.0 module authority"
    - path: go.sum
      provides: "Matching direct and required transitive module integrity sums"
    - path: tests/acceptance/walking-skeleton.ps1
      provides: "Online cache-warm followed by network-denied module/schema probe"
  key_links:
    - from: golc.ps1
      to: config/toolchain.toml
      via: "exact immutable bootstrap pins"
      pattern: "toolchain\\.toml"
    - from: tests/acceptance/walking-skeleton.ps1
      to: internal/bootstrap/bootstrap.go
      via: "checksum-controlled bootstrap mode"
      pattern: "InstallStaged|InstalledMatches"
    - from: go.mod
      to: .tools/cache/go-mod
      via: "online `go mod download all` followed by `GOPROXY=off` readonly resolution"
      pattern: "BurntSushi/toml|invopop/jsonschema"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Implement the checksum-controlled bootstrap half of the walking skeleton.

Purpose: Make clean-checkout verification provision the pinned project-local command before any Go test runs.
Output: Root shim, exact Go module/integrity lock, bootstrap implementation/tests, online cache-warm/offline schema probe, and committed toolchain concern.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `golc.ps1`, `go.mod`, `go.sum`, `internal/bootstrap/bootstrap.go`, `internal/bootstrap/bootstrap_test.go`, `config/toolchain.toml`, `tests/acceptance/walking-skeleton.ps1`.

- Created the root/module/bootstrap/toolchain files and extended the walking-skeleton bootstrap mode to warm and verify the exact Go dependency graph online, then rerun the module/schema probe with network denied.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provision the verified project-local command before testing it</name>
  <files>golc.ps1, go.mod, go.sum, internal/bootstrap/bootstrap.go, internal/bootstrap/bootstrap_test.go, config/toolchain.toml, tests/acceptance/walking-skeleton.ps1</files>
  <read_first>
    - `golc.ps1`, `go.mod`, `go.sum` — new root shim and Go authority files; pin both researched modules and preserve all direct/transitive integrity sums.
    - `internal/bootstrap/bootstrap.go`, `internal/bootstrap/bootstrap_test.go` — new bootstrap implementation/tests plus a compile/run probe that imports TOML and reflects a schema with Invopop.
    - `config/toolchain.toml` — new committed exact-pin concern.
    - `tests/acceptance/walking-skeleton.ps1` — Plan 01 red/bootstrap/green contract, now owned here for the online-warm/network-denied bootstrap sequence.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md` — Standard Stack exact pins, Installation and Bootstrap Contract, Package Legitimacy Audit, and project-local module-cache policy.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-VALIDATION.md` — Plan 16 online bootstrap plus offline module/schema verification command.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-PATTERNS.md` — two-stage PowerShell-to-Go boundary.
  </read_first>
  <behavior>
    - Matching SHA-256 bytes extract to staging and promote atomically; mismatches leave no install.
    - A matching installed manifest makes the second bootstrap perform zero archive-source calls.
    - Project-local paths and `GOTOOLCHAIN=local` prevent host fallback.
    - Online bootstrap downloads the complete `go.mod` graph into the project-local module cache; the following `GOPROXY=off`/readonly probe resolves both direct modules, verifies required transitive sums, and emits schema bytes without changing `go.mod`, `go.sum`, or the cache.
  </behavior>
  <acceptance_criteria>
    - [ ] `go.mod` declares `github.com/lawrence/golc`, Go `1.26.5`, `github.com/BurntSushi/toml v1.6.0`, and `github.com/invopop/jsonschema v0.14.0`; `go.sum` contains matching direct and required transitive integrity entries.
    - [ ] `VerifyArchive`, `InstallStaged`, and `InstalledMatches` reject checksum/traversal failures before promotion and make a matching second install call no archive source.
    - [ ] `golc.ps1` sets repository-local bin/cache paths and `GOTOOLCHAIN=local`, with no host Go fallback after bootstrap begins.
    - [ ] Successful online bootstrap runs `go mod download all` into the repository-local `GOMODCACHE`, records/verifies the complete selected module graph and sums, and confirms both exact direct module versions are present before reporting success.
    - [ ] `bootstrap_test.go` resolves TOML and invokes Invopop reflection on a probe struct; `walking-skeleton.ps1 -Mode bootstrap` reruns that probe with `GOPROXY=off`, readonly module mode, and a network transport that fails any call, while asserting zero downloads and byte-identical `go.mod`, `go.sum`, and warmed-cache inventory.
    - [ ] `powershell -NoProfile -File .\tests\acceptance\walking-skeleton.ps1 -Mode bootstrap` completes the online warm and subsequent network-denied schema-generation probe before exiting 0.
  </acceptance_criteria>
  <action>Implement D-01, D-02, and D-04. Pin `github.com/BurntSushi/toml@v1.6.0` and `github.com/invopop/jsonschema@v0.14.0` in `go.mod` with the complete matching `go.sum` graph. Production tool pins come only from `config/toolchain.toml`; the acceptance archive source is permitted only when the harness explicitly injects it. Verify bytes and contained archive entries before extraction, promote atomically, record installed hashes, and reject missing offline artifacts with stable diagnostics. During the successful online bootstrap, set the repository-local `GOMODCACHE`/`GOCACHE`, run `go mod download all`, verify the selected graph and sums, and compile/run a bootstrap test probe that decodes TOML and emits JSON Schema through Invopop. Extend bootstrap mode so its immediately subsequent pass sets `GOPROXY=off` and readonly module mode, installs a fail-on-call network transport, reruns the schema probe, and compares manifest/lock/cache inventories to prove resolution required neither download nor mutation. Bootstrap must never consult an update feed or rewrite pins.</action>
  <verify>
    <automated>powershell -NoProfile -File .\tests\acceptance\walking-skeleton.ps1 -Mode bootstrap</automated>
  </verify>
  <done>The acceptance harness provisions a checksum-controlled project-local command, warms both exact Go dependencies and their transitive graph online, then proves schema generation resolves unchanged from the local cache with network denied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| archive source -> `.tools` | Executable bytes enter project-local state. |
| Go proxy/checksum data -> project-local module cache | Dependency source and integrity data become offline execution inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | bootstrap archive | high | mitigate | SHA-256, traversal rejection, staging, and atomic promotion in Task 1. |
| T-01-SC | Tampering / Denial of Service | Go dependencies/cache | high | mitigate | Exact TOML/JSONSchema versions, complete `go.sum`, online `go mod download all`, selected-graph verification, and a `GOPROXY=off` readonly schema probe against the unchanged project-local cache; no npm installation. |
</threat_model>

<verification>
Run bootstrap mode from the repository root; require its online cache warm and immediately subsequent network-denied schema-generation probe to pass before any Plan 04 contract work.
</verification>

<success_criteria>
- Bootstrap provisions exact local tooling and is idempotent against matching installed state.
- Both exact Go modules and required transitive sums are present and verified in the project-local cache, and the offline probe performs zero download or lock/cache mutation.
- No route, Wails UI, Linear service, or product packaging is added.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-16-SUMMARY.md` when done.
</output>
