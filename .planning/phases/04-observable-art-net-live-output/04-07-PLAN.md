---
phase: 04-observable-art-net-live-output
plan: 07
type: execute
wave: 7
depends_on: [04-06]
files_modified:
  - docs/artnet/ARTN-06-verification-runbook.md
autonomous: false
requirements: [ARTN-06]
user_setup:
  - service: Wireshark
    why: "Independent packet-level inspection of Art-Net 4 output (addressing, sequencing, payload length) — D-13/D-15."
    dashboard_config:
      - task: "Install Wireshark (Art-Net/DMX dissector ships in core, no plugin download) on a machine that can capture GOLC's pinned-interface traffic."
        location: "wireshark.org"
  - service: Open Lighting Architecture (OLA)
    why: "Independent receiving simulator/node for per-universe value + target behavior verification — D-13/D-14."
    dashboard_config:
      - task: "Run OLA on a separate Linux/macOS host or a bridged-adapter Linux VM (NOT NAT/WSL2 default) reachable from GOLC's pinned Windows interface, since OLA has no first-class native Windows build — D-18/Pitfall 6."
        location: "openlighting.org"
must_haves:
  truths:
    - "ARTN-06's acceptance tools are Wireshark (packet-level inspection) and OLA (independent receiving simulator), both open-source and named now, not deferred (D-13)."
    - "The verification runbook documents running OLA on a separate Linux/macOS host or bridged-adapter VM reachable from GOLC's pinned Windows interface, because OLA has no first-class native Windows build (D-18/Pitfall 6)."
    - "Simulator + packet-level verification can proceed now; real Art-Net hardware compatibility is tracked as an explicit open item — no named hardware-compatibility claim is made until a real device is independently evidenced (D-14, mirroring MIDI-HW-02)."
    - "The evidence bar for a future real-hardware compatibility claim is a recorded Wireshark capture showing correct Art-Net 4 output reaching the node plus an observed/recorded correct physical fixture response; simulator-only verification is not sufficient for a named hardware claim (D-15)."
    - statement: "A release candidate demonstrates packet + timing compatibility against the independent simulator (OLA) with a recorded Wireshark capture before the phase is marked complete."
      verification: backstop
  artifacts:
    - path: docs/artnet/ARTN-06-verification-runbook.md
      provides: "Step-by-step Wireshark capture + OLA-on-separate-host setup + evidence-recording checklist, plus the open real-hardware tracking entry."
  key_links:
    - from: docs/artnet/ARTN-06-verification-runbook.md
      to: internal/command/artnet.go
      via: "Runbook drives the verification through `golc artnet serve/configure/status` against an OLA receiver on a second host."
  prohibitions:
    - statement: "MUST NOT make a named real-Art-Net-hardware compatibility or support claim without recorded evidence (a Wireshark capture of correct Art-Net 4 output reaching the node AND an observed/recorded correct physical fixture response) — selection/simulator verification is not support (D-14/D-15)."
      status: unverified
      verification: flagged
  assumptions:
    - "ARTN-06 category was unclassified by the spec-less edge probe; resolved COVERED via D-13/D-14/D-15/D-18 (named tools, open-hardware tracking, evidence bar, OLA host topology). Recorded for visibility; no silent drop."
---

## Phase Goal

**As an** Art-Net operator, **I want to** drive a small lighting rig with correct Art-Net 4 frames computed from the deterministic playback engine and independently verify protocol, target, and timing health, **so that** I can run reliable live output without needing the desktop UI.

> Derived from the ROADMAP.md Phase 4 `**Goal:**` line (prose form); all three slots present, restated not invented.

<objective>
Close ARTN-06: produce the independent-verification runbook (Wireshark packet inspection + OLA receiver on a separate host) and run it against a release candidate, recording the evidence — while explicitly tracking real hardware as an open, un-claimed item (D-14). This is the phase's compatibility gate: the earlier plans made output correct and inspectable; this plan proves it independently and honestly bounds what can be claimed.

Purpose: ARTN-06 is a manual-only success criterion (no CI substitute exists once D-13 locks Wireshark+OLA). The runbook makes the verification repeatable and the evidence bar (D-15) explicit, so no hardware compatibility is overclaimed.
Output: `docs/artnet/ARTN-06-verification-runbook.md` + a recorded human verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observable-art-net-live-output/04-CONTEXT.md
@.planning/phases/04-observable-art-net-live-output/04-RESEARCH.md
</context>

## Artifacts this phase produces (this plan's contribution)

- `docs/artnet/ARTN-06-verification-runbook.md`: the independent-verification procedure (Wireshark capture steps, OLA-on-separate-host/bridged-VM setup, `golc artnet` driving steps, evidence-recording checklist, and the open real-hardware tracking entry).

<tasks>

<task type="auto">
  <name>Task 1: Author the ARTN-06 independent-verification runbook (D-13/D-14/D-15/D-18)</name>
  <files>docs/artnet/ARTN-06-verification-runbook.md</files>
  <read_first>
    - .planning/phases/04-observable-art-net-live-output/04-CONTEXT.md (D-13 tools, D-14 open hardware, D-15 evidence bar, D-18 OLA host topology)
    - .planning/phases/04-observable-art-net-live-output/04-RESEARCH.md (Common Pitfall 6 OLA Windows gap + bridged-adapter requirement; Environment Availability; Validation Architecture ARTN-06 manual-only row; Wave 0 Gaps manual runbook item)
    - internal/command/artnet.go (the `golc artnet serve/configure/status/discover` routes the runbook drives — from Plans 05/06)
    - .planning/ROADMAP.md (Phase 6 MIDI-HW-01/02 precedent the D-14 open-item tracking mirrors)
  </read_first>
  <action>
Create `docs/artnet/ARTN-06-verification-runbook.md`. Structure it as a runnable checklist covering:

1. Environment setup (D-18/Pitfall 6): how to run OLA on a separate Linux/macOS host or a bridged-adapter Linux VM (explicitly NOT NAT/WSL2 default, which does not present OLA with a directly routable IP for the static-unicast-target model) reachable from GOLC's pinned Windows interface; how to install Wireshark on a capture-capable machine (Art-Net/DMX dissector ships in core).
2. Packet-level verification (D-13, success criterion 2): steps to `golc artnet serve`, `configure` a universe + the OLA host as a unicast target, run playback, and capture with Wireshark filtered to UDP 6454; the exact fields to confirm (Art-Net id, OpOutput opcode, protocol version 14, per-universe Port-Address per the locked Net=0/SubNet/Universe mapping, sequence advancing and never 0, even payload length 2..512, ~40Hz refresh cadence).
3. Independent-receiver verification (D-13, success criterion 4): steps to confirm OLA receives the expected per-universe DMX values and target behavior, cross-checked against `golc artnet status --json`.
4. Evidence bar for a future real-hardware claim (D-15): the recorded Wireshark capture file PLUS an observed/recorded correct physical fixture response are both required before any named hardware claim.
5. Open real-hardware item (D-14): a tracking entry stating no real Art-Net hardware is currently owned, that hardware compatibility is an explicit open item following the MIDI-HW-02 selection-≠-support pattern, and that no named hardware-compatibility claim may be made until independently evidenced.

Cross-reference the requirement (ARTN-06) and the decisions (D-13/D-14/D-15/D-18) inline so the runbook is self-explaining.
  </action>
  <verify>
    <automated>test -f docs/artnet/ARTN-06-verification-runbook.md && grep -qi 'Wireshark' docs/artnet/ARTN-06-verification-runbook.md && grep -qi 'Open Lighting' docs/artnet/ARTN-06-verification-runbook.md && grep -q 'ARTN-06' docs/artnet/ARTN-06-verification-runbook.md && grep -qi 'open item' docs/artnet/ARTN-06-verification-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/artnet/ARTN-06-verification-runbook.md` exists and contains all five sections (env setup, packet-level, independent-receiver, evidence bar, open real-hardware item) — behavior/CLI assertion via grep.
    - The runbook names Wireshark and OLA (D-13), documents OLA-on-separate-host/bridged-VM (D-18), states the D-15 dual-evidence bar, and records the D-14 open real-hardware item (source assertions).
    - The runbook drives the verification through the real `golc artnet serve/configure/status` routes (source assertion).
  </acceptance_criteria>
  <done>A repeatable ARTN-06 verification runbook exists, naming the locked tools, the OLA host topology, the evidence bar, and the open hardware item.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify ARTN-06 packet + simulator compatibility (D-13/D-15)</name>
  <action>Manual-only verification: an operator follows docs/artnet/ARTN-06-verification-runbook.md against a release candidate, captures Art-Net 4 output in Wireshark, confirms OLA-received per-universe values on a separate host, saves the capture as recorded evidence (D-15), and confirms no named real-hardware claim is asserted (D-14). This has no CI substitute (D-13, manual-only per the RESEARCH test map).</action>
  <what-built>The full `golc artnet` stack (serve/configure/status/discover) emitting Art-Net 4 output, plus the ARTN-06 verification runbook. Automated tests already prove byte-exact ArtDMX, non-blocking output, and health; this checkpoint records the independent packet + simulator verification that has no CI substitute (D-13, manual-only).</what-built>
  <how-to-verify>
    Follow `docs/artnet/ARTN-06-verification-runbook.md`:
    1. Bring up OLA on a separate Linux/macOS host or bridged-adapter Linux VM reachable from GOLC's pinned Windows interface (D-18). Install Wireshark on a capture-capable machine.
    2. Run `golc artnet serve`, then `golc artnet configure` a universe + the OLA host as a unicast target; start playback.
    3. In Wireshark (filter UDP port 6454), confirm: Art-Net id + OpOutput opcode + protocol version 14, the expected per-universe Port-Address, sequence advancing and never 0, even payload length in [2,512], and ~40Hz refresh cadence.
    4. In OLA, confirm the received per-universe DMX values and target behavior match `golc artnet status --json`.
    5. Save the Wireshark capture as the recorded packet evidence (D-15).
    6. Confirm the runbook's open real-hardware item (D-14) is present and that NO named real-hardware compatibility claim is made (only the simulator/packet claim is supported now).
    Expected: OLA shows the correct per-universe values; Wireshark shows spec-correct Art-Net 4 packets; a capture file is saved; no real-hardware claim is asserted.
  </how-to-verify>
  <resume-signal>Type "approved" (attach or reference the saved Wireshark capture + OLA observation), or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification environment → claims | Simulator/packet evidence bounds what compatibility can be honestly claimed; overclaiming is the risk. |
| GOLC pinned interface → OLA host | Live unicast output crosses the network to the independent receiver on a second host/VM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Repudiation | ARTN-06 compatibility claim | medium | mitigate | Require recorded Wireshark capture + observed physical response before any named real-hardware claim (D-15); track real hardware as an explicit open item (D-14) so the release never implies unevidenced support. |

No high-severity threat in this plan. ASVS level 1; block-on-high satisfied (none present).
</threat_model>

<verification>
- `docs/artnet/ARTN-06-verification-runbook.md` exists with all required sections (grep gate above).
- Human-verify checkpoint recorded: OLA-received values + saved Wireshark capture, with no named real-hardware claim (D-14/D-15).
- Full suite `golc test` green before the phase gate (ARTN-01..ARTN-05 automated coverage from Plans 01-06).
</verification>

<success_criteria>
- A release candidate demonstrates packet + timing compatibility against Wireshark + OLA with recorded evidence (ARTN-06, D-13/D-15).
- Real hardware remains an explicit, un-claimed open item (D-14); the OLA host topology is documented (D-18).
</success_criteria>

<output>
Create `.planning/phases/04-observable-art-net-live-output/04-07-SUMMARY.md` when done.
</output>
