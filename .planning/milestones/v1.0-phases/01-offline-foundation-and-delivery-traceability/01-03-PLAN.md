---
phase: 01-offline-foundation-and-delivery-traceability
plan: 03
type: execute
wave: 5
depends_on: [01-02]
files_modified:
  - golc.project.toml
  - config/commands.toml
  - config/generation.toml
  - config/application-defaults.toml
  - config/runtime.toml
  - config/integrations/linear.toml
  - internal/projectconfig/model.go
  - internal/projectconfig/decode.go
  - internal/projectconfig/strict_test.go
autonomous: true
requirements: [CONF-01, CONF-02, CONF-04]
must_haves:
  truths:
    - "The root index discovers every Phase 1 concern while each canonical value has one owner, per D-05 and D-10."
    - "Unknown, duplicate, invalid, unresolved/cyclic, locked, and deprecated/current collision inputs fail distinctly; deprecated-only input gives migration guidance, per D-09."
    - "Committed Linear configuration contains names/key declarations only, never credentials or invented UUIDs."
  artifacts:
    - path: internal/projectconfig/model.go
      provides: "Typed concern/provenance/deprecation model"
    - path: internal/projectconfig/decode.go
      provides: "Strict TOML and stable diagnostics"
    - path: golc.project.toml
      provides: "Complete small concern index"
  key_links:
    - from: golc.project.toml
      to: config
      via: "fixed contained concern IDs/paths"
      pattern: "concerns"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Establish the complete strict independently owned configuration concern set.

Purpose: Make every concern discoverable and reject ambiguous authority before layered resolution.
Output: Six concern files, typed model, strict decoder/deprecation registry, and executable strict tests.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-02-SUMMARY.md
</context>

## Artifacts this phase produces

- Modified `golc.project.toml` and `config/runtime.toml`; created `config/commands.toml`, `config/generation.toml`, `config/application-defaults.toml`, and `config/integrations/linear.toml` with the single-authority allocation from research.
- Created `internal/projectconfig/model.go`, `internal/projectconfig/decode.go`, and `internal/projectconfig/strict_test.go` with the exact types, diagnostics, and scope contract below.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish strict independently owned concerns</name>
  <files>golc.project.toml, config/commands.toml, config/generation.toml, config/application-defaults.toml, config/runtime.toml, config/integrations/linear.toml, internal/projectconfig/model.go, internal/projectconfig/decode.go, internal/projectconfig/strict_test.go</files>
  <read_first>
    - `golc.project.toml` and all five listed `config/*.toml` paths — root index and concern authorities.
    - `internal/projectconfig/model.go`, `decode.go`, `strict_test.go` — new model/decoder/test paths.
    - `internal/projectconfig/load.go` and `local.go` — upstream loader/local interfaces.
    - `internal/command/router.go` and `test.go` — route/scope registration and dispatcher contracts.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-05, D-09, D-10.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md` — exact allocation and deprecation behavior.
  </read_first>
  <behavior>
    - Each concern validates alone and the combined declarations assign every canonical key once.
    - Deprecated-only input emits `CFG_DEPRECATED_KEY` with safe origin and actionable replacement/version guidance.
    - Old plus replacement fails `CFG_DEPRECATED_COLLISION`; other malformed cases retain distinct stable diagnostics.
  </behavior>
  <acceptance_criteria>
    - [ ] `golc.project.toml` indexes exactly toolchain, commands, generation, application defaults, runtime, and Linear integration concerns without duplicating owned values.
    - [ ] `Deprecation` contains old/replacement keys, introduced/deprecated/optional-removal versions, and a non-empty migration message.
    - [ ] Strict decode distinguishes unknown, duplicate, invalid, deprecated-only, old-plus-new, duplicate-authority, unresolved, and cyclic inputs.
    - [ ] `config/integrations/linear.toml` contains only taxonomy/environment key names and no credential or invented remote UUID.
    - [ ] `internal/projectconfig/strict_test.go` calls `MustDeclareScope` for exact scope `config-strict`, defines `TestScopeConfigStrict`, and the root scoped test exits 0.
  </acceptance_criteria>
  <action>Implement D-05, D-09, and D-10 using the research allocation. The root stays an index; concern files own non-overlapping declarations. Define machine-readable deprecation metadata and never silently rewrite deprecated input. Keep Linear taxonomy external/reviewed and credential-free. Before verification, explicitly register `config-strict` through the Plan 17 scope contract in the owned test file beside marker `TestScopeConfigStrict`; duplicate scope declarations must fail before tests run.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope config-strict</automated>
  </verify>
  <done>All Phase 1 concerns have one authority and strict actionable validation through a registered root test scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| TOML -> typed concerns | Untrusted configuration can claim authority or hide keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-07 | Tampering | strict configuration | high | mitigate | Unknown/duplicate/authority/reference/deprecation validation in Task 1. |
| T-01-09 | Information Disclosure | Linear concern | high | mitigate | Names/declarations only; no values, credentials, or remote UUIDs. |
| T-01-SC | Tampering | TOML module | high | mitigate | Retain exact researched module and `go.sum`. |
</threat_model>

<verification>
Run the registered `config-strict` scope without `.env` or network.
</verification>

<success_criteria>
- Every concern is discoverable, independent, strict, and credential-free.
- Deprecated and invalid inputs have distinct stable outcomes.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-03-SUMMARY.md` when done.
</output>
