---
phase: 01-offline-foundation-and-delivery-traceability
plan: 26
type: execute
wave: 16
depends_on: [01-14]
files_modified:
  - config/commands.toml
  - tools/linear-sync/src/protocol.ts
  - tools/linear-sync/src/errors.ts
  - tools/linear-sync/src/adapter.ts
  - tools/linear-sync/test/errors.test.ts
  - tools/linear-sync/test/rate-limit.test.ts
  - tests/fixtures/linear/data-plus-errors.json
  - tests/fixtures/linear/rate-limited.json
autonomous: true
requirements: [CONF-04, LINR-03, LINR-04]
must_haves:
  truths:
    - "HTTP-200 data-plus-errors is incomplete/blocked and rate limits stop writes, per D-21."
    - "Reads/5xx retry only within fixed bounds; mutations never flow through generic retry."
    - "Exact scope `linear-transport-errors` is registered before invocation and emits safe metadata only, per D-20."
  artifacts:
    - path: tools/linear-sync/src/errors.ts
      provides: "Partial/rate/retry normalization"
    - path: tests/fixtures/linear/data-plus-errors.json
      provides: "HTTP-200 partial-error oracle"
  key_links:
    - from: tools/linear-sync/src/adapter.ts
      to: tools/linear-sync/src/errors.ts
      via: "data/errors/rate normalization"
      pattern: "normalizeGraphQLResult"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Normalize partial GraphQL errors and rate limits without unsafe mutation retry.

Purpose: Prevent incomplete or rate-limited remote state from becoming applyable intent.
Output: Error/rate protocol/helper/adapter integration, tests, fixtures, and exact scope registration.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-14-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `config/commands.toml`, `tools/linear-sync/src/protocol.ts`, `tools/linear-sync/src/errors.ts`, `tools/linear-sync/src/adapter.ts`, `tools/linear-sync/test/errors.test.ts`, `tools/linear-sync/test/rate-limit.test.ts`, `tests/fixtures/linear/data-plus-errors.json`, `tests/fixtures/linear/rate-limited.json`.

- Modified graph/protocol/adapter and created error helper/two tests/two fixtures at the eight paths listed.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize partial GraphQL errors and rate limits</name>
  <files>config/commands.toml, tools/linear-sync/src/protocol.ts, tools/linear-sync/src/errors.ts, tools/linear-sync/src/adapter.ts, tools/linear-sync/test/errors.test.ts, tools/linear-sync/test/rate-limit.test.ts, tests/fixtures/linear/data-plus-errors.json, tests/fixtures/linear/rate-limited.json</files>
  <read_first>
    - All eight owned paths — graph/protocol/errors/adapter/tests/fixtures.
    - `tools/linear-sync/src/pagination.ts` — completeness diagnostics.
    - `internal/trace/transport/contract.go` — Go error/rate shapes.
    - `internal/command/test.go` — registered Node scope dispatcher.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-20/D-21.
  </read_first>
  <behavior>
    - Any data-plus-errors snapshot is blocked even on HTTP 200.
    - Reads/5xx retry within fixed bounds; rate/partial mutation stops writes.
    - Diagnostics expose safe path/code/operation/rate metadata only.
  </behavior>
  <acceptance_criteria>
    - [ ] Data and errors are inspected together and partial snapshot remains blocked.
    - [ ] Normalization exposes only allowlisted path/code/operation/request/endpoint/complexity/reset fields.
    - [ ] Retry policy permits only bounded reads/5xx and returns typed stop-write state for rate/partial mutation.
    - [ ] `config/commands.toml` registers exact Node scope `linear-transport-errors` before invocation.
    - [ ] Owned tests define matching marker `TestScopeLinearTransportErrors`, and the registered scope exits 0 over both fixtures.
  </acceptance_criteria>
  <action>Implement D-21 GraphQL/rate normalization. Inspect data/errors together, normalize allowlisted metadata, and permit bounded retries only for reads/5xx. Register exact scope `linear-transport-errors` in the command graph through the Plan 17 registry before invocation and require matching Node markers.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-transport-errors</automated>
  </verify>
  <done>Partial data and rate limits are safely reported and cannot continue mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GraphQL result/rate headers -> adapter | Partial or hostile diagnostics enter tooling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-40 | Tampering / Denial of Service | partial errors/rates | high | mitigate | Data+errors block, bounded reads, stop writes, safe metadata. |
| T-01-SC | Tampering | npm dependencies | high | mitigate | Approved exact lock/cache only. |
</threat_model>

<verification>
Run the registered errors scope against fake transcripts only.
</verification>

<success_criteria>
- Partial errors/rates are complete, safe, and mutation-stopping.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-26-SUMMARY.md` when done.
</output>
