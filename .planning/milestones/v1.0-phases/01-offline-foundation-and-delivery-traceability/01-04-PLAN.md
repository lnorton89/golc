---
phase: 01-offline-foundation-and-delivery-traceability
plan: 04
type: execute
wave: 7
depends_on: [01-16, 01-18]
files_modified:
  - internal/contracts/model.go
  - internal/contracts/generate.go
  - internal/contracts/normalize.go
  - internal/contracts/generate_test.go
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03]
must_haves:
  truths:
    - "Authoritative Go types generate deterministic strict Draft 2020-12 contracts, per D-08."
    - "Check mode compares temporary output without rewriting committed bytes."
    - "A compile-safe schema registry rejects empty/duplicate names, enumerates descriptors in stable sorted order, and lets later contract files join global generation without editing the central generator."
  artifacts:
    - path: internal/contracts/model.go
      provides: "Seven Phase 1 configuration schema projections"
    - path: internal/contracts/generate.go
      provides: "SchemaDescriptor registry plus deterministic generation and read-only drift checking"
    - path: internal/contracts/normalize.go
      provides: "Stable JSON ordering and LF normalization"
  key_links:
    - from: internal/contracts/model.go
      to: internal/projectconfig/model.go
      via: "authoritative type projections"
      pattern: "RootIndexSchema|ToolchainSchema"
    - from: internal/contracts/model.go
      to: internal/contracts/generate.go
      via: "seven configuration `SchemaDescriptor` registrations through `RegisterSchema`"
      pattern: "RegisterSchema|SchemaDescriptor"
    - from: internal/contracts/generate.go
      to: go.mod
      via: "Invopop reflection resolved from the Plan 16 warmed project-local module cache with `GOPROXY=off` and readonly module mode"
      pattern: "invopop/jsonschema"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Implement deterministic strict configuration-contract generation from Go source types.

Purpose: Establish source-owned review artifacts before committing generated schemas or routes.
Output: Projection model, extensible deterministic schema registry/generator, normalization, and focused tests.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-16-SUMMARY.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-18-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/contracts/model.go`, `internal/contracts/generate.go`, `internal/contracts/normalize.go`, `internal/contracts/generate_test.go`.

- Created the four contract-model/generator/normalizer/test files listed in frontmatter; `internal/contracts/generate.go` owns `SchemaDescriptor`, `RegisterSchema`, `RegisteredSchemas`, `GenerateAll`, `GenerateInto`, and `CheckDrift`, while `model.go` contributes the seven configuration descriptors.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate deterministic strict contracts from Go types</name>
  <files>internal/contracts/model.go, internal/contracts/generate.go, internal/contracts/normalize.go, internal/contracts/generate_test.go</files>
  <read_first>
    - All four owned paths — new projection/generation/normalization/test files.
    - `internal/projectconfig/model.go`, `registry.go`, and `resolve.go` — authoritative source types.
    - `go.mod`, `go.sum`, and `.planning/phases/01-offline-foundation-and-delivery-traceability/01-16-SUMMARY.md` — exact prewarmed schema-generator dependency lock/cache contract; this plan must not add or download modules.
    - `internal/command/router.go` and `test.go` — scope declaration/dispatch contract.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-08.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md` — Draft 2020-12 normalization contract.
  </read_first>
  <behavior>
    - Unchanged Go types generate byte-identical Draft 2020-12 schemas on Windows.
    - Every object denies additional properties and names its source package/schema version.
    - Check mode uses temporary output and never rewrites committed targets.
    - Registry calls reject blank or duplicate names/output paths; `RegisteredSchemas` returns a defensive stable name-sorted descriptor list consumed by both `GenerateAll` and `CheckDrift`.
  </behavior>
  <acceptance_criteria>
    - [ ] `generate.go` defines compile-safe `SchemaDescriptor`, `RegisterSchema`, `RegisteredSchemas`, `GenerateAll`, `GenerateInto`, and `CheckDrift` APIs without importing a package that imports `internal/contracts`.
    - [ ] `RegisterSchema` rejects empty/whitespace names, empty output paths/factories, duplicate names, and duplicate output paths; `RegisteredSchemas` returns a defensive copy in stable name order.
    - [ ] The seven configuration projections register through `RegisterSchema` under stable names `golc-project`, `config-toolchain`, `config-commands`, `config-generation`, `config-application-defaults`, `config-runtime`, and `config-linear`; `GenerateAll`, `GenerateInto`, and `CheckDrift` iterate every registered descriptor exactly once rather than a closed seven-item switch/list.
    - [ ] `NormalizeSchema`, `SortJSON`, and `NormalizeLF` produce stable strict JSON with LF endings.
    - [ ] Check mode reports changed paths and leaves committed bytes untouched.
    - [ ] `internal/contracts/generate_test.go` proves blank/duplicate rejection, defensive-copy behavior, stable ordering, exactly-once generation/check dispatch, and the presence of all seven configuration descriptors without imposing a registry-size ceiling; it calls `MustDeclareScope` for exact scope `contracts` and defines `TestScopeContracts` before verification.
    - [ ] Generator output contains no timestamp, machine path, credential, remote UUID, or out-of-phase contract.
    - [ ] With Plan 16 complete, contracts verification runs with `GOPROXY=off` and readonly module mode, uses the warmed project-local cache, and leaves `go.mod`/`go.sum` unchanged.
  </acceptance_criteria>
  <action>Implement D-08 against the Plan 16-pinned and prewarmed `github.com/invopop/jsonschema@v0.14.0`; do not change `go.mod`/`go.sum` or fetch a module. In `generate.go`, define `SchemaDescriptor` with a stable name, repository-relative output path, and schema factory; define `RegisterSchema` to validate and atomically reject blank/duplicate names and paths; define `RegisteredSchemas` to return a defensive name-sorted snapshot; and make `GenerateAll`, `GenerateInto`, and `CheckDrift` traverse that snapshot exactly once. Keep the registry in `internal/contracts` and keep descriptor factories in contract files so no consumer package must import back into `internal/contracts`. Register all seven configuration projections through this same API, normalize definition/property order, metadata, indentation, and LF endings, and compare exact bytes in check mode. Tests must prove duplicate rejection and stable ordering while allowing later Linear descriptor files to extend the registry without editing `generate.go`. Explicitly register test scope `contracts` through the Plan 17 scope contract in the owned test file; no central router edit is permitted.</action>
  <verify>
    <automated>$beforeMod=(Get-FileHash .\go.mod -Algorithm SHA256).Hash; $beforeSum=(Get-FileHash .\go.sum -Algorithm SHA256).Hash; $env:GOPROXY='off'; $env:GOFLAGS='-mod=readonly'; powershell -NoProfile -File .\golc.ps1 test --quick --scope contracts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if ((Get-FileHash .\go.mod -Algorithm SHA256).Hash -ne $beforeMod -or (Get-FileHash .\go.sum -Algorithm SHA256).Hash -ne $beforeSum) { throw 'offline contracts verification mutated Go locks' }</automated>
  </verify>
  <done>Generator tests prove deterministic strict source-owned output, extensible exactly-once registry traversal, duplicate rejection/stable order, and network-free readonly resolution from the Plan 16 cache.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Go types -> generated bytes | Nondeterminism could disguise contract drift. |
| contract file -> schema registry | Duplicate or order-dependent registration could omit or overwrite committed contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10 | Tampering | generator | high | mitigate | Source-owned types, canonical bytes, and exact drift tests. |
| T-01-10R | Tampering / Denial of Service | schema registry | high | mitigate | Validated unique descriptor names/paths, defensive sorted snapshots, and exactly-once traversal tests. |
| T-01-SC | Tampering | schema module | high | mitigate | Plan 16 exact version/sums/cache plus `GOPROXY=off`, readonly module mode, and lock hashes during verification. |
</threat_model>

<verification>
Run the registered contracts scope with `GOPROXY=off` and readonly module mode after Plan 16; require unchanged Go locks, deterministic registry order, and exactly-once descriptor traversal.
</verification>

<success_criteria>
- Generation is deterministic, strict, source-owned, registry-extensible, exactly-once, and check-only safe without network access.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-04-SUMMARY.md` when done.
</output>
