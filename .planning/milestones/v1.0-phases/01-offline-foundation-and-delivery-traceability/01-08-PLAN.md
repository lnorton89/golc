---
phase: 01-offline-foundation-and-delivery-traceability
plan: 08
type: execute
wave: 5
depends_on: [01-02]
files_modified:
  - internal/trace/catalog/model.go
  - internal/trace/catalog/id.go
  - internal/trace/catalog/parse.go
  - internal/trace/catalog/validate.go
  - internal/trace/catalog/catalog_test.go
autonomous: true
requirements: [LINR-01, LINR-02]
must_haves:
  truths:
    - "Project, milestone, phase, requirement, every repository-discovered plan, and every executable task receive durable offline IDs, per D-11 and D-14."
    - "Display renames never alter local IDs or immutable mapped UUIDs, per D-14."
    - "Repository scope/text/structure remain authoritative and Linear comments are excluded, per D-11 and D-12."
  artifacts:
    - path: internal/trace/catalog/model.go
      provides: "Offline identity/authority graph"
    - path: internal/trace/catalog/parse.go
      provides: "Dynamic roadmap/requirements/PLAN task discovery"
    - path: internal/trace/catalog/validate.go
      provides: "ID, parent, source, and authority validation"
  key_links:
    - from: internal/trace/catalog/parse.go
      to: .planning/phases/01-offline-foundation-and-delivery-traceability
      via: "dynamic `01-*-PLAN.md` discovery and XML task positions"
      pattern: "PLAN\\.md|task:01-"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Build the complete dynamic repository-owned planning identity catalog without Linear.

Purpose: Make all Phase 1 entities durable independently of plan count or display names.
Output: Catalog model/ID grammar/parser/validator and focused tests.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-02-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `internal/trace/catalog/model.go`, `internal/trace/catalog/id.go`, `internal/trace/catalog/parse.go`, `internal/trace/catalog/validate.go`, `internal/trace/catalog/catalog_test.go`.

- Created the five catalog model/ID/parser/validator/test files listed in frontmatter.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse durable IDs and enforce the repository authority graph</name>
  <files>internal/trace/catalog/model.go, internal/trace/catalog/id.go, internal/trace/catalog/parse.go, internal/trace/catalog/validate.go, internal/trace/catalog/catalog_test.go</files>
  <read_first>
    - All five owned paths — new catalog implementation/tests.
    - `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` — Phase 1 structure and eight requirement IDs.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/*-PLAN.md` — dynamically discovered plan/task authorities.
    - `.planning/linear-map.json` — stable seed/null mapping boundary.
    - `internal/command/router.go` and `test.go` — exact scope registration/dispatch.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-11/D-12/D-14.
  </read_first>
  <behavior>
    - Catalog contains fixed project/milestone/phase/eight requirements plus every valid plan file and executable task exactly once.
    - Parent kinds, acyclicity, source containment, and ID grammar are enforced.
    - Repository fields cannot be reassigned to Linear; comments cannot be stored.
  </behavior>
  <acceptance_criteria>
    - [ ] `BuildCatalog` discovers valid plan filenames dynamically rather than asserting a fixed plan count and includes every executable task exactly once.
    - [ ] The catalog always contains `project:golc`, `milestone:v1`, `phase:01`, and all eight Phase 1 requirements.
    - [ ] ID/parent/source/authority validators reject duplicates, wrong parents, cycles, external sources, comments, and remote ownership of repository fields.
    - [ ] IDs derive from durable structural metadata and remain unchanged by display renames or issue keys.
    - [ ] `catalog_test.go` calls `MustDeclareScope` for exact scope `linear-catalog`, defines `TestScopeLinearCatalog`, and the scope exits 0 offline.
  </acceptance_criteria>
  <action>Implement D-11/D-12/D-14 from durable structural metadata, never titles. Discover `01-*-PLAN.md` files using the enforced filename grammar and sort by parsed numeric plan ID; do not hard-code twenty-nine or any other count. Parse roadmap/requirements/frontmatter/XML task positions into the hierarchy. Explicitly register scope `linear-catalog` through the Plan 17 contract in the owned test file before verification.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope linear-catalog</automated>
  </verify>
  <done>The complete dynamically discovered local graph is unique, source-contained, authority-safe, and usable offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| planning Markdown/XML -> catalog | Repository text is parsed into durable identities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-21 | Spoofing / Tampering | local identity graph | high | mitigate | Filename/ID grammar, uniqueness, parent/cycle/source validation. |
| T-01-22 | Tampering | authority split | high | mitigate | Typed ownership and comment exclusion. |
| T-01-SC | Tampering | dependencies | high | mitigate | Standard-library parsing plus existing exact Go lock. |
</threat_model>

<verification>
Run the dynamic catalog scope without `.env`, Node, network, or Linear.
</verification>

<success_criteria>
- Every discovered plan/task has a stable ID without any exact-count assertion.
- Repository authority and comment exclusion remain enforced.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-08-SUMMARY.md` when done.
</output>
