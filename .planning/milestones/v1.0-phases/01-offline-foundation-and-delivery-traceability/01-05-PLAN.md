---
phase: 01-offline-foundation-and-delivery-traceability
plan: 05
type: execute
wave: 7
depends_on: [01-18]
files_modified:
  - config/toolchain.toml
  - internal/bootstrap/downloader.go
  - internal/bootstrap/archive.go
  - internal/bootstrap/bootstrap_test.go
autonomous: true
requirements: [CONF-01, CONF-03]
must_haves:
  truths:
    - "Official Go/Node archives are allowlisted, checksum-verified, traversal-checked, staged, and atomically promoted, per D-01."
    - "Corrupt or malicious bytes never produce a promoted install."
  artifacts:
    - path: internal/bootstrap/downloader.go
      provides: "Official-source staged acquisition"
    - path: internal/bootstrap/archive.go
      provides: "SHA-256/traversal verification and atomic promotion"
  key_links:
    - from: internal/bootstrap/downloader.go
      to: config/toolchain.toml
      via: "official host/path allowlist and expected hashes"
      pattern: "OfficialSourcePolicy"
---

## Phase Goal

**As a** contributor, **I want to** configure, validate, build, and trace GOLC from durable repository-owned sources, **so that** Linear and secrets are not required for offline work.

<objective>
Harden tool archive acquisition and atomic promotion.

Purpose: Close the executable-byte trust boundary before cache warming or updates.
Output: Official-source policy, safe archive handling, and focused fake-source tests.
</objective>

<execution_context>
@C:/Users/Lawrence/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Lawrence/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md
@.planning/phases/01-offline-foundation-and-delivery-traceability/01-18-SUMMARY.md
</context>

## Artifacts this phase produces

- Complete owned file inventory: `config/toolchain.toml`, `internal/bootstrap/downloader.go`, `internal/bootstrap/archive.go`, `internal/bootstrap/bootstrap_test.go`.

- Modified `config/toolchain.toml` with official source/hash policy; created downloader/archive helpers; extended bootstrap tests with archive cases.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Acquire and promote exact official tool archives safely</name>
  <files>config/toolchain.toml, internal/bootstrap/downloader.go, internal/bootstrap/archive.go, internal/bootstrap/bootstrap_test.go</files>
  <read_first>
    - All four owned paths — exact-source concern, new acquisition/archive helpers, and existing bootstrap tests.
    - `internal/bootstrap/bootstrap.go` — staged install and installed-manifest interfaces.
    - `internal/command/router.go` and `test.go` — exact scope registration/dispatch contracts.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-CONTEXT.md` — D-01/D-04.
    - `.planning/phases/01-offline-foundation-and-delivery-traceability/01-RESEARCH.md` — official hosts/hashes and promotion mechanics.
  </read_first>
  <behavior>
    - Only committed official host/path patterns are accepted.
    - SHA mismatch or unsafe archive entries leave no promoted install.
    - Correct retry promotes a complete verified tree atomically.
  </behavior>
  <acceptance_criteria>
    - [ ] `OfficialSourcePolicy` accepts only the committed official host/path patterns.
    - [ ] `VerifySHA256` and `InspectZipEntries` reject wrong hashes, absolute entries, traversal, and unsafe links before promotion.
    - [ ] `ExtractVerified` writes only staging and `PromoteAtomically` exposes the complete tree or nothing.
    - [ ] `internal/bootstrap/bootstrap_test.go` calls `MustDeclareScope` for exact scope `bootstrap-archive`, defines `TestScopeBootstrapArchive`, and uses injected sources only.
    - [ ] The registered `bootstrap-archive` scope exits 0 without live network.
  </acceptance_criteria>
  <action>Implement the D-01 executable-byte boundary. Download only to contained staging, validate official URL policy and committed SHA-256, inspect every archive entry, extract without following unsafe links, and promote only the complete verified directory. Explicitly self-register exact scope `bootstrap-archive` through the Plan 17 contract before verification; tests never call live sources.</action>
  <verify>
    <automated>powershell -NoProfile -File .\golc.ps1 test --quick --scope bootstrap-archive</automated>
  </verify>
  <done>Corrupt and malicious tool archives fail closed; exact bytes promote atomically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| official/injected source -> staging | Untrusted executable bytes enter bootstrap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-12 | Tampering | downloads/archives | high | mitigate | Allowlist, exact hash, traversal inspection, and atomic promotion. |
| T-01-SC | Tampering | tool sources | high | mitigate | Exact committed sources; no npm installation. |
</threat_model>

<verification>
Run the registered archive scope against injected sources only.
</verification>

<success_criteria>
- Bootstrap cannot promote unchecked or escaping archive content.
</success_criteria>

<output>
Create `.planning/phases/01-offline-foundation-and-delivery-traceability/01-05-SUMMARY.md` when done.
</output>
